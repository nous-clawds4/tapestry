# Open Items Ledger

The single home for **small or cross-cutting open items that have no other surface** — one-off cleanups, "should we update BIBLE?" decisions, follow-ups too small for a handoff doc, anything that would otherwise be remembered only by the session that noticed it.

**This is NOT the whole backlog.** Big, triaged work lives in its proper surface, and those are *derived* into one view by [`scripts/whats-open.sh`](scripts/whats-open.sh) / the `/whats-open` command:

| Kind of open work | Lives in |
|---|---|
| Triaged-but-unbuilt features / bugs / refactors | [`engineering-team/stories/_intake.md`](engineering-team/stories/_intake.md) |
| Session-to-session "I left this open" notes | `docs/*HANDOFF*.md` (`**Status:** 🔴 OPEN`) |
| Per-book deferred scope | `engineering-team/audits/<slug>/audit.md` §6 + `prd-seed.md` §7 |
| Open protocol problems | [`protocols/worksheet.md`](protocols/worksheet.md) |
| In-flight code | open PRs / unmerged branches (git) |
| **Small / cross-cutting / homeless items** | **this file** |

Run **`/whats-open`** (or `bash scripts/whats-open.sh`) for the unified roll-up across all of the above plus this ledger.

## How to use this ledger
- Add a row when you finish a session with a small loose end that has no other home. Keep it one line; link to detail if it has any.
- Flip **Status** to `DONE` (don't delete) when handled — the closed rows are the audit trail.
- **Type:** `bug` · `feature` · `protocol` · `docs` · `cleanup` · `meta`.
- **Opened / Done:** ISO date + the session/book/PR that raised or resolved it.
- **Meta escalation:** `meta` rows are harness lessons (a doc that misled a session, a process defect, a proposed amendment). `/whats-open` lists them with their age and — at the thresholds the script defines (any item aged past ~a month, or several open at once) — prints an escalation banner: at triage, group the related items and **propose a concrete harness story** (title + the items it closes) rather than letting the inbox silt up. See `.claude/commands/whats-open.md` step 2.

## Items

| # | Type | Item | Opened | Status | Done | Pointer |
|---|---|---|---|---|---|---|
| 1 | cleanup | Review/flip the stale handoff status: `docs/POST_TIMEOUT_FIX_COMPLETION_HANDOFF_2026-05-26.md` is still `🔴 OPEN`, but its "24–48h passive-verification window" (late May 2026) is long past — confirm the verification held and flip to `✅ ADDRESSED`, or capture whatever remains. | 2026-06-14 | DONE | 2026-06-14 | Flipped to ✅ ADDRESSED: timeout series shipped (6 prod PRs), no regression in the ~2.5 wks since, residuals tracked in `_intake.md`; held branch `fix/launch-child-task-protection-audit` retained by design as the ADR 0023 audit trail. |
| 2 | cleanup | Delete the merged branches `feat/reputation-info-popup` and `chore/clarify-gate5-status-flip`. | 2026-06-14 (reputation-info-popup) | DONE | 2026-06-14 | branches deleted; remotes were auto-deleted on merge |
| 3 | docs | Decide whether BIBLE needs a `reputation-info-popup` entry. | 2026-06-14 (reputation-info-popup) | DONE | 2026-06-14 | No — it's a UI explainer, not architecture / protocol / data-model / API; out of BIBLE's scope. Recorded here so the judgment isn't lost. |
| 4 | cleanup | The `live-feed` book's **feature** is merged to `staging` (PR #296, `80a39afd`), but the **post-merge audit trail** (Stage 2–3 journal entries + `completion-report.md` + `tier4-dom-extract.txt` + `audit.md` + `prd-seed.md` + the book-close commit) lives only on the local `feat/live-feed` branch. Merge it to `staging` as a **docs-only PR**, then delete `feat/live-feed` (local + remote). | 2026-06-15 (live-feed) | DONE | 2026-06-15 | Audit trail merged to `staging` via PR #297 (`75a12622`, docs-only); `feat/live-feed` deleted (local + remote — remote was auto-deleted on the #297 merge). Engineering hygiene + deferred scope from this book live in `audits/live-feed/audit.md` §6. |
| 5 | docs | verified-reporters #4 (Report Type + Reported columns) shipped to prod (PRs #299→staging, #300→main) — decide BIBLE/doc impact. | 2026-06-16 (verified-reporters #4) | DONE | 2026-06-16 | **Yes, BIBLE warranted** (it tracks this feature family): added a changelog entry for #4. While in BIBLE, also (a) added the missing **live-feed** changelog entry (it had shipped to prod with no BIBLE line) and (b) corrected stale "On staging; prod held" notes on the verified-followers (#33/#34) and profile (#35/#36) lines — both are on `main`/prod now. Updated `engineering-team/epics/verified-reporters.md` (story #4 + ADR 0004; report-type-breakdown now *partial*). **No** protocol/AGENTS/data-model edit: `report_type`/`timestamp` pre-existed on the `REPORTS` edge (ingestion-set), NIP-56 is an external spec we consume, and BIBLE doesn't enumerate edge properties. |
| 7 | feature | The profile page will show **two "Hops" figures with different PoVs** once profile #38 ships: the new prominent counts-row **HOPS** (live `shortestPath` from the logged-in viewer, else the **Owner**) and the existing Reputation trust card **"Hops" / "Degrees of separation"** (precomputed, a different PoV). Accepted as a known discrepancy for now (per [`stories/profile/38-profile-follows-hops.md`](engineering-team/stories/profile/38-profile-follows-hops.md) — "no reconciliation"). Reconcile the two PoVs in a future story. | 2026-06-17 (follows-hops) | OPEN | | Deliberate deferral confirmed by the requester during test design. |
| 8 | docs | **Never-clobber doesn't survive a firmware reinstall** (live-verified 2026-06-17, tag-federation Story 2). The b-tag emitter's never-clobber (ADR 0034 fp1 / ADR 0030 fp3) only protects *within* an install run: firmware `pass1`/`pass2` rebuild each TA header from the static concept definition (no `b`) before `pass_communityReferences` runs, so the emitter always re-seeds the firmware-default `b` on reinstall — an operator's manually re-pointed `b` is reverted. **Accepted as defensible** (reinstall = restore firmware defaults; requester agreed 2026-06-17). Documented in ADR 0002 (Consequences) + ADR 0034 should note the within-run-only scope. Future idea (requester): a "firmware update" path so a reinstall pulls the *updated* version rather than always the baked default — not a current concern. | 2026-06-17 (tag-federation Story 2 live verify) | OPEN | | The map deliverable is unaffected (one `b`, one edge, correct target after every install); only the operator-re-point-survives-reinstall guarantee is narrowed. |
| 6 | cleanup | Local Docker stack runs a **stale neo4j image** (predates commit `c62f6a79`): the live `/etc/supervisor/conf.d/tapestry.conf` lacks `NEO4J_CONF="/etc/neo4j"`, so neo4j boots with the wrong `--config-dir` and **GDS + APOC are not loaded at runtime** (`gds.version()`/`apoc.version()` are unknown functions; 0 `gds.*`/`apoc.*` procedures, only the 48 bare-Community kernel procs), and the local graph holds only **1 `NostrUser`** node. A fresh `docker build` from current source loads both plugins. Until rebuilt, don't trust the local stack for graph/GDS/APOC features — verify them on **staging** (≈ prod scale). | 2026-06-17 (follows-hops feasibility) | OPEN | | Found via a live container probe during the follows-hops shortest-path investigation; JARs are present in `/var/lib/neo4j/plugins/` but unscanned because the running image points `server.directories.plugins` at the empty `/usr/share/neo4j/plugins`. |
| 9 | docs | **Staging-held note/event batches + prod sequencing.** Three additive batches are on `staging` but **not on `main`/prod**: (a) Live-feed enhancements + shared note module (2026-06-18), (b) `note-surfaces` — profile **Content** section + **`/user/:pubkey/notes`** (PR #319, `da269ba8`), (c) `event-page` — the **`/event`** view (PR #320, `a4ae90bc`). They **stack**: (b) reuses (a)'s `NoteCard`/`enrichNotes`; (c) reuses (a) + the by-author read path from (b) + adds `src/api/_shared/relaySource.js`. A prod promotion must carry them **in order a→b→c** (or together). Related staging-held work: the tags/identity-popover bundle (`#306`) and profile #38/#39 (follows-hops). Decide the prod plan when ready. | 2026-06-19 | OPEN | | Held at staging per operator (both #319 + #320). Close-books: `engineering-team/audits/{note-surfaces,event-page}/`. Re-typed meta→docs at the 2026-07-04 book retro: predates story 4's meta=harness-lesson definition; it is a prod-sequencing hold, not a harness lesson. |
| 10 | docs | Decide BIBLE/doc impact of `note-surfaces` + `event-page` (shipped to staging). | 2026-06-19 | DONE | 2026-06-19 | **Yes, BIBLE warranted** (new public read-path **endpoints** + relay-union/outbox **architecture** are in BIBLE scope, unlike a pure UI explainer — cf. #3/#5). Added: a §11 **"Notes & Events (read paths)"** subsection documenting `/api/feed` (backfilled — it had no row), `/api/user/:pubkey/notes`, `/api/event`; updated §13 "Shared note rendering" consumers (feed + Content section + `/user/:pubkey/notes` + `/event`, sharing `_shared/relaySource.js`); added the two route-tree lines; and two changelog entries. Recorded here so the judgment isn't re-litigated. |
| 11 | docs | Ship the `open-ranking` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed) **+ BIBLE §28** to `staging` as a **docs-only PR** — the *endpoints* are already live (#318/#322), but the as-built docs are branch-local on `feat/open-ranking`. Then delete `feat/open-ranking` (local + remote). | 2026-06-19 (open-ranking) | DONE | 2026-06-19 | Bulk close artifacts + BIBLE §28 + ADRs shipped via #326 (feature PR); the re-close (audit §7 + book→Closed) + ORE doc-consistency fixes shipped via the `open-ranking` docs-only PR; `feat/open-ranking` auto-deleted on that merge. Mirrors the live-feed close (#4). The larger ORE carry-forward — **Story 3** (personalized search, W13), the **W12 personalized-stats prod gate**, other ORE endpoints — lives in `audits/open-ranking/audit.md` §6 + `prd-seed.md` §7; don't duplicate it here. |
| 12 | docs | Ship the `verified-muters` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed) + the epic retirement (`stories`/`decisions`/`reviews` `→ done/verified-muters/`, epic `Status: Done`) — the feature already shipped to `staging` via PR #333 (`e4a505a2`, deploy `27914727602` green), but the close commit is branch-local on `feat/verified-muters`. Land it on `staging`, then delete `feat/verified-muters` (local + remote). Mirrors the live-feed (#4) / open-ranking (#11) closes. | 2026-06-21 (verified-muters) | OPEN | | Director-mode book close. The feature is on staging and **prod-held** — it stacks behind the other staging-held bundles in #9 (note-surfaces/event-page) and #306/#38/#39; sequence the prod promotion with those. Larger deferred scope (DRY `<GrapevineList>` refactor; per-PoV muter counts; unverified/all-muters view; cutoff source-of-truth) lives in `audits/verified-muters/audit.md` §6 + `prd-seed.md` §6 — don't duplicate here. |
| 13 | bug | **Test-suite hermeticity + no CI.** (a) In a bare checkout (no `node_modules`), live-feed test B9 fails misleadingly: `feedReadPath.js:133` reaches a non-injected `require('../../utils/assistantKeys')` whose transitive top-level `require('nostr-tools')` throws, and the silent catch degrades to fallback relays before the injected mock is hit. (b) With deps installed but no local stack, ~12 live-API suites fail with `fetch failed` (expected, but `npm test` exits 1 → the "npm test must be clean" gate can't be evaluated stack-free). (c) No `.github/workflows/` job runs any test. Fix direction: make the TA-pubkey read injectable or its failure legible; split stack-free vs live-API suites; add a minimal `npm ci && npm test` (stack-free subset) CI job on PRs to staging/main. Reviewer input (PR #337, vcavallo): (a) some tests are flaky/"sometimes fail" and the tester agent tends to hand-wave them — a CI gate must surface flakes, not normalize retry-until-green; (b) e2e/Playwright in CI has a heavy dependency setup and relay-state pollution (hundreds of test tags accumulate on a dev relay) — first CI story is stack-free suites ONLY, e2e deferred; the hosted-throwaway-relay question belongs to that later phase. | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.1 R-E3; verified in a clean remote checkout 2026-07-02. **Picked up 2026-07-05** → book `engineering-team/audits/test-hermeticity-ci/book.md` (epic `test-hermeticity-ci`, branch `feat/test-hermeticity-ci`). **DONE 2026-07-06** — all 4 stories shipped to `staging` via PR #338 (merge `11004374`); AC-5 proven live (CI run 28821124841 green on the book's own PR); book closed high-confidence. Close artifacts + epic folder retirement ride the follow-up in #23. |
| 14 | docs | **OPERATIONS.md drift:** §1 says "six" long-lived branches while §2/§3 say "four"; §4's restrict-deletions ruleset names only main/staging/feature-magic-carpet/relay-discovery/tapestry-discovery — verify the live ruleset covers the three newer sandboxes (`feat/tags`, `feat/communities`, `feat/curate`; auto-delete once deleted `staging`); §1/§7 still list `feat/pubkey-tagging-target`, which no longer exists on origin (renamed `feat/tags`); §7's person↔branch table is stale. Reconcile with `scripts/long-lived-branches.txt` (new single source for the keep-list). | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.7. |
| 15 | docs | **ROADMAP.md refresh:** last touched 2026-05-19; "Current State" still says profile-search-only, predating the shipped live-feed, verified-reporters, note-surfaces, event-page, and open-ranking surfaces; the doc is wired into no loop (not read by Discovery, no whats-open section, no freshness line). Refresh Current State and wire it into Discovery's grounding reads + /close-book's sweep. | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.7. |
| 16 | meta | **live-feed epic: post-close reviews without stories.** Reviews #3–6 (`reviews/live-feed/`, 2026-06-18: note-actions-menu, nostr-entities, mention-names, notecard-refactor) have PASS verdicts but no story files in `stories/live-feed/`, and landed after the live-feed book closed (2026-06-14). Epic retirement for live-feed was deliberately held during the 2026-07-02 sweep. Backfill the four stories (or record why they're review-only) and either open a successor book or fold them into the note-surfaces lineage, then retire the epic. | 2026-07-02 (harness sweep) | OPEN | | Held at sweep time; see commit "retire epics for three closed books". |
| 17 | cleanup | **Retire the event-page, note-surfaces, open-ranking epics.** All three books are Closed with every story Done, but epic retirement never ran (Status still Active, folders not under `done/`). Found by `scripts/harness-lint.sh` (L2) on its first run — waived there pending disposition at the harness-self-improvement story-1 Review gate. | 2026-07-02 (harness-lint first run) | DONE | 2026-07-02 | Operator ratified retire-now at the Review gate: all three epics Done + folders moved under `done/`; the three L2 waivers removed. |
| 18 | meta | **`design-architecture.md` house rule contradicts the AGENTS.md §2 ladder.** `.claude/commands/design-architecture.md` says "If the local stack is not running, ask the user whether to bring it up before proceeding" — written before the ladder existed; web/remote sessions can't bring the stack up, and the ladder's stack-absent branch (firmware → BIBLE ToC) is now the normative path. One-line repoint to AGENTS.md §2. (`plan-feature.md`'s equivalent already points and degrades gracefully.) | 2026-07-04 (story 7 review) | DONE | 2026-07-04 (book close) | Fixed in the harness-self-improvement close commit (retro disposition, audit §7). |
| 19 | meta | **`collect-meta.sh` age trigger is GNU-date-only.** On macOS/BSD `date` (the team's local machines), `date -d` fails, ages degrade to `?`, and `META_MAX_AGE` never rises — so the >30d escalation trigger is dead locally; only the ≥3-count trigger fires. Candidate fix: BSD `date -j -f '%Y-%m-%d'` fallback in `scripts/lib/collect-meta.sh` (one place — the lib is the single source). Pre-existing story-4 behavior, surfaced at story-6 review. | 2026-07-04 (book retro) | DONE | 2026-07-06 | Fixed by story `test-hermeticity-ci` #3: shared `scripts/lib/date-epoch.sh` GNU/BSD fallback consumed by collect-meta ages + lint L9 + stats durations — the >30d trigger is live on macOS (review 3 PASS). |
| 20 | meta | **The SessionStart hook never shipped: `.claude/settings.json` is gitignored and absent.** Story 6 (harness-self-improvement) configured the digest hook in `.claude/settings.json` and listed the path in `scripts/harness-def-paths.txt:29`, but `.gitignore:109` (`.claude/*`) has no un-ignore for it — the file has zero git history and exists on no checkout (it lived only in the cloud session where story 6 ran, where the hook was genuinely verified firing). Effect: the digest runs nowhere, CLAUDE.md's "auto-runs" claim is prose again, and `test/session-start.test.js`'s existence assertion fails 1/8 in every checkout. Secondary lint blind spot: `harness-lint.sh:242` silently drops nonexistent def paths (`[ -e "$p" ] &&`), so L10 cannot see a def-path row whose file is missing. Fix direction: `!.claude/settings.json` un-ignore + commit the hook file (platform convention: `settings.json` project-shared, `settings.local.json` personal); add a lint check for def-path rows that don't exist on disk. | 2026-07-05 (row-13 book recon) | DONE | 2026-07-06 | Fixed by story `test-hermeticity-ci` #3: `.gitignore` un-ignore + tracked `.claude/settings.json` (hook ships to every checkout) + lint L12 (def-path rows must exist on disk — the blind spot that hid this) (review 3 PASS). Lesson class stands: "verified firing in-session" ≠ "shipped". |
| 21 | meta | **`check_L8` crashes under bash-3.2 `set -u` on trees with zero wiring files.** `scripts/harness-lint.sh` (~:198) expands an empty `files[@]` array — `unbound variable` on macOS system bash 3.2 — when no wiring file matches. The real repo and all suite fixtures always have wiring files, so nothing hits it today; found by review 3's audit. Same class as the bash-3.2 empty-array guards already applied to `violation()` and whats-open `def_paths`; one-line `${files[@]+…}` guard candidate. | 2026-07-06 (review 3) | OPEN | | Ride the next harness-lint touch. |
| 22 | meta | **`ci-test-job` W5's ordering check reads comment prose.** `test/ci-test-job.test.js` W5 asserts `npm ci` precedes `npm test` via `indexOf` over the whole whitespace-flattened workflow — comments included — so a future comment in `.github/workflows/test.yml` mentioning either command *before* the `steps:` block would false-fail it (it caught, harmlessly, the impl's own header comment during story #4). Low-severity test-robustness item: scope the check to the `steps:` region on the next touch of that suite. | 2026-07-06 (review 4) | OPEN | | Assertion is otherwise correct; contract (ci-before-test) holds. |
| 23 | docs | Ship the `test-hermeticity-ci` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed, epic `Status: Done`) **+ epic folder retirement** (`stories`/`decisions`/`reviews` `test-hermeticity-ci/` → `done/test-hermeticity-ci/`, one `git mv` each) to `staging` as a follow-up — the code already merged via PR #338, but this close commit is branch-local on `feat/test-hermeticity-ci`. Land it on `staging`, then delete `feat/test-hermeticity-ci` (local + remote). Mirrors the live-feed (#4) / open-ranking (#11) / verified-muters (#12) closes. **Not prod-held** — this is harness/CI/test infra; it ships to staging on its own cadence, independent of the prod-held feature bundle (#9/#12). | 2026-07-06 (test-hermeticity-ci close) | OPEN | | Larger deferred scope (required-check flip, e2e-in-CI, cross-module clones, live-suite stabilization) lives in `audits/test-hermeticity-ci/audit.md` §6 — don't duplicate here. |
| 24 | ops | **`brainstorm.world` → `tapestry.brainstorm.world` cutover — residual loose ends.** The apex `brainstorm.world` moved to the **NosFabrica** codebase (host `74.208.86.220`); Tapestry `main` now serves at `tapestry.brainstorm.world` on the *same old prod droplet* `159.203.150.156` (deploy via renamed `deploy-tapestry.yml` + `DEPLOY_*_TAPESTRY` secrets). DONE: prod deploy (PR #341) live + smoke-verified; ORE personalized-stats gate live (ADR open-ranking/0005); doc/skill sweep. **PENDING:** (a) **firmware reinstall** on the droplet for the new `communityReference` concept blocks (AGENTS.md §6) — operator SSH action, not yet done; (b) **decide** `ui/src/pages/BrainstormPersonalization.jsx:42` — a hardcoded UI link to `https://brainstorm.world` now pointing at NosFabrica; left as-is because context reads as the NosFabrica trust-score ecosystem, but a maintainer should confirm intent; (c) OPERATIONS.md §9.9 audit-checklist droplet list still names `brainstorm.world` + says "four" droplets — folds into the §1/§9 drift reconciliation in **#14** (not repointed piecemeal). | 2026-07-11 (cutover) | OPEN | | Cutover done via PRs #340 (gate+workflow rename) + #341 (promotion) + the doc-sweep PR. |
| 25 | docs/ops | **`docs/TAG_FEDERATION_OPS.md` is dangerously stale + the tag-sync plan awaits execution.** The runbook's "dcosl is clean, zero junk" claim and its unfiltered 4-kind sync commands predate the junk influx: the 2026-07-12 live census found kind 9999 ≈ 549k fetched / est ~1.28M events (100% non-tags: podcastindex music firehose) and kind 39999 = 31,928 with only **451 tags-family** events — an unfiltered `{"kinds":[9998,9999,39998,39999]}` sync would import ~1.3M junk events per instance. Correct the runbook, then execute the scoped plan: per-instance one-shot `strfry sync wss://dcosl.brainstorm.world --filter '{"kinds":[39999],"#z":[<3 canonical handles under 82b75e47…3833>]}' --dir both` + a custom both-direction router stream with the same filter (drop dead dcosl.brainstorm.social; keep limit:5). Full analysis + instance census (staging 44 = live dcosl read-union; tags.bw 33 = WoT-filtered local; tapestry 1 = near-empty store): session 2026-07-12; verified numbers in the census artifacts. | 2026-07-13 (nip-reorg close sweep) | OPEN | | Also: kind-5 deletions don't ride a `#z` filter (zombie-pin risk — count dcosl kind-5 volume before deciding); all three Docker relays run `writePolicy plugin=""` (no ingest gate — spam surface for a both-direction mesh); `strfry sync` CLI default dir is BOTH (always pass `--dir` in runbooks). |
| 26 | ops | **Promote the protocol-docs batch `staging`→`main` (PRs #344–#352: nip-reorg epic + book close + runbook correction + w14-settlement + its book close).** Docs-only; no prod-hold, no runtime surface; one `/cycle-prod`. | 2026-07-13 (nip-reorg close) | OPEN | | Book closed high-confidence; close-commit itself rides a follow-up PR to staging (this row's cycle-prod should include it). |
| 27 | ops | **Local dev stack is stale vs origin (bind-mounts the shared checkout pinned at PR #305).** Any worktree `npm test` fails 11 tag/pin/TL stack-dependent suites environmentally (missing tags-era endpoints + empty graph), with per-suite count wobble — each nip-reorg story burned a clean-base differential run to prove non-regression. Fix direction: update the shared checkout (coordinate with co-tenant sessions) or restack the local Docker env; interim practice: differential baseline + CI `stack-free` as the binding gate (precedented in `reviews/nip-reorg/*`). | 2026-07-13 (nip-reorg close retro, audit §7) | OPEN | | Related memory: local-dev-stack-empty-graph; the checkout also holds stale node_modules used via symlink for worktree test runs. |
| 28 | meta | **`review-verdict.awk` known-edge fired live (2nd occurrence):** a CHANGES_REQUESTED review parsed PASS-final because prose after the Verdict section contained trailing PASS tokens ("…becomes a PASS", "## On PASS" heading) — L1 fired against the correct story status until the parent reworded two format lines (verdict content untouched; reviewer ratified). First occurrence: the tq-#22 waiver. Candidate fixes: one line in the review template/reviewer role ("no verdict-shaped tokens after the Verdict section"), or a lint warning when a CR-worded file parses PASS. | 2026-07-13 (w14-settlement close retro, audit §7) | OPEN | | `reviews/w14-settlement/1-…` round-1; `scripts/lib/review-verdict.awk` header documents the last-token rule. |
