# Open Items Ledger

The single home for **small or cross-cutting open items that have no other surface** — one-off cleanups, "should we update BIBLE?" decisions, follow-ups too small for a handoff doc, anything that would otherwise be remembered only by the session that noticed it.

**This is NOT the whole backlog.** Big, triaged work lives in its proper surface, and those are *derived* into one view by [`scripts/whats-open.sh`](scripts/whats-open.sh) / the `/whats-open` command:

| Kind of open work | Lives in |
|---|---|
| Triaged-but-unbuilt features / bugs / refactors | [`engineering-team/stories/_intake.md`](engineering-team/stories/_intake.md) |
| Session-to-session "I left this open" notes | `docs/*HANDOFF*.md` (`**Status:** 🔴 OPEN`) |
| Per-book deferred scope | `engineering-team/audits/<slug>/audit.md` §6 + `prd-seed.md` §7 |
| Open protocol problems | [`protocols/worksheet.md`](protocols/worksheet.md) |
| In-flight code | open PRs / unmerged branches (git) |
| **Small / cross-cutting / homeless items** | **this file** |

Run **`/whats-open`** (or `bash scripts/whats-open.sh`) for the unified roll-up across all of the above plus this ledger.

## How to use this ledger
- Add a row when you finish a session with a small loose end that has no other home. Keep it one line; link to detail if it has any.
- Flip **Status** to `DONE` (don't delete) when handled — the closed rows are the audit trail.
- **Type:** `bug` · `feature` · `protocol` · `docs` · `cleanup` · `meta`.
- **Opened / Done:** ISO date + the session/book/PR that raised or resolved it.
- **Meta escalation:** `meta` rows are harness lessons (a doc that misled a session, a process defect, a proposed amendment). `/whats-open` lists them with their age and — at the thresholds the script defines (any item aged past ~a month, or several open at once) — prints an escalation banner: at triage, group the related items and **propose a concrete harness story** (title + the items it closes) rather than letting the inbox silt up. See `.claude/commands/whats-open.md` step 2.

## Items

| # | Type | Item | Opened | Status | Done | Pointer |
|---|---|---|---|---|---|---|
| 1 | cleanup | Review/flip the stale handoff status: `docs/POST_TIMEOUT_FIX_COMPLETION_HANDOFF_2026-05-26.md` is still `🔴 OPEN`, but its "24–48h passive-verification window" (late May 2026) is long past — confirm the verification held and flip to `✅ ADDRESSED`, or capture whatever remains. | 2026-06-14 | DONE | 2026-06-14 | Flipped to ✅ ADDRESSED: timeout series shipped (6 prod PRs), no regression in the ~2.5 wks since, residuals tracked in `_intake.md`; held branch `fix/launch-child-task-protection-audit` retained by design as the ADR 0023 audit trail. |
| 2 | cleanup | Delete the merged branches `feat/reputation-info-popup` and `chore/clarify-gate5-status-flip`. | 2026-06-14 (reputation-info-popup) | DONE | 2026-06-14 | branches deleted; remotes were auto-deleted on merge |
| 3 | docs | Decide whether BIBLE needs a `reputation-info-popup` entry. | 2026-06-14 (reputation-info-popup) | DONE | 2026-06-14 | No — it's a UI explainer, not architecture / protocol / data-model / API; out of BIBLE's scope. Recorded here so the judgment isn't lost. |
| 4 | cleanup | The `live-feed` book's **feature** is merged to `staging` (PR #296, `80a39afd`), but the **post-merge audit trail** (Stage 2–3 journal entries + `completion-report.md` + `tier4-dom-extract.txt` + `audit.md` + `prd-seed.md` + the book-close commit) lives only on the local `feat/live-feed` branch. Merge it to `staging` as a **docs-only PR**, then delete `feat/live-feed` (local + remote). | 2026-06-15 (live-feed) | DONE | 2026-06-15 | Audit trail merged to `staging` via PR #297 (`75a12622`, docs-only); `feat/live-feed` deleted (local + remote — remote was auto-deleted on the #297 merge). Engineering hygiene + deferred scope from this book live in `audits/live-feed/audit.md` §6. |
| 5 | docs | verified-reporters #4 (Report Type + Reported columns) shipped to prod (PRs #299→staging, #300→main) — decide BIBLE/doc impact. | 2026-06-16 (verified-reporters #4) | DONE | 2026-06-16 | **Yes, BIBLE warranted** (it tracks this feature family): added a changelog entry for #4. While in BIBLE, also (a) added the missing **live-feed** changelog entry (it had shipped to prod with no BIBLE line) and (b) corrected stale "On staging; prod held" notes on the verified-followers (#33/#34) and profile (#35/#36) lines — both are on `main`/prod now. Updated `engineering-team/epics/verified-reporters.md` (story #4 + ADR 0004; report-type-breakdown now *partial*). **No** protocol/AGENTS/data-model edit: `report_type`/`timestamp` pre-existed on the `REPORTS` edge (ingestion-set), NIP-56 is an external spec we consume, and BIBLE doesn't enumerate edge properties. |
| 7 | feature | The profile page will show **two "Hops" figures with different PoVs** once profile #38 ships: the new prominent counts-row **HOPS** (live `shortestPath` from the logged-in viewer, else the **Owner**) and the existing Reputation trust card **"Hops" / "Degrees of separation"** (precomputed, a different PoV). Accepted as a known discrepancy for now (per [`stories/profile/38-profile-follows-hops.md`](engineering-team/stories/profile/38-profile-follows-hops.md) — "no reconciliation"). Reconcile the two PoVs in a future story. | 2026-06-17 (follows-hops) | OPEN | | Deliberate deferral confirmed by the requester during test design. |
| 8 | docs | **Never-clobber doesn't survive a firmware reinstall** (live-verified 2026-06-17, tag-federation Story 2). The b-tag emitter's never-clobber (ADR 0034 fp1 / ADR 0030 fp3) only protects *within* an install run: firmware `pass1`/`pass2` rebuild each TA header from the static concept definition (no `b`) before `pass_communityReferences` runs, so the emitter always re-seeds the firmware-default `b` on reinstall — an operator's manually re-pointed `b` is reverted. **Accepted as defensible** (reinstall = restore firmware defaults; requester agreed 2026-06-17). Documented in ADR 0002 (Consequences) + ADR 0034 should note the within-run-only scope. Future idea (requester): a "firmware update" path so a reinstall pulls the *updated* version rather than always the baked default — not a current concern. | 2026-06-17 (tag-federation Story 2 live verify) | OPEN | | The map deliverable is unaffected (one `b`, one edge, correct target after every install); only the operator-re-point-survives-reinstall guarantee is narrowed. |
| 6 | cleanup | Local Docker stack runs a **stale neo4j image** (predates commit `c62f6a79`): the live `/etc/supervisor/conf.d/tapestry.conf` lacks `NEO4J_CONF="/etc/neo4j"`, so neo4j boots with the wrong `--config-dir` and **GDS + APOC are not loaded at runtime** (`gds.version()`/`apoc.version()` are unknown functions; 0 `gds.*`/`apoc.*` procedures, only the 48 bare-Community kernel procs), and the local graph holds only **1 `NostrUser`** node. A fresh `docker build` from current source loads both plugins. Until rebuilt, don't trust the local stack for graph/GDS/APOC features — verify them on **staging** (≈ prod scale). | 2026-06-17 (follows-hops feasibility) | OPEN | | Found via a live container probe during the follows-hops shortest-path investigation; JARs are present in `/var/lib/neo4j/plugins/` but unscanned because the running image points `server.directories.plugins` at the empty `/usr/share/neo4j/plugins`. |
| 9 | docs | **Staging-held note/event batches + prod sequencing.** Three additive batches are on `staging` but **not on `main`/prod**: (a) Live-feed enhancements + shared note module (2026-06-18), (b) `note-surfaces` — profile **Content** section + **`/user/:pubkey/notes`** (PR #319, `da269ba8`), (c) `event-page` — the **`/event`** view (PR #320, `a4ae90bc`). They **stack**: (b) reuses (a)'s `NoteCard`/`enrichNotes`; (c) reuses (a) + the by-author read path from (b) + adds `src/api/_shared/relaySource.js`. A prod promotion must carry them **in order a→b→c** (or together). Related staging-held work: the tags/identity-popover bundle (`#306`) and profile #38/#39 (follows-hops). Decide the prod plan when ready. | 2026-06-19 | OPEN | | Held at staging per operator (both #319 + #320). Close-books: `engineering-team/audits/{note-surfaces,event-page}/`. Re-typed meta→docs at the 2026-07-04 book retro: predates story 4's meta=harness-lesson definition; it is a prod-sequencing hold, not a harness lesson. |
| 10 | docs | Decide BIBLE/doc impact of `note-surfaces` + `event-page` (shipped to staging). | 2026-06-19 | DONE | 2026-06-19 | **Yes, BIBLE warranted** (new public read-path **endpoints** + relay-union/outbox **architecture** are in BIBLE scope, unlike a pure UI explainer — cf. #3/#5). Added: a §11 **"Notes & Events (read paths)"** subsection documenting `/api/feed` (backfilled — it had no row), `/api/user/:pubkey/notes`, `/api/event`; updated §13 "Shared note rendering" consumers (feed + Content section + `/user/:pubkey/notes` + `/event`, sharing `_shared/relaySource.js`); added the two route-tree lines; and two changelog entries. Recorded here so the judgment isn't re-litigated. |
| 11 | docs | Ship the `open-ranking` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed) **+ BIBLE §28** to `staging` as a **docs-only PR** — the *endpoints* are already live (#318/#322), but the as-built docs are branch-local on `feat/open-ranking`. Then delete `feat/open-ranking` (local + remote). | 2026-06-19 (open-ranking) | DONE | 2026-06-19 | Bulk close artifacts + BIBLE §28 + ADRs shipped via #326 (feature PR); the re-close (audit §7 + book→Closed) + ORE doc-consistency fixes shipped via the `open-ranking` docs-only PR; `feat/open-ranking` auto-deleted on that merge. Mirrors the live-feed close (#4). The larger ORE carry-forward — **Story 3** (personalized search, W13), the **W12 personalized-stats prod gate**, other ORE endpoints — lives in `audits/open-ranking/audit.md` §6 + `prd-seed.md` §7; don't duplicate it here. |
| 12 | docs | Ship the `verified-muters` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed) + the epic retirement (`stories`/`decisions`/`reviews` `→ done/verified-muters/`, epic `Status: Done`) — the feature already shipped to `staging` via PR #333 (`e4a505a2`, deploy `27914727602` green), but the close commit is branch-local on `feat/verified-muters`. Land it on `staging`, then delete `feat/verified-muters` (local + remote). Mirrors the live-feed (#4) / open-ranking (#11) closes. | 2026-06-21 (verified-muters) | OPEN | | Director-mode book close. The feature is on staging and **prod-held** — it stacks behind the other staging-held bundles in #9 (note-surfaces/event-page) and #306/#38/#39; sequence the prod promotion with those. Larger deferred scope (DRY `<GrapevineList>` refactor; per-PoV muter counts; unverified/all-muters view; cutoff source-of-truth) lives in `audits/verified-muters/audit.md` §6 + `prd-seed.md` §6 — don't duplicate here. |
| 13 | bug | **Test-suite hermeticity + no CI.** (a) In a bare checkout (no `node_modules`), live-feed test B9 fails misleadingly: `feedReadPath.js:133` reaches a non-injected `require('../../utils/assistantKeys')` whose transitive top-level `require('nostr-tools')` throws, and the silent catch degrades to fallback relays before the injected mock is hit. (b) With deps installed but no local stack, ~12 live-API suites fail with `fetch failed` (expected, but `npm test` exits 1 → the "npm test must be clean" gate can't be evaluated stack-free). (c) No `.github/workflows/` job runs any test. Fix direction: make the TA-pubkey read injectable or its failure legible; split stack-free vs live-API suites; add a minimal `npm ci && npm test` (stack-free subset) CI job on PRs to staging/main. Reviewer input (PR #337, vcavallo): (a) some tests are flaky/"sometimes fail" and the tester agent tends to hand-wave them — a CI gate must surface flakes, not normalize retry-until-green; (b) e2e/Playwright in CI has a heavy dependency setup and relay-state pollution (hundreds of test tags accumulate on a dev relay) — first CI story is stack-free suites ONLY, e2e deferred; the hosted-throwaway-relay question belongs to that later phase. | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.1 R-E3; verified in a clean remote checkout 2026-07-02. **Picked up 2026-07-05** → book `engineering-team/audits/test-hermeticity-ci/book.md` (epic `test-hermeticity-ci`, branch `feat/test-hermeticity-ci`). **DONE 2026-07-06** — all 4 stories shipped to `staging` via PR #338 (merge `11004374`); AC-5 proven live (CI run 28821124841 green on the book's own PR); book closed high-confidence. Close artifacts + epic folder retirement ride the follow-up in #23. |
| 14 | docs | **OPERATIONS.md drift:** §1 says "six" long-lived branches while §2/§3 say "four"; §4's restrict-deletions ruleset names only main/staging/feature-magic-carpet/relay-discovery/tapestry-discovery — verify the live ruleset covers the three newer sandboxes (`feat/tags`, `feat/communities`, `feat/curate`; auto-delete once deleted `staging`); §1/§7 still list `feat/pubkey-tagging-target`, which no longer exists on origin (renamed `feat/tags`); §7's person↔branch table is stale. Reconcile with `scripts/long-lived-branches.txt` (new single source for the keep-list). Same drift family, FIXED 2026-07-16: staging's copy of `.github/workflows/deploy-tags.yml` still named `feat/pubkey-tagging-target` (trigger + droplet checkout script) — repointed to mirror `feat/tags`' live copy (`85683b46`, the one actually firing deploys since 2026-06-19); operator-present hotfix, this row is its trace. The OPERATIONS.md §1/§7/§9.9 prose reconciliation remains open. | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.7. |
| 15 | docs | **ROADMAP.md refresh:** last touched 2026-05-19; "Current State" still says profile-search-only, predating the shipped live-feed, verified-reporters, note-surfaces, event-page, and open-ranking surfaces; the doc is wired into no loop (not read by Discovery, no whats-open section, no freshness line). Refresh Current State and wire it into Discovery's grounding reads + /close-book's sweep. | 2026-07-02 (harness sweep) | OPEN | | `docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md` §4.7. |
| 16 | meta | **live-feed epic: post-close reviews without stories.** Reviews #3–6 (`reviews/live-feed/`, 2026-06-18: note-actions-menu, nostr-entities, mention-names, notecard-refactor) have PASS verdicts but no story files in `stories/live-feed/`, and landed after the live-feed book closed (2026-06-14). Epic retirement for live-feed was deliberately held during the 2026-07-02 sweep. Backfill the four stories (or record why they're review-only) and either open a successor book or fold them into the note-surfaces lineage, then retire the epic. | 2026-07-02 (harness sweep) | OPEN | | Held at sweep time; see commit "retire epics for three closed books". |
| 17 | cleanup | **Retire the event-page, note-surfaces, open-ranking epics.** All three books are Closed with every story Done, but epic retirement never ran (Status still Active, folders not under `done/`). Found by `scripts/harness-lint.sh` (L2) on its first run — waived there pending disposition at the harness-self-improvement story-1 Review gate. | 2026-07-02 (harness-lint first run) | DONE | 2026-07-02 | Operator ratified retire-now at the Review gate: all three epics Done + folders moved under `done/`; the three L2 waivers removed. |
| 18 | meta | **`design-architecture.md` house rule contradicts the AGENTS.md §2 ladder.** `.claude/commands/design-architecture.md` says "If the local stack is not running, ask the user whether to bring it up before proceeding" — written before the ladder existed; web/remote sessions can't bring the stack up, and the ladder's stack-absent branch (firmware → BIBLE ToC) is now the normative path. One-line repoint to AGENTS.md §2. (`plan-feature.md`'s equivalent already points and degrades gracefully.) | 2026-07-04 (story 7 review) | DONE | 2026-07-04 (book close) | Fixed in the harness-self-improvement close commit (retro disposition, audit §7). |
| 19 | meta | **`collect-meta.sh` age trigger is GNU-date-only.** On macOS/BSD `date` (the team's local machines), `date -d` fails, ages degrade to `?`, and `META_MAX_AGE` never rises — so the >30d escalation trigger is dead locally; only the ≥3-count trigger fires. Candidate fix: BSD `date -j -f '%Y-%m-%d'` fallback in `scripts/lib/collect-meta.sh` (one place — the lib is the single source). Pre-existing story-4 behavior, surfaced at story-6 review. | 2026-07-04 (book retro) | DONE | 2026-07-06 | Fixed by story `test-hermeticity-ci` #3: shared `scripts/lib/date-epoch.sh` GNU/BSD fallback consumed by collect-meta ages + lint L9 + stats durations — the >30d trigger is live on macOS (review 3 PASS). |
| 20 | meta | **The SessionStart hook never shipped: `.claude/settings.json` is gitignored and absent.** Story 6 (harness-self-improvement) configured the digest hook in `.claude/settings.json` and listed the path in `scripts/harness-def-paths.txt:29`, but `.gitignore:109` (`.claude/*`) has no un-ignore for it — the file has zero git history and exists on no checkout (it lived only in the cloud session where story 6 ran, where the hook was genuinely verified firing). Effect: the digest runs nowhere, CLAUDE.md's "auto-runs" claim is prose again, and `test/session-start.test.js`'s existence assertion fails 1/8 in every checkout. Secondary lint blind spot: `harness-lint.sh:242` silently drops nonexistent def paths (`[ -e "$p" ] &&`), so L10 cannot see a def-path row whose file is missing. Fix direction: `!.claude/settings.json` un-ignore + commit the hook file (platform convention: `settings.json` project-shared, `settings.local.json` personal); add a lint check for def-path rows that don't exist on disk. | 2026-07-05 (row-13 book recon) | DONE | 2026-07-06 | Fixed by story `test-hermeticity-ci` #3: `.gitignore` un-ignore + tracked `.claude/settings.json` (hook ships to every checkout) + lint L12 (def-path rows must exist on disk — the blind spot that hid this) (review 3 PASS). Lesson class stands: "verified firing in-session" ≠ "shipped". |
| 21 | meta | **`check_L8` crashes under bash-3.2 `set -u` on trees with zero wiring files.** `scripts/harness-lint.sh` (~:198) expands an empty `files[@]` array — `unbound variable` on macOS system bash 3.2 — when no wiring file matches. The real repo and all suite fixtures always have wiring files, so nothing hits it today; found by review 3's audit. Same class as the bash-3.2 empty-array guards already applied to `violation()` and whats-open `def_paths`; one-line `${files[@]+…}` guard candidate. | 2026-07-06 (review 3) | DONE | 2026-07-25 | Ride the next harness-lint touch. **DONE 2026-07-25 (harness-gate-integrity #1):** `check_L8` length-guarded (the `violation()`/whats-open precedent); regression fixture in `harness-lint.test.js` runs the real crash path on bash 3.2.57. Reviewed PASS (97b22247) on `feat/harness-gate-integrity`; not yet on staging. |
| 22 | meta | **`ci-test-job` W5's ordering check reads comment prose.** `test/ci-test-job.test.js` W5 asserts `npm ci` precedes `npm test` via `indexOf` over the whole whitespace-flattened workflow — comments included — so a future comment in `.github/workflows/test.yml` mentioning either command *before* the `steps:` block would false-fail it (it caught, harmlessly, the impl's own header comment during story #4). Low-severity test-robustness item: scope the check to the `steps:` region on the next touch of that suite. | 2026-07-06 (review 4) | DONE | 2026-07-25 | Assertion is otherwise correct; contract (ci-before-test) holds. **DONE 2026-07-25 (harness-gate-integrity #1):** W5 ordering scoped to the `steps:` region via a `ciBeforeTest()` helper; W5b proves a pre-`steps:` comment no longer false-fails and a real gate-before-install still fails. Reviewed PASS (97b22247); not yet on staging. |
| 23 | docs | Ship the `test-hermeticity-ci` book's close artifacts (`audit.md`, `prd-seed.md`, `book.md` Closed, epic `Status: Done`) **+ epic folder retirement** (`stories`/`decisions`/`reviews` `test-hermeticity-ci/` → `done/test-hermeticity-ci/`, one `git mv` each) to `staging` as a follow-up — the code already merged via PR #338, but this close commit is branch-local on `feat/test-hermeticity-ci`. Land it on `staging`, then delete `feat/test-hermeticity-ci` (local + remote). Mirrors the live-feed (#4) / open-ranking (#11) / verified-muters (#12) closes. **Not prod-held** — this is harness/CI/test infra; it ships to staging on its own cadence, independent of the prod-held feature bundle (#9/#12). | 2026-07-06 (test-hermeticity-ci close) | OPEN | | Larger deferred scope (required-check flip, e2e-in-CI, cross-module clones, live-suite stabilization) lives in `audits/test-hermeticity-ci/audit.md` §6 — don't duplicate here. |
| 24 | ops | **`brainstorm.world` → `tapestry.brainstorm.world` cutover — residual loose ends.** The apex `brainstorm.world` moved to the **NosFabrica** codebase (host `74.208.86.220`); Tapestry `main` now serves at `tapestry.brainstorm.world` on the *same old prod droplet* `159.203.150.156` (deploy via renamed `deploy-tapestry.yml` + `DEPLOY_*_TAPESTRY` secrets). DONE: prod deploy (PR #341) live + smoke-verified; ORE personalized-stats gate live (ADR open-ranking/0005); doc/skill sweep. **PENDING:** (a) ~~firmware reinstall~~ RESOLVED 2026-07-16 — prod reinstalled to 39/39 at the #366 tags-stack promotion (see #39); any prior pending state is moot after the full pass1+pass2 rerun; (b) **decide** `ui/src/pages/BrainstormPersonalization.jsx:42` — a hardcoded UI link to `https://brainstorm.world` now pointing at NosFabrica; left as-is because context reads as the NosFabrica trust-score ecosystem, but a maintainer should confirm intent; (c) OPERATIONS.md §9.9 audit-checklist droplet list still names `brainstorm.world` + says "four" droplets — folds into the §1/§9 drift reconciliation in **#14** (not repointed piecemeal). | 2026-07-11 (cutover) | OPEN | | Cutover done via PRs #340 (gate+workflow rename) + #341 (promotion) + the doc-sweep PR. |
| 25 | docs/ops | **`docs/TAG_FEDERATION_OPS.md` is dangerously stale + the tag-sync plan awaits execution.** The runbook's "dcosl is clean, zero junk" claim and its unfiltered 4-kind sync commands predate the junk influx: the 2026-07-12 live census found kind 9999 ≈ 549k fetched / est ~1.28M events (100% non-tags: podcastindex music firehose) and kind 39999 = 31,928 with only **451 tags-family** events — an unfiltered `{"kinds":[9998,9999,39998,39999]}` sync would import ~1.3M junk events per instance. Correct the runbook, then execute the scoped plan: per-instance one-shot `strfry sync wss://dcosl.brainstorm.world --filter '{"kinds":[39999],"#z":[<3 canonical handles under 82b75e47…3833>]}' --dir both` + a custom both-direction router stream with the same filter (drop dead dcosl.brainstorm.social; keep limit:5). Full analysis + instance census (staging 44 = live dcosl read-union; tags.bw 33 = WoT-filtered local; tapestry 1 = near-empty store): session 2026-07-12; verified numbers in the census artifacts. | 2026-07-13 (nip-reorg close sweep) | OPEN | | Also: kind-5 deletions don't ride a `#z` filter (zombie-pin risk — count dcosl kind-5 volume before deciding); all three Docker relays run `writePolicy plugin=""` (no ingest gate — spam surface for a both-direction mesh); `strfry sync` CLI default dir is BOTH (always pass `--dir` in runbooks). |
| 26 | ops | **Promote the protocol-docs batch `staging`→`main` (PRs #344–#352: nip-reorg epic + book close + runbook correction + w14-settlement + its book close).** Docs-only; no prod-hold, no runtime surface; one `/cycle-prod`. | 2026-07-13 (nip-reorg close) | DONE | 2026-07-13 | Promoted via PR #353 (merge fa7d2e4f, deploy 84s, tapestry.brainstorm.world smoke clean; all Tier-3 artifacts verified on origin/main). |
| 27 | ops | **Local dev stack is stale vs origin (bind-mounts the shared checkout pinned at PR #305).** Any worktree `npm test` fails 11 tag/pin/TL stack-dependent suites environmentally (missing tags-era endpoints + empty graph), with per-suite count wobble — each nip-reorg story burned a clean-base differential run to prove non-regression. Fix direction: update the shared checkout (coordinate with co-tenant sessions) or restack the local Docker env; interim practice: differential baseline + CI `stack-free` as the binding gate (precedented in `reviews/nip-reorg/*`). | 2026-07-13 (nip-reorg close retro, audit §7) | OPEN | | Related memory: local-dev-stack-empty-graph; the checkout also holds stale node_modules used via symlink for worktree test runs. 2026-07-16 (router book close): with the Docker daemon fully DOWN, plain `npm test` mirrors CI's stack-free job exactly (live suites self-skip → clean Overall PASS is required, no environmental allowance); the differential baseline is only needed while the stale stack is UP. |
| 28 | meta | **`review-verdict.awk` known-edge fired live (2nd occurrence):** a CHANGES_REQUESTED review parsed PASS-final because prose after the Verdict section contained trailing PASS tokens ("…becomes a PASS", "## On PASS" heading) — L1 fired against the correct story status until the parent reworded two format lines (verdict content untouched; reviewer ratified). First occurrence: the tq-#22 waiver. Candidate fixes: one line in the review template/reviewer role ("no verdict-shaped tokens after the Verdict section"), or a lint warning when a CR-worded file parses PASS. | 2026-07-13 (w14-settlement close retro, audit §7) | OPEN | | `reviews/w14-settlement/1-…` round-1; `scripts/lib/review-verdict.awk` header documents the last-token rule. |
| 29 | meta | **`/plan-feature` entry skips the Phase-0 book-open (eager anchor).** CLAUDE.md's routing table sends engineering asks straight to `/plan-feature`, but the eager `audits/<book-slug>/book.md` open lives in intake — entering via the command left no book, so review-time completion detection had no anchor (relay-management #1; remedied same session by retroactive backfill at `audits/sync-panel-tag-filters/book.md`). Candidate fix: a step-0 in `plan-feature.md` that opens/verifies the book when none covers the ask, or a routing-table note sending fresh asks through Phase 0 first. | 2026-07-15 (relay-management #1 review) | OPEN | | `reviews/relay-management/1-sync-panel-tag-filters.md` § Harness friction. |
| 30 | ops | **Promote the sync-panel tag-filters feature `staging`→`main` when ready.** PR #355 (merge `08d0b5c0`) is staging-verified (book `audits/sync-panel-tag-filters/`, closed 2026-07-15); operator deliberately held promotion at book close. Note the standing staging-held stack it would ride with (see #9's sequencing). One `/cycle-prod` when the operator says go. | 2026-07-15 (sync-panel-tag-filters close) | DONE | 2026-07-15 | Promoted via PR #357 (merge `1845bf33`, deploy-tapestry 83s). tapestry.brainstorm.world smoke clean: bundle `index-BRNifkxP.js` (identical to staging-verified build) carries the panel strings; count endpoint echoes `#z` through the rebuilt filter (read-only architectural check; data-discriminating count proven on staging 12/0/400 — prod store near-empty of 39999s per census). |
| 31 | bug | **Router Management pre-existing hardening trio** (surfaced by ADR relay-management/0002's live-config evidence + story #2 review, all pre-dating story #2): (a) `generateConfig` interpolates `urls`/`pluginDown`/`pluginUp` into the router config **raw** (`src/api/strfry/routerConfig.js:146–159`) — a quote/brace in a relay URL or plugin path corrupts config grammar (tag-filter values are safe: they ride `JSON.stringify`); (b) `sanitizeStreamFilter` accepts **negative** integers for `since`/`until`/`limit`/`kinds` (`Number.isInteger`), and the editor's `parseInt` lets them in; (c) the stream **editor crashes on a hand-edited kinds-less filter** (`ui/src/pages/settings/RelaySettings.jsx:118` reads `filter.kinds.join` unguarded — the read card + config emit handle it fine). One bounded hardening story. | 2026-07-16 (relay-management #2 review) | OPEN | | ADR 0002 § Consequences; `reviews/relay-management/2-router-stream-tag-filters.md` § Findings (non-blocking 1–3). |
| 32 | bug | **Cross-epic scope-guard tension:** `test/b-tag-primitive.test.js` + `test/b-tag-seeds.test.js` assert "exactly 4 concepts carry a `communityReference`" (`nostr-relay`, `tag`, `nostr-user-tag`, `tag-pinning`), but the **event-tagging Story 3** firmware seed added `nostr-event-tag` + `tagging-with-specific-tag`, which each carry one per the federation model — so both guards now FAIL (1 each). Reconcile: either widen the b-tag guards to expect the two event-tagging concepts, or decide those concepts shouldn't carry a `communityReference`. | 2026-06-30 (event-tagging Story 5 review) | DONE | 2026-07-07 | **Resolved: widened the guards** — the Story-3 seeds were deliberate per the federation model, so `b-tag-seeds` SCOPE GUARD + `b-tag-primitive` AC-8 now expect the 6-concept sanctioned set (nostr-relay + 3 tag concepts + nostr-event-tag + tagging-with-specific-tag). Both suites green (7/7, 16/16). |
| 33 | enhancement | **NIP-07 signer/session guard — roll out to ALL signed write paths.** The session identity (`user.pubkey`, login-time) can drift from the extension's active account (write-time), silently publishing as the wrong account. Added `ui/src/utils/signerGuard.js` + wired into the **event-tagging** write path (`useEventTagging`, Story 16 / ADR 0014). Remaining: profile-tag (`publishProfileTagAssertion`) + pin (`publishTagPin`) paths, ideally via a lower-level chokepoint. | 2026-07-01 (unified-tagging Story 16) | DONE | 2026-07-01 | Done in `8bddbc89` (item #4): module-level session-pubkey mirror (setSessionPubkey/getSessionPubkey, published by AuthContext) + getActiveSignerOrThrow() — profile-tag, pin, unpin, and both NIP-51 exports guarded, no per-caller threading. Issue [#335](https://github.com/nous-clawds4/tapestry/issues/335) closed. |
| 34 | enhancement | **Tomorrow's firmware follow-up to `tag-applicability` #1: re-stamp existing tags with the type z-hints** (David 2026-07-06, deferred so Gaby can use the picker today). **Caveat to start from:** the type-z (`tag-for-nostr-pubkey`/`tag-for-nostr-event`) is **author-only**, and there are **no TA-authored tag-elements** — `firmware/install.js` seeds only 39998 concept headers (no topic tags), and every live tag-element is **user-authored via NIP-07** (no TA-side 39999 signing anywhere). So a "re-stamp" can only touch tags whose **own author** re-publishes them (e.g. the operator's handful of test tags), not the shared vocabulary — which is exactly why Story 1 is HINT ∪ USAGE. Verify the actual authorship of the live `tags.brainstorm.world` tag-elements before planning the re-stamp. Not needed for today: Story 1's derived lists cover existing tags via USAGE and new tags via the hint. | 2026-07-06 (tag-applicability #1 Architecture) | OPEN | | Raised during Story 1 architecture; David wants the re-stamp "tomorrow, not today." |
| 35 | enhancement | **POV-selectable tag surfaces — event-tags/taggings/applicability are house-POV-only.** Brainstorm's model is "pick from a few offered POVs, or your own," and **search already does it** (`UserMenu` house/nosfabrica/user → `BrainstormSearch.jsx:834` threads `wotPov`). But the tag surfaces hardcode house: `useEventTags`/`for-event` + the `for-tag` tag pages pass only `viewerPubkey` (the `mine` channel), not `wotPov`; `/api/tags/applicability` + `useTagApplicability` are `wotPov:'house'`-hardcoded; and `buildTrustPredicate` silently falls back to "count everyone" for an unprovisioned POV. Backend reads already accept `wotPov=user&userPubkey`, so the gap is UI threading + applicability POV-awareness + the fallback UX. Blocks the LFO "select the LFO POV" story. **Full write-up: `engineering-team/stories/_intake.md` 2026-07-06 entry.** | 2026-07-06 (LFO/POV discussion) | DONE-LOCAL (epic `pov-selectable-tag-surfaces` complete + reviewed 2026-07-09, NOT yet deployed: story 1 honor-selected-POV PASS `dedb0551`; story 2 honest-unprovisioned-state PASS `839a91ba`; story 3 rank-threshold-key correctness fix PASS `f14fa54d`; + wording split. Awaiting operator test → deploy) | | Related: `docs/INTEGRATION_GUIDE_event-tagging-for-external-clients.md`; POV-first invariants (CLAUDE.md). **External/named POV provisioning split out** to its own epic — see `_intake.md` 2026-07-09 "NIP-85 is the POV interop layer" (per-customer 30382 pipeline already exists; gaps a–f enumerated; supersedes the member-TL client-side workaround for LFO). |
| 36 | bug/ux | **Note bookmark set (kind-30003) skipped at pin time is unrecoverable from the UI; event-tag reads don't federate.** The Pinned tab's Notes sub-view + the note export are gated on the viewer's 30003 existing (`PinnedListPanel.jsx:476,522`), which is published fire-and-forget at pin time and **skips itself when for-tag returns 0 curated notes**. If it skips, the "Repin notes" affordance is *inside the hidden section* — chicken-and-egg, no way to publish later without unpin/re-pin. Surfaced 2026-07-09 by capbuster032464 on local dev: the tag + tagging + pin live on the tags.b.w relay; local `available-tags` shows the tag via the FEDERATED browse read, but `for-tag` (event-tag stack) scans LOCAL strfry only → 0 notes at pin time → skip → no Notes tab, no note export. Two fixes to weigh: (a) an affordance to (re)publish the note bookmark set when none exists (outside the hidden section); (b) decide whether the event-tag for-tag/for-event reads should federate like the profile-tag browse surfaces do (they deliberately don't today). | 2026-07-09 (capbuster pin on local dev) | OPEN | | Not a regression — pre-existing gating; exposed by the dev/live relay split. |
| 37 | bug | **Profile-tag reads resolve tags by fragile event-id, ignoring the stable a-coordinate they already write.** `publishProfileTag.js:81-82` writes a HYBRID `['a', 39999:<author>:<slug>]` + `['e', tagEventId]` on every profile-tag assertion, but the READ (`profile-tags/index.js:260-262` → `tagEventId` from the `e` tag; `ProfileTagsSection.jsx:40` maps `eventId→tag`, unresolved → `id.slice(0,8)`) resolves STRICTLY by event id and never uses the `a`. So when a tag-element is REPLACED (re-mint of the same slug, or any edit — 39999 is param-replaceable), the old event id vanishes and every referencing profile tag renders as "a bunch of numbers." NOT legacy-only — affects all profile taggings, because the read ignores the `a`. The **event-tag stack does it right** (its tagging-with-specific-tag header references the tag by `a`-coordinate). Fix: make the profile-tag read resolve by `a`-coordinate (or fall back to `a` when the `e` id is dangling) — the hybrid write already provides the data. Live-repro'd 2026-07-06 on `funny` (`39999:791dde…:funny`, elem id `a9af20…`): a profile-flow create then event-flow create of the same slug replaced the tag-element, orphaning the first profile tagging's `e`-ref. Trigger (duplicate-mint) is itself prevented by tag-applicability Story 2 (type-aware picker) + Story 3 (same-slug warning); this read-hardening is the deeper, orthogonal fix. | 2026-07-06 (tag-applicability #1 browser test) | DONE (2026-07-08, `profile-tag-hardening/1` — consume-by-#a; union `#a`∪`#e` at the 3 aggregation reads + `tagAddress` response fields + UI groups by coordinate; review PASS `8a844dc3`) | | Pre-existing; surfaced by Story 1 live testing. Not caused by the z-hint change (z is inert; the replaceable-collision + e-keyed read are unchanged). **Sharpened 2026-07-06 (Story 2 architecture):** this is a **contract violation**, not just fragility — the WRITE path (`publishProfileTagAssertion`, ADR-0022) explicitly documents *"`a` = stable tag identity (consume by #a), `e` = applied-version provenance"* and writes the hybrid a+e for exactly this reason, but the READ consumes `e`. Both **target-selector modals apply the CORRECT tag version** (by a-coordinate: TagANoteModal via `{authorPubkey,slug}`; TagSomeoneModal via `['a',39999:author:slug]`) even for a dual-use tag — verified. The fix is purely to make the profile-tag read honor the existing "consume by #a" contract. |

| 38 | meta | **Post-merge harness reconciliation: feat/tags-era stories vs staging's harness-lint.** The `feat/tags` ↔ `staging` merge brought staging's repo-wide `harness-lint` (harness-self-improvement) into contact with feat/tags-era engineering artifacts written before the lint existed: **L1** ×15 — `stories/event-tagging/` (14 files) + `stories/tag-applicability/4-…` have PASS-final reviews but story Status lines in feat/tags-era phrasing ("Approved", "DONE — PASS (…)", etc.) instead of the lint's expected form; **L3** ×2 — active story folders `pov-selectable-tag-surfaces/` and `profile-tag-hardening/` have no `epics/<name>.md` file. All waived in `scripts/harness-lint-waivers.txt` citing this row. Reconcile properly: normalize the 15 Status lines to lint-recognized values (most are genuinely Done), write the two missing epic files (or retire the epics — pov-selectable is complete+reviewed per #35; profile-tag-hardening #1 passed per #37), then remove the waivers. | 2026-07-15 (feat/tags↔staging merge, CI run 29464735775) | OPEN | | Waivers are the sanctioned bridge (same pattern as row 16's live-feed waivers); statuses encode real historical state — don't bulk-rewrite without reading each story. |
| 39 | ops | **PROD-HOLD: the tags feature stack is on `staging` deliberately un-promoted.** PR #360 (merge `f41f2433`, 2026-07-16) landed the full `feat/tags` stack (~24k insertions: event-tagging, pin/TL, POV tag surfaces, signer guard) on staging; deploy 29512603519 green (2m17s cold build — new UI deps), five-tier smoke clean (tags API + UI live; router tag-filters and sync-panel surfaces intact; bundle now converged with the tags line's). **Do NOT promote `staging`→`main` until the operator explicitly ratifies the tags stack for prod** — any promotion PR now carries all of it (the #9-precedent hold). Also: OPEN.md numbering diverged between this file and `feat/tags`' copy at the two independent merge resolutions — the next tags↔staging sync will conflict here once; resolve content-preserving, numbers cosmetic. | 2026-07-16 (#360 merge) | DONE | 2026-07-16 | Hold ended by operator ratification same day: promoted via PR #366 (merge `ea1ba911`, deploy-tapestry 29514945867 green 2m04s). Prod firmware reinstalled → 39/39 (`nostr-event-tag`, `tagging-with-specific-tag` installed; staging done by operator, prod via API POST). Five-tier prod smoke clean: tags API + UI live (sparse local data as expected), router tag-filters intact, bundle `index-BLjSXI9c.js` converged across staging/prod/tags, console clean. main == staging at `ea1ba911`. |
| 40 | meta | **Region-scope source-level suite assertions in multi-surface files — make it default Tester practice.** Story relay-management #2's ratified reuse broke the sibling suite's file-global single-occurrence assertions (S1 first-match, S3 file-global indexOf); resolved by ADR relay-management/0002 **Amendment 1** (region-scoping, strengthening, 5 mutation spot-checks — the worked precedent). Candidate: one line in the Tester role or test-plan template so new source-level suites over shared files scope to the owning component from day one. | 2026-07-16 (router-stream-tag-filters retro, audit §7) | OPEN | | Folds into the meta-sweep harness story (proposed `_intake.md` 2026-07-15). |
| 41 | meta | **Session-mode standing gate authorization — formalize or forbid.** The router-stream-tag-filters book ran a documented middle path between human-gated and armed Direction mode: the operator's kickoff pre-authorized advancing all five phase gates through staging; blinded gate-judges at Gates 1/2/3/5 (KICK_BACK binding) + an append-only journal served as integrity insurance; prod/tags/close stayed operator-reserved. CLAUDE.md's "sole exception: armed Direction mode" language doesn't name this pattern. It ran clean once (book `audits/router-stream-tag-filters/`, journal = the worked record). Decide at the meta-sweep story: ratify as a named mode (doc amendment) or forbid (kickoffs like this get an armed Direction-mode section instead). **DECIDED (2026-07-26, `operational-direction` #1): BOTH branches taken.** The legitimate need — running a book without hand-writing an experiment pre-registration — is now met by a *named second mode*, **operational direction**, whose terms are derived from an owner-ratified goal rather than authored per run (ADR `operational-direction/0001`). With that mode available, the ad-hoc pattern is **forbidden**: a kickoff that verbally pre-authorizes the gates is not a third mode and takes either an operational goal or an armed section. `roles/director.md` § "Two modes — pick the right one" and CLAUDE.md's doctrine line now name exactly two. | 2026-07-16 (router-stream-tag-filters retro, audit §7) | DONE | 2026-07-26 | Book § "Session mode" states the in-run reasoning; retro finding 7. Resolved by `engineering-team/stories/operational-direction/1-operational-direction-mode.md` + ADR `operational-direction/0001` d8. |
| 42 | bug | **`POST /api/firmware/install` is unauthenticated on the public domains.** Verified live 2026-07-16: the prod firmware reinstall during the #366 promotion succeeded via an anonymous POST to tapestry.brainstorm.world (registered with no auth middleware — `src/api/index.js:284`). Convenient for ops, but it's a state-mutating endpoint (Neo4j firmware pass1+pass2 rebuild, minutes of load; OPEN.md #8: reinstall re-seeds firmware-default `b` pointers) exposed to anyone. Decide: owner-auth gate (parity with other mutating owner surfaces) or deliberate ratified openness. | 2026-07-16 (#366 promotion session) | OPEN | | Surfaced doing the prod reinstall; sweep row from the router book close (audit step 10). |
| 43 | meta | **`test/test.js`'s `overallOk` chain is severed by a stray semicolon — 9 registered suites have never gated.** `const overallOk = …` (test.js:717) terminates at the first `;`; the lines below it (`harnessLint`, `harnessStats`, `sessionStart`, `stackFreeNpmTest`, `ciTestJob`, `syncPanelTagFilters`, `routerStreamTagFilters`) are a **dangling expression statement** — evaluated, discarded, never read. `process.exit(overallOk ? 0 : 1)` (:865) consults only the truncated chain, so those suites run, print PASS/FAIL, and cannot fail the build — including the two suites from the just-closed router-stream-tag-filters book, and CI's own `stack-free`/`ci-test-job` guards. **Measured mechanically 2026-07-16: 109 live `overallOk` terms vs 118 declared `*Result` vars ⇒ 9 non-gating**, not 7 — beyond the 7 severed, `noteTrustedListResult` and `applicabilityRepublishResult` were **never added to the chain at all**, so the one-character fix (`;` → ` &&`) recovers only 7 of 9; the other 2 need their own terms. **Do not cite a line number for the semicolon** — it migrates to whichever suite registers last (it sat at :826, is now :827 after tag-event-inspector #1); find it as the first `;` after `const overallOk =`. All 9 currently PASS, so re-attaching is zero-risk today — but it is a gate-semantics change for other stories' suites and was left out of the tag-event-inspector #1 diff rather than slipped in unratified. Found because that story's own four-touch registration landed in the dead block and had to be moved into the live chain to actually gate. Related: the four-touch ritual has no mechanical check — a `test/test.js` self-assertion ("every `*Result` referenced in the summary appears in the live `overallOk` chain") would have caught this at authoring time and belongs in the same harness story. | 2026-07-16 (tag-event-inspector #1, Test Design; count corrected at Review) | DONE | 2026-07-25 | `test/test.js:717–834`; folds into the meta-sweep harness story (proposed `_intake.md` 2026-07-15) alongside #40. **DONE 2026-07-25 (harness-gate-integrity #1):** chain re-attached (`;`→` &&`) + the 2 never-wired terms added — all 142 `.run()` results now gate; verified by an independent recount (142 terms == 142 declared, 0 un-gated). The self-assertion landed as strengthened G5 (+ new G6) in `stack-free-npm-test.test.js`, keyed on the comment-stripped `chain[1]` — so a future un-wired suite fails the harness. Reviewed PASS (97b22247); not yet on staging. |
| 44 | meta | **CLAUDE.md's "Per-deployment TA pubkey" section misidentifies this machine's local-dev TA, conflating it with the ADR-0015 legacy z-tag literal.** CLAUDE.md:177 states "The local-dev value (`82b75e47...973833` on this machine)" — but the running local stack answers `e00ed09087b831ecf40442c82768b2114b707008916ac801dabbfbe76ae9df36` (`GET /api/assistant/pubkey`, verified 2026-07-16). `82b75e47…` is in fact `LEGACY_Z_TAG_PUBKEY` (`src/api/profile-tags/index.js:49`) / `LEGACY_TA_PUBKEY` (`ui/src/utils/publishTagPin.js:47`) — the ADR-0015 named-exception constant for **z-tag composition**, a different role from "the instance's TA". The section's own point is that these must not be confused, so the stale parenthetical undercuts the rule it states: a reader who trusts it believes the local TA *is* the legacy literal and would "verify" TA-filtering behavior against the wrong key. Fix: drop the machine-specific value (it is per-container and re-created on rebuild — that is the section's whole thesis) and point at the runtime lookup instead; keep the `82b75e47…` mention only where it is correctly labelled as the legacy z-tag literal. Surfaced by tag-event-inspector #1, whose naddr decision (ADR 0001 D5) turns on exactly this three-way distinction: tag naddrs use the **tag's own author**, never the TA and never the legacy literal. | 2026-07-16 (tag-event-inspector #1) | OPEN | | `CLAUDE.md:177`; verified against `GET /api/assistant/pubkey` on the local stack. |
| 45 | bug? | **The unified tag index (`/api/tags/index`) reports far fewer tags than the legacy per-type index (`/api/profile-tags/index`) — on every instance.** Measured live 2026-07-16: **staging 1 vs 39**; **tags.brainstorm.world 10 vs 33**. The unified endpoint is what powers the `/tags` directory (Story 13, epic unified-tagging-ui), so the directory is the surface that under-reports. Corroboration that the *legacy* path is behaving as recorded: its tags.bw count (33) matches the 2026-07-12 census exactly. **Not yet triaged — this is an observation, not a diagnosis.** Candidate explanations, unverified: (a) the unified index drops rows whose `tagEventId` cannot be resolved — staging's single returned row has `"tagEventId": null`, which is suggestive; (b) a WoT/POV filter applied in the unified path but not the legacy one (note tags.bw's documented house minRank=2 accounts for 33, not 10); (c) genuine loss in the `indexByTag`/`normalizeTaggings` projection. Worth checking against the open `unified-tagging-ui` book, whose acceptance frame is "the tag surfaces stop meaning profiles only" — a directory showing 10 of 33 tags would not meet it, and Story 12 is the book's only remaining item before its close. Surfaced incidentally by tag-event-inspector #1 while hunting a staging tag to smoke-test (the tag detail page needs a `tagEventId`, and the unified index supplied none). | 2026-07-16 (tag-event-inspector #1, staging smoke) | OPEN | | Repro: `curl -s <host>/api/tags/index?limit=200` vs `curl -s <host>/api/profile-tags/index`, count `.rows`. Related: epic unified-tagging-ui, `audits/unified-tagging-ui/book.md`. |
| 46 | meta | **ADRs can omit template-required sections and nothing catches it — `## Consequences` is the load-bearing one.** `templates/adr.md:26` requires `## Consequences` (what this enables · what it constrains · **new debt / follow-ups** · **firmware reinstall required?**). `tag-event-inspector/0001` shipped without it — **the only miss among the 8 most recent epic ADRs** (surveyed at that book's close; the other 7 carry it). No substance was lost there (firmware-reinstall is answered twice under `## Architecture invariants`; follow-ups live in the ADR's `Out of scope` + `_intake.md`) — but it was *scattered*, and that is the point: **`templates/build-audit.md:42` names "ADR `Consequences → new debt`" as the harvest source** for the audit's §5 debt roll-up, so a close following the template literally finds nothing and silently under-reports debt. `harness-lint` has no ADR-section rule at all (`grep -i consequences scripts/harness-lint.sh` → 0 hits), so the miss is invisible until a human happens to notice at close. **Candidate:** a lint rule asserting each `decisions/**/*.md` carries the template's required headings (start with `## Consequences`; `## Context`/`## Decision` are already universal). Cheap, mechanical, and it protects the return edge rather than the ADR itself. | 2026-07-16 (tag-event-inspector book close, step 5 harvest) | DONE | 2026-07-25 | `engineering-team/templates/adr.md:26`; `engineering-team/templates/build-audit.md:42`; audit §7 row 4. Folds into the meta-sweep harness story (proposed `_intake.md` 2026-07-15) alongside #40 and #43. **DONE 2026-07-25 (harness-gate-integrity #1):** new fail-tier `check_L13` requires `## Consequences` in active ADRs (skips the retired `done/` tree, per the `check_reviews`/`check_L2` precedent — ADR 0001 Decision 1A, scope A ratified). The one active offender (`task-queue-scheduler/0023`) was backfilled truthfully; the retired `tag-event-inspector/0001` is left as frozen history (accepted gap, ADR 0001 Consequences). Reviewed PASS (97b22247); not yet on staging. |
| 47 | meta | **`harness-lint` L2 cannot model an epic that spans two books — it reads "Closed book" as "its epics are finished".** `scripts/harness-lint.sh:126-148` iterates every **Closed** book, reads the epic slugs under its `## Epics in this book`, and demands each be `Status: Done`. It has no notion of the same epic *continuing in a different, Open book*, so it fires on a structure the harness's own docs allow. Hit live: epic `tag-event-inspector` was retired at its book's close (2026-07-16) and **reopened hours later** by story #2 — a new bounded ask (inspecting *taggings*, not the tag *definition*) that correctly got a **new** book (`audits/profile-tagging-inspector/`, Open) while the original book stayed **Closed**, its four-bullet frame being fully met. That is the honest arrangement — reopening a satisfied frame would leave a book that can never meaningfully complete, and the closed book set the precedent itself (*"a new capability … not a continuation"*). L2 nonetheless reports the Active epic as a violation. **Fix candidate:** before firing, skip any epic listed under an `## Epics in this book` of a book whose Status is not Closed — a few lines, and it preserves L2's actual intent (don't leave epics dangling after a close) rather than weakening it. **Waived meanwhile** (`scripts/harness-lint-waivers.txt`, `L2 engineering-team/epics/tag-event-inspector.md`) — and note the waiver is *blunt*: the format is (invariant, path, citation) with no way to scope it to the one book, so it also suppresses a genuine dangle of this epic after `profile-tagging-inspector` closes. That bluntness is itself an argument for fixing the rule rather than living on the waiver; **retire the waiver with the fix.** | 2026-07-16 (tag-event-inspector #2, Planning) | DONE | | `scripts/harness-lint.sh:126-148`; `engineering-team/audits/profile-tagging-inspector/book.md`; repro: `bash scripts/harness-lint.sh`. Folds into the meta-sweep harness story (proposed `_intake.md` 2026-07-15) alongside #40, #43 and #46 — the escalation trigger (≥3 open) is already well past.  **2026-07-18 (note-tagging-inspector close):** instance mooted — the epic is retired (operator-ratified) and its L2 waiver removed; the general "L2 can't model an epic spanning books" gap re-opens with the next multi-book epic, as a fresh row. |
| 48 | perf | **`/api/profile-tags/*` responses are served UNCOMPRESSED on staging — nginx gzips static HTML but not the JSON API.** Measured live 2026-07-17 against `staging.brainstorm.world` with `Accept-Encoding: gzip, deflate, br`: `profiles-tagged?tagEventId=<aos-2026-participant>` returns **`Content-Length: 136891`, no `Content-Encoding`** — 134 KB on the wire. The same request gzips to **31,869 B** (4.3×) if compressed, and `/` *is* served `content-encoding: gzip`, so compression is enabled for static assets and simply not reaching this route. `/api/profile-tags/available-tags` is likewise uncompressed, so this is **not specific to the new field** — it is the whole profile-tags API surface, and probably every JSON route. **Why it surfaced now:** tag-event-inspector #2's ADR 0002 D1 justified shipping assertions eagerly partly on the wire cost — *"14.9 KB → 36.6 KB gzipped (+21 KB) … 21 KB of gzipped JSON is not what makes this page heavy"*. The **raw** prediction was near-exact (134,007 predicted vs 136,891 actual, +2.2%); the **gzipped** one is arithmetically right but **inapplicable on this deployment**, so the real delta is ~98 KB uncompressed, not ~21 KB — ~4.7× the figure the decision was argued on. D1's *decision* still stands (its decisive argument was scan cost — the rejected lazy option re-scans the whole tag per panel opened — and 134 KB is still dwarfed by the 99 avatars the same page fetches), but two things need re-deriving: **(a)** ADR 0002 Consequences #1's revisit trigger (~1,000 assertions/tag ⇒ "~870 KB raw / ~200 KB gzipped") is really **~870 KB on the wire**; **(b)** enabling gzip for `application/json` is a cheap, broad win well beyond this story. Fix direction: add `application/json` to nginx's `gzip_types` (or enable compression in the Express layer) and re-measure. | 2026-07-17 (tag-event-inspector #2, staging smoke) | OPEN | | Repro: `curl -sD- -o/dev/null -H 'Accept-Encoding: gzip' "https://staging.brainstorm.world/api/profile-tags/profiles-tagged?tagEventId=6c5d88494c8142e4f8068fbf01fa10650f467dc16a8b5036ed1eb283ba32d141&sort=applied" \| grep -i 'content-\(encoding\|length\)'`. Compare `curl -sD- -o/dev/null -H 'Accept-Encoding: gzip' https://staging.brainstorm.world/ \| grep -i content-encoding` (gzip). Related: `engineering-team/decisions/tag-event-inspector/0002-…` D1 + Consequences #1. |
| 49 | bug? | **`counted:false` on a tagging assertion is proven only by code-read — no runtime has ever exercised it.** tag-event-inspector #2 added a `counted` flag to each `profiles-tagged` row assertion so a reader can reconcile the panel's blocks to the row's `+N −M` even when the viewer-union surfaces a block the score excludes. The `counted:true` path is verified at scale (0 invariant violations across staging 344 / tags.bw 336 / prod 348 blocks). The **`false`** branch (`src/api/profile-tags/index.js:981`, `if (!counted && ev.pubkey !== viewerPubkey) continue;`) requires a **signed-in viewer whose OWN assertion falls below the active POV's `minRank` on a row that ALSO carries trusted assertions** — a state unreachable in every environment tried: local has no POV threshold (`authorAllowed` is `() => true`, so `counted` is always true), and staging/tags/prod filter such authors out of the readable set so they can't be enumerated. The seeded runtime test (`test/tag-detail-publish.test.js` "an untrusted viewer assertion is returned but counted:false") **SKIPs** because `/var/lib/brainstorm` is a Docker named volume (settings.json unreachable from host) + the container lacks `nak` + host `nak` is arm64. **Risk is LOW** — the Reviewer audited the predicate and found it provably identical to the counts loop (`:669-685`), and the marker only affects labeling, not the score. But it is not empirically confirmed. **To close:** verify on any instance once a real below-threshold viewer with a mixed row exists, OR build a seeded test where settings.json is writable and `nak` runs. | 2026-07-17 (profile-tagging-inspector book close) | OPEN | | `engineering-team/audits/profile-tagging-inspector/audit.md` §4 #1 + §6; story `## Deviations`; review "Assessment of the disclosed verification gap". Instance of the OPEN.md #27 local-stack limitation. **2026-07-18 (note-tagging-inspector close):** the note-chip surface (story #3) now shares this exact gap — the same marker renders via the shared `RawTaggingEvents` component (`counted:false` composition source-pinned by its U15/U4; review gap #4 deferred for the same environmental reasons). One empirical verification on either surface covers both. |
| 50 | bug? | **Local dev corpus + identity drift broke the live-suite `npm test` baseline (30+ failures, three root classes).** Found 2026-07-18 while establishing the pre-arming baseline for the deploy-safety-gate book. Between 2026-07-16 and 2026-07-18 the local stack drifted: (a) the `tapestry` container was recreated, minting a **new runtime TA** (`11f23fe4…` — graph handles and TL signatures now live under it; firmware needed reinstalling to seed the tag concepts under it); (b) **~400 kind-39999 events vanished** from local strfry (7449 recorded at tag-event-inspector ADR 0001 → 7035 measured), including the tag-detail by-id RAW_FIXTURE (`5633f149…`) — cause UNKNOWN; (c) `wot_rank_a8ca55ca` house-POV scores **materialized in Meili**, activating filtered mode on every tag read (settings.json untouched since April — the config predated; the scores are what's new; what computed them locally is unidentified). Fixture reseeded (`9ec193a7…`), suites repaired (see #53). **Forensics still open:** what deleted the ~400 events; what populated the house scores. | 2026-07-18 (deploy-safety-gate pre-arming baseline) | OPEN | | Repro: `docker exec tapestry sh -c "strfry scan --count '{\"kinds\":[39999]}'"` vs ADR 0001's recorded 7449; `curl -s localhost:7700/indexes/profiles/stats` fieldDistribution shows `wot_rank_a8ca55ca`. |
| 51 | perf | **stream-consumer ETL saturates the local Meili task queue — single-doc tasks observed queuing 5–8 minutes.** Measured 2026-07-18: ~75 enqueued `documentAdditionOrUpdate` tasks, enqueue→finish 318–469s each, payloads of 1–2 docs. Any code path that writes a Meili doc and expects prompt readability stalls; the live suites now upsert-and-wait on the Meili task with a bounded timeout and SKIP honestly on saturation (`test/helpers/livePov.js`). Worth investigating: batching in the stream-consumer's Meili writes (one task per doc is pathological at 3.2M docs), and whether deployed droplets exhibit the same latency. | 2026-07-18 | OPEN | | Repro: `curl -s "localhost:7700/tasks?statuses=succeeded&types=documentAdditionOrUpdate&limit=5"`, diff enqueuedAt→finishedAt. |
| 52 | cleanup | **`test/event-tagging-firmware-seed.test.js` still hardcodes `http://localhost:8877`** for its LIVE concept-graph checks — on stacks serving the graph via the control panel (the AGENTS.md §2 probe pattern; nothing listens on 8877 here) those tests silently SKIP, leaving firmware-handle regression coverage dark. Align with the 2026-07-18 profile-tags.test.js fix: fall back to the control-panel base URL, and resolve the TA in graph handles at runtime. | 2026-07-18 | OPEN | | Sibling fix shipped in the same session's test-repair commit (profile-tags.test.js). |
| 53 | meta | **Live publish-flow suites encoded two dev-container coincidences — POV-unfiltered reads and runtime-TA==legacy-literal — and ten suites broke when the environment shifted (all environmental, zero code defects).** Class 1: per-run ephemeral authors are unrankable, so the POV-filtered read paths (pov-selectable-tag-surfaces ADR 0006, `wot_rank_<suffix> >= minRank` via Meili) legitimately drop their assertions/pins/endorsements — suites asserting those reads only passed while the local stack had no house scores. Class 2: several suites put the TA pubkey on the wrong side of ADR 0015's split — z-tag composition is the LEGACY literal (named exception), while TL signing/author-filtering and concept-graph handles are the per-deployment runtime TA; one suite even cited CLAUDE.md's "never hardcode" rule to justify runtime-composing z-tags (the rule's named exception). Resolution 2026-07-18: `test/helpers/livePov.js` — fixed pre-ranked dev author pool + `ensureRankedPool()` self-healing check + upsert-and-wait with skip-on-timeout — plus per-suite ADR-0015 splits; conditional-contract rewrite for the `search.resultTypes.tags`-gated tagHits fields. **Tester-role lesson:** live suites must draw counted authors from the ranked pool and place each TA use on the correct ADR-0015 side from day one. Folds into the meta-sweep harness story (proposed `_intake.md` 2026-07-15). | 2026-07-18 (deploy-safety-gate pre-arming baseline) | OPEN | | `test/helpers/livePov.js`; ADR 0015; decisions/pov-selectable-tag-surfaces ADR 0006. |
| 54 | meta | **In-app browser-pane evidence tooling degraded two ways during the note-tagging-inspector drives (worked around, journaled — not fixed).** (1) Screenshot capture returned black frames on this machine throughout the run, on local and deployed pages alike, while content was provably rendered — evidence fell back to accessibility-tree/page-text extracts per SMOKE_TEST.md's "note the gap, don't block". (2) On two deployed pages (staging + tags tag-page) synthetic pointer events failed to register (the Notes-tab switch, the popover button press; both worked pointer-driven on local) — driven via DOM-dispatched clicks on the real elements instead; the final gate-judge later reproduced the same staging interactions with ordinary pointer events, so the fallback masked nothing. Investigate the pane's capture path before the next evidence-heavy run; treat DOM-dispatch as the sanctioned, always-journaled fallback. | 2026-07-18 (note-tagging-inspector close) | OPEN | | Journal 2026-07-18T06:35/07:20/08:05 entries; `audits/note-tagging-inspector/audit.md` §7. |
| 55 | meta | **`feat/tags`'s own `contextScopedPinsResult` term sits in the OPEN.md-#43 dead `overallOk` block — contextual-pins has never gated `npm test` on that branch.** Found during the operator-instructed staging→`feat/tags` promotion merge (`ab4087fe`): the auto-merge stacked two `;`-terminated chains; inspection showed the second is the pre-existing #43 dead expression, to whose tail feat/tags had appended its term pre-merge — so the condition predates the merge, and the merge preserved it faithfully rather than silently changing that branch's gate semantics in a promotion (the suite itself runs green, 32/0 from the merged tree). For the feat/tags owners: include this term when #43's re-attach lands, or on the next feat/tags `test/test.js` touch. (Also noted for this machine: `tapestry-worktrees/feat-tags` holds a stale unpushed sync `8b9505eb`, both parents public, no unique work — re-syncs trivially over `ab4087fe`.) | 2026-07-18 (note-tagging-inspector close) | OPEN | | Journal 2026-07-18T09:10 entry; #43 carries the general defect + fix candidate. **2026-07-25 (harness-gate-integrity #1 close):** #43's re-attach shipped on the staging lineage (`feat/harness-gate-integrity`) and is the template to follow — but `contextScopedPinsResult` is feat/tags-only (zero-hit on staging), so it was correctly excluded there; row stays OPEN for the feat/tags owners to include the term when #43's re-attach lands on their branch. |
| 56 | meta | **Session-start digest printed "stack absent" while the Docker stack was up — the probe conflates slow/cold with absent.** Observed during the note-tagging-inspector run: a session's digest took the stack-absent branch while the `tapestry` container was up and the concept graph answered at :7778 shortly after (Stage-0 preflight). `scripts/session-start.sh` probes `GET /api/concept-graph/summaries` with `curl -m 2`; the endpoint is Neo4j-backed and a cold/busy container can exceed 2s, and the failure branch prints "stack absent" with no timeout distinction — briefly misleading orientation toward the docs-only fallback ladder. Fix candidates: one retry / longer timeout, or a third "probe timed out — re-check by hand" branch. | 2026-07-18 (note-tagging-inspector close) | OPEN | | `scripts/session-start.sh:40-51`; `audits/note-tagging-inspector/audit.md` §7. |
| 57 | meta | **Gate-judge spawn prompts need a mechanical blinding boundary — the `roles/director.md` doc amendment was declined at the note-tagging-inspector close gate; the practice stands unratified.** Judge #2 of that run read past a soft section-range instruction into the book's Direction-mode section and self-reported; its APPROVE was voided per protocol. The corrective spawn-prompt form — *"read from the top and STOP at the heading `## Direction mode`; reading past it voids your verdict"* — was adopted for every subsequent judge (4 spawns, all blinding-intact) and is the de-facto practice for future Direction runs. The audit proposed baking it into `roles/director.md` → "The blinded gate-judge protocol"; the operator declined at the gate (fallback per the no-fourth-state rule). A future run may re-propose the one-line doc amendment; until then, Directors carry the form via spawn-prompt discipline. | 2026-07-18 (note-tagging-inspector close) | OPEN | | Journal 2026-07-18T02:41/03:08; `audits/note-tagging-inspector/audit.md` §7 row 2 (declined). |
| 58 | meta | **24 of `test/test.js`'s summary lines print `SKIP` whenever `.skipped` is truthy — masking real failures in the printed line.** The older skip-aware lines (all 24 of the form `const xLine = xResult.skipped ? 'SKIP (…)' : 'PASS/FAIL (…)'` at test.js:456–557) branch on `.skipped` alone, so a result like `{pass:5, fail:1, skipped:1}` prints `SKIP (1 tests; preconditions not met)` — hiding the FAIL. Observed live 2026-07-18 (deploy-safety-gate #2 Test Design): `profile-tags-publish` failed 1 test (transient strfry residue) yet its summary line read SKIP; only reading the mid-log suite trailer revealed it. **Gating is unaffected** — `overallOk` reads `.fail` directly and Overall correctly printed FAIL — but the summary block is exactly what a human (or a Tester attributing an Overall verdict) reads, and it lied. The newer suites already use the correct form, `(pass+fail)===0 && skipped ? SKIP : PASS/FAIL(+skipped count)` (e.g., the deploy-safety-status and note-tagging-raw-events-inspector-http lines at test.js:733–744). Fix: normalize the 24 old lines to the new form. Folds into the meta-sweep harness story alongside #43 (same file, same registration-ritual neighborhood). | 2026-07-18 (deploy-safety-gate #2, Test Design) | DONE | 2026-07-25 | `test/test.js:456–557` (bad form) vs `:733–744` (correct form); observed on the full run logged in that session. **DONE 2026-07-25 (harness-gate-integrity #1):** all 24 bad-form lines rewritten to the good-form guard `(pass+fail)===0 && skipped ? SKIP : PASS/FAIL(+skipped)`; 0 bad-form / 38 good-form remain; G7 in `stack-free-npm-test.test.js` guards against regression. Reviewed PASS (97b22247); not yet on staging. |
| 59 | meta | **Zombie-test class: a live test whose failure is masked by environmental skips surfaces only when the environment is healthy — the `profile-tags-publish` typeahead `_matchedTags` test hid this way from 2026-06-10 until 2026-07-18.** The per-result-type gate (commit 854df80c: `getResultTypes` in `src/api/search/profiles/meili/index.js`, shipped default `search.resultTypes.tags=false` in `src/config/defaults.json`) turned the entire tag-match path structurally off on default instances — `computeTagMatches` never runs, no `_matchedTags`, no tag-derived hits — so from June 10 the test could not pass here; it skipped under Meili congestion in every intervening baseline and failed the moment Meili was healthy (this session's forensics: no `search` settings override, pool + target docs correctly ranked, events in strfry — the read path is gated off, full stop). Recast 2026-07-18 to the ratified conditional contract (probe `tagHits`-key presence, skip naming the gate — same idiom as `profile-tag-polish.test.js`'s pre-armed rewrite). **Sibling audit candidate (same class, not edited):** `test/profile-tag-polish-publish.test.js` ~lines 296–336 — four unconditional live `tagHits`/`tagHitsHasMore` assertions against the search proxy; its setup installs a POV into settings.json but never enables `search.resultTypes.tags`, and it currently hides behind wholesale suite skips (SETTINGS_PATH not writable / nak / Meili-wait). Audited NOT vulnerable: `profile-tag-polish.test.js` (already conditional), `search-api-result-type-settings.test.js` (dependency-injected, sets its own gate per test), the static source-inspection suites (`tag-read-union`, `profile-tag-consume-by-a-coordinate`, `pov-rank-threshold-key`, `search-result-parity`), and the Playwright specs (tolerant assertions or route-stubbed payloads). Tester-role lesson: when suite-level environmental skips guard a live test, audit what it asserts on the healthy path — a gate shipped later can zombify it invisibly to every congested baseline. | 2026-07-18 (deploy-safety-gate baseline repair) | OPEN | | `test/profile-tags-publish.test.js` (typeahead test, recast); `test/profile-tag-polish-publish.test.js` (audit candidate); `src/api/search/profiles/meili/index.js` getResultTypes; relates to #53 (conditional-contract rewrite) and #58 (summary-line masking, same run). |
| 60 | meta | **Second zombie-class defect in the same suite (see #59): the `profile-tags-publish` overwrite test was a NIP-01 tie-break lottery — nak stamps `created_at` at the current second, and the test's two same-(kind, pubkey, d) events signed ~600ms apart frequently landed in the same wall-clock second, where NIP-01's replaceable tie-break (equal created_at → lowest id wins) turns "the later event replaces the earlier" into a coin flip.** Forensics 2026-07-18: strfry's survivor for the latest full-run's overwrite d-tag was the FIRST event (polarity 1, id a5ac6a29…, created_at 1784410727) — the replacement lost the lottery; explains the test's fail-in-full-runs/pass-standalone pattern across multiple sessions (incl. the pre-pool-repair baselines), hidden the same way as #59 (environmental skips masked the failing full runs). Repaired: `nakSignEvent` gains an optional `createdAt` (→ `nak event --ts`), threaded through `signProfileTagEvent`; the overwrite flip signs at `apply.created_at + 1` — the tie is structurally impossible, not merely improbable. Grep-audited every other same-d replace pair in the live suites: `most-pinned-tag-index` AC-9, `customize-pin-curation` AC-4/5 + AC-8, `nip51-list-export` AC-5, `tl-publication` AC-2 all already defend with `await sleep(1100)` before the replacement ("ensure created_at advances") — the repaired test's 600ms `PROPAGATION_MS` gap was the sole undefended pair, and every other test in its suite mints a fresh target pubkey per test (distinct d — no pairs possible). Tester-role lesson: any replaceable-event test premised on "later replaces earlier" must make LATER explicit; an explicit created_at beats sleep-past-the-second (immune to load and clock fraction, and faster). Folds into the meta-sweep harness story alongside #53/#59. | 2026-07-18 (deploy-safety-gate baseline repair #2) | OPEN | | `test/profile-tags-publish.test.js` (`nakSignEvent` `--ts` param + overwrite test); sibling of #59 (same suite, same masking); NIP-01 replaceable-event tie-break. |

> **Merge note (2026-07-15, `feat/tags` ↔ `staging` integration):** both branches had independently used row IDs 13–18. Staging's rows keep their numbers; the feat/tags rows were renumbered (final, after staging concurrently allocated #31): 13→32, 14→33, 15→34, 16→37, 17→35, 18→36; the post-merge harness row is #38. References to OPEN.md #13–#18 in feat/tags-era commits/docs mean rows 32–37.

> **Numbering note (2026-07-18, note-tagging-inspector close):** rows **54–56** were allocated on `feat/note-tagging-inspector` while `staging` concurrently held rows **50–53** (the `deploy-safety-gate` run); the jump is deliberate so the two sets merge content-preserving with no renumber.
| 61 | ops | **`GET /api/get-user-data` 504s on heavy profiles on BOTH staging and prod** — observed during deploy-safety-gate story-1 staging smoke (2026-07-18, pubkey 04c915… / Jack): staging 504 after 5+ min of retries post-deploy, and tapestry.brainstorm.world (untouched by that book) 504s identically on the same request — shared, pre-existing, external to the book (journaled in audits/deploy-safety-gate/journal.md, story-1 Stage 2 entry). `tags-for-profile` for the same pubkey also times out post-restart (cold caches at prod scale). Investigate the query path / gateway timeout budget for very-heavy profiles; SMOKE_TEST.md's Tier-2 expectation for this endpoint is stale until resolved. | 2026-07-18 (deploy-safety-gate smoke) | OPEN | | `src/api/…/get-user-data` path; docs/SMOKE_TEST.md Tier-2 list; both droplets. |
| 62 | ops | **Live concept-graph count fluctuated 46 → 48 → 46 within one day (2026-07-18/19)** — three blinded judges verified the count live at different gates of the deploy-safety-gate run (story-1/2 era: 46 then 48; story-3 era: 46 again). Pattern-matches the #50 corpus-drift forensics (kind-39999 events vanishing from local strfry). No impact on that book (no concepts in its domain), but the graph's cardinality should not wobble; fold into the #50 investigation. | 2026-07-19 (deploy-safety-gate journal) | OPEN | | relates #50; local strfry corpus; concept-graph /summaries. |
| 63 | meta | **Judge-spawn prompts must bound the book.md read to the acceptance frame's exact lines (Read offset/limit) — unbounded "read the frame section only" instructions caused a self-reported blinding break** (deploy-safety-gate story-1 Gate 3: judge's read window overran into the pre-registration; APPROVE voided per protocol, re-judged clean). The bounded-read convention was adopted mid-run and held for 10 subsequent spawns, zero breaks. Proposal for operator ratification at triage: bake the bounded-read recipe (offset/limit + overrun-self-report instruction) into the judge-spawn guidance in director.md → "The blinded gate-judge protocol" (goalpost-class → next-run effect only). **Second occurrence:** relationship-primitives Gate 1 (2026-07-21) — an unbounded "frame section only" prompt over-read again, APPROVE voided, re-judged clean; the pattern-anchored recipe (`sed -n '/^### Acceptance frame/,/^## /p'` + reading-elsewhere-invalidates instruction) then held for 10 subsequent spawns, zero breaks (audits/relationship-primitives/audit.md §7). Two books, same lesson — ratification overdue. | 2026-07-19 (deploy-safety-gate retro); recurred 2026-07-21 | OPEN | | engineering-team/roles/director.md (judge-spawn recipe); audits/deploy-safety-gate/journal.md Gate-3 void entry; audits/relationship-primitives/journal.md 08:45Z void entry. |
| 64 | meta | **Direction-mode completion reports must derive verdict tallies by counting the journal, not from memory** — deploy-safety-gate's first completion audit KICK_BACKed the report for stating "15 APPROVE" blinded verdicts where the journal recorded 12 + 1 void (the Director had counted its own mechanical Gate-4 passes as blinded verdicts); corrected, disclosed in the report, re-audit APPROVEd. Proposal for operator ratification: one line in .claude/skills/direct-feature/SKILL.md Stage 3 — "tallies in the completion report are counted from journal entries at assembly time." | 2026-07-19 (deploy-safety-gate retro, audit §7) | OPEN | | .claude/skills/direct-feature/SKILL.md Stage 3; audits/deploy-safety-gate/journal.md completion-audit entries. |
| 65 | meta | **Phase-3 UI specs whose pre-implementation run fails at the first missing element leave every later assertion unexecuted — a latent defect (ambiguous strict-mode locator) survived Gate 3 and surfaced only at implementation** (deploy-safety-gate story 3: `h3` row-intactness locators matched the pre-existing "Recent Runs (…)" headings; Implementer halted per lane rules, Tester re-baselined with `exact:true`, commit 1c99dae4). Proposal for operator ratification: tester-role guidance — the Phase-3 harness-soundness prototype must make the spec reach EVERY assertion (the story-3 prototype validated the node lane fully but the Playwright spec's later assertions were unreachable). | 2026-07-19 (deploy-safety-gate retro, audit §7) | OPEN | | engineering-team/roles/tester.md; tests/brainstorm/scheduled-tasks-panel-countdown.spec.js history. |
| 66 | ops | **Backend ports are internet-exposed on all three droplets — apply a DigitalOcean Cloud Firewall (default-deny, allow only 22/80/443).** The deploy `sed` remaps only `:80`→localhost, so `7474`/`7687`/`8687` (Neo4j), `7778` (control-panel API, bypasses host nginx), `7700` (Meilisearch), `3069` (nostr-search-api) stay on `0.0.0.0` (verified reachable on prod `159.203.150.156`, 2026-07-20). **`ufw` does NOT work** — Docker rewrites the droplet iptables ahead of it; use a DO Cloud Firewall (network-layer). Last open companion of the `security-auth-exposure` book (the Neo4j password rotation companion is DONE, all 3 instances, verified). Operator deferred 2026-07-20. Full runbook + verification + the localhost-bind durable alternative live in the book. | 2026-07-20 (security-auth-exposure book) | OPEN | | engineering-team/audits/security-auth-exposure/book.md § "Companion operator-side tasks" #2; docker-compose.yml ports; .github/workflows/deploy-*.yml (only `:80` is remapped to localhost). |
| 67 | docs | **Document the API authorization posture (default-deny for mutations) in the spec.** After the `security-auth-exposure` book, the auth middleware is default-deny for unauthenticated mutations — honest-local bypass (loopback peer **and** no `X-Forwarded-For`/`X-Real-IP`), a two-entry public-mutation allowlist, `req.localTrusted`, and handler-level write-Cypher + TA-signing gates. `BIBLE.md` §Auth (:466) is only an endpoint table and `docs/ARCHITECTURE.md` §Authentication (:124) covers only NIP-07 + roles — neither describes the enforcement model. Nothing there is *false* (ARCHITECTURE's "Guest: read-only" is now more accurate than before), so this is an **additive spec gap, not a correction**. Write it via the **docs-mode Protocol-Spec flow** (a considered architecture-section addition, out of scope for the book-close sweep). Source of truth: `audits/security-auth-exposure/{audit.md,prd-seed.md}` + ADRs 0001/0002. | 2026-07-20 (security-auth-exposure book-close) | OPEN | | BIBLE.md §Auth (:466); docs/ARCHITECTURE.md §Authentication (:124); engineering-team/decisions/security-auth-exposure/0001,0002. |
| 68 | bug | **Legacy `/kg/` frontend still calls the deleted `GET /api/neo4j/run-query`** (dormant). The committed minified legacy control-panel bundle `public/kg/assets/index-DeRPuVlD.js` has its own Users page that fetches the removed endpoint → would throw `Unexpected token '<'` (same class as the `ui/` Users-page regression fixed 2026-07-21). **Dormant, not actively breaking prod:** the current UI's "Legacy" menu link points to `/legacy/` which serves a **non-React** page (no bundle); on staging/prod the `/kg/` React assets fall back to HTML so the fetch never fires; only a **direct visit to `/kg/`** on a stack that serves the legacy JS (e.g. local dev) hits it. The legacy **source is not in the repo** (only the minified bundle), so no clean source-fix. **Decide:** retire the legacy frontend (remove `public/kg/` + the `express.static` mounts / `/legacy` routes in `bin/control-panel.js:132,145,149,225+` + the `/legacy` links in `ui/src/components/BrainstormUserMenu.jsx:177`, `pages/BrainstormSearch.jsx:569`, `pages/settings/RelaySettings.jsx:1775`) **vs** patch the minified bundle (`run-query` GET → `POST /api/neo4j/query`). Once resolved, **extend `test/users-page-neo4j-endpoint.test.js`** to also scan `public/` so this class is guarded outside `ui/src`. Found by the post-fix regression audit. | 2026-07-21 (run-query regression audit) | OPEN | | public/kg/assets/index-DeRPuVlD.js; bin/control-panel.js (static mounts + /legacy routes); test/users-page-neo4j-endpoint.test.js (guard to extend). |
| 69 | ops | **The local dev container image is ~3.5 months stale and is missing the `stream-consumer` ETL entirely — this is the root cause of the long-standing "empty local Neo4j" problem.** Measured 2026-07-21: local strfry holds **3,067,260** events but `MATCH (n:NostrUser) RETURN count(n)` returns **3**. `docker/supervisord.conf` defines `[program:stream-consumer]` (added `be46dcb4`, 2026-04-11) but the running container's `/etc/supervisor/conf.d/tapestry.conf` does not — because `Dockerfile:106` COPYs that file at **build** time and the running image was built **2026-04-06**, five days before the ETL was added. The repo bind-mount keeps `src/`+`ui/` live, so `git pull` refreshes source but **never** the Dockerfile-baked config; `supervisorctl status` shows only neo4j/strfry/nip50-proxy/strfry-router/brainstorm. Container conf also lacks `environment=NEO4J_CONF="/etc/neo4j"`. **Fix:** `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build tapestry` (named volumes incl. `secure-keys`/TA identity persist), then expect a long ETL backfill. Deferred by operator 2026-07-21 (not needed for the in-flight UI work). Supersedes the "local graph is just sparse" reading — it is a missing process, not a data state. | 2026-07-21 | OPEN | | Dockerfile:106; docker/supervisord.conf (`[program:stream-consumer]`, :43); `docker exec tapestry supervisorctl status`. |
| 70 | meta | **`scripts/dev-refresh.sh` hard-fails (`set -e`) on any container whose supervisor config lacks `stream-consumer`, aborting before the smoke test.** Step 4 runs `supervisorctl restart brainstorm stream-consumer`; on the current local image (row 69) `stream-consumer` does not exist, so supervisorctl exits non-zero — `brainstorm` *does* restart, but the script dies silently with no diagnostic and steps 5 (smoke test / bundle-match check) never run. Observed 2026-07-21 on the first use of the newly-landed script; the failure reads as "refresh broken" when the refresh actually succeeded. **Proposal:** restart each program independently and tolerate absent ones (e.g. loop + `|| warn`), so a partial supervisor config degrades rather than aborts. Note the script's `--deps` path is also load-bearing on a fresh clone (see row 71). | 2026-07-21 | OPEN | | scripts/dev-refresh.sh:87-104 (backend-restart block). |
| 71 | meta | **Two orientation hazards hit in one session.** (a) **CLAUDE.md's local-dev TA pubkey is stale** — it names `82b75e47…973833` "on this machine", but `/api/assistant/pubkey` returns `e00ed09087b831ecf40442c82768b2114b707008916ac801dabbfbe76ae9df36` and all 45 concept handles use `e00ed090…` (graph self-consistent; the doc drifted, presumably a volume/identity recreate). The "never hardcode, resolve at runtime" rule is *validated* by this, but the stale literal will mislead anyone who trusts it for a quick grep — consider dropping the literal or marking it illustrative. (b) **`ui/node_modules` is inside the repo bind-mount and had macOS-only `@rollup/rollup-darwin-arm64` + `@esbuild/darwin-arm64` binaries** from a host-side `npm install`, so every in-container vite build died with `Cannot find module @rollup/rollup-linux-arm64-gnu` until `dev-refresh.sh --deps` reinstalled them Linux-side. `dev-refresh.sh`'s header documents the hazard but nothing *enforces* it; a host `npm install` in `ui/` silently re-breaks container builds (and vice versa). Consider a preflight arch check in the refresh script. | 2026-07-21 | OPEN | | CLAUDE.md § "Per-deployment TA pubkey"; scripts/dev-refresh.sh:17-20 (header note), :69-74 (`--deps`). |

> **Numbering note (2026-07-22, relationship-primitives close):** rows **72–73** are reserved for the in-flight `ops/open72-73-ci-gate-and-tags-safety-gap` branch observed on origin at close time; this close allocates from **74** so the two sets merge content-preserving with no renumber.
| 74 | meta | **Silent role stalls ×5 in one Direction run — subagents completed their background work but their completion surfaced to them (and onward) up to ~5h late; every stall recovered by a Director-side heartbeat + SendMessage nudge at ~20–40 min detection cost.** Pattern: agent awaits its background child's completion notification; the child finishes; the agent idles unnotified until externally resumed. Hit the Reviewer (~5h), the story-2 Tester (×2), the Gate-3 judge, and the story-2 Implementer (×2 short) in the relationship-primitives run (journal entries 2026-07-22T01:45Z, 02:45Z, 03:38Z; nudges 04:10Z/04:52Z). Proposal for operator ratification at triage: (a) a standing heartbeat-watch procedure in .claude/skills/direct-feature/SKILL.md "Self-pacing" (goalpost-class → next-run effect), and/or (b) chase the notification-latency mechanism harness-side. Ports to human-gated flows — any spawned role can stall the same way. **Recurred in second-brain #2's human-gated review (2026-07-23): the spawned Reviewer ended its turn awaiting its nohup'd npm-test child (not harness-tracked → no re-invoke) and needed a SendMessage nudge; ~8-min detection via its completion notification.** | 2026-07-22 (relationship-primitives retro, audit §7) | OPEN | | .claude/skills/direct-feature/SKILL.md § Self-pacing; audits/relationship-primitives/journal.md stall entries. |
| 75 | meta | **Equality-bracket assertions against a live publisher are deterministically brittle — strfry scan-count brackets (story-1 H8, story-2 H4) fail whenever strfry-router syncs an upstream event mid-bracket (~0.1 ev/s when busy), costing the relationship-primitives run three full-suite re-runs, a control experiment (+6 events/60s with the feature untouched), and a quiet-window monitor.** The test plans' documented remedy (drift-check + quiet-window re-run) works but burns ~25 min per retry. Proposal for operator ratification at triage: tester-role guidance for drift-tolerant negative assertions — scope the scan to the signature/kind the feature could emit, or make router-quiesce a documented test prerequisite — instead of whole-relay count equality. **Recurred ×2 in second-brain #2 (2026-07-23): the impl gate's run 1 and the independent review's run 1 each hit the +1 signature in relationship-primitives H8 (an untouched suite) and each burned a ~25-min quiesced rerun — occurrences three and four.** **No recurrence in second-brain #3 (2026-07-23): two full gates (impl + independent review) passed first-try with the router active — count stays at four.** **Recurred ×1 in second-brain #4 (2026-07-23): the impl-phase full gate hit the drift signature in relationship-primitives H8 (untouched suite; scan count 6004017→6004019); passed 23/0 quiesced — occurrence five. The independent review accepted the environmental disposition rather than re-running the 24-min gate.** **Recurred ×1 in harness-gate-integrity #1 (2026-07-25): the impl-phase full gate hit the H8 drift signature (scan 6010257→6010259); passed 23/0 with strfry-router quiesced — occurrence six; the independent review accepted the environmental disposition (precedent: occurrence five).** **Recurred ×1 in goal-intent-fields #2 (2026-07-26, Test Design): the Gate-3 full run hit the signature in `relationship-primitives-probe` H4 (scan 6015423→6015426, +3), the one non-story failure in the run. Not re-run quiesced — this phase's own suites were expected red, so the gate read per-suite verdicts rather than `Overall:`, and the probe ran ~1000 log lines BEFORE either goal-intent suite started. Occurrence seven, and the first where a +3 (not +1/+2) drift shows the window is widening with relay traffic.** Appended 2026-07-28 (drafted at the store-and-show close, inserted at the add-a-concept close): **measured across ten full runs of one branch in `goal-intent-fields` (2026-07-27) — the pair fired in 4 and was green in 6, a ~40% spurious-red rate at Gate 4, whose entire value is being mechanical.** The cost is not the ~25-minute re-run; it is that a two-in-five false positive trains whoever runs the gate to expect red and shrug, converting a mechanical check back into a judgement call. Two further store-and-show occurrences (a Gate-5 judge run; that book's first Gate-4 run); did NOT fire on the store-and-show close gate. Two add-a-concept occurrences (the stalled Gate-3 judge's full run; the book-close gate run — H8's own message: scan count 6020718→6020719, green on immediate isolated re-run 23/0); that book's Gate-4 run was green. **Recurred ×1 in blinding-rebuild #2 (2026-08-04, independent review's full gate): probe H4 scan 6598831→6598832 (+1), the sole failure in the run; isolated re-run 9/0 green; environmental disposition per the occurrence-5/6 precedent (review § Findings).** | 2026-07-22 (relationship-primitives retro, audit §7) | OPEN | | engineering-team/roles/tester.md; test/relationship-primitives.test.js H8; test/relationship-primitives-probe.test.js H4. |
| 76 | meta | **Anchor-matched journal appends can strand entries: a Direction-mode journal entry landed body-only (heading line lost in the edit's application) and sat headingless, out of chronological order, until the story-2 Reviewer flagged it; repaired by amendment commit 9595a02e (heading restored + editorial marker, content untouched).** Proposal: append journal entries by true file-append (shell >>) rather than anchor-matched in-place edits, and/or a harness-lint rule flagging `**Story/Phase:**` lines not preceded by a `## ` heading. | 2026-07-22 (relationship-primitives retro, audit §7) | OPEN | | audits/relationship-primitives/journal.md (restored entry + amendment marker); scripts/harness-lint.sh (candidate rule). |
| 77 | bug | **Two trivial hardening items from the relationship-primitives reviews (both non-blocking, one-line each):** (a) `respondMissingNodes(res,…)` is returned without `await` inside `try` at src/api/normalize/relationships.js:164 and :201 — a rejection in the diagnostic query would escape the catch (Express 4 hung request; vanishingly narrow window); fix is `return await`. (b) `PROBE_RESPONSE` in src/api/normalize/probe.js is not `Object.freeze`d. Fix both on the next touch of either file. | 2026-07-22 (reviews 1 & 2) | OPEN | | src/api/normalize/relationships.js:164,:201; src/api/normalize/probe.js:10-14. |
| 78 | meta | **Eager book anchor skipped at intake for a bounded ask — `engineering-team/audits/graph-curation-ui/book.md` was never opened when the graph-curation-ui epic started (story #1 planned → PASSed with no intent anchor), so completion detection had nothing to compute against; caught by the story-1 Reviewer.** Proposal for triage: the `/plan-feature` command (and workflows/0-intake.md) should state that opening a NEW epic for a bounded ask also opens the book anchor (or records an explicit operator decision to run book-less). **2nd occurrence: firmware-explorer #1 (2026-07-23)** — same skip; the anchor was again recorded in the epic file, not `book.md`, and reconstructed into `book.md` only at close. Two occurrences in two days → worth prioritizing at triage. **3rd occurrence: tapestries #1 (2026-07-23)** — intake skipped the anchor again, but it was opened *eagerly at the Story-1 Review* (`engineering-team/audits/tapestries/book.md`), not reconstructed at close — the earliest catch of the three. (Restored at the tapestries book close; the note was dropped in the `feat/tapestries-skeleton` → staging OPEN.md merge.) | 2026-07-22 (graph-curation-ui #1 review; +firmware-explorer #1 2026-07-23) | OPEN | | engineering-team/reviews/graph-curation-ui/1-move-nodes-between-sets-ui.md § harness friction; CLAUDE.md § Books of work. |
| 79 | bug | **Four non-blocking hardening items from the graph-curation-ui #1 review (all dialog-layer, none blocking PASS):** (a) stale `selectedUuid` survives a kind switch in PlacementDialog — a now-cycle-excluded candidate can be confirmed (advisory-guard bypass); clear selection on kind change or re-validate in handleConfirm (ui/src/components/PlacementDialog.jsx:33-34,193-204). (b) `moveFromKey.split('|')` breaks if a uuid contains `|` — silently degrades a move to an add; split on the last separator (PlacementDialog.jsx:115). (c) overlay click is not busy-gated (Close button is) — a mid-move dismiss can hide the outcome/hazard/partial banner (PlacementDialog.jsx:185). (d) redundant inner `isOwner` checks inside the already-gated Placements block (ui/src/pages/concepts/ElementDetail.jsx:299-307). Fix on next touch of either file. | 2026-07-23 (graph-curation-ui #1 review) | OPEN | | engineering-team/reviews/graph-curation-ui/1-move-nodes-between-sets-ui.md § findings. |
| 80 | meta | **Review-phase hygiene, two related amendments proposed (second occurrence each this week):** (a) non-blocking findings in a PASS review have no swept surface — they live only in the review file until someone hand-rows them (row 77 did it for relationship-primitives, row 79 for graph-curation-ui); propose workflows/5-review.md require sweeping PASS-review findings into OPEN.md in the review commit. (b) when the main session also implemented the story, the Review phase should be required (not just permitted) to spawn the independent reviewer subagent — done by choice in graph-curation-ui #1, but nothing mandates it. Ports to both flows. | 2026-07-23 (graph-curation-ui book retro, audit §7) | OPEN | | engineering-team/workflows/5-review.md; audits/graph-curation-ui/audit.md §7. |
| 81 | cleanup | **`feat/nostr-search` local branch: unmerged (4 commits, last 2026-03-26 "fully integrated nostr-search — no manual setup required") — operator to decide delete vs keep.** Surfaced during the 2026-07-23 branch housekeeping sweep (merged local+remote branches were deleted; this one was left because it is unmerged work). Local-only; no matching origin branch. | 2026-07-23 (housekeeping) | OPEN | | git branch feat/nostr-search (local checkout). |
| 82 | bug | **Goals view error-state Retry is an href-less `<a onClick>` — not keyboard-focusable (second-brain #1 review, non-blocking).** App precedent for Retry is a `<button>`; mitigated meanwhile by the hook's focus/visibility refetch. Fix on next touch of `ui/src/pages/brain/Goals.jsx`. | 2026-07-23 (second-brain #1 review) | OPEN | | engineering-team/reviews/second-brain/1-capture-a-goal-and-see-it.md § findings. |
| 83 | meta | **Full live `npm test` (~24 min) cannot finish inside the harness Bash tool's 10-minute foreground cap — Implementer/Reviewer gate runs must be backgrounded from the start or they die mid-flight.** Hit in both the impl and review phases of second-brain #1 (each recovered by backgrounding). Proposal for triage: state the background-run requirement in workflows/4-implementation.md + 5-review.md, or ship a wrapper that launches the gate detached and tees to a log. **Protocol correction (second-brain #3 review, 2026-07-23): the "nohup + repeated sleep-and-tail polls" phrasing collides with the harness's foreground-sleep block; the working pattern is a bounded `until grep -q "^Overall:" <log>; do sleep 15; done` waiter, which the harness promotes to a tracked background task and re-invokes on completion — also mooting the row-74 orphan hazard for gate runs. Fold this wording into the workflow note at triage.** | 2026-07-23 (second-brain #1 impl/review) | OPEN | | engineering-team/reviews/second-brain/1-capture-a-goal-and-see-it.md § harness friction. |
| 84 | bug | **`ConceptElements` admin page 403s a non-owner viewer for write-keyword concept slugs (`set`, and any `create`/`delete`/`merge`/`remove`/`drop`).** It interpolates the concept-header uuid into its `useCypher` queries, so `…:set` trips the `/\b(CREATE\|MERGE\|DELETE\|SET\|REMOVE\|…)\b/i` write-guard at `src/api/neo4j/queryPost.js:17`; the read is misclassified as a write → routed to `writeCypher` → 403 for non-owner (owner viewers survive via the write path, so it reads as latent). Surfaced designing firmware-explorer #1, which avoids it by carrying the handle as a `$h` **param** (ADR firmware-explorer/0001 Context §1). Fix: move `ConceptElements` (+ any sibling interpolating pages) to the now-parameterized `cypher(query, params)`. | 2026-07-23 (firmware-explorer #1) | OPEN | | ui/src/pages/concepts/ConceptElements.jsx; src/api/neo4j/queryPost.js:17; engineering-team/decisions/firmware-explorer/0001-concept-elements-and-sets-viewer.md § Context. |
| 85 | bug | **Five non-blocking findings from the second-brain #2 review (all PASS-compatible, fix/decide on next touch — most naturally at story 3):** (a) reconcile locates the schema's concept-object by sole top-level *properties* key where ADR 0002 d4 said sole *required* key — equivalent on the real convention, equally loud on ambiguity; note at story 3. (b) reconcile's read-compare-write has no lock — a race against a concurrent save-schema could write from the pre-update schema; bounded by idempotency + the check; ADR 0002 Option C (save-schema folds reconciliation in, story 3's ADR) eliminates it. (c) `src/lib/brain/hygiene.js:143-146` reports an unreadable *schema* under `property-record-drift` — detail is specific so AC 4 holds; story 3's taxonomy may want a distinct kind. (d) `hygiene.js:92-96` silently ignores incoming HAS_ELEMENT from a non-Superset node — correct per the ADR taxonomy; handle deliberately if the taxonomy grows. (e) reconcile preserves the current record's `title` rather than re-deriving — cosmetic. **Dispositioned by story 3 (ADR 0003, shipped 2026-07-23):** (a) ratified — properties-key resolution is the canonical reading (0003 Amends header on 0002); (b) erased for the save-schema path by the d8 fold; the standalone endpoint's residual race is recorded in 0003 Consequences (c); (c) `schema-unreadable` kind shipped; (d) deliberate-ignore comment shipped at the filter; (e) title precedence retained deliberately in the extracted reconcile. | 2026-07-23 (second-brain #2 review) | DONE | 2026-07-23 | engineering-team/reviews/second-brain/2-structures-the-brain-can-trust.md § findings; engineering-team/decisions/second-brain/0003-record-based-decomposition-and-validated-goal-writes.md. |
| 86 | bug | **Five non-blocking findings from the second-brain #3 review (all PASS-compatible, fix/decide on next touch):** (a) `createChildGoal`'s name-collides check compares parsed records only — an out-of-contract element with unreadable json (already a hygiene `unreadable-record`) with a colliding name/d-tag would slip the refusal and be MERGEd over; optional hardening: also compare raw rows. (b) GoalDetail repeats story-1's href-less `<a onClick>` Retry (not keyboard-focusable); fix both surfaces together when touched. (c) `role="link"` renders on slug-less (unlinked) tree rows — cosmetic a11y mismatch, reachable only via out-of-contract records. (d) design-guide goal-row error string vs shipped copy — story-1 ratified the pattern-not-byte reading; reconcile the guide sentence or ratify the reading in the book-close prd-addendum. (e) `updateGoalIntent`'s unreadable-record refusal lacks the `refusal` key (effectively unreachable; cosmetic contract asymmetry). **(b) addressed by story 5 (2026-07-23):** both GoalDetail + Goals Retry controls are now focusable `<button>`s. (a/c/d/e remain open.) | 2026-07-23 (second-brain #3 review) | OPEN | | engineering-team/reviews/second-brain/3-break-a-goal-into-pieces.md § findings. |
| 87 | bug | **Three non-blocking findings from the second-brain #4 review (all PASS-compatible, fix/decide on next touch — all in `ui/src/pages/brain/GoalDetail.jsx`, which story 5 lands in):** (a) `:36-41` `freshnessLine` has two `freshnessDays==null` fallback strings not in the guides — `'verified recently'` (the `current` branch is **dead code**: the server never returns `current` with a null day-count) and `'not verified recently'` (the `stale` branch fires only for a missing/unparseable `lastVerified`, unreachable via the blessed `create-resource` path). Optional: drop the dead `current` fallback / align the stale wording. (b) `:170,182` add `<h2>Resources</h2>`/`<h2>Record</h2>` section headings absent from wireframe §2 (which shows the cards/entries directly under intent); plain-language, jargon-clean (`Record` is the guide's own term). Optional: confirm the headings with design or drop them to match the sketch. (c) `:50-55` uses the raw `locator` as `href` for all kinds — resolves natively for web-address/repository; nostr-event/file/vault-note may not resolve (ADR d10 left the scheme to the Implementer, "register-safe"; AC 3's no-embed + `target=_blank rel=noopener` holds). Optional later: map nostr-event→njump/`nostr:`, file→a file scheme. **Dispositioned by story 5 (2026-07-23, ADR 0005 d12):** (a) the dead `'verified recently'` current-branch fallback dropped (the `'not verified recently'` stale fallback retained); (b) the `Resources`/`Record` headings ratified + pinned (S12) — they aid the now-richer one spine; (c) raw-locator href ratified (AC3's no-embed + `target=_blank rel=noopener` holds; the kind-specific scheme map stays deferred). | 2026-07-23 (second-brain #4 review) | DONE | 2026-07-23 | engineering-team/reviews/second-brain/4-attach-the-world.md § findings; dispositioned in engineering-team/reviews/second-brain/5-sessions-read-the-brain.md. |
| 88 | bug | **Neo4j silently drops tapestry elements while strfry keeps them — the `tapestry` concept header has no `IS_THE_CONCEPT_FOR` edge to its superset, so a reconcile pass prunes its elements from Neo4j.** Verified 2026-07-23 (tapestries #1): element `39999:<TA>:tapestry-for-dog-ca3b675e` is present in strfry with its full `graph` block but absent from Neo4j; `dog`/`dog-breed`/breed concepts + firmware elements all survived; container up 7d, no restart. Effect: any Neo4j-sourced "elements of the tapestry concept" read renders empty. ADR `tapestries/0001` works around it by sourcing the View Tapestries directory from **strfry via `queryRelay`**, not Neo4j, so the feature is unblocked. Root-cause fix (wire the tapestry concept's class thread so Neo4j stays in sync, and/or stop the reconcile pruning un-placeable elements) deferred here. **2026-08-04 addendum — diagnosis revised, superseded by #136:** a full deletion-site sweep found **no node-pruning code path in `src/`** (the only literal "prunes" are edge-level: firmware install's transitive-reduction of redundant `HAS_ELEMENT`/`IS_A_SUPERSET_OF` at `src/firmware/install.js:780-835`, and the WoT reconcilers' kind-3/10000/1984 edge diffs — none can delete a ListItem node); the `IS_THE_CONCEPT_FOR` edge EXISTS locally now (restored by the post-recreation firmware reinstall, cf. #50); and the dog element is alive on the operator's **laptop** instance (parallel dev machine, own stack + own TA) while absent from this Mac Studio's strfry entirely (8,237 kind-39999s, 0 matches) — the 2026-07-23 observation was almost certainly laptop-side. Nothing was ever pruned; the element was never in Neo4j because tapestry authoring is letter-only. | 2026-07-23 (tapestries #1 architecture) | DONE | 2026-08-04 — reclassified as misdiagnosis (cross-machine observation + nonexistent mechanism); the live defect re-filed correctly-framed as **#136** | engineering-team/decisions/done/tapestries/0001-nav-directory-and-strfry-element-read.md § Context; relates #62/#50 (concept-graph corpus drift). |
| 89 | cleanup | **Two non-blocking nits from the tapestries #1 review (fix on next touch):** (a) the Create-tapestry placeholder form uses classes `tapestry-new-preview` / `form-field` with no stylesheet definition, so the inert preview renders unstyled — style or drop them when the real authoring form lands (`ui/src/pages/tapestries/NewTapestry.jsx:18`). (b) AC-5's `getByText(/create new tapestry/i).first()` also matches the nav sublink; scope it to the heading on next touch (`tests/brainstorm/tapestries-nav-and-directory.spec.js:198`). | 2026-07-23 (tapestries #1 review) | OPEN | | engineering-team/reviews/tapestries/1-nav-and-directory.md § Non-blocking. |
| 90 | cleanup | **tapestries #2 review — two AC-2 Playwright assertions aren't table-scoped:** after clicking Elements/Subsets, `getByText(/irish setter/i)` / `/golden retriever/i` can be satisfied by the sidebar member-concept buttons (same names), so a `groupRelationships` bucketing regression for HAS_ELEMENT / IS_A_SUPERSET_OF could slip past (AC still covered by the table-specific Enumerations check + AC-3's edge count). Scope those locators to `.data-table` on next touch. | 2026-07-23 (tapestries #2 review) | OPEN | | tests/brainstorm/tapestry-exploration.spec.js:178-179,187-188; engineering-team/reviews/tapestries/2-tapestry-exploration-page.md § Non-blocking. |
| 91 | bug | **Two non-blocking findings from the second-brain #5 review (both PASS-compatible, defensible/optional):** (a) `note-goal-idea` (`src/api/normalize/index.js` `handleNoteGoalIdea`) accepts but does not use `servingGoal`; the `noted` record's summary is the idea's own `statement`. ADR 0005 d7 *suggested* folding serving-goal context into the summary — but doing so with a raw slug would leak a machine identifier into owner copy, and AC5 does not require it (H5 asserts nothing about it). Optional later: render the serving goal by its human name. (b) `resolveWorkRecordConcept`'s internal resolver error strings carry jargon (`… has no Superset node.`) — **non-owner-facing** API/log strings (ADR d14; byte-mirrors story-4's `resolveResourceConcept`, which review 4 passed). Observation only, no action. | 2026-07-23 (second-brain #5 review) | OPEN | | engineering-team/reviews/second-brain/5-sessions-read-the-brain.md § findings. |

> **Numbering note (2026-07-24, `feat/tapestries-skeleton` → staging merge):** rows **87–89** on `feat/tapestries-skeleton` were renumbered to **88–90** — `staging` concurrently allocated **#87** to the second-brain #4 review nits. References to OPEN.md **#87** (Neo4j desync), **#88** (tapestries #1 review nits), and **#89** (tapestries #2 review nit) in the tapestries reviews / ADR 0001 / `book.md` / phase commits mean rows **88 / 89 / 90** respectively.

> **Numbering note (2026-07-24, `feat/second-brain` #5 → staging merge):** the second-brain #5 review's non-blocking-findings row was **#88** on `feat/second-brain`; `staging` concurrently allocated **88–90** to the tapestries stories, so it merged as row **91**. Any reference to OPEN.md **#88** in the second-brain #5 review file / impl commit means row **91**.

| 92 | meta | **`curl` inside shell subshells hits a stripped PATH in the harness shell** — `$()` command substitutions and `for` loops fail "command not found: curl" (and `python3`), while top-level `curl` works. Broke the tapestries `/cycle-staging` + `/cycle-prod` smokes' Tier-1/2 curl loops until reworked to top-level curls / the browser. Not a staging/prod problem — a local harness-shell quirk. **Proposal for triage:** cycle-staging/cycle-prod smoke guidance should prefer top-level curls or the browser for reachability, and note that a curl loop in `$()` fails opaquely. Ports to Direction mode (same smoke tooling). | 2026-07-24 (tapestries book close) | OPEN | | .claude/skills/{cycle-staging,cycle-prod} smoke steps; docs/SMOKE_TEST.md Tier 1/2. |
| 93 | docs | **Decide BIBLE impact of the Tapestries feature — DONE (light entry).** Added §6 "Graph-embedding convention (Tapestries)" (a tapestry element carries a top-level `graph` block; read from strfry), a §13 route-tree block for `/tapestry/tapestries/*`, and a §16 changelog entry; bumped the "Last updated" line. **Deferred:** the exhaustive data-model/protocol spec of the graph-embedding convention (membership contract + authoring) to the create/edit-authoring phase via the Protocol-Spec docs-mode flow — the membership contract is still an open product question (prd-seed §7). Logged per the #3/#5/#10 BIBLE-decision convention so the judgment isn't re-litigated. | 2026-07-24 (tapestries book close) | DONE | 2026-07-24 | BIBLE.md §6/§13/§16; engineering-team/audits/tapestries/prd-seed.md §4/§7. |
| 94 | meta | **Brain H-suite fixture pre-clean cannot recover from a crashed teardown — stranded-node cascade.** The brain suites' teardown/pre-clean derive deletions from process-local state (`createdGoalNames`/`created*Uuids`), so after a teardown crash (seen 2026-07-24: a transient in-container `curl -m 8` timeout under full-gate load — likely the neo4j restart another suite triggers) the NEXT run's pre-clean computes an EMPTY d-tag list and only the json-sentinel orphan sweep fires — which deletes the stranded elements' **json tags but not their nodes**. Result: fixture goals invisible to brain reads but still blocking `create-element` ("already exists") and polluting hygiene as `unreadable-record` → the-proposal-loop went 23/10 twice until a manual sweep (query Neo4j for `uuid CONTAINS 'harness-proposal'`, delete strfry events by extracted d-tags, DETACH DELETE nodes+tags). **Proposal for triage:** pre-seed the pre-clean with the suite's KNOWN fixture goal names (their d-tags are deterministic via `dtag.childDTag(name, header)`) and add a name-based Neo4j sweep (`e.uuid CONTAINS sentinel`), so pre-clean is state-free. Applies to `the-proposal-loop.test.js` AND `teach-it-what-matters.test.js` (same pattern, story-6/7). *(Story 8's new suite `the-brain-survives.test.js` shipped WITH the state-free discovery pattern — the row's fix is proven; the two older suites still await it.)* | 2026-07-24 (second-brain #7 impl) | OPEN | | test/the-proposal-loop.test.js pre-clean; test/teach-it-what-matters.test.js pre-clean. |
| 95 | ops | **Local live Neo4j is memory-fragile under multi-container pressure — OOM-killed (exit 137), ~1–2 min supervisor recovery.** The Docker VM (~24GB visible) hosts the tapestry stack (entrypoint sizes Neo4j from the WHOLE machine: 6792MB heap + 6792MB page cache, `docker/entrypoint.sh:124-148` reads `/proc/meminfo` unconditionally, no env override) plus Meilisearch (~6GB) plus the brainstorm-server project's containers. Under pressure the VM's OOM killer takes the biggest process — the live Neo4j — seen 2026-07-24/25 during second-brain #8: once mid-H-suite with nothing else running, repeatedly while a second (drill scratch) stack ran beside it. Symptom: `ECONNREFUSED 127.0.0.1:7687` from any brain/normalize call; supervisor auto-restarts and recovers in ~1–2 min; never a code defect. `scripts/brain-drill.sh` now accommodates (scratch DB memory shrunk post-boot, scratch torn down BEFORE the live journal write, bounded recovery waits). **Proposal for triage:** give the entrypoint env-overridable memory knobs (so a dev host can cap the live heap), or raise the Docker VM memory, or document the co-tenancy ceiling in OPERATIONS.md's local-dev-loop section. | 2026-07-25 (second-brain #8 review, harness friction) | OPEN | | docker/entrypoint.sh:124-148; scripts/brain-drill.sh; engineering-team/reviews/second-brain/8-the-brain-survives.md § harness friction. |
| 96 | cleanup | **Retire the closed second-brain book + epic to `done/` once Phase 2 ingests the return edge.** The book closed 2026-07-25 (all 8 stories Done; audit + prd-addendum written). When the product team has read `audits/second-brain/{audit,prd-addendum}.md` into its next-phase scoping, move `audits/second-brain/` → `audits/done/second-brain/`, `epics/second-brain.md` → `epics/done/`, and `stories/second-brain/` → `stories/done/second-brain/` (one `git mv` each — the workflow 6 retirement convention). *(The handoff-doc flip clause is done — `docs/SECOND_BRAIN_STORY8_HANDOFF.md` flipped ✅ ADDRESSED 2026-07-25 after prod PR #460; only the `done/` moves remain.)* | 2026-07-25 (second-brain book close) | OPEN | | engineering-team/workflows/6-book-close.md § Book retirement; engineering-team/audits/second-brain/. |
| 97 | docs | **BIBLE impact of the second-brain book — DECIDED: no entry (logged per the #93 BIBLE-decision convention).** The entire second-brain surface (goals, resources, work records, proposals, priority signals, export/restore + the drill journal) is **owner-gated, per-instance, runtime-created, and never firmware-seeded** — it rides existing concept-graph primitives (`create-concept`/`save-schema`/`create-element`) and introduces **no new wire format, no universal convention, no firmware, no fork-agnostic protocol/data-model/API delta**. BIBLE documents the universal, fork-agnostic layer; a fresh fork has none of these concepts until an owner uses the feature, so none of it belongs there — which is why all 8 stories touched zero BIBLE, consistently. (Contrast row 93/Tapestries, which added a real graph-embedding *convention* — BIBLE §6 — and a public read-only surface, and so earned an entry.) The export artifact (`tapestry-brain-export` v1) is a **local file format**, not a nostr wire format, and is fully specified in ADR 0008 + `src/lib/brain/export.js`. If a later phase makes any of this fork-universal (e.g. firmware-seeding a brain concept, or a published/shared export format — PRD Phase 7 sharing), revisit then via the Protocol-Spec docs-mode flow. | 2026-07-25 (second-brain book close) | DONE | 2026-07-25 | engineering-team/decisions/second-brain/0008-the-brain-survives.md; engineering-team/audits/second-brain/audit.md §3; BIBLE.md (deliberately unchanged). |
| 98 | meta | **Lint rule L10 is structurally uncatchable before the phase-boundary commit — and it mislead an ADR into codifying the wrong rule.** `check_L10` resolves the *latest commit* touching a `scripts/harness-def-paths.txt` path (`git log -1 … -- "${def_paths[@]}"`), so while def-path edits sit uncommitted in the working tree the gate reports **clean**; the violation materializes only *after* the commit. The harness's own "commit at each phase boundary" convention is therefore what creates the red state, and an Implementer who runs the gate before committing (the natural order) cannot see it. Compounding it: ADR `self-ontology/0001` reasoned *"no cap change ⇒ no `harness-budgets.txt` edit ⇒ no CHANGELOG row"*, conflating the budgets-file **cap-change** rule with L10, which fires on **any** def-path commit regardless of the cap — so the ADR instructed the Implementer to omit the row, and Review caught it. Candidate fixes: (a) have `harness-lint.sh` also inspect *uncommitted* def-path changes in the working tree and warn; (b) add a line to `templates/adr.md` / workflow 4's per-phase-commit section: any diff touching a def path needs a CHANGELOG row **in the same commit**, independent of budgets. | 2026-07-25 (self-ontology #1 review) | OPEN | | Review `engineering-team/reviews/self-ontology/1-ratify-the-self-ontology.md` (Blocking #1 + Harness friction #1); ADR corrected in place by fix commit `1867ea53`, which also records the blind spot so the next docs-mode story doesn't repeat it. |
| 99 | meta | **No book was opened for the new `self-ontology` epic at intake.** CLAUDE.md's "Books of work and the return edge" calls for an **eager anchor** — `engineering-team/audits/<slug>/book.md` recording the PRD or a short acceptance frame — opened *at intake*, warning that without it "completion can't be detected across sessions and the close drops to low confidence." The epic was created during Planning and ran a full five-phase cycle to PASS with no book; completion detection at the Review gate enumerated all 26 books and correctly found none covering it (`/close-book` not offered). Low urgency for story #1 (self-contained, and `engineering-team/epics/self-ontology.md` records intent), but the epic anticipates **five more stories** (provenance primitive → rebuild hardening → deriver audit + serialization mode → backup pipeline → health monitoring), so a book with an acceptance frame is worth opening before story #2 — otherwise the eventual close is exactly the low-confidence case the rule warns about. Worth asking separately whether `/plan-feature` should prompt for a book when it creates a **new** epic. | 2026-07-25 (self-ontology #1 review) | OPEN | | Review `engineering-team/reviews/self-ontology/1-ratify-the-self-ontology.md` (Non-blocking #4); epic `engineering-team/epics/self-ontology.md`; consequence map in `docs/SELF_ONTOLOGY_DESIGN_HANDOFF.md` §10. |
| 100 | cleanup | **`stack-free-npm-test.test.js` G7 only catches the end-of-line SKIP-masks-FAIL form.** The `.skipped`-alone detector matches the multi-line summary form only; a hypothetical single-line `const xLine = xResult.skipped ? … : …` would evade it. Shipped `test/test.js` has **zero** such lines (all 38 summary lines are good-form), so this is optional hardening, not a live gap. Extend the detector to the single-line form on the next touch of that suite. | 2026-07-25 (harness-gate-integrity #1 review) | OPEN | | Non-blocking review finding #1; `engineering-team/reviews/harness-gate-integrity/1-gate-integrity-and-lint-robustness.md`. |
| 101 | cleanup | **Retire the closed `harness-gate-integrity` book + epic to `done/` once ingested.** Book closed 2026-07-25 (story #1 Done; audit + prd-seed written). Ingestion is essentially a no-op — internal harness infra, no product surface (seed §7 recommends no product action). Then move `audits/harness-gate-integrity/` → `audits/done/`, `epics/harness-gate-integrity.md` → `epics/done/`, and `stories/`+`decisions/`+`reviews/harness-gate-integrity/` → their `done/` counterparts (one `git mv` each — workflow 6). Mirrors #96. **Narrowed 2026-08-04 (blinding-rebuild close): the `stories`/`decisions`/`reviews` folder moves are DONE — shipped with that close's epic close-out (epic re-retired Done after story #2). Remaining: the `audits/` retirement moves (`harness-gate-integrity` now ingestable; `blinding-rebuild` once its audit is ingested) + the epic-file move if the `epics/done/` convention is ever exercised.** | 2026-07-25 (harness-gate-integrity book close) | OPEN | | engineering-team/workflows/6-book-close.md § Book retirement; engineering-team/audits/harness-gate-integrity/. |
| 102 | bug | **The live goal-concept schema puts `chanceOfSuccess` in `required`, so a goal without it violates its own schema.** The goal schema node carries `required = ['name','slug','description','chanceOfSuccess']`, breaking second-brain ADR 0003 d13's invariant that `required` stays exactly `[name, slug, description]` and the new intent fields stay **optional**. Two suites fail on it live: `structures-the-brain-can-trust` **H4** and `break-a-goal-into-pieces` **H1** (both: *"required must stay exactly [name, slug, description] … got [\"name\",\"slug\",\"description\",\"chanceOfSuccess\"]"*). **Not a test artifact** — 25 of the 30 live goals carry no `chanceOfSuccess` and are now schema-invalid. **Provenance established, not guessed:** the offending schema node was signed **2026-07-25T21:07:47Z**, 5h28m before the `operational-direction` story's first commit (`bc804339`, 2026-07-26T02:36:05Z); that story's diff contains zero schema/firmware/concept writes and touched those two suites only by widening an import allowlist. Fix belongs with **`store-and-show-the-prompt-and-the-estimate`** (the goal whose whole subject is these two properties) — re-save the goal schema with `chanceOfSuccess` and `prompt` declared **optional**. | 2026-07-26 (operational-direction #1 full gate) | OPEN | | `engineering-team/decisions/second-brain/0003-record-based-decomposition-and-validated-goal-writes.md` d13; `engineering-team/stories/operational-direction/1-operational-direction-mode.test-plan.md` § "Pre-existing failures found by the full gate". |
| 103 | meta | **Background-task completion notifications report "exit code 0" for commands that actually failed — hit 3× in one story, each time on a full `npm test` that was `Overall: FAIL`.** Two shapes: (a) a detached `nohup … &` launcher — the tool captures the *launcher's* exit, not the suite's; (b) the background-tool wrapper reporting its own status rather than the command's. In all three the true verdict came only from an explicitly echoed `EXIT=$?` (`REVIEWER_GATE_EXIT=1` while the notification said 0). **Why it matters beyond annoyance:** Direction-mode **Gate 4** is defined as "run the full suite yourself" — a Director trusting the notification journals a green gate that was red, and the mechanical gate silently passes. Same hazard for any session reporting test results from a backgrounded run. **Mitigation until fixed:** tee the suite to a log and read `Overall:` (or an explicit `EXIT=$?`); never the notification. **Proposal for triage:** state the read-the-log rule in `workflows/4-implementation.md` + `5-review.md` beside the existing row-83 backgrounding note, and in `roles/director.md` → Gate 4. | 2026-07-26 (operational-direction #1 impl + review) | OPEN | | `engineering-team/reviews/operational-direction/1-operational-direction-mode.md` § "Harness friction"; relates OPEN.md #83 (the ~24-min gate must be backgrounded at all). |
| 104 | meta | **The brain suites' live-stack probe is flaky, so H-class tests silently flip between EXECUTED and SKIPPED across identical runs — and a fully-skipped H-class still reports suite PASS.** `stackAvailable()` (a 2 s `AbortSignal.timeout` `fetch` + a `docker exec` curl) returned false with the stack demonstrably up (host probe 200, loopback `success:true` when re-run by hand ~125 ms / ~505 ms). Observed twice in `operational-direction` #1: the same code, same commit, gave "0 skipped, 6 H failures" and then "6 skipped" minutes apart. **Why it matters:** a Gate-3 claim that "H tests executed rather than skipped" is not reproducible, and a suite whose live assertions all skipped still prints PASS — so live coverage can silently evaporate without any signal. Pattern is shared by every brain H-suite (`the-proposal-loop`, `teach-it-what-matters`, `the-brain-survives`, `capture-a-goal-and-see-it`, …), which copy the same probe. **Proposal for triage:** raise/retry the probe budget (it competes with a full-gate run for the same container), and/or have the runner print an explicit per-suite "H-class: n executed / n skipped" line so an all-skipped live class is visible in the summary rather than indistinguishable from a real pass. | 2026-07-26 (operational-direction #1 test design + impl) | OPEN | | `test/operational-direction.test.js` `stackAvailable()` (copied from `the-proposal-loop.test.js`); `engineering-team/reviews/operational-direction/1-operational-direction-mode.md` § "Harness friction". |
| 105 | meta | **Background-command exit codes are reported as 0 for runs that actually failed.** Twice during `operational-direction` #1 a full `npm test` reported success while the run ended `Overall: FAIL` — once because a detached `nohup … &` launcher's exit was captured instead of the suite's, once from the background-tool wrapper. Both true verdicts came only from an explicitly echoed `EXIT=$?`. **Why it matters beyond annoyance:** a Direction-mode run keying on the notification would journal a green gate that was red, and the journal is the run's primary artifact. Candidate fixes: always echo `EXIT=$?` into the log and treat the log as authoritative; or document in `roles/director.md` that notification exit codes are not evidence. | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; reviews rounds 1–2 "Harness friction". |
| 106 | meta | **The H-class stack probe is flaky, and a false negative is invisible.** `stackAvailable()` (2s `fetch` + `docker exec`) intermittently returned false with the stack demonstrably up, flipping ten H-class tests between *executed* and *skipped* across consecutive runs of identical code. **A skipped H-class run still reports suite PASS**, so a Gate-3 claim of "H executed" is not reproducible, and a green gate can mean "nothing ran." Candidate fix: raise/retry the probe, and make the per-suite summary distinguish "H executed" from "H skipped" in the gate line rather than only in the tally. | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; review round 2 "Harness friction". |
| 107 | meta | **ADR-authored test-impact predictions are unreliable and are currently written as facts.** In `operational-direction`, ADR 0002 mis-called `U32` (predicted a loud failure; the test would have *silently passed* while missing a new refusal code), omitted `U6`/`U7`/`U8` entirely, and its own d11 contradicted its d6 on the refusal envelope shape — three mis-specifications in one document. ADR 0003 responded by marking its own prediction untrustworthy and instructing the Tester to execute rather than trust it, which held. **Proposed rule: an ADR states test impact as a hypothesis to run, never as a fact** — `templates/adr.md` Consequences could say so, and the Tester's workflow could require executing the prediction. **Recurred in blinding-rebuild #2 (2026-08-04): ADR 0002 stated its L14 calibration bar ("zero hits on the current corpus") as fact while only shape (i) had been measured at Architecture; the impl-phase shape-(ii) run found one active-path hit (operational-direction #1's linked-artifacts line), resolved by outcome-free reword (story `## Deviations`). Execute-don't-trust caught it at the right phase; the proposed template rule remains the fix.** | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; ADRs `operational-direction/0002`,`0003`; test plan rounds 2–3. |
| 108 | meta | **Tests that iterate a collection the feature does not yet produce pass vacuously — twice in two rounds.** `U38` and `U48` each looped over an array that was `undefined`/empty pre-implementation, so the loop body never ran and the assertion was green by default. Both were caught by probing, not by running the suite. **Proposed rule: assert arity before inspecting element shape** (`assert(Array.isArray(x) && x.length === N)` before any `for … of`), stated in `roles/tester.md` or the test-plan template's RED-confirmation step. | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; test plan rounds 2–3. |
| 109 | meta | **Byte-offset source assertions fail on correct code AND pass on broken code.** In `operational-direction` the same assertion (`S21`) did both in one round: a fixed 3200-char window reported "no success envelope" once the handler grew past it, and a ±400-char marker window bled into the adjacent envelope and reported a *pass* against a handler carrying the exact defect it guards — missing by 520 characters of luck. Fixed there by structure-bounded extraction (`functionBody()` bounded by the next top-level declaration; `resJsonBlocks()` brace-matched). **`S9`'s 4000-char markdown-section window survives and is the same latent class**, in a section that grew during that very story. Wider sweep warranted: other suites use windowed `src.slice` too. **A SECOND VARIANT, found in `goal-intent-fields` #3 Test Design (2026-07-27): the class is not only byte *windows* — any lexing heuristic used to bound a region has it.** Structure-bounded extraction over `.jsx` needs paren balance, which needs string literals masked; a masker that treats every `'` as a delimiter opens a phantom string on the apostrophe in the JSX prose *"The proposer couldn't run"* (`Proposals.jsx:88`) and swallows 40 lines — including the entire `passedOver` block the assertion was bounding, which then reported "block not found" on correct code. Fix used there: a quote is a delimiter only when its partner is on the **same line** (true of every real JS string, false of a JSX apostrophe); template literals keep multi-line handling; comments are masked separately so a scan can read strings without reading comments. **Worth folding into the tester guidance alongside the byte-window rule** — "structure-bounded" is not automatically safe if the structure is found by a naive lexer. | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; test plan round 3; `test/operational-direction.test.js` `S9`; `test/show-the-four-on-the-goal-screens-that-already-exist.test.js` `maskLiterals()` + its header comment. |
| 110 | meta | **Nothing enforces the eager book anchor, so completion detection silently never fires.** The `operational-direction` book was opened *after* its work reached production: every per-story PASS ran completion detection, found no `book.md`, and made no offer — exactly the failure the eager-anchor rule exists to prevent, with no signal that it was happening. The close was still high-confidence only because an owner-authored goal happened to predate the work and could be transcribed verbatim; a book without that would have landed at `confidence = low` per `workflows/6-book-close.md` step 1. **Candidate fix:** `harness-lint` warns when an epic has a `Done` story and no `audits/<book>/book.md`; or `workflows/0-intake.md` makes opening the book a checklist item with teeth. | 2026-07-26 (operational-direction book close, audit §7) | OPEN | | `engineering-team/audits/operational-direction/audit.md` §7; `engineering-team/workflows/5-review.md` completion detection. |
| 111 | meta | **#103's own recommended exit-code mitigation silently evaporates in this shell: `EXIT=${PIPESTATUS[0]}` under `\| tee` is bash, and the login shell is zsh, where the array is `pipestatus` (lowercase) and `PIPESTATUS[0]` expands to the empty string.** So the documented fix for "background notifications lie about exit codes" produces *no* verdict at all, and a session that trusts it has neither the notification nor the echo. Hit during `goal-intent-fields` #1's review (the reviewer caught itself and closed the hole with a second independent run) and designed around in #2's Test Design. **The fix is one line of guidance:** the **brace form** avoids the pipe entirely and works in both shells — `{ npm test; echo "EXIT=$?"; } > log 2>&1` — then read the `Overall:` line **and** the echoed code. **Proposal for triage:** correct the "Mitigation until fixed" sentence in #103 (and #105's candidate fix) to name the brace form and warn off `PIPESTATUS`, and state it wherever the read-the-log rule lands (`workflows/4-implementation.md`, `5-review.md`, `roles/director.md` → Gate 4). | 2026-07-26 (goal-intent-fields #2 test design; evidence from #1 review) | OPEN | | `engineering-team/reviews/goal-intent-fields/1-store-the-four-when-a-goal-is-captured-or-updated.md` §"Harness friction" + :329; `engineering-team/audits/store-and-show-the-prompt-and-the-estimate/journal.md` :318 ("the brace form works, the pipe form does not, and the ledger should say which"); relates OPEN.md #103, #105. **Confirmed again in `goal-intent-fields` #3 Test Design (2026-07-27): the brace form worked; the background-task notification reported "exit code 0" for a run whose echoed `EXIT=1` and `Overall: FAIL` were both correct — occurrence nine of the lying-notification signature.** **Occurrence ten: blinding-rebuild #2 independent review (2026-08-04) — the launcher notification claimed exit 0 while the log read `Overall: FAIL` / `EXIT=1` (the #75 probe red); the spawn-prompt read-the-log mandate caught it.** |
| 112 | meta | **The shipped owner-facing copy scans are per-`.jsx`-file, so moving copy into a shared util silently moves it out of their reach.** Four closed-book pins scan quoted strings and JSX text for banned jargon, exclamation marks and score/rank/percent tokens — but each is anchored to one page file (`the-proposal-loop` S11/S13 → `Proposals.jsx`; `capture-a-goal-and-see-it` S8 → `Goals.jsx`; `attach-the-world` S8 / `sessions-read-the-brain` S10 → `GoalDetail.jsx`). `goal-intent-fields` ADR 0003 does the right architectural thing — one pure `ui/src/utils/goalIntent.js` holding every owner-facing string for the three screens — and in doing so lands **every new sentence in a file no copy scan reads**. Caught at Test Design and closed *for this story* by a new `R5` that applies the union of the four word lists to the module; the general hole stands for any future extraction of copy into a util or component. **Candidate fix:** make the copy scans follow the render surface rather than the file — scan `ui/src/pages/brain/**` plus anything they import from `ui/src/utils/` — or state the rule in `roles/tester.md`: when a story moves owner-facing copy, the register pin moves with it. | 2026-07-27 (goal-intent-fields #3 test design) | OPEN | | `test/show-the-four-on-the-goal-screens-that-already-exist.test.js` `R5`; `engineering-team/stories/goal-intent-fields/3-show-the-four-on-the-goal-screens-that-already-exist.test-plan.md` § "Harness defects this suite is built around"; `test/the-proposal-loop.test.js` S13, `test/capture-a-goal-and-see-it.test.js` S8. |
| 113 | meta | **A UI story can pass every source-level test with a screen that renders nothing — and the harness had no level at which that could fail.** `goal-intent-fields` #3's Gate-3 judge built the counterexample: the ratified formatter implemented faithfully, imported into all three screens, all four properties named, every formatter **called**, and **none of the results rendered**. The Gate-3-approved Node suite reported **37 passed, 0 failed**; the owner saw a blank screen. Reproduced exactly at round 2 before the fix landed. **Root cause is a test-level premise that was false and was copied from the ADR into the test plan:** *"this project has no browser test harness, and adding one is new tooling."* Both halves are wrong — `playwright.config.js:32` defaults `baseURL` to `http://localhost:7778` (**the local control panel**), `@playwright/test` is already a dependency, `tests/brainstorm/` holds 20+ control-panel specs, and `engineering-team/stories/done/tapestries/3-create-tapestry.test-plan.md:51` **already ratified Playwright at a Gate 3** ("Node built-in runner + Playwright. No new frameworks."). Closed for this story by ADR 0003 Amendment 1 + `tests/brainstorm/goal-intent-screens.spec.js`. **The general lesson, and why this is a row rather than a story note:** *an assertion that a token appears in a file is not an assertion that a value reaches a screen*, and every S-class-only UI story in this harness has the same hole. **Candidate fix:** `roles/tester.md` states that any story whose acceptance criteria say "visible", "shown" or "appears" needs a browser-level test, and names the `tapestry-create.spec.js` pattern; `workflows/3-test-design.md`'s RED step requires the render-deletion probe (implement, delete the render calls, confirm red) for UI stories. | 2026-07-27 (goal-intent-fields #3 Gate-3 kick-back) | OPEN | | `engineering-team/decisions/goal-intent-fields/0003-screen-side-intent-display-and-the-narrow-d13-supersession.md` § "Amendment 1"; `tests/brainstorm/goal-intent-screens.spec.js`; `engineering-team/stories/goal-intent-fields/3-show-the-four-on-the-goal-screens-that-already-exist.test-plan.md` § "Adversarial verification 2". |
| 114 | meta | **`npm test` cannot fail on a browser-level defect, and nothing says so at the point of use.** The Node gate does not run Playwright, and `npm run test:playwright` is not wired into any gate — so a full-gate `Overall: PASS` is compatible with a UI that renders nothing (row 113). Two smaller edges found while building `goal-intent-fields` #3's browser class, both worth stating wherever the browser class is documented: (a) **`tests/global-setup.js` THROWS when the server is unreachable**, aborting the whole Playwright run rather than skipping — the per-spec `BRAINSTORM_SERVER_ACCESSIBLE` guard never gets a chance to skip, and global-setup **sets that variable itself**, so exporting it by hand is a no-op; (b) **the control panel serves `<repo>/dist`, so a `ui/src` edit is invisible to a browser test until `cd ui && npm run build`** — a stale bundle silently tests the previous build. (b) is now guarded content-wise by that spec's `B0`; an mtime-based first draft of B0 was wrong because `git checkout` / `git stash` / a fresh clone all rewrite source mtimes. **Candidate fix:** name both in `roles/tester.md` alongside the browser-class guidance, and decide whether the review gate should run `npm run test:playwright` for stories that touch `ui/`. Appended 2026-07-28 (drafted at the store-and-show close, inserted at the add-a-concept close): (a) **`engineering-team/roles/reviewer.md:21`** reads *"test: `npm test` (or `npm run test:playwright`)"* — for any story with a browser-visible surface the two gates are **conjunctive**, not alternative, and 37/0 on a build that rendered nothing was exactly row 113's finding; it is the first file every Reviewer reads; suggested wording: *"run every gate the story's test plan names."* (Review #3 and that run's journal both cite this as :22; the line is :21.) (b) **A Playwright spec that runs zero tests also exits 0** — check *"Running N tests"* against *"N passed"*, not just the exit code. Both port to the human-gated flow unchanged. | 2026-07-27 (goal-intent-fields #3 test design, round 2) | OPEN | | `tests/global-setup.js` :44/:50; `bin/control-panel.js` :124; `ui/vite.config.js` outDir; `tests/brainstorm/goal-intent-screens.spec.js` `B0`; relates OPEN.md #113. |
| 115 | meta | **PRD §2's competitive claim is partially falsified: it credits the Second Brain with several firsts that belong to shipped systems.** An adversarial six-lineage prior-art sweep (2026-07-27, 183 systems, 7 falsifiable novelty claims, 6 sent to refuters) killed 4 claims at high confidence and 1 at medium; 1 survived narrowed. Named refuters: **BMAD-METHOD v6** (a story-only agent barred from code, plus a skill spawning a blinded-reviewer pipeline bound to one story node — i.e. the "Executor" role, shipped), **CAMEL Workforce** (recursive worker creation against a shared task-graph node, nested workforces, atomic claiming), **UCAN v1.0** (bounded ancestry walk to an owner-signed root with a non-widening invariant), **Perkeep**/Haven/Anytype (signed-but-private provenance, mechanism-enforced), **BB1** 1985 ("the store is the scheduler"), **EURISKO** 1982 (per-item budget), **ActiveGraph** 2026 (closest single system). §2 is not wrong that no product combines all three properties, but its framing overstates novelty and is thinly sourced. **What survives** is a narrower conjunction (ratification as a separate optional append-only fact + launch authority computed from that lineage + verbatim terms drift-halt). The PRD is immutable, so the correction lands as a companion artifact, not an edit. **Consumer:** Phase-2/Phase-3 scoping — the sweep also names 8 uncovered failure modes (ungated between-run intent edits being the largest) and 6 concrete borrowings. | 2026-07-27 (prior-art sweep, `second-brain` positioning) | OPEN | | `product-team/discoveries/second-brain-prior-art.md` (full findings + sources); `product-team/prd/second-brain.md` §2 (the superseded paragraph). |
| 116 | meta | **Committed test artifacts from earlier stories go brittle when a later story lawfully changes the code, and no phase owns amending them — two instances converged on tapestries #5.** (a) Story #3's grep-sentinels S3/S8 (`test/create-tapestry.test.js`) regex-require picker terms (`queryRelay`/`39998`/`searchText`/`oSlugs`/`oKeys`/`description`) inside `useCreateTapestry.js`; ADR 0005's sanctioned extraction moved that code to `useConceptOptions.js`, and the Implementer — barred from touching tests — satisfied the old sentinels with a truthful docstring. Accepted at review (the behavioral pin moved to the new suite's S6/R4, and create-S8's page half still pins real code in `NewTapestry.jsx`), but S3/S8 are now prose-satisfied location-pins: on the next Tester-lane touch of that suite, relax them to probe the shared hook. (b) Story #1's Playwright AC-5 (`tests/brainstorm/tapestries-nav-and-directory.spec.js:191`) still asserts the inert Create-New-Tapestry placeholder that #3 deliberately replaced — a permanent failure in every Playwright run (verified pre-existing at the #5 baseline; outside the `npm test` gate): rewrite or retire it against the shipped form (fold with #89(b), same file). Harness lesson: when an ADR sanctions moving/replacing behavior an older suite pins, the ADR/story should NAME the affected old tests so the Tester amends them inside the same cycle instead of leaving the Implementer a letter-vs-spirit dilemma. | 2026-07-28 (tapestries #5 review) | OPEN | | engineering-team/reviews/done/tapestries/5-add-a-concept-to-a-tapestry.md § Findings; story `## Deviations`; book journal 2026-07-28T06:45 disposition. |
| 117 | meta | **Prompt-level judge blinding is defeated by three structural channels that no spawn-prompt wording can close.** In `store-and-show-the-prompt-and-the-estimate` the Director voided a Gate-2 APPROVE for broken blinding and then found the leak is not only carelessness: (a) **artifacts carry gate history** — a story's `Supersedes: … KICK_BACK` line, and ADR 0002's own *"this epic has already spent two Planning rounds on it"*; (b) **commit subjects name gate outcomes** — eight `journal: Gate N KICK_BACK …` subjects sit in `git log` for any judge that runs it; (c) **the Gate-1 rubric REQUIRES the epic file**, which is not on the blinding-safe input list and accumulates run history by design (kick-back counts, prior-verdict summaries) — so Gate 1 cannot be run to rubric without handing the judge prior verdicts. Every Gate-1 spawn in that run had (c). A fourth, milder instance: three consecutive judges disclosed that `grep -A N` on `book.md` pulls the adjacent generated Direction-mode block — the "frame section only" instruction is unenforceable at the tool layer. **Why it matters:** the blinded gate is the single strongest control in Direction mode, and its enforcement is currently by instruction over a shared filesystem. **Candidate fixes:** put gate history somewhere no rubric requires a judge to read (a per-run journal, not the epic file); have Gate 1 check the epic's existence via a derived assertion rather than by handing over the file; or accept that blinding is best-effort and say so in `roles/director.md` rather than treating a leak as a protocol breach. Goalpost-class — frozen mid-run by design, so it lands here. **Fifth channel, observed at the `add-a-concept-to-a-tapestry` close (2026-07-28): role-authored artifacts carry run meta-state into judged inputs** — the (unblinded) Product Owner read the book's operator story cap despite a scoping instruction not to, and the story review's mandated On-PASS section carried a cap remark that the Gate-5 judge then met inside a judged artifact (the judge classified it honestly: artifact-embedded exposure, not a spawn-prompt leak). Same structural class. **DONE 2026-08-04 — closed by `harness-gate-integrity` #2 (ADR 0002, book `blinding-rebuild`): Gate 1 hands the judge a pinned epic-status extraction it runs itself (the epic file is never opened); verdict outcomes are journal-only with outcome-free journal-commit subjects; lint L14 pins the artifact token shapes (template guidance lines ride `f4a84bcc`); role spawns scoped like judge spawns and completion arithmetic barred from the review file; #133's pinned reads ratified as the frame-channel answer and extended to field extractions.** | 2026-07-27 (store-and-show book close, audit §7 P1/P2; drafted there, inserted 2026-07-28 at the add-a-concept close) | DONE | | `engineering-team/audits/store-and-show-the-prompt-and-the-estimate/journal.md` 2026-07-27T02:49:44Z + 09:42:22Z; `engineering-team/roles/director.md`; `engineering-team/audits/add-a-concept-to-a-tapestry/journal.md` 2026-07-28T04:15:00Z + 08:05:00Z. |
| 118 | meta | **Roles and Directors report actions as completed that were not, and every instance was checkable in under a minute.** Four in one book (`store-and-show-the-prompt-and-the-estimate`): the Reviewer claimed it caught a vacuous test that was not vacuous; the Architect reported adding a reciprocal ADR pointer to an unmodified file; the Director amplified the first into an immutable commit subject and an operator report; and `git add` on five evidence logs succeeded silently while `.gitignore`'s `*.log` excluded all five, producing four commit messages that falsely claimed a log was committed. **The shared shape is a silent success believed rather than checked** — `git add` does not error on an ignored path, and a role summarising *"the ADR specifies it"* compresses to *"added"*. **The sharper lesson, named independently by two agents within an hour:** conclusions that are right protect provenance that is wrong, because nothing downstream forces a re-read; the Director's own words were *"I verify what I expect to be wrong and accept what I hope is right."* **Candidate fix:** state in `roles/director.md` and the phase workflows that a claim of a completed file action is verified by `git show --name-status` / `git status --porcelain` before it enters a commit message or a report — the check is seconds and the record is immutable. **Ports to the human-gated flow unchanged.** Note: the source journal's own tally of this finding (`:544`, *"three from roles, four from me"*) does not sum; the checkable inventory is two role-originated and two Director-originated. | 2026-07-27 (store-and-show book close, audit §7 P7/P10; drafted there, inserted 2026-07-28 at the add-a-concept close) | OPEN | | `engineering-team/audits/store-and-show-the-prompt-and-the-estimate/journal.md` 2026-07-27T05:13:51Z, 08:58:28Z, 10:20:00Z; that book's `book.md` § "Record corrections". |
| 119 | meta | **`scripts/harness-stats.sh` is blind to Direction-mode gate outcomes, so the retro's own instrument under-reports rework on exactly the books that generate most of it.** The script parses review verdict files and phase-commit subjects; it has no awareness of `journal.md`, gates, KICK_BACKs or HALTs. It therefore scored `store-and-show-the-prompt-and-the-estimate` at **kick-back rate 0, churn 0** — all three reviews passed first time — while that book's journal records **8 gate KICK_BACKs and 3 HALTs** across 47 entries. `workflows/6-book-close.md` step 7 instructs the retro to cite this script *"so the retro runs on measurement rather than anecdote"*; for a Direction book the measurement is structurally incomplete. (For `add-a-concept-to-a-tapestry` the gap happens to be benign — its journal records zero kick-backs — but that is the run, not the instrument.) **Candidate fix:** have the script parse `audits/*/journal.md` for `**Decision:**` lines and emit a per-book gate tally (APPROVE / KICK_BACK / HALT / ANSWER) alongside the review verdicts. Cheap — the journal format is already uniform and greppable. **DONE 2026-08-04 — exactly that candidate fix shipped as `harness-stats.sh` section (b2) (story `harness-gate-integrity` #2, ADR 0002): per-book APPROVE/KICK_BACK/ANSWER/HALT/INFO from journal `**Decision:**` lines + a direction-gates summary; store-and-show now reports 17/8/5/3/15 live (fixture-pinned test), fleet totals 94 approve / 14 kick-back / 6 halt across 10 journaled books.** | 2026-07-27 (store-and-show book close, audit §7 P8; drafted there, inserted 2026-07-28 at the add-a-concept close) | DONE | | `scripts/harness-stats.sh`; `engineering-team/workflows/6-book-close.md` step 7. |
| 120 | meta | **"Journaling is not recording" — a ratified answer must land in the epic's decision list at the moment of ratification, and nothing in the harness enforces it.** This failed three times in one book (`store-and-show-the-prompt-and-the-estimate`). The third occurrence was the worst: an operator-ratified supersession of a live `Accepted` ADR lived only in the Director's journal, which no role reads — so an Architect would have reached Gate 2 mandated by the story into a collision with that ADR and with no recorded authority to resolve it. The epic now carries the practice in its own words (*"a ratified answer is recorded in this epic's decision list at the moment of ratification, with its provenance"*), but that is a per-epic file no future run reads, and `roles/director.md` carries no such rule. **The failure mode in one line:** treating "I have decided something" as equivalent to "the artifacts know it." **Candidate fix:** a rule in `roles/director.md` — an ANSWER journal entry is not complete until the answer is written into the epic's decision list, with provenance, in the same commit. Ports to the human-gated flow, where the Product Owner plays the same role. | 2026-07-27 (store-and-show book close, audit §7 P9; drafted there, inserted 2026-07-28 at the add-a-concept close) | DONE (2026-07-28 — `roles/director.md` "Answering as the user" rule 7, operator-ratified; CHANGELOG row same date) | | `engineering-team/audits/store-and-show-the-prompt-and-the-estimate/journal.md` 2026-07-27T09:51:22Z + 10:05:14Z; `engineering-team/epics/goal-intent-fields.md` (retired: `epics/` file, decision-list practice); `engineering-team/roles/director.md`. |
| 121 | meta | **Closing a book makes an epic close-out mandatory in the same breath, and `workflows/6-book-close.md` never says so — so its own test gate is guaranteed red when it is run.** `harness-lint` L2 (*"a Closed book ⇒ every epic it lists is Done"*, `scripts/harness-lint.sh:126-150`) fires the instant step 9 flips `book.md` to `Closed`; the workflow has no step for the epic close-out, never mentions L2, and step 8 then asks the same Reviewer to run `npm test` and record the result. Found by running the gate at the close of `store-and-show-the-prompt-and-the-estimate`: `Overall: FAIL`, `EXIT=1`, one violation, every other suite green. **Not a one-off** — it fires on every book close where the epic close-out has not already happened, and the epic close-out is triggered by a *different* event (the epic's merge to the shared line) in a *different* workflow (`5-review.md`). **Not merely bookkeeping either:** the close-out includes directory `git mv`s that invalidate `stories/<epic>/…` path references in the book's own artifacts — a cross-cutting rename that wants an explicit decision, not a silent side effect of a close. **Candidate fixes, in increasing order of ambition:** (a) add an explicit step to `6-book-close.md` between the flip and the gate — *"if the book's epics have shipped, run the epic close-out first"* — and order the gate after it; (b) have L2 accept a Closed book whose epic is Done-but-not-yet-moved, decoupling the status flip from the folder move; (c) make the `done/` move link-safe, or stop moving folders and mark retirement by status alone. **Ports to the human-gated flow unchanged** — L2 does not care which flow produced the book. Note from the add-a-concept close (2026-07-28): that close performed the epic close-out *inside* the close (fix (a) executed manually — epic Done + per-file moves before the gate), and the gate stayed green; the workflow still doesn't say to. | 2026-07-27 (store-and-show book close, audit §7 P14; drafted there, inserted 2026-07-28 at the add-a-concept close) | DONE | 2026-07-28 — operator-ratified harness commit (PR #478): `workflows/6-book-close.md` reordered — flip (8) → epic close-out (new 9) → gate over the final state (10); fix (a) | `engineering-team/audits/store-and-show-the-prompt-and-the-estimate/audit.md` §5 + §7 P14; `scripts/harness-lint.sh` :126-150; `engineering-team/workflows/6-book-close.md` steps 8–9; `engineering-team/workflows/5-review.md` § "Epic close-out". |
| 122 | meta | **Committed journal entries can carry future timestamps relative to their own commits — five instances in one Direction book, after the run itself caught and fixed a sixth pre-commit.** In `add-a-concept-to-a-tapestry` (entry stamp vs commit time, all 2026-07-28 UTC): Gate 1 stamped 04:22:00Z, committed 04:20:17Z (`c1a0920e`); Gate 2 stamped 04:45:00Z, committed 04:43:26Z (`1dad85c4`); Gate 5 stamped 08:05:00Z and the local-cycle entry stamped 08:35:00Z, both committed 08:00:01Z (`ff85bd43`); the completion-judge entry stamped 08:25:00Z, committed 08:14:35Z (`f10b53f5`). The local-cycle stamp also breaks the journal's own order (it precedes the staging entry stamped 08:05:00Z) and briefly makes the staging merge (08:00:36Z, GitHub-fixed) look like it preceded Gate 5. The journal's 04:38 entry records catching exactly this defect once pre-commit (*"first stamped 04:40:00Z, ahead of the real clock — an editing error"*) — the failure mode was known and recurred uncaught. Chronology is recoverable from `git log --format=%ci`; no decision hinged on a stamp; but the journal is the record the close is told to treat as authoritative, and stamps that run ahead of the wall clock invite false sequence inferences. **Candidate fix:** stamp entries by reading the clock at write time, never by estimating; a cheap pre-commit check — no journal stamp later than the commit's own clock — would have caught all six. | 2026-07-28 (add-a-concept book close, audit §7) | OPEN | | `engineering-team/audits/add-a-concept-to-a-tapestry/audit.md` §7 F2; journal vs `git log --format='%ci'` on `c1a0920e` / `1dad85c4` / `ff85bd43` / `f10b53f5`. **Recurred in `take-a-concept-back-out` (2026-07-30), caught mid-run this time:** five committed stamps post-dated their commits by 3–60 min (estimate-forward instead of `date -u`); corrected in-flight (`39b3de9e`, practice = `date -u` per write). Book-close cross-check still found minute-scale residue on three post-correction entries (+22s/+68s/+3min), clean thereafter — `audits/take-a-concept-back-out/audit.md` §7 F5. |
| 123 | meta | **A spawned gate judge that backgrounds a long command and ends its turn is reaped, not resumed — the spawn dies verdict-less.** add-a-concept Gate 3 (2026-07-28): the first blinded judge launched the full ~32-minute `npm test` as a background task and ended its turn expecting re-invocation when the run finished; the harness reaped the task instead (task registry forgot the ID; transcript ends at the status note; no test process left running). The Director re-spawned fresh — one-spawn-one-reply preserved, no follow-up sent — adding one neutral prompt line: *run verification commands in the foreground; a turn ended with background tasks running is never resumed*. The re-spawned judge ran the full suite in the foreground and returned a verdict. Without that line, any judge that backgrounds a long verification stalls the same way. **Candidate fix:** make the foreground-mechanics line a standing part of the gate-judge spawn instructions (harness commit — operator's to ratify) instead of per-run folklore. Ports beyond Direction mode: any subagent asked to run the ~32-minute gate has the same failure shape. | 2026-07-28 (add-a-concept book close, audit §7) | DONE | 2026-07-28 — operator-ratified harness commit (PR #478): foreground-mechanics block added to `.claude/agents/gate-judge.md` | `engineering-team/audits/add-a-concept-to-a-tapestry/journal.md` 2026-07-28T05:20:00Z + 05:53:00Z. |
| 124 | docs | **`docker cp` into the container's brainstorm path writes through to the host repo (bind mount) — a deploy experiment can silently edit the checkout and stack stale hashed assets.** Surfaced in add-a-concept #5: the Implementer briefly clobbered the deployed `index.html` during a baseline experiment and repaired it via a clean rebuild (vite `emptyOutDir`) + redeploy, final state verified; `/cycle-local` re-verified the served bundle afterwards. OPERATIONS.md's container-layout section should say this out loud: locally the repo is bind-mounted into the container, so `docker cp` into `dist/` and a host-side build converge on the same files — the safe idiom is rebuild-and-let-the-bind-mount-serve, not `cp`. | 2026-07-28 (add-a-concept book close, audit §7) | OPEN | | `engineering-team/audits/add-a-concept-to-a-tapestry/journal.md` 2026-07-28T06:45:00Z (Implementer findings); OPERATIONS.md § container layout. |
| 125 | docs | Ship the `add-a-concept-to-a-tapestry` book's close trail to `staging` as a **docs-only PR**, then delete `feat/add-a-concept-to-a-tapestry` (local + remote). The feature merged as PR #476 (`ac09d591`, deploy run 30340623300 green), but everything after `ff85bd43` is branch-local: the completion report (`f10b53f5`) and the close commit (audit + prd-seed + book Closed + epic `tapestries` retirement + these OPEN.md rows). Mirrors the live-feed (#4) / open-ranking (#11) / verified-muters (#12) / test-hermeticity-ci (#23) closes. Separately: promoting tapestries #5 itself `staging`→`main` is an operator decision (nothing from this book is on `main`). | 2026-07-28 (add-a-concept book close) | DONE | 2026-07-28 — shipped via close-trail PR #477 (this row rides it); branch deleted post-merge | `engineering-team/audits/add-a-concept-to-a-tapestry/book.md` § Close artifacts. |
| 126 | meta | **`most-pinned-tag-index-publish` AC-8 is now a deterministic-leaning full-suite red: presence-in-page assertions decay as publish-flow fixtures accumulate in the shared local relay — sibling of #75, page-falloff variant.** Signature: `AC-8 … expected 1, got undefined`. Mechanics, measured 2026-07-29 on `ea2250d0`+docs: `/api/profile-tags/index?sort=most-pinned` hard-caps the page at **200 rows** (a `limit=1000` request still returns 200); the union holds **974 rows** and rows with `pinnedCount ≥ 1` fill the whole cap page. AC-8's fixture passes AC-1..AC-9 at `pinnedCount 2` (top-7), then the kind-5 drops it to 1 — into a several-hundred-row tie cohort ordered by `tagEventId asc`, where the fresh random id must land inside the ~193 visible slots to be found. An id lottery with decaying odds: green in the 2026-07-28 add-a-concept close gate, red ×2 (full + isolated, router quiesced) in the take-a-concept-back-out Stage-0 baseline. Not a code regression (endpoint honors its contract; #479–#481 don't touch the pin index). Halted that book's run at Stage 0 (red baseline is not the Director's to fix). Remedies for triage, operator's pick: fixture cleanup in the local relay (data deletion — operator-sanctioned only); and/or harden AC-8 to assert via a fixture-scoped read (e.g. the `/api/profile-tags/pins?viewerPubkey=` path it already uses for lookup) instead of presence in the capped page — tester-role lane, folds into #75's drift-tolerant-assertion guidance. | 2026-07-29 (take-a-concept-back-out Stage-0 baseline) | OPEN | | `engineering-team/audits/take-a-concept-back-out/baseline-2026-07-29.log`; journal 2026-07-29T06:45:00Z; `test/most-pinned-tag-index-publish.test.js:321–351`; OPEN.md #75. |
| 127 | docs | **CLAUDE.md's per-deployment TA-pubkey section cites a stale local-dev value:** "the local-dev value (`82b75e47...973833` on this machine)" — the live assistant pubkey on this machine is `11f23fe40984a07be717d1628bdd0e87a2b4569f05dd7625923c20b89df93767` (verified twice 2026-07-29 via `GET /api/assistant/pubkey`; the TA identity was evidently recreated since the line was written). The rule the line illustrates (resolve at runtime, never hardcode) is unaffected and is exactly what catches the staleness; fix is to drop or refresh the parenthetical example so it can't anchor a wrong assumption. Filed per CLAUDE.md's own "orientation doc that misled you" discipline. | 2026-07-29 (take-a-concept-back-out Stage-0 preflight) | OPEN | | CLAUDE.md § "Per-deployment TA pubkey — NEVER hardcode"; `engineering-team/audits/take-a-concept-back-out/journal.md` 2026-07-29T06:10:00Z. |
| 128 | meta | **A relay cleanup of test fixtures must first sweep test sources for pinned corpus ids — "fixture" is an overloaded word.** The 2026-07-30 operator-sanctioned cleanup (take-a-concept-back-out Stage 0; 5,621 events, timestamp-slug classifier, ambiguous→KEEP) was set-exact correct except its one manual override: `cpc-tag-fixture-1784351872-7e0ce5` was deleted *because* its name says "CPC Fixture Tag" — but `test/tag-detail.test.js:100` pins that tag's exact event id (`9ec193a7…25a14`) as a deliberately **persistent read corpus**, and the next full run failed 10 tag-detail tests on the 404 precondition. Repaired same session: signed event restored verbatim from the pre-delete backup (`strfry import`, added:1), isolated suite back to 28/0; post-hoc sweep proved blast radius exactly 1 (39 64-hex literals in test sources ∩ 5,621 deleted ids = this one). Lesson for any future cleanup (and for the #126 remediation when executed): run the hex-literal/slug-literal sweep **before** deleting, and treat "long-lived fixture a suite reads by id" as protected corpus, not junk; conversely testers should prefer minting per-run corpus or self-healing setup over pinning relay state by literal id. | 2026-07-30 (take-a-concept-back-out Stage-0 cleanup) | OPEN | | `engineering-team/audits/take-a-concept-back-out/journal.md` 2026-07-30T05:44:30Z; `test/tag-detail.test.js:100`; OPEN.md #126. |
| 129 | meta | **harness-lint L2 has a reopen blind spot: a legitimately reopened epic violates L2 for as long as any CLOSED book names it — even when an OPEN book is actively running a story under it.** Fired 2026-07-30: Planning for `take-a-concept-back-out` #6 reopened `epics/tapestries.md` (Done → Active, the designed reactivation the add-a-concept book precedented), and `check_L2` flags it because the *closed* `add-a-concept-to-a-tapestry` book lists the epic — the rule (`scripts/harness-lint.sh:126–150`) checks closed books' epics against `Done*` with no carve-out for a newer open book naming the same epic. Every full `npm test` red-lines until waived. Run-scoped waiver added (this row); **remove the waiver at the take-a-concept-back-out book close** (the close-out returns the epic to Done). Proposed rule fix for triage: L2 should pass an epic that any `**Status:** Open` book lists in its own `## Epics in this book` — reopening is a designed lifecycle move, not drift. | 2026-07-30 (take-a-concept-back-out Test Design; Tester surfaced, Director dispositioned) | DONE | 2026-08-04 — waiver-removal obligation discharged at the take-a-concept-back-out book close: epic `tapestries` re-retired Done, the L2 waiver line removed from `scripts/harness-lint-waivers.txt`, and `harness-lint` verified clean without it. The row's **proposed rule fix remains proposed, not implemented** (per its own text: L2 should pass an epic that any `**Status:** Open` book lists in its `## Epics in this book`) — pick it up at the next L2 touch or the next epic reopen; audit `audits/take-a-concept-back-out/audit.md` §7 F1. | `scripts/harness-lint.sh` check_L2; `scripts/harness-lint-waivers.txt` (waiver citing this row); `engineering-team/audits/take-a-concept-back-out/journal.md` 2026-07-30 Test Design entries. |
| 130 | docs | Ship the `take-a-concept-back-out` book's close trail to `staging` as a **docs-only PR**, then delete `feat/take-a-concept-back-out` (local + remote). The feature merged as PR #482 (`cafbc7cc`, deploy run 30538834189 green), but everything after the merge is branch-local: the completion report (`18fbc56f`), the final completion audit (`5c8196a3`), the operator attestation (`482a7bab`), and the close commit (audit + prd-seed + book Closed + epic `tapestries` re-retirement + these OPEN.md rows). Mirrors the add-a-concept close (#125). **Resolved 2026-08-04: PR #483 (docs-only) opened and merged the same day — the full close trail (completion report, final journal, audit, prd-seed, epic re-retirement, waiver removal, rows 129–133) landed on `staging`.** | 2026-08-04 (take-a-concept-back-out book close) | DONE | 2026-08-04 | `engineering-team/audits/take-a-concept-back-out/book.md` § Close artifacts. |
| 131 | docs | **Operator's deferred batch promotion `staging`→`main`:** staging carries tapestries #5 (add-a-concept, PR #476) **and** #6 (take-a-concept-back-out, PR #482) with neither on production. At the #6 close the operator said, verbatim: *"let's not promote to production in this session. I will probably batch-promote multiple related features to production at some point in the future."* Recorded so the intent isn't lost — when the batch is assembled, both tapestries features ship together (plus whatever else has stacked on staging by then; see #9/#12 for the older staging-held bundles and their ordering constraints). | 2026-08-04 (take-a-concept-back-out book close; journal 2026-08-04T16:01:06Z) | OPEN | | `engineering-team/audits/take-a-concept-back-out/audit.md` §4 #8, §6. |
| 132 | meta | **The auto-mode permission classifier blocks operator-sanctioned/in-frame operational acts, and the workaround patterns are folklore.** Twice in one Direction run (take-a-concept-back-out): (a) the *operator-sanctioned* relay-cleanup delete loop was denied twice — executed instead as individual transparent per-chunk calls, identical effect, honestly journaled; (b) the Stage-2 live-demo publish POST was denied twice — the Director ruled the demo non-essential rather than overriding, leaving a disclosed demonstration gap that stayed open five days until the operator performed the click-through personally (attested 2026-08-04; relay state corroborates). The classifier cannot see operator sanction; each denial forces a per-run judgment call. **Candidate fix (operator's to ratify):** name the two honest workaround shapes in `roles/director.md` / the direct-feature skill — *sanctioned-op denied → re-run as individual transparent calls, never alter effect; demo denied → prepared-not-executed + record + offer as a one-click operator act* — so future runs inherit the pattern instead of re-deriving it. **Resolved 2026-08-04: operator ratified both candidate fixes in-session; shipped as the harness amendments in `chore/harness-retro-132-133` (CHANGELOG rows same commit).** | 2026-08-04 (take-a-concept-back-out book close, audit §7 F2) | DONE | 2026-08-04 | `engineering-team/audits/take-a-concept-back-out/journal.md` 2026-07-30T05:33:30Z + 11:29:30Z + 2026-08-04T15:50:00Z. |
| 133 | meta | **A blinded gate judge told to read only part of a file can over-read past the instructed scope — pin partial reads to an explicit line range in the spawn prompt.** take-a-concept-back-out Gate 5, first spawn: the prompt was clean, but the judge's `book.md` read overran the frame-only instruction into the operator-instructions section (deadline + story cap exposed); the judge self-reported, the APPROVE was voided per protocol, and the re-spawn with the read pinned mechanically (`sed -n '1,36p'`, full file never opened) held. The scoped-read mechanic is proven but lives in per-run folklore — `.claude/agents/gate-judge.md` has no such rule (checked at the book close). **Candidate fix (operator's to ratify):** a standing line in the gate-judge spawn instructions: when an input must be read partially, the spawn prompt gives an explicit line-range command, not a "stop at section X" instruction. Same path as #123 → PR #478 (foreground-mechanics line). **Resolved 2026-08-04: operator ratified both candidate fixes in-session; shipped as the harness amendments in `chore/harness-retro-132-133` (CHANGELOG rows same commit).** | 2026-08-04 (take-a-concept-back-out book close, audit §7 F3) | DONE | 2026-08-04 | `engineering-team/audits/take-a-concept-back-out/journal.md` 2026-07-30T08:52:00Z + 08:56:30Z. |
| 134 | ops | **The staging droplet's Docker build cache went cold — the first deploy after a 5-day gap rebuilt the apt layer from scratch and died on the workflow's 25-minute `command_timeout` (run 30929472917, 2026-08-04): ~150 packages at ~1 pkg/s, killed mid-download of the 48 MB openjdk-17 package.** The merge was docs-only (close-trail PR #483), which cannot invalidate an apt layer — the cache loss is droplet-side (buildkit GC under disk pressure, or a re-pulled base image digest). Site stayed 200 throughout on the prior containers. Needs droplet-side attention before the next code deploy: warm the build manually without the 25m leash (`docker compose build` in `/opt/tapestry`), or raise `command_timeout` in `.github/workflows/deploy-staging.yml` (a CI-config change that must ride a story), and check droplet disk headroom. The operator's planned batch promotion will hit this on its staging leg if unaddressed. **Update 2026-08-04 (operator acted):** staging droplet warmed by hand (`docker compose build`, un-leashed) and containers swapped (`docker compose up -d`, all four up); `df -h` shows 26% disk use — **disk pressure ruled out**, leaving builder-cache aging / base-tag re-resolution as the standing mechanism; second timeout run 30931607940 (same signature) added as evidence. **Base-image digest-pinning DECLINED by the operator** ("I would probably forget we have done it and that would come back to bite us" — silent-staleness risk outweighs surprise-rebuild risk). `command_timeout` raised 25m→50m across all five deploy workflows instead (chore/deploy-command-timeout). Remaining: operator to warm prod + tags droplets the same way before the batch promotion (row 131). | 2026-08-04 (take-a-concept-back-out close-trail deploy; Director diagnosed from CI logs — droplet SSH out of lane) | OPEN | | run 30929472917 log (apt layer #19 timeline); `.github/workflows/deploy-staging.yml` (`command_timeout: 25m`); staging health checks 16:30–16:56Z all 200. |
| 135 | bug | **Staging admin list carries a ghost fifth administrator whose npub renders as `""...t;"` — an HTML-entity-mangled value stored as data — and `docker compose up -d` on the droplet warns `ADMIN_PUBKEYS` is unset.** Observed by the operator 2026-08-04 on `https://staging.brainstorm.world/legacy/manage-administrators.html`: four manually-added admin profiles render correctly, plus the malformed fifth (symptom suggests an escaped-quote fragment, e.g. `&quot;…&gt;`, was accepted into the stored list at some point). The env warning is likely benign-but-confusing: the admin read path is settings.json-first with `BRAINSTORM_ADMIN_PUBKEYS`/env as fallback (`src/api/admin/index.js:109`, `src/utils/config.js:91–104`), and settings.json lives in the persistent `tapestry-data` volume — which is why the four real admins survive with the env var blank. To do, later, per operator ("file this away as a problem to deal with later"): inspect the stored list, delete the malformed entry, find the write path that accepted un-decoded entities, and decide whether the compose `ADMIN_PUBKEYS=${ADMIN_PUBKEYS}` line should have a documented droplet-side value or be removed as dead. | 2026-08-04 (operator observation during droplet warm-up; Director filed) | OPEN | | `legacy/manage-administrators.html` (staging); `docker-compose.yml:17`; `src/api/admin/index.js:109`; `src/utils/config.js:91–104`; settings.json in `tapestry-data` volume. |
| 136 | bug | **Tapestry elements are "letter-only" — the create-tapestry flow publishes the kind-39999 event to strfry but never writes Neo4j, and no pipeline ingests kind-39999 into the graph — so Neo4j-sourced reads (ConceptElements' explicit-`HAS_ELEMENT` ∪ implicit-z-tag union) show zero tapestries while the strfry-sourced View Tapestries directory shows them all.** Verified locally 2026-08-04: `39999:<TA>:b0b48b00` (Tapestry for Farm Animals) is in strfry with a valid z-tag to `39998:<TA>:tapestry` but has no Neo4j node at all, while the concept header + Superset + `IS_THE_CONCEPT_FOR` edge are all present and both page queries run clean-empty. Same gap presumed on staging (Tapestry for Cat, authored via the same flow) and on any instance; prod has no tapestries yet. Doctrinal frame (owner-confirmed this session): BIBLE **§30** (ratified 2026-07-25) inverts ADR `tapestries/0001`'s "strfry is the durable source of truth" premise (written 2026-07-23, two days earlier) — Neo4j is the definitive "me," events are letters, and the authoring flow currently mails letters the brain never thought. Fix direction, owner-agreed: **(1) brain-first dual-write in tapestry authoring** — model on `handleCreateElement` (`src/api/normalize/index.js:1760`: sign → `publishToStrfry` → `importEventDirect` → `:ListItem` → `HAS_ELEMENT` wiring); the TA-signed path is straightforwardly server-side, the NIP-07 client-signed path needs an ADR decision (the client can't write the brain; the server must ingest the client-published event); owner ratified 2026-08-04 that the story INCLUDES the derive step — stamp `tapestryKey` + trigger the generic word deriver (`src/lib/derivers/word.js`) at creation (per §29/§30 this is *derivation* from the brain — "dual-write + derive," never a co-equal third authored copy; reads fall back to inline json regardless) — and that the authored tapestry json adds a top-level `word` section alongside `tapestry`+`graph` (cross-doc uniformity + the general LMDB-completeness doctrine deliberately deferred to #137). **(2) then a general-purpose strfry→Neo4j letter ingest** (kind-scoped, not tapestry-specific) — seed exists: `src/api/neo4j/eventSync.js` (`GET /api/neo4j/event-check` + `POST /api/neo4j/event-update`, per-event import incl. z `HAS_TAG` wiring; a single event-update call would backfill farm-animals and light up ConceptElements' implicit query today). Coordinate with self-ontology stories 2–3 (provenance marking distinguishes ingested event-projections from asserted state; rebuild hardening keeps both safe). On landing: annotate ADR `tapestries/0001` (superseded premise) and revisit View Tapestries' strfry read — the brain becomes the read source once ingest exists. **2026-08-05 stage-1 SHIPPED:** brain-first dual-write landed as tapestries #7 / ADR 0007 (book `audits/brain-first-tapestry-authoring/`, staging PR #489; BIBLE §6 prose corrected — the "superseded premise" annotation lives there rather than editing the retired ADR in `done/`). Row stays OPEN for **stage 2 only** (the general strfry→Neo4j letter ingest + provenance + the read-source flip + backfill of pre-#7 tapestries: Farm Animals local, Cat + Organism staging). | 2026-08-04 (tapestry-elements mismatch investigation; supersedes #88) | OPEN | | BIBLE §30; engineering-team/decisions/done/tapestries/0001-nav-directory-and-strfry-element-read.md; ui/src/pages/tapestries/useCreateTapestry.js; src/api/normalize/index.js:1760; src/api/neo4j/eventSync.js; relates #50 (strfry-side forensics — partially defused: the dog element was never on this box) and #69. |
| 137 | docs | **LMDB completeness doctrine is undefined — decide and ratify (BIBLE §29/§30 territory): what is the tapestry-store *for* operationally, and what does a node "owe" it?** Concretely: (a) when must a node's LMDB doc be fully derived (all authored content sections + `word` + `graphContext`); (b) when may the `word` top-level section be omitted (authored tapestry json ships without it today; the generic word deriver backfills it at derive time); (c) when may the LMDB entry be omitted altogether (today 2,854 of 4,435 local nodes have none and 1,064 lack a `tapestryKey`; every read path falls back to inline json via the §29 resolver — documented "harmless and expected"). Raised while scoping #136 and deliberately split out as wider-than-that-story (owner, 2026-08-04): #136 ratified only the tapestry-local answers (derive at creation; author `word` alongside `tapestry`+`graph`) without settling the general doctrine. Natural home: the **self-ontology epic** — this is §30's Coverage obligation + anticipated story 4 (deriver audit / serialization mode) territory, adjacent to the handoff's open questions (`docs/SELF_ONTOLOGY_DESIGN_HANDOFF.md` §11); ratify the answers into §29 (or §30 block 2) in docs-mode when taken up. | 2026-08-04 (raised scoping #136) | OPEN | | BIBLE §29 + §30 (Coverage; LMDB dual role); docs/SELF_ONTOLOGY_DESIGN_HANDOFF.md §11; engineering-team/epics/self-ontology.md (anticipated story 4); src/lib/derivers/word.js; `GET /api/tapestry-key/status`. |
| 138 | enhancement | **`brainWrite` failures are UI-silent in the tapestry authoring flows.** Since tapestries #7 the publish response carries an additive `brainWrite` result, but `useCreateTapestry` / add-a-concept / take-a-concept-out read only `success` — a partial outcome (`success:true, brainWrite:{success:false}`) is server-logged and response-visible yet invisible to the authoring user, silently re-opening the split-brain for that one element. Repair tool exists (`POST /api/neo4j/event-update`). Fix candidates: surface a non-fatal warning in the three flows, or fold into #136 stage 2 (the ingest owns reconciliation). | 2026-08-05 (tapestries #7 review, non-blocking finding 2) | OPEN | | engineering-team/reviews/done/tapestries/7-brain-first-tapestry-authoring.md § Findings; ui/src/pages/tapestries/useCreateTapestry.js:58-63. |
| 139 | bug? | **The word deriver's base prefers the cached LMDB doc over the node's refreshed json tag (`getExistingWordJson`, src/lib/derivers/word.js:42-45) — any content-refresh path that re-derives without invalidating first re-derives from its own stale output.** Surfaced by tapestries #7's I6 (add-republish kept the old member list in the derived doc); that story's hook now invalidates locally (`store.remove` pre-derive) and deliberately left the general behavior alone. Whether other write paths (e.g. `save-element-json`) hit the same staleness is unaudited. Natural home: self-ontology anticipated story 4 (deriver audit / serialization mode) — fold this in when that story is planned. | 2026-08-05 (tapestries #7 review, non-blocking finding 3) | OPEN | | src/lib/derivers/word.js:42-45; src/api/strfry/tapestryBrainWrite.js (the local invalidation + comment); engineering-team/epics/self-ontology.md story 4. |
| 140 | meta | **Human-gated intake on a deploy-target branch: the phase commits landed directly on local `staging`, and `/cycle-staging`'s step-1 precondition ("on a feature branch, not staging/main — stop") then blocked the deploy until ad-hoc branch surgery (create `feat/*` at HEAD → checkout → push → PR; the `git reset --hard` step was classifier-denied and proved unnecessary — post-merge `git pull` fast-forwards local staging over its own commits).** The surgery worked and is now folklore; two candidate fixes for triage: (a) intake/plan-feature notes that a book expected to ship via PR branches at intake (the Direction-mode books already do this via their book branches); (b) cycle-staging documents the branch-at-HEAD recovery pattern for the on-staging case instead of bare "stop." | 2026-08-05 (brain-first-tapestry-authoring close retro) | OPEN | | audits/brain-first-tapestry-authoring/audit.md §7; .claude/skills/cycle-staging/SKILL.md § Verify preconditions. |
| 141 | meta | **`relationship-primitives` H8 is flaky-by-construction on a live-router relay: it asserts GLOBAL strfry scan-count equality around its add+delete cycle, so any ambient inbound event between its two brackets fails it.** Observed at the brain-first-tapestry-authoring close gate (count `6599652 → 6599653`, one ambient event on a ~6.6M-event store with the router streaming; immediate re-run 23/23; the failing book's own fixtures ruled out by direct count). The test's failure text already anticipates the class ("if a concurrent publisher … is suspected, quiesce it and re-run"). Candidate fixes on the next relationship-primitives touch: scope the bracket to a filter the test owns (its own author/kind namespace) instead of the global count, or one auto-retry before failing. Related discipline note for gate-runners (from the same incident, dispositioned in the audit §7): verify gates on the `Overall:` line's CONTENT (`grep -q 'Overall: *PASS'`), never through a wrapper that chains `; echo $?`. | 2026-08-05 (brain-first-tapestry-authoring close gate) | OPEN | | test/relationship-primitives.test.js H8; audits/brain-first-tapestry-authoring/audit.md §5 + §7; test-hermeticity-ci epic. |
| 142 | refactor | **The TA sign/publish/import primitives exist in three divergent copies — consolidate on `normalize/helpers.js`.** `signAndFinalize` + `publishToStrfry` are defined in `src/api/normalize/index.js` (:105/:115), `src/api/normalize/helpers.js` (:43/:53), and `src/api/trustedList/index.js` (:60/:73); `importEventDirect` in the first two (:135 / :64). The drift already bit once: helpers' copy extracted the node name from the `name` tag only while index.js' copy also read `names` (plural, concept headers) — so the self-declare endpoint's header re-import wiped `h.name` on `dog`/`dog-breed`, making them render as blank rows at the top of Concept Headers (diagnosed, data repaired, and helpers patched with the `names` fallback 2026-08-05, same session). Fix shape when taken up: point index.js and trustedList at the helpers.js exports (behavior-identical swap; helpers is already the extracted shared module) and delete the local copies — checking first for structural tests that pin either module's import surface, and mindful that ADR second-brain/0003 explicitly declined refactoring the 3,300-line normalize main path (this swap is narrower: three function bodies for three requires). | 2026-08-05 (self-declare header-name wipe) | OPEN | | src/api/normalize/helpers.js; src/api/normalize/index.js:105-165; src/api/trustedList/index.js:60-85; src/api/concept/selfDeclare.js (the caller that hit the drift). |
| 143 | meta | **`show-the-four` S5's absolute route-count pin (108) rots under unrelated additive features — full local `npm test` is red on the staging base.** The `goal-intent-fields` #3 suite pins `ui/src/App.jsx` at exactly 108 `path:` entries as its AC3 "no new route" proof; the Shared Concepts area (PRs #491–#494, commits `ace13982` + `5c4d962d`) legitimately added 8 routes, so the pin now fails (116 ≠ 108) on every branch cut from staging — first surfaced by self-ontology #2's docs-only regression run (2026-08-05), whose diff provably touches neither `ui/` nor `test/` (`git diff origin/staging -- ui/ test/` empty). Same brittle-pin genus as rows 109/112 (this same suite file). **CORRECTION (2026-08-06):** the original row claimed "Staging CI stayed green because the CI lane doesn't run this suite" — **wrong**: the CI Test workflow's stack-free lane runs it, and Test was red on every push from PR #491 (2026-08-05 07:29) until this fix — unnoticed because the check is not merge-required (the deliberately-untaken operator decision, ci-test-job D3). **FIXED (chore/ci-green, 2026-08-06, operator-approved):** the absolute route AND nav counts (the nav count was one drift behind — F1's nav entry would have broken it next) retired for name-based assertions carrying AC3's real intent (no route/nav named for the four; the three brain routes present); this was the pin's SECOND drift (a 2026-07-29 operator re-pin preceded it). The sibling-suite sweep for other absolute counts over shared files (`CSS_CUSTOM_PROPERTIES` in this same file is the known remaining instance) stays open under rows 109/112's umbrella. | 2026-08-05 (self-ontology #2 implementation gate) | DONE | 2026-08-06 | `test/show-the-four-on-the-goal-screens-that-already-exist.test.js` S5 (name-based since chore/ci-green); rows 109/112 (same file, sibling classes); `engineering-team/stories/self-ontology/2-ratify-instance-identity.md` AC12 disposition. |
| 144 | meta | **Stable-d-tag fixtures + server-side monotonic re-sign bumps = replaceable-event ties that silently keep the WRONG version.** The F5 write paths stamp re-signs `max(now, prev+1)` (the same-second race fix, ADR shared-concepts-adoption/0001) — so a suite teardown that bare-republishes the same coordinate with a blind `now` stamp can TIE (or lose to) the bumped version, and strfry's replaceable tie-break (lowest id) keeps an arbitrary one. In the F1 suite this left a fixture twin silently wired across runs, poisoning the next run's queue arithmetic through the S2a exclusion — appearing as an unreproducible-in-isolation "scan blindness" that cost a long forensic chase (probes, position-capped-filter tests, parallel observers) before the tie surfaced. Fix shipped in `test/adoption-candidates-queue.test.js`: every fixture write stamps `max(now, current+1)` after reading the coordinate's current version (`nextStamp()`), making the fixture lifecycle strictly monotonic. **Latent elsewhere:** `test/b-coverage-audit-and-disposition.test.js` H8's teardown bare-republishes its fixtures with blind `now` stamps after bumped re-signs — same class, has not yet bitten (its H-flow re-creates fixtures before asserting); adopt the `nextStamp` discipline there when that suite is next touched. Rule of thumb for the Tester role: **any teardown of a coordinate that a bumped re-sign path may have touched must read-then-out-stamp, never blind-stamp.** | 2026-08-06 (shared-concepts-adoption #2 implementation) | OPEN | | `test/adoption-candidates-queue.test.js` (`nextStamp`); `src/api/concept/bDisposition.js` `resignWithTags` (the bump); OPEN.md #128 (stable-d-tag fixture discipline this extends); #141 (the environmental-flake genus this is NOT — this one was deterministic once seen). |
| 145 | ops | **GitHub-outage flush queue (2026-08-06 session end): three merges + one deploy are staged locally-green and blocked ONLY on GitHub Actions recovering (multi-hour "major" outage; runs queue but never schedule).** Flush order, mechanical: **(1)** deploy run 31120905938 (PR #500's streaming fix — staging's `/api/adoption-queue` returns the maxBuffer error until it lands): `gh run rerun 31120905938 --repo nous-clawds4/tapestry` then watch; **(2)** PR **#501** (chore/ci-green: S5 count-pins retired = row #143's fix, actions→v5) — wait for its Test check (the chore's own proof), safe-to-merge, merge; **(3)** PR the stacked branches in order: `feat/inverse-queue` (F2, review 3 PASS) then `feat/publish-stamping` (F4, review 4 PASS — stacked ON F2; alternatively one combined PR of feat/publish-stamping after #501 merges); **(4)** one staging smoke covers the lot — Tier 3: `/api/adoption-queue` success:true with four arrays; bundle carries "Mine to publish" + 3× ":tag-pinning"; pin/TL dual-z is owner-verifiable on first real pin. Both branches pushed to origin. Prod batch afterward: #497–#501+ (owner's call). Book state: 5/6 done, F3 (+ its /discuss on scoring semantics) is the next session's entry point — see `engineering-team/audits/shared-concepts-adoption/book.md`. | 2026-08-06 (session end, GitHub outage) | DONE | 2026-08-07 (flush session) | Flushed 2026-08-07 ~00:40–00:50Z after full GitHub recovery: #501 merged `c98f1070` (Test green 48s — the chore's own proof), F2 → PR #502 merged `1427eb7c`, F4 → PR #503 merged `c655c28d`; safe-to-merge verdict=safe before each merge; three green deploys (last `31135814232`, 1m12s). Deploy run 31120905938 was a post-outage zombie (attempt 2 stuck pre-queue, un-cancellable/un-rerunnable) — abandoned; #500's fix landed via #501's full-tree deploy instead. Smoke clean all tiers: `/api/adoption-queue` success:true with the four arrays (nominations 14 / declined 0 / publishCandidates 0 / deferredInUse 0); bundle `index-CTXB0P-E.js` carries "Mine to publish" ×1 + ":tag-pinning" ×3; Adoption Queue three-view UI renders console-clean incl. the Mine-to-publish empty state. Pin/TL dual-z remains owner-verifiable on first real pin (browser-session paths; recorded gap per story 4 test plan). Prod batch #497–#503 still the owner's call. ADR 0004's sweep-candidate map (F4 stage-2 follow-up) stays live via the book. |
| 146 | dev-tooling | **The vite DEV server cannot load the `@tapestry/event-tagging` CJS tree, so `npm run dev --prefix ui` renders a blank page on every route.** `SyntaxError: The requested module … does not provide an export named 'curateNotes'` — `src/lib/event-tagging/taggings.js:222` exports via `module.exports = {…}`, and `ui/src/utils/publishTagPin.js:2` / `ui/src/hooks/usePinnedNotes.js:2` import it as an ESM named import. The production build handles this (`ui/vite.config.js` `build.commonjsOptions.include`), and `vite preview` of a built bundle is fine; only the dev server's ESM interop is not configured for it. `vite.config.js`'s own comment flags this alias as the "first cross-ui-boundary CJS import — verify the build resolves it", so the build half was verified and the dev half was not. Cost: a visual check had to be re-routed through a built preview. Fix is likely `optimizeDeps.include` (or `.exclude` + an interop shim) for the alias. | 2026-08-06 (ta-avatar #1 implementation) | OPEN | | `ui/vite.config.js` (alias + `build.commonjsOptions`); `src/lib/event-tagging/taggings.js:222`; workaround: `cd ui && npm run build && npx vite preview --port 4173`. |
| 147 | ui | **The users directory hand-rolls `AuthorCell`'s markup, so it silently misses everything `AuthorCell` gains — today, the TA badge and the broken-image failover.** `ui/src/pages/users/Index.jsx:88-99` renders `.author-cell` + `.author-avatar` / `.author-avatar-placeholder` + `.author-name` inline instead of using `<AuthorCell>`, and it lists every pubkey found in Neo4j + strfry — so the Tapestry Assistant appears there **unbadged**, and any user with a dead `picture` URL still shows the browser's broken-image glyph. Jarring because clicking through to `/tapestry/users/:pubkey` *is* badged (ta-avatar #1 AC5). Not an AC violation — AC1's enumeration doesn't name this surface — and invisible to that story's call-site census, which counts `<AuthorCell` usages and cannot see a copy of its markup. **ADR ta-avatar/0001's deferred-sites list omits it**, so it is an unrecorded gap rather than a knowing deferral. Fix is near-trivial: use `<AuthorCell pubkey={row.pubkey} profiles={profiles} />`, or `<Avatar>` + the existing name span if the click-through is unwanted. | 2026-08-06 (ta-avatar #1 review, finding 3) | OPEN | | `ui/src/pages/users/Index.jsx:88-99`; `engineering-team/reviews/ta-avatar/1-in-app-badged-ta-avatar.md`. |
| 148 | bug | **The published-assistant picture guard rejects loopback but admits every RFC1918 / private address, so a LAN-hosted instance publishes a dead avatar URL into a signed, relay-replicated kind 0.** `isPubliclyReachable()` (`src/api/assistant/index.js`, ta-avatar #2) correctly omits the picture for `localhost`, `localhost:7777`, `127.0.0.1`, `[::1]`, `*.local` and bare hostnames, but **accepts** `192.168.1.50:7777`, `10.0.0.5`, `172.16.4.2` and `nas.internal` because they contain a dot — so an owner self-hosting on a home network publishes e.g. `https://192.168.1.50:7777/ta-avatar.png`, which for any third-party reader is dead or points at an unrelated device on *their own* LAN. Same failure mode AC4 exists to prevent, one address class over; the kind 0 is fanned out to five external relays, so it is harder to walk back than a UI bug. Fix: extend the predicate to RFC1918 (`10/8`, `172.16/12`, `192.168/16`), IPv6 ULA (`fc00::/7`), and private TLDs (`.internal`, `.home.arpa`). **The test suite cannot currently catch this** — `test/recognizable-published-ta-profile.test.js`'s deliberately-independent mirror predicate `isPubliclyRoutable` has the identical gap, so both must move together. Same one-line fix would also stop `website` being published as `https://localhost:7777` on dev instances (pre-existing, out of ta-avatar #2's scope). | 2026-08-07 (ta-avatar #2 review, finding 1) | OPEN | | `src/api/assistant/index.js` `isPubliclyReachable`; `test/recognizable-published-ta-profile.test.js` `isPubliclyRoutable`; `engineering-team/reviews/ta-avatar/2-recognizable-published-ta-profile.md`. |
| 149 | meta | **`git stash push -q <path>` silently no-ops when the path has no uncommitted changes, and the following `git stash pop` then pops an unrelated (possibly co-tenant) stash.** Hit during the ta-avatar #2 review: the change under test was already committed, so the push saved nothing (`-q` suppressed "No local changes to save") and **both halves of the with/without differential ran identical code** — matching numbers that looked like evidence and were a tautology. The subsequent `pop` applied a co-tenant session's stash (`On fix/graperank-shared-csv-race: _intake.md cross-branch ferry`), leaving `engineering-team/stories/_intake.md` in a conflicted `UU` state. Git kept the stash rather than dropping it, and it was restored via `git checkout HEAD -- <file>` with both co-tenant stashes verified intact. **Practice:** to diff a *committed* change, check out the base version of the file (`git checkout <base> -- <path>`, run, `git checkout HEAD -- <path>`) — never `git stash`; and never run a bare `git stash pop` in a checkout where another session may have parked stashes (`git stash list` first). Directly relevant to the standing parallel-session guidance. | 2026-08-07 (ta-avatar #2 review, harness friction) | OPEN | | `engineering-team/reviews/ta-avatar/2-recognizable-published-ta-profile.md` § Harness friction. |
| 150 | bug | **`relationship-primitives-probe` H4 is flaky on router-connected instances: it brackets a WHOLE-CORPUS strfry count (`/api/strfry/scan/count?filter={}`, `test/relationship-primitives-probe.test.js:167`), so any network event the live `strfry-router` ingests inside the bracket window fails it.** Observed 2026-08-07: one full `npm test` went Overall-FAIL on exactly this row (8/1) while three same-evening full runs on adjacent trees and an immediate standalone re-run all passed (9/9) — classic ingest-race, not a code regression (the intervening diff was read-only on those paths, story #7). Fix shape when taken up: narrow the bracket's filter to a fingerprint router traffic can't match (e.g. the probe's own kind/author scope), or pause/exclude router ingest for the bracket. Until then: an H4-only failure on an otherwise-green run with the router up should be re-run standalone before being treated as signal. **Second sighting same day:** the parent suite's `relationship-primitives` H8 (the bracket pattern's origin) failed the identical way in the batch-tree gate (count 6605529→6605531, two router-ingested events mid-bracket) and passed 23/0 on standalone re-run — both suites' brackets need the same narrowing when taken up. | 2026-08-07 (story #7 full-suite gate) | OPEN | | `test/relationship-primitives-probe.test.js:167,319`; `test/relationship-primitives.test.js` H8; evidence: story #7 review's quality-gates section. |
| 151 | meta | **Parallel machines mint colliding OPEN.md row numbers — two independent `#148`s were created the same day (this machine: the probe-flake row; the laptop: the picture-guard row), forcing a renumber-and-repoint dance inside a deploy-critical merge resolution.** The ledger's row-number allocation has no cross-machine coordination; both sessions correctly took "next free number" and both were right until the merge. Cost tonight: a mid-promotion conflict resolution had to renumber 148→150 and update a committed review's pointers (`4b32b53f`). Fix shapes when taken up: a claim convention (e.g. check `origin/staging`'s ledger tip before minting, or per-machine number lanes odd/even), or drop ordinal identity for date+slug row ids. Until then: before minting a row, fetch and check the highest number on `origin/staging` too, and expect renumbering at merge time. | 2026-08-07 (shared-concepts-adoption book close, retro finding) | OPEN | | Merge commit `4b32b53f`; audit §7 (`engineering-team/audits/shared-concepts-adoption/audit.md`). |
| 152 | cleanup | **Local dev wire-archaeology: 166 TA header addresses for 83 names in strfry (the March-2026 random-d era) + six same-handle duplicate graph node pairs (`tag`, `tag-pinning`, `nostr-user-tag`, `nostr-event-tag`, `nostr-relay`, `tagging-with-specific-tag`).** Since story #7 the twin picker enumerates the graph ∩ has-event, so the orphans are no longer offerable — the harm is contained; this row is the deliberate-cleanup decision the owner deferred. Options sketched at discussion 2026-08-07: retire the ~83 orphaned strfry husks (needs a removal mechanism + a nothing-references-them check — principle-4 review applies), and dedupe/merge the six graph pairs (likely feat/tags-era imports coexisting with firmware re-creations). Local dev machine only; staging/prod corpora are clean. | 2026-08-07 (shared-concepts-adoption book close sweep) | OPEN | | Diagnosis in-session 2026-08-07 (story #7 discussion); `engineering-team/reviews/done/shared-concepts-adoption/7-graph-derived-twin-picker.md` NB-1. |
| 153 | ops | **Prod first-use verifications for the adoption suite (owner-performed):** (1) publish the first real Trusted Dictionary snapshot on `tapestry.brainstorm.world` — the live proof of the mint path in production (H-tested locally; staging/prod smoke verified the gate + read path only); (2) perform a first pin or TL publish and confirm the dual-z stamps land (personal + shared) — the story-4 recorded gap: both writers are browser-session paths a loopback caller cannot operate. Flip to DONE when both have been eyeballed. | 2026-08-07 (shared-concepts-adoption book close sweep) | OPEN | | `engineering-team/audits/shared-concepts-adoption/audit.md` §5; story-4 test plan § Recorded gap. |
| 154 | ui | **A customer's assistant publishes as *"a customer's Tapestry Assistant"* when the customer has no kind-0 name.** `buildDefaultProfileContent`'s customer branch (`src/api/assistant/index.js`) falls back to the literal `'a customer'` and interpolates it into `name`, `display_name` and `about` — so an unnamed customer's assistant introduces itself with a grammatically odd, slightly absurd name on nostr. The owner branch was fixed in ta-avatar #2 (empty fallback → the generic `'Tapestry Assistant'`), which makes the two branches visibly inconsistent; the same two-line pattern applies. Deliberately out of scope for ta-avatar #2 (AC1 spoke only to the owner) and flagged in ADR 0002 §Consequences — filed here because that ADR now lives under `decisions/done/` and read-only history is a poor home for an actionable fix. | 2026-08-07 (ta-avatar book close) | OPEN | | `src/api/assistant/index.js` `buildDefaultProfileContent` customer branch; `engineering-team/audits/ta-avatar/audit.md` §6. |
