# Epic: w14-settlement

**Created:** 2026-07-13
**Status:** Active

## Goal

Settle worksheet **W14** (subset/ancestor stamping — the layer-selection rule) and its prerequisite, the correspondence-closure question, per the 2026-07-13 protocol-spec `/discuss` ratification: define **Reach** (the any-type transitive `b` closure; permission-shaped; publisher-side SHOULD, never a reader validity gate) in Shared Concepts, and convert Stamping's open layer-selection section into the normative floor-plus-extras write rule with its co-stated MUST-expand read contract. Direct successor to the `nip-reorg` book (whose S3 opened W14 by design).

Docs-mode (protocol-spec workflow §3): `protocols/` prose + worksheet flip, no code, Test Design skipped.

## Stories

1. `stories/w14-settlement/1-ratify-reach-and-layer-selection.md` — **the sole story**: Reach definition (shared-concepts), write-rule extension + read-contract completion + Open-section resolution (stamping), W14 → Resolved, one inherit-from cross-ref.

## Not on this epic's path

Implementation (resolver, clouds, stamp writers, pins dual-`z`); publication-ladder moves; target-typed tag definitions (W10); event-tagging rollout.
