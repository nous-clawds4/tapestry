# Completion report — take-a-concept-back-out

**Written:** 2026-07-30 (Director, at Stage 3 of the Direction run)
**Scope:** every bullet of the book's acceptance frame (`book.md` § Acceptance frame), with the raw evidence for each. Evidence only — verification state is stated plainly, including what was *not* demonstrated live and why.

**Shipped as:** `tapestries` story #6 (`stories/tapestries/6-take-a-concept-back-out.md`, `**Status:** Done`) · ADR `decisions/tapestries/0006-remove-concept-remove-only-republish.md` · review `reviews/tapestries/6-take-a-concept-back-out.md` (**PASS**) · merged to `staging` via [PR #482](https://github.com/nous-clawds4/tapestry/pull/482), deploy run `30538834189` **success** · staging smoke green (below).

**Production surface** (diff `90c6d930..ab4cf64c`): `ui/src/pages/tapestries/tapestryDraft.mjs` (+`buildRemoveConceptDraft`, +`authoredConceptMembers`), NEW `ui/src/pages/tapestries/RemoveConceptFromTapestry.jsx`, `ui/src/pages/tapestries/TapestryDetail.jsx` (gate rename + wiring). No other production file; no test file (`git diff 90c6d930..ab4cf64c -- test/ tests/` is empty).

**Test evidence base** (each run recorded with real counts and exit codes; independently reproduced by the Reviewer and by two Gate-5-side judges): story Node suite **22 passed / 0 failed** (P1–P15 transform, S1–S4 sentinels, R1–R3 regression guards); story Playwright spec **14 passed / 0 failed** (E1–E14, both signing branches, all network mocked, assertions run against the *actually published* body); full `npm test` **`Overall: PASS`, exit 0** (Gate-4 run 2, log `gate4-full-npm-test-2026-07-30-run2.log`; independently re-run green by the Reviewer and a judge); shipped add suite **23/0** and add spec **13/13** as regression.

## From the deliverable

- [x] **From a Tapestry I am looking at, I can take out a concept that is in it.**
  Per-member "Take out ⟨name⟩" controls on the existing Exploration page for editable tapestries (E1 TA-authored, E9 owner-authored — both green); choosing arms an inline confirm naming the member; confirming publishes the replacement through the existing paths (E8 assistant-signed, E9 owner-NIP-07 — both green, same `d`-tag asserted on the published body). The pure transform was additionally exercised against the **live** local element (`b0b48b00`): fed the real event, it produced the exact correct replacement draft — same coordinate, the `cat` node + its solely-carried `cat-concept-graph` import removed, everything else byte-identical, strictly newer `created_at` (draft + pre-change backup preserved in the run's session scratchpad). **Demonstration state, honestly:** the one-click *live publish* of that draft was prepared but **not executed** — the session's permission layer declined the mutation of the operator's real tapestry and the Director ruled it non-essential rather than overriding (journal, 2026-07-30T11:29:30Z); the live end-to-end click-through remains a one-minute operator act, exactly as the add-a-concept book's staging demo remained one.
- [x] **After I save, the Tapestry no longer shows that concept to me.**
  E10 (green): the removed member is the *currently selected* concept; after the save the page re-reads the same coordinate (scan-count observed to grow), the member is gone from the sidebar, the other members remain, and no stale detail pane survives. The mock deliberately serves back the implementation's own published body **and keeps every concept-graph import resolvable** — so a replacement that removed the node but kept its import would re-materialize the concept at compose time and fail this test (ADR 0006 Decision 2-A's functional guarantee, exercised end-to-end).
- [x] **…or to anyone else who opens it afterwards.**
  E11 (green): a fresh, *not signed in* browser context opens the same uuid afterwards — the removed concept is absent, the remaining members render, and no affordance is offered. One read mechanism serves owner and stranger alike (`readByUuid`, exact-coordinate scan), so "gone for me" and "gone for others" are the same replacement observed twice — the structural argument the Reviewer audited (review § manual sweep 3), resting on relay-native NIP-01 replacement verified by the epic's evidence goal and proven in production by shipped story #5's identical publish paths.
- [x] **Everything else in it stays as it was.**
  Structural, and tested from both sides: the transform copies the event verbatim except the one node and its attributed import(s) — remaining nodes/imports byte-identical **in order**, `d`/`name`/`z`/unknown tags untouched, unknown json keys preserved, authored `relationships`/`relationshipTypes` passed through, input event not mutated (P2, P14); an import attributable to **no** member passes through untouched (P6); an import shared with a remaining member **stays** (P7); E8 asserts the same on the published body (name tag unchanged, other members present, one directory row).

## From the boundary

- [x] **Removing only — adding stays as it is; changing how concepts connect stays out.**
  Zero lines of the add/create path changed (R2 greps the shipped files for any remove machinery — green; the ADR's "NO changes" list verified by name-only diff at review and by a judge). Shipped add suite **23/0** and add Playwright spec **13/13** re-run green at HEAD. Connections: no connection-handling machinery exists in the diff; whatever an element carries passes through verbatim (P14); the Architect's census found **zero** tapestries anywhere carrying authored connections (both reachable elements: `relationships: []`), so the story's surface-a-counter-example stop-condition never fired.
- [x] **A Tapestry keeps at least one concept — taking out the last one is refused.**
  Up-front, plain-language, not offered-then-errored: at exactly one authored concept the component renders the refusal sentence *instead of any control* — no save is reachable (E6: refusal visible, no control exists, publish sink stays empty). Defense in depth: the transform independently throws the same `/keeps at least one concept/i` sentence (P8), counting **concepts only** (a graph with one concept plus property/superset nodes still refuses — P8/P13). The sentence is verifiably **in the deployed staging bundle** (`assets/index-CfnR6BmO.js` contains it — smoke, below).
- [x] **Works on Tapestries published under my own key or my assistant one; anyone else cannot be edited here and the option is not offered.**
  One owner-strict gate for both affordances (`canEdit`: session classification `owner` AND author ∈ {runtime TA, session pubkey} — S4 pins the expression and the absence of the old name). Negatives each have a green test: guest (E2), unauthenticated (E3), **admin-who-is-not-owner** (E4 — the gate is owner-strict, the #5 Director ruling carried forward), foreign-author tapestry even for the owner (E5). Demonstrated live on both real surfaces: the local `b0b48b00` page and the staging `tapestry-tapestry-for-cat-abc0dc1d` page, each opened unauthenticated in a real browser during smoke — full render, **no take-out control anywhere** (screenshots taken in-session; zero console errors both).
- [x] **No new page and no new server endpoint — lives on the existing Tapestry view, publishes the way Tapestries are already published.**
  The affordance renders inside `TapestryDetail`'s existing sidebar (no route or nav change in the diff); the component's only network call is the pre-existing `POST /api/strfry/publish` (`RemoveConceptFromTapestry.jsx:57`); no server file in the diff; both signing branches are byte-for-byte the shipped #3/#5 paths (S3-mirror sentinel green; side-by-side compared at review and by a judge).

## Knowingly surrendered in this mode

- [x] **Baseline commit and pinned governing versions** — deliberately not captured, stated with the endpoint's own reasons in the book's generated section (`book.md` § "Knowingly surrendered in this mode"), not quietly dropped.

## Deploy record

- **Local:** UI built (`npm --prefix ui run build`, 10.69s) and served via the bind-mount; Tier-2 sanity green; Tier-4 browser check green (full render, clean console, live AC-1 negative). Local relay verifiably unmutated by the run's verification (element id `4902ba14…`, `created_at 1785296263`, unchanged before/after).
- **Staging:** [PR #482](https://github.com/nous-clawds4/tapestry/pull/482) merged (plain merge); `deploy-staging.yml` run **30538834189** → `success`; smoke **read-only by rule**: Tier 1 stability 3×200; Tier 2 `/api/assistant/pubkey` answers (staging's own TA `8e901369…`); Tier 3 the staging tapestry element intact (1 element, 4 members) **and the new code verifiably in the served bundle** (`index-CfnR6BmO.js` carries the refusal sentence); Tier 4 real-page browser check green (render + clean console + no affordance unauthenticated); Tier 5 regression green (directory page 200, `get-user-counts` live data, search page 200). **Nothing was published to, deleted from, or triggered on staging; production untouched.**

## Where the raw evidence lives

`engineering-team/audits/take-a-concept-back-out/`: `baseline-2026-07-29.log`, `baseline-2026-07-30-postcleanup.log`, `baseline-2026-07-30-run3.log`, `gate4-full-npm-test-2026-07-30.log`, `gate4-full-npm-test-2026-07-30-run2.log`, `journal.md` (every gate, verdict, answer, and halt, timestamped). Review with the Reviewer's own recorded runs: `engineering-team/reviews/tapestries/6-take-a-concept-back-out.md`. Story/ADR/test-plan as linked above. CI: run `30538834189` on `nous-clawds4/tapestry`.
