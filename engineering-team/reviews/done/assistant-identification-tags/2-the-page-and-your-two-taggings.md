# Review: Story 2 — The Identification Tags page, and your two taggings of your Assistant

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-09-22
**Diff:** `git diff cbae0c77..860ec615` (the code: the ADR's six files, plus the ADR's Amendment 1, the story's § Deviations, and one test path constant — see Harness friction 1), with the tests `git diff feb8da37..cbae0c77 -- test tests` plus `d7611992`; HEAD `7180ce6c` is docs-only (the plan's after-implementation record). Branch point `85e4e30d` = `origin/staging` after `git fetch origin staging`; `git merge-tree --write-tree HEAD origin/staging` is clean (exit 0, tree `1956b8c9`).
**Story:** `engineering-team/stories/assistant-identification-tags/2-the-page-and-your-two-taggings.md` (Approved 2026-09-22; § Deviations 1–6 by the Implementer)
**ADR:** `engineering-team/decisions/assistant-identification-tags/0002-the-page-reads-the-one-answer-and-publishes-through-the-tagging-publisher.md` (Accepted; Amendment 1: the copy module is `identificationTagsCopy.js`)
**Test plan:** `engineering-team/stories/assistant-identification-tags/2-the-page-and-your-two-taggings.test-plan.md` (its "After implementation" section was re-derived here, not trusted)

## Quality gates (run by reviewer, not trusted)

- [x] **The book's gate** (the plan's § How to run: story 1's recipe with the widened pattern, minus the registry's `excluded` suites; the launcher printed `named=121 walkers=24 excludedDropped=1 [signer-guard-rollout.test.js] want=131`), Node 22.23.2, label `assistant-identification-tags-2-review`. The `npm run gate:status -- --label assistant-identification-tags-2-review` line (exit 1):

  > `20260922T113726Z-8213-b384 [assistant-identification-tags-2-review] started 2026-09-22T11:37:26.590Z on 7180ce6c — FAIL, exit 1, 2435 passed, 25 failed, 59 skipped, 131/131 suites; failed: capture-a-goal-and-see-it, structures-the-brain-can-trust, break-a-goal-into-pieces, attach-the-world, sessions-read-the-brain, the-proposal-loop, teach-it-what-matters, the-brain-survives, show-the-four-on-the-goal-screens-that-already-exist, concept-count-canonical, summaries-element-count`

  Read against the baseline record `20260922T111447Z-47997-3777 [assistant-identification-tags-2-baseline]` on `cbae0c77` (FAIL, 2422 passed, 38 failed, 59 skipped, 131/131), suite by suite on the record fields `file`, `verdict`, `pass`, `fail`, `skipped`: **130 of 131 suites identical; the one difference is `assistant-identification-tags-page.test.js`, FAIL 2/13/0 → PASS 15/0/0.** With that suite set aside the FAIL set is the same eleven in both records — the host's live-graph set (memory `host-gate-at-ci-parity`), none touched by this story — and the six SKIP suites (the `*-publish.test.js` six) are the same six. The FAIL verdict is the host's, not the diff's. The record's git state: `7180ce6c`, `dirty: false`.
- [x] **Suites alone**, through their `run()` export with Node 22.23.2: `assistant-identification-tags-page` 15 passed, 0 failed, 0 skipped; `dual-z-writer` 14/0 (it pins the wrapper's destructured signature, `860ec615`); `assistant-management-page` 24/0/0 (W1 re-aimed); `assistant-attention` 37/0/0.
- [x] **`npm run test:playwright` equivalent** (a UI change): `BRAINSTORM_BASE_URL=http://localhost:7778`, chromium, `--reporter=line`, Node 22.23.2, against the UI the stack serves (bundle `index-D7NZovvU.js`, built after `860ec615`; nothing rebuilt or restarted here). Checked "Running N tests" against the totals (OPEN.md row 114 b):
  - **Group A** — `assistant-identification-tags-page.spec.js` + `assistant-management-page.spec.js` + `assistant-attention.spec.js` + `assistant-alert.spec.js`, one run, "Running 53 tests using 4 workers": **52 passed, 1 skipped (3.1m), exit 0.** Per spec: this story's 13/13 (B0–B12); the hub's 22 passed + 1 skipped — the one skip is B8's identification-tags case (`assistant-management-page.spec.js:360`, the re-aim; the only conditional skip in the four specs besides the server guard); attention 7/7; alert 10/10.
  - **Group B** — the publisher's sharers: `tag-detail-write.spec.js` + `authored-tagging.spec.js` + `tapestry-create.spec.js` + `tapestry-add-concept.spec.js` + `tapestry-remove-concept.spec.js`, "48 tests": **39 passed, 9 failed (1.1m), exit 1.** `authored-tagging` 2/2; the three tapestry specs 8/8, 13/13, 14/14 — they mock `/api/strfry/publish` and go through the changed `publishOrThrow` in the built bundle; `tag-detail-write` 2 passed / 9 failed. The nine (spec lines 176, 201, 243, 280, 330, 370, 403, 439, 489) each fail at one of three locators — `getByRole('button', { name: /^apply$/i })` (six), `getByText(/Viewer Only Target/i)` (one), `getByPlaceholder(/find a profile to tag/i)` (two) — exactly the three shapes ledger `2026-09-22-staging-browser-class-83-red` lists for that spec ("Apply" buttons, a "Viewer Only Target" row, the "find a profile to tag" field; 9). None of the nine reaches a publish call: every one dies at its locator before the click or fill. Pre-existing on the shared line, not this diff's.
- [x] `bash scripts/harness-lint.sh`: exit 0, clean (0 violations, 4 known waivers).
- [ ] _Lint not configured — skipped._
- [ ] _Typecheck not configured — skipped._
- [ ] _Build not configured — skipped._ (The served bundle was built by the main session after `860ec615`; B0 confirms it contains the first card's title.)

## Spec adherence
- [x] Every acceptance criterion has a passing test; the plan's coverage map holds against the suite and the spec as they are:
  AC-1 → B1 (the words, the back link, two cards, four rows, no "Placeholder page."), B6 (signed out; no Assistant; sign-in resolving shows neither line), S1 · AC-2 → C2, C3, C4; B1 Done, B2 marked, B3 tag not found, B4 the reason on each row, B5 "did not answer" · AC-3 → B2 (checked by default, the button follows the boxes, a reload checks them again), B3 (disabled and unchecked), B1 (no box on a Present row), B4 (none on a could-not-check row), C3 `definitionKnown`, S6 · AC-4 → C5, C6, C7, S2, S4; B7 (two signatures in card order, `a` = the canonical address, `p` = the viewer's own Assistant, the `d` rule, polarity `1`, `signAs: 'client'`, the kept-local summary and five skipped lines per tagging, the answer re-read, the card flips, the hub counts nine), B8 (the second declined: the first keeps its result), B9 (no extension: nothing sent), B10 (the failed-local line; still marked) · AC-5 → B5, S6 · AC-6 → B11 (375 px with missing and with present rows, a reload, a direct load, the hub's card leads here), S3, the hub's W1 (24/24) and B8's nine remaining placeholder cases · AC-7 → B12, S1.
- [x] No criterion silently dropped. The story's "Local write failed" copy row is superseded by ADR sub-decision 5 (the browser publish is parallel: `nostrPublish.js:226-227`), recorded as § Deviations 1 and pinned by C6 and B10. § Deviations 2–6 each name what changed and why; none drops an AC.
- [x] No behavior added that isn't in the story. The unused `buttons.assistant` string ("Have your Assistant publish", `identificationTagsCopy.js:31`) is the story's settled open question 2 for story 3, not a new surface. The `request-failed` and definition-unknown sentences (§ Deviations 2) fill states AC-2 names without words.

## ADR adherence
- [x] Files changed match § Implementation notes 1–6 under Amendment 1's name: `ui/src/pages/assistant/identificationTagsCopy.js`, `ui/src/utils/taggingPublishReport.js`, `ui/src/utils/publishProfileTag.js`, `ui/src/pages/assistant/IdentificationTags.jsx`, `ui/src/App.jsx`, `ui/src/styles.css`. § 7's "Unchanged" list verified: `git diff cbae0c77..860ec615 --stat -- ui/src/pages/assistant/actions.js …/ActionPage.jsx …/ActionText.jsx …/Index.jsx ui/src/context ui/src/utils/nostrPublish.js ui/vite.config.js src ui/src/components` is empty. Nothing outside the ADR's list changed in the code diff.
- [x] Sub-decision 1: `ACTION_PAGES` names exactly one key; `ASSISTANT_ACTIONS` still has ten entries (R2), so nine route to `<AssistantActionPage>` (S3 by source, B8's nine cases in the browser).
- [x] Sub-decision 3: "callers unchanged" verified my own way — `git diff cbae0c77..860ec615 --stat -- ui/src/hooks ui/src/pages/Tag.jsx` is empty; `publishOrThrow` has eleven call sites in nine files (`publishTagPin.js` ×2, `useProfileTags.js` ×2, `DListCurationPanel.jsx`, `TlOptInCard.jsx`, `TreasureMapManualEdit.jsx`, `CurateHereOffer.jsx`, `useCreateTapestry.js`, `RemoveConceptFromTapestry.jsx`, `AddConceptToTapestry.jsx`) and `publishProfileTagAssertion` three in two (`useProfileTags.js:77`, `Tag.jsx:109,115`); none of those files is in the diff. The wrapper's behavior is the old one line for line: the same guards, build and sign, `publishEverywhere(signed)` with the default relays (the variant only passes a second argument when given one, `publishProfileTag.js:110`), then `assertPublished` — the throw rule moved out of `publishOrThrow` unchanged — and `return signed`. `publishOrThrow` still returns the result after the same rule.
- [x] Sub-decision 4/5: the util imports nothing (C1), derives rows in relay order from `external.details`, treats a relay missing from `details` as unreachable "no publish result" (the chokepoint's own rule, `nostrPublish.js:137`), and says what the relays did on a failed local write.
- [x] Sub-decision 6: pre-flight `window.nostr` → the notice; the loop is sequential over a snapshot (`toPublish`, `IdentificationTags.jsx:160`); a throw before publishing becomes the row's refusal line and the loop continues; the button reads "Publishing…" and is disabled with the boxes; results in an `aria-live="polite"` region; `refresh()` once after the loop (`:181`). Delivery cannot throw: `publishToLocalStrfry` and `publishToRelays` both catch (`nostrPublish.js:76-90, 166-207`) and the policy read fails open, so the catch never mislabels a network failure as a signature refusal.
- [x] Sub-decision 6.5, the reset effect keyed on the answer object (`IdentificationTags.jsx:158`): safe. `summarizeAttention` returns the stored response's own `actions` object (`ui/src/utils/assistantAttention.js:22`), so `attention.actions['identification-tags']` is one stable reference per answer and `null` while not answered — no render loop; each new answer, and the `null` during a re-check, rebuilds the set. The mid-loop re-check (the chokepoint announces after the first local write → the provider's `refresh()` → phase `checking` → `answer` null → the set emptied → rebuilt from the next answer) never touches the snapshot; `publishing` keeps the boxes and the button disabled throughout; results survive because `rows` always carries all four keys. B7 and B8 exercise it.
- [x] Sub-decision 7: `<section class="bs-idtags-card [is-marked|is-done]" aria-labelledby>`, the marker, the head with the screen-reader prefix and the badge, `<ul class="bs-idtags-rows">` with `<label>`-wrapped checkboxes (the accessible name is the tagging's name — the spec's `getByRole('checkbox', { name, exact: true })` relies on it), the button, the results region; the card box as its own class (no `/setup` link hover rules); the block's own `@media (max-width: 480px)` rules (S5).
- [x] Layering: the two pure modules import nothing (C1, verified by the load in Node); the page builds no tagging (S2: no `kind: 39999`, no `profile-tag-`), never fetches (my grep, S1), stores nothing; `actions.js` and `PUBLISH_RELAYS` untouched.
- [x] No new dependencies (`ui/package.json` not in the diff).
- [x] Phase-4 commits touch no test: `d0b9d9bf` (the story + six code files), `1089f04e` (ADR, story, the page's import, the module's header), `860ec615` (the publisher only). The one test change after Phase 3 is the `test:` commit `d7611992` (the path constant) — which also carried the source rename; see Harness friction 1.

## Concept-graph integrity
- [x] Handles: the page composes no concept handle. The publisher's `NOSTR_USER_TAG_HANDLE` (`39998:<literal>:nostr-user-tag`, the ADR 0015 named exception) and the runtime local z (`39998:${localTaPubkey}:nostr-user-tag`) are untouched context lines. `39999:<owner>:<slug>` is a tag address in the `a` tag, as every tagging carries.
- [x] No concept definitions changed; no firmware reinstall needed (story § Concepts touched; ADR § Concept-graph orientation).
- [x] Orientation was done at Architecture through `/summaries` (ADR 0001's, reused); the code reads no graph.

## Things tests can't catch
- [x] No secrets. No `82b75e47…` literal in the three new files; `CANONICAL_TAG_AUTHOR` is imported from the library, not re-declared. The TA pubkey is resolved at runtime (`useConfig().taPubkey` → `localTaPubkey`).
- [x] No leftover debug logging, no `console.log`, no `debugger`, no TODO/FIXME in the added lines (grep over the diff). The publisher's `console.warn` on a missing local TA is pre-existing.
- [x] No commented-out code; the one JSX comment mirrors the hub's ("Failures are reported by the shared sign-in modal.").
- [x] Error paths: no extension → the notice, nothing signed (B9); the extension declining, `SignerMismatchError` (`getActiveSignerOrThrow`, issue #335) or the `tag.authorPubkey` guard → that row's refusal line with the error's message, the loop continues (B8); a failed local write → the sub-decision-5 lines (B10); a relay with no answer → unreachable "no publish result" (C5); an answered response with no row for an entry → could-not-check/request-failed (C2). A `missing` row whose definition check did not finish gets a disabled box and the definition-unknown sentence (C3; § Deviations 2).
- [x] Concurrency: one signer prompt at a time (two at most per press); the button and the first card's boxes disabled while publishing; the second card's boxes stay live (§ Deviations 5 — harmless, nothing reads them yet); state sets after an unmount mid-loop are silent under React 19. The provider drops stale answers by request key (story 1).
- [x] Security: the page sends nothing itself; the publisher validates `tag.authorPubkey` (64-hex) and refuses an extension account that drifted from the session; `targetPubkey` is the session's own `assistantPubkey`; `polarity` is fixed at 1; nothing user-typed reaches an event. Unchecking stores nothing (B2's reload).
- [x] 375 px: B11 measures `scrollWidth − clientWidth ≤ 0` with missing rows (boxes) and with present rows; the results block wraps (`.bs-idtags-result-relays code { word-break: break-all }`, `.bs-idtags-card-body { min-width: 0 }`).

## House rules check
- [x] Concept Graph API authority respected.
- [x] No new lint/typecheck/build tooling.
- [x] The TA pubkey is never hardcoded; the ADR 0015 `LEGACY_*`-class literal in the publisher is untouched.

## Product-guide adherence
Not applicable: an acceptance-frame book, no PRD. The words were checked against the story's § Copy as amended: C1 pins the copy module to the fixture, and the fixture matches the story row for row (the heading, description and hub words come from `actions.js`; the three added sentences are § Deviations 2; the "Local write failed" row is superseded by ADR sub-decision 5). The tone icons (§ Deviations 4) are status marks matching the shipped `AssistantProfileEditor.jsx`; accepted here — see Non-blocking 7.

## Findings

### Blocking
None.

### Non-blocking
1. **`ui/src/utils/taggingPublishReport.js:70-71`** — with no relays given *and* a failed local write, the summary reads "…and local-only publish mode kept it from any other relay", which is not true for that input (no relay was given). Unreachable from the page (`PUBLISH_RELAYS` has five entries) and not pinned by C6. Optional: a fourth branch, or fold `n === 0` into the "no outside relay was given" wording.
2. **`ui/src/utils/taggingPublishReport.js:27`** with **`ui/src/utils/nostrPublish.js:141-142`** — a browser-side timeout always carries the reason `'publish timed out'`, so the line reads "timed out: publish timed out"; the story's per-relay row is "— timed out". The fixture pins the editor's reason-appending rule, so this is the approved form. Unreachable on this stack (local-only mode). Optional: drop the reason for `timeout` when it is the chokepoint's own sentinel.
3. **AC-2's two sentences pull apart in one corner** — a tagging present through another author's same-named tag while the canonical definition is not found: the page marks the card ("Tag not found", whatever the check found — C3 pins it) while the hub's card is unmarked (the server's `done` reads `present` only, `src/api/assistant/attention.js:168`). The approved reading; a carry-forward for the book's close, not this story's defect.
4. **`ui/src/pages/assistant/IdentificationTags.jsx:150-153`** — `answerRows` is a fresh `[]` whenever there is no answer, so the `useMemo` recomputes every render in the pre-answer states. Harmless (nothing depends on `rows` by identity).
5. **`tests/brainstorm/assistant-identification-tags-page.spec.js:319-322`** — "the hub counts nine" is read after a full `page.goto(X.HUB)`, so it shows the hub reads the queue's last answer, not the in-place refresh; what proves the refresh is the same test's `is-done` flip and `attentionCalls ≥ 2` on the page itself. The pill on the page is not asserted. A test-strength note.
6. **`test/assistant-identification-tags-page.test.js:212-214` (S1)** forbids `fetch(` and any `/api/` string in the page. Story 3's second-card publish is a server call, so story 3's Tester either re-aims S1 or the call lives in a util as the browser publish does (`publishProfileTag.js`). A design note for story 3, not a defect.
7. **`ui/src/pages/assistant/IdentificationTags.jsx:38`** — the tone icons (✅ ⚠️ ℹ️ ❌) against the product guardrail's "no emoji in product copy": § Deviations 4 weighs it, and the shipped editor already does the same. Accepted as status marks beside the sentence; if the owner disagrees it is one line (`TONE_ICON`).
8. **Story file `:192`, § Linked artifacts** — "Review: (filled in after Review phase)" left as is; ledger `2026-09-22-linked-artifacts-not-written-at-gates` (OPEN) already covers it, and this review's sanctioned story edit is the Status flip.
9. **`engineering-team/stories/assistant-identification-tags/2-the-page-and-your-two-taggings.test-plan.md:140`** — the after-implementation record says `tag-detail-write.spec.js 20/29`; that spec has 11 tests (2 passed, 9 failed here). The nine are the same nine; the denominator is not that spec's.

### Harness friction
1. **A `test:` commit carried a source rename.** `d7611992` ("test: the copy module's path is identificationTagsCopy.js") re-aims the suite's path constant *and* renames `ui/src/pages/assistant/{identificationTags.js => identificationTagsCopy.js}`; the page's import was fixed only in the next commit, `1089f04e` (`git show 1089f04e -- ui/src/pages/assistant/IdentificationTags.jsx`). At `d7611992` the page imports `./identificationTags` while the file is `identificationTagsCopy.js`: on Linux that commit does not build, and on macOS the import resolves to the page itself — the very bug Amendment 1 describes. Both the plan's "After implementation" section and the session brief place the rename in `1089f04e`. Candidate `meta` row: a Tester-lane `test:` commit carries no `ui/src`/`src` change; the rename belongs in the `impl:` commit that fixes the import, and the path constant follows in its own `test:` commit (or the two land together, labelled as such).
2. **The book's gate recipe exists only as prose plus scratchpad copies.** The plan's § How to run says the widened pattern "must drop the registry's `excluded` suites" (otherwise the launcher exits 2 on `signer-guard-rollout.test.js`), but the executable form with that drop lives in no committed file; each role re-derives it (the sibling session's copy was gone when I looked). Ledger `2026-09-22-gate-recipe-relative-require` (OPEN) already covers the same recipe's relative-`require` defect; this is its second gap. Candidate: fold the `excluded` drop into the recipe text where it is next written, or let the recipe skip `excluded` files instead of exiting 2.

## Verdict
**PASS**

## On PASS (same commit)
- [x] Story `**Status:**` flipped to `Done` in place (`stories/assistant-identification-tags/2-the-page-and-your-two-taggings.md:3`).
- [x] Completion detection performed; the result is recorded in the chat, not here. (Story 3 is Approved, not Done; the acceptance frame's "Your Assistant's two taggings" bullet is its. No `/close-book` offer.)
