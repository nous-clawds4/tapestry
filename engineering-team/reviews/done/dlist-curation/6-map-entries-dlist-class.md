# Review: Story 6 — Map Entries: classify, label, verify, and link per-DList curation entries

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-09-10
**Diff:** `git diff dc7834ca..HEAD` (base `dc7834ca`, HEAD `56a08763`; branch `feat/dlist-curation`)
**Story:** `engineering-team/stories/dlist-curation/6-map-entries-dlist-class.md`
**ADR:** `engineering-team/decisions/dlist-curation/0006-map-entries-dlist-class.md`
**Test plan:** `engineering-team/stories/dlist-curation/6-map-entries-dlist-class.test-plan.md`

Branch commits audited (oldest first): `50051353` ADR · `e5e0a87e` failing tests · `56a08763` implementation. Eight files: three source (`ui/src/utils/treasureMap.js` +53/−3, `ui/src/pages/grapevine/TreasureMapTagsPanel.jsx` +118/−14, `ui/src/pages/lists/DListDetail.jsx` +2/−1), two test (`test/dlist-curation-map-entries.test.js` new, 202 lines — the Tester's commit only; the implementation commit touches no test file; `test/test.js` +9/−2 registration), three harness artifacts (ADR, story, test plan). No `package.json`, no `src/`, no firmware, no `protocols/`, no build artifacts in the diff. Story 5's panel and the TL card are consumed, not changed.

## Quality gates (run by reviewer, not trusted)

- [x] **Story-scoped gate** (the test plan's six suites, each via `node -e "require('./test/<name>.test.js').run()"`): **all green, 110 pass / 0 fail.** `dlist-curation-map-entries` 14/0 · `tl-treasure-map-panel` 18/0 (story 2's own pins on `classifyEntry`) · `treasure-map-panel-summary` 18/0 · `dlist-curation-panel` 18/0 · `dlist-curation-tl-panel` 19/0 · `tl-treasure-map-optin-publish` 23/0.
- [x] **Every other suite that reads a touched file** (`grep -lE 'treasureMap|TreasureMapTagsPanel|DListDetail' test/*.test.js` → ten suites; the four outside the gate): `scheduled-search-and-house-scores-refresh` 12/0 · `treasure-map-relay-presence` 35/0 · `treasure-map-relay-sync` 22/0 · `treasure-maps-router-preset` 5/0. **Ten suites, 184 assertions, 0 failures under my own run.**
- [x] **Full `npm test` — the Implementer's log** (`scratchpad/npm-test-story6.log`, 4704 lines, mtime 17:20; the implementation commit is 16:38:34 and the tree is clean at HEAD, so the log's tree is HEAD). Checked in it: 160 suite lines `PASS`; the four dlist-curation suites at lines 4698–4701 incl. `dlist-curation-map-entries suite: PASS (14 passed, 0 failed)` (line 4701) — the `test/test.js` registration proven end-to-end; `Total skipped: 56`; three suite lines red — `tl-membership-method-selector` 11/1, `tl-weighted-sum-method` 6/1 (+2 skipped), `tl-certainty-method` 4/2 (+1 skipped), each on `L0 GUARD publish policy is local-only` (lines 4098/4110/4123) — OPEN.md row 191: the guard reads the container's `/api/publish-policy`, external-on here; none of the three touches, imports, or renders anything in this diff. `Overall: FAIL`, `EXIT=1` for those reasons only. **Not attributable to this story.** I did not re-run the ~41-minute gate: the diff touches UI source plus one additive runner registration, and every suite that reads a touched file was re-run green above.
- [x] **The judge still failed before the implementation.** Rebuilt the tree at `e5e0a87e` in the scratchpad (`git archive` of `ui/src`, `ui/package.json`, `package.json`, the suite) and ran it: **5 pass / 9 fail** — U1, U2, U4–U6, S1–S4 failing, U3 and R1–R4 green — identical to the test plan's record.
- [x] **Live browser check (B-class, which the suite says it cannot cover)** — headless Chromium (Playwright 1.56.1 with `executablePath` → the cached `chromium_headless_shell-1228`) against the local stack at `:7778` (the deployed bundle `index-D_o_03IY.js` carries every new string), with `page.route` mocks for `/api/auth/status` and `/api/auth/user-classification` (a customer with a per-user `assistantPubkey`), `/api/profiles`, and `/api/strfry/scan` (the kind-10040 Map; the kind-39998/39999 header scans answered from a synthetic header list). No `window.nostr` exists in the automated browser and every request the page makes is a read; nothing was signed or published. **45 checks, 0 failures, zero page errors in every scenario** (script and log: `scratchpad/review6-live.js`, `review6-live.log`; screenshots `scratchpad/shots6/`):
  - **A** (the brief's seven rows: `30382:rank`→other, `30392`→assistant, `39998:dogs`→assistant @ dcosl, a duplicate `39998:dogs`→other, `39998:cats`→other @ dcosl, `39999:birds`→assistant no hint, `39998:dlist-header`→assistant; two `dogs` headers by the assistant, older `olddog` and newer `dog`; **the relay step ran for real** through the server proxy): rows in Map order; TA row "Trusted Assertion" · badge exactly `external` · relay · no details line, no duplicate pill (children `CODE,SPAN,A,SPAN,SPAN` — the pre-story shape); TL row "Trusted List" · "Your assistant"; `dogs` → "Curated DList · dogs", "Your assistant", avatar link `/tapestry/users/<assistant>`, details: link text `dog` (the newer header won) → `/tapestry/lists/39998%3A<assistant>%3Adogs`, "inherits from `39998:33333333…3333:dogs`" → `/tapestry/lists/39998%3A<other>%3Adogs`, "(inherit-items)", "· found locally"; the second `dogs` → "external · 33333333…3333", "duplicate — ignored", **no** details line; `cats` → "external · 33333333…3333", relay, "⚠️ Header not found locally or on wss://dcosl.brainstorm.world" in the warn colour with no links — the real `GET /api/relay/external` answered `200 {success:true, count:0}` in 299 ms; `birds` → "Curated DList · birds", "Your assistant", "⚠️ Header not found locally; no relay hint"; `dlist-header` → pill exactly "TA designation", "Your assistant", relay, no details. **Exactly three local header scans** — `{kinds:[39998],authors:[assistant],#d:[dogs]}`, `{…[other],#d:[cats]}`, `{kinds:[39999],authors:[assistant],#d:[birds]}` — none for the duplicate; **exactly one relay fetch**, for `cats`, `relays=wss://dcosl.brainstorm.world`; counts unchanged after 3 s (no effect loop); one deduped panel profile fetch. Clicking `dog` lands on `/tapestry/lists/…` and renders the header ("dogs"), not "Event not found". Console: one `401` (`/api/user-prefs` under the mocked session — pre-existing).
  - **B** (the route): `/tapestry/lists/39999%3A<assistant>%3Abirds` with a synthetic kind-39999 header renders `birds` via `{kinds:[39999],authors:[assistant],#d:[birds]}`; `9999:<pk>:birds` takes the a-tag branch (`kinds:[9999]`); `39998:<pk>:a:b` rejoins the colons (`#d:["a:b"]`); a raw 64-hex id still takes the `{ids:[…]}` branch and reads "Event not found" when absent.
  - **C** (the relay step, `/api/relay/external` mocked): `cats` found on the relay with two headers → newest wins (`cat`), link by coordinate, a `b` without a type reads "(pointer)" with its link, "· found on wss://dcosl.brainstorm.world"; `fish` with a sentinel-only `b` → "no community pointer"; `eels` @ `ws://localhost:7777` is queried and reads "…or on ws://localhost:7777"; `crabs` @ `https://not-a-relay.example` is **not** queried (see Non-blocking 1 for its text); `owls` with `success:false` → missing; `bats` whose relay answer is another author's event → filtered out → missing; five relay fetches, none for `crabs`; one local scan for the `(39998, other)` group carrying all six d-tags.
  - **D** (TA/TL-only Map): rows as before; no header scan, no relay fetch.
  - **E** (assistant unresolved, `assistantPubkey: null`): no locality badge on any of the seven rows; the curated rows still carry their details and the duplicate pill; every header scan's `authors` is the mocked per-user value — this deployment's TA (`11f23fe4…`) is never consulted.
- [x] `bash scripts/harness-lint.sh` — clean (0 violations) before this review; re-run after the story flip (see On PASS).
- [ ] `npm run test:playwright` — N/A (no Playwright half for this story per the test plan). The installed driver still wants Chromium build 1194, absent from the cache (1187/1223/1228 present) — Harness friction 2.
- [ ] _Lint not configured — skipped._
- [ ] _Typecheck not configured — skipped._
- [ ] _Build not configured — skipped._

## Spec adherence
- [x] Every acceptance criterion has a passing test (line references are the current HEAD):
  - **AC-1 classification** → U1, U2, U3 (`test/dlist-curation-map-entries.test.js:60-94`). `treasureMap.js:35-40`: the delegate check first (`pubkey === null → 'other'`), then kind 39998/39999 with a non-null, non-empty name → `designation` only for `kind === 39998 && name === 'dlist-header'`, else `dlist`; the 3038x/3039x ranges and `other` unchanged after it. All eight story-2 expectations hold (U3 restates them; the story-2 suite is green): bare `39998`/`39999` → `other` (name null); delegate-less → `other` with `pubkey: null` (also for `39998:dlist-header`); `30389`/`30390`/`30399` edges; uppercase hex normalised; `['d','x']` → `other`; garbage never throws. `39999:dlist-header` is a Curated DList (U2; the draft reserves the word for the blanket kind-39998 entry, `protocols/drafts/assistant-designation.md:76`). JSDoc enumeration updated (`:17-19`).
  - **AC-2 the row** → S1, S3, R1. `TreasureMapTagsPanel.jsx:8-9` labels/colours (`dlist: '#f0883e'`, `designation: '#8b949e'` per the ADR); `:126` `pillName` adds `· <d-tag>` for `dlist` only; the raw element `:139-141`, avatar + profile link `:154-163` unchanged; `:181` the external badge reads `external · <8>…<4>` for `dlist` rows and the bare `external` otherwise; `designation` rows render the label only. Live A1–A3, A7, A8, A12.
  - **AC-3 the header, verified** → U5, U6, S2, S3, S4. The effect `:53-96`: local first, batched one `queryRelay({kinds:[kind], authors:[pubkey], '#d': ds})` per `(kind, delegate)` group (`:59-67`), matched back by the event's own `kind:pubkey:d` (`:69-71`; a cross-group hit is impossible by filter and would land under a coordinate no row has), newest `created_at` wins (`:72`); the relay step only for rows still missing and only with a `ws://`/`wss://` hint (`:76-83`), newest wins (`:85-87`), every failure a miss (`:74`, `:88-90`). `DListRowDetails` `:205-231`: name = `names[1]` else the d-tag (`:214`) linked to `/tapestry/lists/<encodeURIComponent(coord)>` (`:218`); the community pointer via `communityPointerOf` (`treasureMap.js:239-245`: first `b` whose value is an a-tag; sentinel and event-id skipped; `type` = element 3 or `pointer`) rendered as `<kind>:<pk8>…<pk4>:<d>` (`shortCoord` `:13-18`) with a link and the type (`:219-224`), "no community pointer" otherwise (`:226`); the warning from `describeHeaderLookup` (`treasureMap.js:252-259`, both texts verbatim, U6) in the warn colour (`:210-212`). The route: `DListDetail.jsx:34` accepts `39998:`/`39999:`/`9998:`/`9999:`; the parse (`:36-40`) and the `ids` branch (`:41-44`) untouched. Live A4–A6, A9, A11, A19, B1–B6, C1–C4, C6, C7.
  - **AC-4 duplicates** → U4, S3. `markDuplicateEntries` (`treasureMap.js:224-232`): first occurrence wins per raw first element across `dlist` rows only (so `39999:dogs` is distinct from `39998:dogs`, and a repeated TL row is never marked), new row objects, non-arrays → `[]`; wired at `:38` under `useMemo` on `tags`; the pill `:184-191`; no lookup (`:54`), no `lookup` prop (`:114`), no details block (`:199`) for duplicates. Live A7, A13.
  - **AC-5 baseline** → S1. `useAuth().user.assistantPubkey` (`:32-33`); `locality` null until it resolves (`:124`); `taPubkey` absent; no 64-hex literal. Live E1.
  - **AC-6 nothing else moves** → U3, S4, R1–R4 + the guards. For `ta`/`tl`/`other` rows `pillName` (`:126`) and the badge text (`:181`) reduce to the pre-story expressions, `row.duplicate` is always `false` (`:227`), and the details block is `dlist`-gated (`:199`) — the rendering for those classes is unchanged (live A1, A2, D1: the same child nodes as before). The ten referencing suites are green; the TL card and story 5's panel are not in the diff (R2); page order intact (R3); util exports intact (R4); the route's other branches unchanged (S4, live B5, B6).
- [x] No criterion is silently dropped.
- [x] No behavior added that isn't in the story. Beyond the ACs: a `⏳ checking the header…` pending line while the lookup runs (`:208`), a tooltip on the duplicate pill (`:188`), and "· found locally / on <relay>" beside a found header (`:228`, the ADR's `where`). No persistence, no publish path, no change to what any panel composes or signs.

> **Story `## Deviations` judged (not taken on trust):**
> 1. *"The route condition is a prefix list, not the ADR's regex literal"* — true and justified. S4 (`test/dlist-curation-map-entries.test.js:162-163`) pins `/39999:/`, `/9999:/`, `/39998:/`, `/9998:/` against the source text; the ADR's literal `/^(39998|39999|9998|9999):/` contains none of those substrings (an `|` or `)` sits between every prefix and the colon), so an ADR-verbatim implementation would fail the judge. `['39998:', '39999:', '9998:', '9999:'].some(startsWith)` (`DListDetail.jsx:34`) is the same rule; live B1–B6 exercise all four prefixes and the `ids` branch. No test was touched by the implementation commit (its stat: story + three source files). Accepted; the ADR/test mismatch itself is Harness friction 1.
> 2. *"Live verification via the fetch-stub remount"* — every observation reproduced under my own Playwright run with the same seven rows (scenario A), including the real relay round-trip to `wss://dcosl.brainstorm.world` for `cats` and the single mocked-session `401` in the console. The bullet's "no signer needed for these render paths" is correct — every request is a read.
> 3. *"Observed, not fixed (story 5's lane)"* — true and correctly classed. `describeDListCuration` (`treasureMap.js:209-213`) counts every `findDListEntries` row (`:149-162`, no dedupe), so scenario A's collapsed label reads "4 DLists curated" (screenshot `A-map-entries.png`) while Map Entries marks one of the four ignored. This story's AC-6 and Out-of-scope keep the DList Curation panel untouched, so fixing it here would have been scope creep; it belongs with review #5's NB-1 (`byD` last-wins on the same duplicates) as one consistency follow-up — a candidate for the book close's chore list or an OPEN.md row (Non-blocking 4).

## ADR adherence
- [x] Files changed match the ADR's implementation notes 1–3: note 1 — the classifier change and the three additive exports with JSDoc in the file's style (`treasureMap.js:35-40`, `:215-259`; `describeHeaderLookup` returns exactly the ADR's `{status, where}` / `{status, text}` shapes); note 2 — labels/colours, `rows = markDuplicateEntries(tags.map(classifyEntry))`, one effect keyed on `rows` with `headers` state keyed by coordinate (`{event, checkedRelay}` / `{missing:true, checkedRelay}`), `EntryRow` and `DListRowDetails` as described; note 3 — the route condition (Deviation 1, judged above; same rule). Sub-decisions 1–5 walked: the 39999 link resolves by extending the route (live B1); local batching per `(kind, pubkey)` with a multi-`#d` filter, relay per missing hinted row, newest wins (live A13, A14, C1, C9); the community pointer's a-tag form, type default, sentinel skipped, link (U5, live A5, C2, C3); the inline short pubkey on `dlist` rows only (`:181`; live A1 vs A7); duplicates per raw first element across `dlist` rows, no lookup, no details (U4, live A7, A13).
- [x] Layering / module boundaries respected: pure helpers in the epic's util (ESM-imported by the U class), the panel composes and renders, the route gains two prefixes; `queryRelay` (`ui/src/api/relay.js`) and `/api/relay/external` (`src/api/relay/fetchEvents.js`, which validates `ws://`/`wss://` at `:39` and passes the filter through at `:49`) are consumed unchanged.
- [x] No new dependencies the ADR didn't authorize: the only new import is `queryRelay` from the existing `../../api/relay`; no `package.json` change.

## Concept-graph integrity
- [x] Handles are in `kind:pubkey:slug` form — the code composes `<kind>:<pubkey>:<d>` coordinates from parsed input (`:71`, `:77`, `:206`) and reads a-tags by their first two colons (`:13-18`); no concept handle is composed or read. `GET /api/concept-graph/summaries` on the local stack lists `39998:11f23fe4…:shared-concept` ("shared concept") and `39998:11f23fe4…:tapestry-assistant` ("tapestry assistant") — the two concepts the story names, under this deployment's TA (`/api/assistant/pubkey` → `11f23fe4…`); that pubkey appears nowhere in the diff (S1's sweep on the panel; my own 64-hex sweep over every added line → none; live E3).
- [x] Firmware reinstall: **not required** — no concept definition, schema, property, firmware or `protocols/` file changes (ADR "Firmware reinstall required? No"; the diff's eight files listed above). Confirmed and so stated.
- [x] New code orients via `/api/concept-graph/summaries` rather than re-reading BIBLE.md — the code needs no concept definitions; the ADR recorded the orientation it performed.

## Things tests can't catch
- [x] No secrets in committed files (fixtures are `'a'.repeat(64)`, `'0123456789abcdef'.repeat(4)`; the synthetic pubkeys of my live script never left the scratchpad; the sweep for `nsec|secret|password|token` over added lines matched only the ADR's prose "two-token change").
- [x] No leftover debug logging or `console.log` in source (added source lines swept for `console.`, `debugger`, `TODO`, `FIXME` → none; the diff's only `console.log`s are the suite runner's and `test.js`'s reporting lines).
- [x] No commented-out code (the added `//` lines are explanatory comments).
- [x] Error paths and edge cases handled where it matters: both lookups are wrapped (`:66-74`, `:81-91`) and a thrown `res.json()` or a `success:false` envelope reads as missing (live C6); `ev?.tags?.find` and `d == null` guards (`:69-70`); a wrong-author relay answer is filtered out (`:86`, live C7); `markDuplicateEntries` and `communityPointerOf` tolerate garbage (U4, U5); hooks (`:32-96`) all precede the early return (`:98`), so hook order is stable; `short()` is only reached with a non-null pubkey (`:124`, `:181`).
- [x] Concurrency / race conditions considered: the `cancelled` flag (`:56`, `:93`, `:95`) suppresses a late `setHeaders` after unmount or after `rows` changes; `rows` is memoised on `tags` (`:38`), so both effects key stably (live A16: scan and fetch counts unchanged after 3 s); `setHeaders({})` on the no-curated-rows branch runs only when `rows` changes — no loop (live D2). The relay fetches are awaited sequentially and the state is set once at the end (`:93`), so every details line resolves together after the slowest fetch (Non-blocking 6).
- [x] Security: every relay-sourced string (names, `b` values, relay hints, d-tags) is rendered as a React text node; the only URLs built from data are `encodeURIComponent`-ed (`:83`, `:218`, `:222`); the local filter is JSON-encoded by `queryRelay`; the `relays=` value is the Map's own hint, encoded, and the server accepts only `ws://`/`wss://` (`fetchEvents.js:39-43`); `title` attributes are static; the fetches are same-origin reads.

## House rules check
- [x] Concept Graph API authority respected.
- [x] No new lint/typecheck/build tooling without an ADR (the only tooling touch is the additive suite registration in `test/test.js:281-282`, `:697-698`, `:1210`, `:1453-1454`, `:1497`).
- [x] Per-user assistant baseline (OPEN.md row 188): `useAuth().user.assistantPubkey` only (`:32-33`); `taPubkey` absent (S1); no 64-hex literal; live E1/E3.

## Product-guide adherence *(when the story traces to a PRD)*
- N/A — acceptance-frame book, no PRD. The two labels and the two warning texts are the story's own copy (AC-2, AC-3), pinned by S1 and U6 and seen on screen.

## Findings

### Blocking
None.

### Non-blocking
1. **`ui/src/utils/treasureMap.js:254-257` with `ui/src/pages/grapevine/TreasureMapTagsPanel.jsx:79-80`** — the panel queries the hint only when it is a `ws://`/`wss://` URL, but the helper's missing text names `row.relay` regardless, so a non-websocket hint reads "Header not found locally or on <hint>" for a place that was never checked (live C5: `39998:crabs` @ `https://not-a-relay.example`, no fetch issued). Reachable only through a hand-edited Map — story 5's writer emits the configured DList relay or `''` — and the story's AC-3 specifies only the hint / empty-hint cases, so this is an unspecified edge, not drift. Optional: derive the text from `checkedRelay` (null → the no-hint text, or a third text for "hint is not a relay URL"); U6 would then need a pin for the mixed case.
2. **`ui/src/pages/grapevine/TreasureMapTagsPanel.jsx:85-88`** — the relay step filters returned events by kind and author but not by `d`, whereas the local step keys by the event's own `d` (`:69-71`). A relay that ignored the `#d` filter could surface a sibling header under the row. NIP-01 relays honour `#d` and the proxy passes the filter through unchanged, so not reachable in practice. Optional: add `dOf(e) === r.name` to the filter for symmetry.
3. **`ui/src/utils/treasureMap.js:217`** — `A_TAG_FORM` accepts lowercase hex only; an uppercase-hex pubkey inside a `b` value would read "no community pointer". NIP-01 pubkeys are lowercase and story 4's endpoint composes the `b` from the community event's own pubkey, so unreachable through the shipped writer. Note only.
4. **`ui/src/utils/treasureMap.js:209-213` and `ui/src/pages/grapevine/DListCurationPanel.jsx:118-122`** (story 5, not this diff) — the collapsed count includes duplicate entries and the "already empowered" index is last-wins, while Map Entries now applies first-occurrence-wins and marks later duplicates ignored (Deviation 3; review #5 NB-1). One small consistency follow-up (count effective entries; `if (!m.has(e.d))`) for the book close or an OPEN.md row — not this story's lane.
5. **`engineering-team/epics/dlist-curation.md:55-64`** — `## Decisions` lists ADRs 0001–0005; 0006 landed in `50051353`. The `## Stories` entry for 6 still reads *(planned)*. Update at the next harness commit (the same gap reviews #1, #4 and #5 noted).
6. **`ui/src/pages/grapevine/TreasureMapTagsPanel.jsx:76-93`** — the relay fetches run one after another and `headers` is set once at the end, so on a Map with N missing hinted rows every curated row reads "⏳ checking the header…" for up to N × 8 s (the proxy's `FETCH_TIMEOUT_MS`, `src/api/relay/fetchEvents.js:19`). Bounded by the number of curated DLists on one Map (ADR Consequences accepted this); optional later polish: `Promise.all` over the missing rows, or set state per row.
7. **Tester's lane, next time the suite is opened** — the pending line (`:208`), the "· found locally / on <relay>" suffix (`:228`) and the "no community pointer" text (`:226`) are unpinned (the live check covers them today); S2's `/useEffect\s*\(/` is already satisfied by the pre-existing profile effect, so the "one effect runs the lookup" half does not discriminate (S2's `queryRelay` + `/api/relay/external` halves do).

### Harness friction *(each a candidate OPEN.md `meta` row; not edited by me)*
1. **An ADR literal the Tester's S-class cannot match textually.** ADR 0006 §Implementation 3 prescribed `/^(39998|39999|9998|9999):/`; the Tester's S4 pinned the prefixes as substrings (`/39999:/`, `/9999:/`), which that regex alternation does not contain. The Implementer had to deviate from the ADR's literal to satisfy the judge (Deviation 1 — the right call, and recorded). When an ADR prescribes a literal source form, Phase 3 should check its S-class regexes against that literal (or pin behaviour rather than text); when the Tester pins text, the ADR's literal should be the text pinned.
2. **`npm run test:playwright` still cannot launch as installed** — Playwright 1.56.1 wants Chromium build 1194; the cache holds 1187/1223/1228. Worked around with `executablePath` → `chromium_headless_shell-1228` and `NODE_PATH=<repo>/node_modules` for a script outside the repo (reviews #1 and #5 friction, restated).
3. **The full `npm test` (~41 minutes here) cannot run inside the agent's 600 s foreground ceiling** — reviews #1, #4 and #5 friction, restated; I relied on the Implementer's brace-redirected log for the full gate and re-ran every suite that reads a touched file.
4. **Row 191 restated** — the three trusted-lists L0 guards are red-by-default on this posture; already on the ledger.

## Verdict
**PASS**

## On PASS (same commit)
- [x] Story `**Status:**` flipped to `Done` in place; the story's "Linked artifacts" review placeholder replaced with this file's path. No files moved (retirement is per-epic).
- [x] Completion detection performed; the result and the book arithmetic are recorded in the chat, not here. Every non-optional bullet of the acceptance frame in `engineering-team/audits/dlist-curation/book.md` is now covered by stories 1–6; the last bullet (merge-preserve, story 7) is marked as the operator's include-or-drop call, so whether the book is complete turns on that decision — `/close-book` is not run.
