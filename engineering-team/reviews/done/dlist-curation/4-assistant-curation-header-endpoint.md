# Review: Story 4 — Author the assistant's curation header for a chosen community DList

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-09-10
**Diff:** `git diff e5617ab0..HEAD` (base `e5617ab0`, HEAD `1a6f5e20`; branch `feat/dlist-curation`)
**Story:** `engineering-team/stories/dlist-curation/4-assistant-curation-header-endpoint.md`
**ADR:** `engineering-team/decisions/dlist-curation/0004-assistant-curation-header-endpoint.md`
**Test plan:** `engineering-team/stories/dlist-curation/4-assistant-curation-header-endpoint.test-plan.md`

Branch commits audited (oldest first): `b00b46fa` ADR · `a19a5105` failing tests · `b10a7b50` Tester-lane fixture fix · `5fe877b8` implementation · `9a3a2f21` Tester-lane RE1 re-scope · `1a6f5e20` ADR amendment. Nine files: three source (`src/api/dlist-curation/index.js` new, 310 lines; `src/api/index.js` +2; `src/api/trustedList/index.js` +2), three test (`test/dlist-curation-header-endpoint.test.js` new; `test/test.js` registration; `test/publish-export-a-concept.test.js` RE1 re-scope), three harness artifacts (ADR, story, test plan). No `package.json`, no `src/middleware/`, no UI, no firmware, no Neo4j code in the diff.

## Quality gates (run by reviewer, not trusted)

- [x] **Story-scoped gate** (the test plan's six suites, each via `node -e "require('./test/<name>.test.js').run()"`, plus the RE1 sentinel): **all green, 0 failures.** `dlist-curation-header-endpoint` 28/0 · `default-deny-mutations` 14/0 · `brain-first-tapestry-authoring` 19/0 · `trusted-list-pin-publish-blockers` 11/0 · `tag-applicability` 19/0 · `global-publish-gate` 8/0 · `publish-export-a-concept` 3/0 (RE1 green after the re-scope).
- [x] **Full `npm test` — the Implementer's log** (`scratchpad/npm-test-story4.log`, 11:39 EDT; its tree is `5fe877b8`, before `9a3a2f21`/`1a6f5e20` at 11:49). Checked in it: 157 suite lines `PASS`; the new suite reports `dlist-curation-header-endpoint suite: PASS (28 passed, 0 failed)` (line 4664), which proves the `test/test.js` registration end-to-end; `Total skipped: 56`; four suite lines red — `publish-export-a-concept` (2/1: RE1, the sentinel this diff trips by design; re-scoped in `9a3a2f21` and re-run green by me above) and the three trusted-lists L0 guards (`tl-membership-method-selector` 11/1, `tl-weighted-sum-method` 6/1, `tl-certainty-method` 4/2) — OPEN.md row 191: the guard reads the container's `/api/publish-policy`, which answers `{"success":true,"allowExternalPublish":true}` here; none of those suites touches this diff. The `Overall` line is red for those reasons only.
- [x] **Full `npm test` — my own detached re-run** (`{ npm test; echo "NPM_TEST_EXIT=$?"; } > scratchpad/npm-test-review4.log 2>&1`, run in the background and polled; no `tail` pipe, row 157; ~41 minutes wall-clock, 4667 log lines): `NPM_TEST_EXIT=1`, the `Overall` line red, `Total skipped: 56`. Every suite line `PASS` except the same three trusted-lists L0-guard suites (`tl-membership-method-selector` 11/1, `tl-weighted-sum-method` 6/1 + 2 skipped, `tl-certainty-method` 4/2 + 1 skipped) — row 191: the guard reads the container's `/api/publish-policy`, which answers `allowExternalPublish:true` here; the trio touches nothing in this diff. In this tree `publish-export-a-concept suite: PASS (3 passed, 0 failed)` (RE1 re-scoped) and `dlist-curation-header-endpoint suite: PASS (28 passed, 0 failed)`. **Not attributable to this story.**
- [x] **The judge still failed before the implementation.** Rebuilt the tree at `b10a7b50` (the amended fixture, pre-implementation) in the scratchpad and ran the suite: **4 pass / 24 fail** — the U/H/S classes on the missing module, the four R sentinels green — identical to the test plan's record. (Gotcha for the next reviewer: the archive needs the repo-root `lib/` — `trustedList` requires `../../lib/config`; a `src`+`test`-only archive under-reports as 2/26.)
- [x] **Live wiring (read-only, nothing published, no session created).** Unauthenticated POST via the host-mapped `:7778` → `401 {"error":"Authentication required for this action"}` — the default-deny middleware (`src/middleware/auth.js:448-456`; the peer inside the container is the docker bridge, not loopback, so the honest-local bypass does not apply). Direct in-container `POST http://127.0.0.1:7778/api/dlist-curation/header` → `401 {"success":false,"error":"authentication required"}` — the bypass at `auth.js:328-336` stamps `localTrusted` and passes the request to the handler, whose own guard refuses it (`src/api/trustedList/index.js:192-196`). So the route is wired to this handler and the two guards stack exactly as the story's Deviations claim. `GET` on the route → 404 (POST-only). `PUBLIC_MUTATIONS` (`auth.js:454`) is unchanged and not in the diff. The module is present in the container's bind mount and `src/api/index.js` there carries the registration.
- [x] **Live real seams (read-only, in the container, through the module's own exports):** `scanLocal({kinds:[39998],limit:3})` → 3 events in 42 ms (`strfry` resolves to `/usr/local/bin/strfry` on the control-panel process's PATH) · `fetchFromRelays({kinds:[39998],limit:8}, ['wss://dcosl.brainstorm.world'])` → 8 events in 270 ms, 2 of them self-declared by `dispositionOf` against their own coordinate · `composeCurationHeader` on a real one (`cat-breed`) → tags `d, names, slug, json, b(inherit-items)`, the real header's `concept-graph` and self-pointer `b` dropped, content empty · `isLocalOnly()` → `false` (matches `/api/publish-policy`) · `getSettings().aRelays.aDListRelays` → `["wss://dcosl.brainstorm.world"]` · `trustedList.requireAuth`/`publishToStrfry` and `getAssistantKeys` resolve as functions · the signer mechanics (`finalizeEvent(template, Uint8Array.from(Buffer.from(hex,'hex')))`, the same conversion `trustedList/index.js:48` uses for the stored hex key) verified with a throwaway `generateSecretKey()` key on a dummy template: `verifyEvent` true, pubkey matches. No real key touched.
- [x] **nostr-tools 2.10.4 (the container's copy)** — `lib/types/abstract-pool.d.ts:30` `querySync(relays, filter, params?)`, `:32` `publish(relays, event): Promise<string>[]`, `:24` `close(relays)`: the module's `r[0]`-per-relay race and `pool.close(relays)` are correct against the real API. `lib/cjs/abstract-relay.js:302` delivers an EVENT only if `this.verifyEvent(event) && matchFilters(so.filters, event)` — see AC-3 below for why that matters.
- [x] `bash scripts/harness-lint.sh` — clean (0 violations) before this review; re-run after the story flip (see On PASS).
- [ ] `npm run test:playwright` — N/A (no browser half; server-only story).
- [ ] _Lint not configured — skipped._
- [ ] _Typecheck not configured — skipped._
- [ ] _Build not configured — skipped._

## Spec adherence
- [x] Every acceptance criterion has a passing test (line references are `src/api/dlist-curation/index.js` unless stated):
  - **AC-1 who may call** → H1, H2, H8. Guard `:225-226` is trustedList's `requireAuth` (`session.authenticated === true` + 64-hex pubkey, `trustedList/index.js:186-199`) — the signature-verified one. Keys via `getAssistantKeys(sessionPubkey)` (`:204`, `:228`); the module never references `getOwnerAssistantKeys` (grepped); the owner's TA is reachable only when the session *is* the owner (`src/utils/assistantKeys.js:20-26`). The guard `!keys || !keys.pubkey || !keys.privkey` (`:229`) covers both the `null` shape (the catch path) and the object-of-nulls `getCustomerRelayKeys` actually returns for a customer without keys (`src/utils/customerRelayKeys.js:170-175`).
  - **AC-2 input** → U2, H3. `parseATag` `:46-53` (`A_TAG_RE`, split at the first two colons — d-tags with colons preserved); kind check `:238`; the exclusion rule `:241` against both the session pubkey and the assistant pubkey; all before any fetch.
  - **AC-3 fetched, not trusted from the client** → H4, H5, H12. `:246-252` relays first then local strfry with the same filter; `newest()` `:112-116`; self-declared check `:253-256`. The only body field read is `target` (`:233`). **Soundness of the check:** `dispositionOf(bValues, target).selfDeclared` compares the fetched header's `b` values to the *requested* coordinate rather than to one re-derived from the event; the two are equal because both sources honor the filter `{kinds:[39998], authors:[pubkey], '#d':[d]}` built from that same string — strfry by construction, nostr-tools by `matchFilters` + `verifyEvent` at `abstract-relay.js:302` — so a misbehaving relay cannot substitute a differently-addressed or unsigned event. Confirmed live on real relay headers.
  - **AC-4 the header** → U4, U5, H8. `composeCurationHeader` `:80-104`: `d` from the community `d` tag (`:94-95`), `COPIED_TAGS = ['names','slug','json']` (`:25`, `:97-101`), slug synthesized when absent (`:100`), exactly one `b` (`:82`, `:102`), content `''`, fresh `created_at` (`:103`). `concept-graph` and the community `b` are excluded by construction — a copy-list, not a deny-list — confirmed on a real header.
  - **AC-5 idempotent, never-clobber** → U3, U6, H6, H7, H13. `classifyExisting` `:66-72`: `unpointed` = no `b`; `exact` = exactly one `b` equal to `["b", <target>, "inherit-items"]`; everything else `conflict` — same target with a different type (`pointer`), untyped, another target, or the contract `b` plus any other. Handler `:259-270`: (a) 200 `existing:true, published:null`; (b) 409 `{existing:{b, event}}`, nothing signed; (c) `:272` composes with `existing` → every tag preserved in order + the contract `b` appended, content kept, `created_at = max(now, existing.created_at + 1)` (`:88`). The scan targets the assistant's pubkey, local strfry only (`:259`), as the story says.
  - **AC-6 publication, reported honestly** → H8, H9, H11, H14, S4. `:276` local import (`publishToStrfry` — `strfry import --no-verify` over stdin, `trustedList/index.js:73-105`); `:282-287` gate then relays. `isLocalOnly` `:193-199` is line-for-line the publish-policy rule (`src/api/publish-policy/index.js:28-31`: env `!== undefined` first, else `getConfigFromFile(…, 'false')`, exact `'true'`). `publishToRelays` `:170-190`: one `pool.publish([url], event)` per relay, `r[0]` (the per-relay promise — the row-200 mistake avoided), raced against 5 s, `Promise.allSettled`, rows `ok` / `failed` + message, `pool.close` in `finally`. `wsOnly` `:106-110` applied at the handler (`:247`) and again inside both relay helpers.
  - **AC-7 no graph write** → S1, R2, plus my lane sweep beyond the sentinel: the module's requires are `bValueForms` (top-level), `child_process`, `../trustedList`, `utils/assistantKeys`, `utils/config`, `config/settings`, and nostr-tools/ws at the container paths — nothing from `src/api/neo4j`, `src/lib/neo4j-driver`, or `src/firmware`. `strfry import` never reaches `POST /api/strfry/publish` or its `maybeBrainWriteTapestry` hook (R2). Nothing ingests a local kind-39998 letter on its own either: every `buildImportCypher` caller is an explicitly-triggered lane (`src/firmware/install.js`, `src/api/io.js`, `src/api/neo4j/eventSync.js` — HTTP handlers `:88/:318/:367`, no scheduler — `src/api/concept/pullClassThread.js`); the `stream-consumer` supervisor process (`src/pipeline/stream/redis-consumer.js:4`, `:111-114`) handles kinds 3/10000/1984 only; strfry's write-policy `plugin = ""` (`/etc/strfry.conf:121`) and the router streams carry no plugins. A header imported here stays a letter.
  - **AC-8 what the panel needs** → H6, H7, H8. `{success, existing, header, published:{local, relays:[{url, status, error?|reason?}]}}` (`:262`, `:288`); the 409 payload `:265-269`.
  - **AC-9 failure order** → H10, H11. `:275-279` local failure → 500 `stage:'local'` and return, nothing else attempted; `:285-286` a relay-helper throw → every row `failed` with the message; `success:true` whenever local succeeded (`:288`).
- [x] No criterion is silently dropped.
- [x] No behavior added that isn't in the story. The only additions beyond the ADR's export list are the four real seams exported "for the suite / future reuse" (`:305-309`) — inert, and what the ADR's lift-to-`src/lib` chore will want.

> **Phase-4 test edits judged (not a test-deliverable story, so `templates/adr.md:37-39`'s standing rule applies: test edits belong to the Tester's lane, and the Implementer must not weaken their own judge). Verified on the merits:**
> 1. **`b10a7b50` — `makeDeps` records over overridden seams** (`test/dlist-curation-header-endpoint.test.js:76-115`). Before: recording lived inside the default stubs, so H4 — which overrides `fetchFromRelays` with a bare stub and then asserts `calls.fetchFromRelays.length === 1` — was unsatisfiable by any implementation. After: a uniform recording wrapper over whichever implementation is in effect. **Stricter:** the same recorders now bind under every override (H4's "relays asked" + "local fallback with the same filter" and H12's "existing scan targets the assistant" became real assertions); nothing was removed; the un-recorded seams (`requireAuth`, `isLocalOnly`, `getDListRelays`, `now`) are unchanged. Separate `test:` commit landed *before* `impl:`, confined to this story's own suite, recorded in the story's Deviations and in the fixture's own comment. Judge still failing before implementation: 4/24 at that commit (rebuilt above).
> 2. **`9a3a2f21` — RE1 re-scoped** (`test/publish-export-a-concept.test.js:121-126`): `SUPERSEDED_BY_ADR = [<ROOT>/src/api/dlist-curation/]`, prefix-excluded before the `SimplePool` + `.publish(` sweep; `API_FILES` is still the recursive walk (`:50-63`), so every other file under `src/api/` remains guarded; TE1/TE2 untouched. Narrow, ADR-cited in the comment, its own `test:` commit five seconds before the ADR amendment, recorded in the story's Deviations. This sentinel belongs to another epic (community-reference #9), so it is not "the Implementer's own judge" being loosened — it is the codified form of the operator's escalation decision; whether that decision is justified is the ADR question below.

## ADR adherence
- [x] Files changed match the ADR's implementation notes 1–4 exactly: the new module with `parseATag` / `classifyExisting` / `composeCurationHeader` pure and exported, `scanLocal` (10 s, settled-once, kill on timeout `:121-144`), `fetchFromRelays` (8 s race, `:147-164`), `publishToRelays` (per-relay 5 s race inside `allSettled`, `:170-190`), the factory whose deps are the ADR's list verbatim (`:201-217`), `register(app)` at `POST /api/dlist-curation/header` (`:295-297`); `src/api/trustedList/index.js:489-490` exports `publishToStrfry` (no behavior change; R1 holds); `src/api/index.js:566-567` registers next to trustedList. Decision §1–8 walked: every status, branch and message matches; the one literal drift is the skipped-row `reason` (Non-blocking 1).
- [x] Layering / module boundaries respected: pure helpers (no I/O) → real seams → the handler; the lazy container-path requires (`:30-41`, `:203-214`) keep the module loadable stack-free (the suite proves it on the host) and the top-level require is the zero-require `bValueForms` only.
- [x] No new dependencies the ADR didn't authorize: nostr-tools and `ws` at the same container paths `src/api/relay/fetchEvents.js:10-16` already uses; no `package.json` change.
- [x] **The amendment (`1a6f5e20`) judged on its merits.** Header `Amends:` + a Context paragraph + a Consequences bullet supersede `community-reference` ADR 0004's Option-B rejection *for `src/api/dlist-curation/` only*. Each rationale checked: (i) the signer is a server-held per-user key (`getCustomerRelayKeys` → SecureKeyStorage) that no NIP-07 browser session can use — true, and it is exactly the condition the older ADR relied on to reject a server publisher ("the export had an owner's browser session"); (ii) the acceptance frame requires "published to local strfry and the community relay" (`audits/dlist-curation/book.md`, bullet 5) — true; (iii) the router's `dcosl` stream is `enabled:false` on this instance (`/api/strfry/router-status`, confirmed live), so router-only propagation would leave the header local — true; (iv) per-destination honesty (OPEN.md row 200) needs a send that settles per relay — true, and `publishToRelays` does it. The supersession is directory-scoped, RE1 still enforces the posture everywhere else, and the ADR states that any second server-side publisher needs its own ADR and exclusion. Justified and correctly scoped. Gap: the superseded ADR carries no back-reference (Non-blocking 7).
- [x] **Story `## Deviations` accuracy:** all three bullets hold against the diff and my live reproduction — the fixture amendment (as judged above), the escalation (RE1 tripped in the Implementer's log at line 4531; amendment and re-scope landed as described, in that order), and the wiring-only live verification (both 401s reproduced). The ledger row the second bullet asks for is listed under Harness friction; I do not edit OPEN.md.

## Concept-graph integrity
- [x] Handles are in `kind:pubkey:slug` form — the module composes `39998:<community pubkey>:<d>` from parsed input and addresses the assistant's header as `39998:<assistant>:<d>` by construction; never a literal (S4; my own sweep of the diff's added source lines for a 64-hex run → none). `GET /api/concept-graph/summaries` on the local stack lists `39998:11f23fe4…:shared-concept` and `39998:11f23fe4…:tapestry-assistant` — the two concepts the story names, under this deployment's TA; that pubkey appears nowhere in the diff.
- [x] Firmware reinstall: **not required** — no concept definition, schema, property, or firmware file changes (ADR "Firmware reinstall required? No"; the diff touches no `src/firmware/`, no concept JSON).
- [x] New code orients via `/api/concept-graph/summaries` rather than re-reading BIBLE.md — the code needs no concept definitions (it manipulates events by coordinate); the ADR recorded the orientation it performed.

## Things tests can't catch
- [x] No secrets in committed files (fixtures are `'b'.repeat(64)`-style; the only real key handling is `keys.privkey` in memory, never logged or returned).
- [x] No leftover debug logging or `console.log`: the one `console.warn` (`:159`) is an operational warning on the degraded relay-fetch path (URLs + message, no key material), in the codebase's style — not debug output.
- [x] No commented-out code.
- [x] Error paths and edge cases handled where it matters: every 4xx/5xx carries a message; the local-then-relays order leaves no state the response does not describe; a relay-helper throw degrades to `failed` rows rather than a 500 after a successful local write; the strfry spawn is settled-once with a kill on timeout; `existing.created_at` skew handled (`:88`); an empty or non-array relay group yields `[]`/no sends rather than a throw.
- [x] Concurrency / race conditions considered: two concurrent calls for the same d-tag by the same user both compose the same header; replaceable-event semantics keep one; the result is idempotent by design (a second call then reads `exact`). No shared mutable state in the handler; `_nt` is a load-once cache.
- [x] Security: authenticated-only by two stacked layers (default-deny middleware; `requireAuth` in the handler); `spawn` with argv, never a shell, and the strfry filter is built from regex-validated parts (`A_TAG_RE`; a newline in the d-tag fails `.+$`); the body contributes one string, and it reaches the signed header only as the `b` value after a signature-verified, filter-matched fetch proves it is a real self-declared coordinate; the response carries the signed public event only.

## House rules check
- [x] Concept Graph API authority respected.
- [x] No new lint/typecheck/build tooling without an ADR (the only tooling touch is the additive suite registration in `test/test.js:277-278`, `:687-688`, `:1198`, `:1439-1440`, `:1483`).
- [x] Per-deployment TA pubkey never hardcoded; per-user assistant baseline (OPEN.md row 188) honored — `getAssistantKeys(sessionPubkey)` only.

## Product-guide adherence *(when the story traces to a PRD)*
- N/A — acceptance-frame book, no PRD.

## Findings

### Blocking
None.

### Non-blocking
1. **`src/api/dlist-curation/index.js:283`** — the skipped-row `reason` is `'local-only publish policy'`; ADR 0004 sub-decision 5 / Decision §7 wrote `reason:'local-only'`. H9 matches by regex, so the suite does not pin the literal; story 5's panel will. Align one side when story 5's contract is written.
2. **`:153-156`, `:179-182`** — the race timers are not cleared when the primary promise settles first (a pending 8 s / 5 s timer per call; no unhandled rejection, since `Promise.race` subscribes to both). Cosmetic; `clearTimeout` in a `finally` when next touched.
3. **`:289-291`** — the outer catch carries no `stage`; anything thrown after `publishLocal` resolved (in practice only response serialization) would report a plain 500 although the local header exists. Optional: track `stage` through the flow and include it.
4. **`:121-144`** — `scanLocal` leaves stderr piped and undrained, where the shipped `src/api/strfry/queries/scan.js:86-88` uses `stdio: ['ignore','pipe','ignore']`. strfry's log lines are a few hundred bytes, far below the 64 KiB pipe buffer, so it cannot wedge in practice; align with `scan.js` when the ADR's lift-to-`src/lib` chore lands.
5. **ADR 0004 Context ("Per-user assistant keys")** says `getCustomerRelayKeys` "returns `null` when no keys exist (`customerRelayKeys.js:179`)"; the no-keys path actually returns `{pubkey:null, npub:null, privkey:null, nsec:null}` (`src/utils/customerRelayKeys.js:170-175`) — `:179` is the catch path. The handler's guard (`:229`) covers both shapes, so behavior is right; H2 exercises only `null`. Tester's lane, next time the suite opens: add the object-of-nulls case.
6. **`classifyExisting` and the sentinel** — an assistant header carrying only `["b","b-tag-deferred"]` classifies as `conflict` → 409 with the sentinel surfaced as `existing.b`. Consistent with the firmware never-clobber rule (`src/firmware/install.js:1057`: any `b` present suppresses the seed) and the story's "never re-point silently"; story 5's panel copy should expect `existing.b` to sometimes be the sentinel rather than a coordinate.
7. **`engineering-team/decisions/community-reference/0004-publish-export-a-concept.md`** carries no back-reference to the narrow supersession — its Revision 1 still reads "RE1 stays valid … no server-side `SimplePool.publish`", and the sentinel's comment (`test/publish-export-a-concept.test.js:121-125`) is the only pointer from that side. Suggest a short "Revision 2 (2026-09-10)" paragraph naming `dlist-curation` ADR 0004 and the excluded directory, at the next harness commit.
8. **`engineering-team/epics/dlist-curation.md:54-56`** — `## Decisions` lists ADR 0001 only; 0002–0004 have landed. Same gap review #1 noted (its finding 6 / friction 4).

### Harness friction *(each a candidate OPEN.md `meta` row; not edited by me)*
1. **Workflow 2 step 6 ("check for ADR conflicts") has no way to find *sentinel-enforced* postures.** `community-reference` ADR 0004's "no server-side external publisher" is pinned by a source sweep in `test/publish-export-a-concept.test.js`, so the conflict surfaced only ~40 minutes into the Phase-4 full run, and cost an escalation plus two lane commits — the ledger row the story's Deviations asks for. Cheap fix: step 6 also greps `test/` for the ADR numbers it is about to contradict, or the decisions index lists which ADRs carry sentinels.
2. **The full `npm test` cannot run inside the agent's 600 s foreground ceiling** (~41 minutes here, ~20 of them in the publish-flow suites waiting on relays) — review #1's friction 1, restated, not new.
3. **Row 191 restated** — the three trusted-lists L0 guards are red-by-default on this posture; already on the ledger.

## Verdict
**PASS**

## On PASS (same commit)
- [x] Story `**Status:**` flipped to `Done` in place; the story's "Linked artifacts" review placeholder replaced with this file's path. No files moved (retirement is per-epic).
- [x] Completion detection performed; the result and the book arithmetic are recorded in the chat, not here. The book (`engineering-team/audits/dlist-curation/book.md`) is not complete — stories 5–6 (and 7 pending the operator) remain — so `/close-book` is not offered.
