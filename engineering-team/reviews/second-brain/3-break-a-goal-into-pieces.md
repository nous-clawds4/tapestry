# Review: Story 3 — Break a goal into pieces

**Reviewer:** Claude (acting as Reviewer — independent subagent; the main session ran PO/Architect/Tester/Implementer, so this audit is the OPEN.md row-80(b) independent check)
**Date:** 2026-07-23
**Diff:** `git diff 85c20662..6747b16c` (story `5c766020` → adr `d7bd8b53` → tests `7622d77c` → impl `6747b16c`, on `feat/second-brain`; HEAD = `6747b16c`, tree clean)
**Story:** `engineering-team/stories/second-brain/3-break-a-goal-into-pieces.md`
**ADR:** `engineering-team/decisions/second-brain/0003-record-based-decomposition-and-validated-goal-writes.md` (amends ADR 0002 — reciprocal header pointer verified)
**Test plan:** `engineering-team/stories/second-brain/3-break-a-goal-into-pieces.test-plan.md`
**Binding guides:** `product-team/guides/second-brain-design-guide.md` (goal-row tree §, a11y baseline), `second-brain-style-guide.md` (canonical strings, banned-word list), `second-brain-wireframes.html` §1/§2, PRD `product-team/prd/second-brain.md` §5.2/§6/§7

## Quality gates (run by reviewer, not trusted)

- [x] `node test/break-a-goal-into-pieces.test.js` — **30 passed, 0 failed, 0 skipped**, stack up, all 7 H rows ran live. Fixture teardown ran clean (three strfry deletes observed in the log; count-0 verified by the suite's own teardown assertion).
- [x] `node test/capture-a-goal-and-see-it.test.js` — **27 passed, 0 failed, 0 skipped** (amended sibling).
- [x] `node test/structures-the-brain-can-trust.test.js` — **24 passed, 0 failed, 0 skipped** (amended sibling).
- [x] `npm test` (full gate, backgrounded per OPEN.md row 83) — **Overall: PASS on the first run.** Zero `  FAIL` lines across the whole log; all 202 suite-summary lines PASS; Total skipped 51 (normal live-precondition skips). Notably `relationship-primitives suite: PASS (23 passed, 0 failed)` **with the router active the whole run** — no row-75 `+1 scan count` drift this time, so no quiesced rerun was needed (router verified RUNNING before and after; I never stopped it). The three story suites gated in the same run: break-a-goal-into-pieces 30/0, capture 27/0, structures 24/0.
- [x] JSX gap-filler: in-container `npx vite build` — **clean** (`✓ built in 1m 12s`; only the pre-existing chunk-size warnings).
- [x] Live spot-checks (loopback = the `localTrusted` caller class; TA resolved per-run via `/api/assistant/pubkey` — `11f23fe4…` on this instance):
  - `GET /api/brain/hygiene` → `sound:true`, 0 problems, 3 + 5 elements across the two work-item concepts — green, before and after the full gate's fixture churn.
  - `GET /api/brain/goals` → exactly the 14 ADR-d5 keys per goal (`uuid, name, slug, statement, origin, capturedOn, createdAt, standing, captureDate, deliverable, boundary, parent, parentUuid, hasChildren`); the three legacy goals present at the root as `captured` leaves, `parent`/`parentUuid` null, `hasChildren` false.
  - Live schema node (`39999:<TA>:tapestry-owner-goal-schema`, read raw from strfry): eight properties — the original five plus optional `deliverable`/`boundary`/`parent` with exactly the d13 descriptions; `required` still `[name, slug, description]`; `x-tapestry.unique` unchanged. Live primary-property node: the same eight keys in stripped `{type}` form, `required` mirrored — the d8 fold's journaled `reconciled` outcome agrees with current live state.
  - Host-side caller classes: `GET /api/brain/goals` → **403**; `POST /api/normalize/create-child-goal` → **401**; `POST /api/normalize/update-goal-intent` → **401** (default-deny before the handler; `PUBLIC_MUTATIONS` untouched).
  - Post-gate residue check: goal set = exactly the legacy three; hygiene green. My own checks were read-only throughout.
- [x] _Lint / typecheck not configured — skipped. Playwright not applicable (no Playwright coverage exists for this surface; the plan's declared gap-filler is the vite build + S/U/H composition, run above)._

## Spec adherence

- [x] **AC 1 — Children in conversation, one parent each.** U2 (parse round-trip), U4 (attachment), U7 (the full refusal/ok matrix incl. multi-level trees and adoption), H3 (live create-child round-trip: `result:'created'`, child reads back with `parent` slug and resolved `parentUuid` — durable through the brain read), H4 (live adoption of a parentless fixture), H5 (live refusal matrix — 7 cases including the case-variant `name-collides` d-tag collision, self-parent, cycle, already-has-parent, empty-value — each `success:false` **with full-record snapshot equality proving nothing written**, the snapshot extended to the new fields). One-parent-at-most is structural (a single `parent` slug per record) plus the `already-has-parent` refusal; re-parenting stays refused (out of scope honored).
- [x] **AC 2 — Viable = leaf + deliverable + boundary.** U3 (full derivation matrix incl. whitespace-only and one-argument compatibility), S8 (hint line byte-exact in Goals.jsx; `viable` renders), H3/H4 (live standing flips to `viable` on sharpen). Standing derived on every read in `deriveStanding` (`src/lib/brain/goals.js:68-72`) — never stored; no flag field exists anywhere in the diff.
- [x] **AC 3 — A goal with children is never viable.** U3 (`hasChildren` override), and H3 proves it live the strong way: the parent fixture carries **both** fields yet derives `captured` once its child lands. `deriveStanding` checks `hasChildren` first, so the property holds by construction for every surface that passes context.
- [x] **AC 4 — Tree renders with disclosure.** S8 (glyph `▸`, navigation, no toggle-token), U4/U5/U6 (the one resolution truth: order preservation, duplicate-slug determinism, dangling/cycle tolerance — cycle-break nulls members only, descendants stay attached), H6 (legacy three at the root, `captured`). Story-1 S5/S5b/S12 ran unamended and green (27/27). Cold start, privacy line, loading, error and the new-row highlight are untouched hunks in the Goals.jsx diff. The three legacy goals carry the hint by the `!hasChildren && standing !== 'viable'` condition (true for all three — the gate-ratified honest-onboarding behavior).
- [x] **AC 5 — Deliverable and boundary, labelled, in the owner's words.** U2 (extraction), S9 (**"Done means"** / **"Stays inside"** byte-exact, *part of* context, back link), H4 (live sharpen → exact words read back, **and the d7 `capturedOn` backfill observed** — the adoptee fixture was created without one and had a pinned date after the update). Sharpening (overwrite with new words) is allowed per d7; the detail surface is minimal exactly as scoped (no record entries, no pointer cards, no edit affordances).
- [x] **AC 6 — Copy discipline and no regression.** S8/S9 mechanical jargon guards + my own full-list manual audit of every owner-facing string in both UI files (see Product-guide section); S12 + my own diff-wide grep — zero 64-hex literals in any added line; U8/U9 (taxonomy kinds specific and deterministic; the row-85c split); H1 (schema extended, `required` unchanged); H2 + my live check (hygiene green after the extension — the fold's live proof); H6 + my post-gate check (legacy three intact, names and statements unchanged).
- [x] No criterion silently dropped. No behavior beyond the story: the full-cycle file list is exactly the ADR's implementation-notes list plus harness artifacts; every addition traces to a d-number.

## ADR adherence (all thirteen sub-decisions)

- [x] **d1 (record shape):** `parseGoalRow` extracts `slug`/`deliverable`/`boundary`/`parent` with the same loose-null conventions; legacy rows parse with nulls (U2); statement stays ≡ description.
- [x] **d2 (slug refs + enforcement point):** `parent` is the json slug throughout; `createChildGoal` derives the slug with **`dtag.slug(name)`** (the diacritic-stripping variant — verified against `src/lib/dtag.js:23-29`), so json slug ≡ d-tag base by construction; the `name-collides` refusal checks name, derived slug, **and** d-tag base (`r.uuid.split(':').pop()`), closing the same-d-tag silent-overwrite hazard on this path — proven live by H5's case-variant fixture (`Harness Decomposition PARENT Goal` refused).
- [x] **d3 (standing derivation):** grown in place at the 0001 d4 extension point; `hasChildren` short-circuits first; `hasWords` trims; one-argument calls unchanged (U3).
- [x] **d4 (resolution + validation core):** `resolveDecomposition` — slug index with oldest-`createdAt`-wins and uuid tie-break, `slugShadowed`, dangling → root + `parentUnresolved`, cycle-break via three-state chain coloring (correct for a ≤1-parent graph; I audited it) nulling **members only**, `hasChildren` computed post-break so cycle members keep intact descendants (U6 proves exactly this). `validateDecompositionOp` — all five ADR kinds plus a defensive internal `unknown-op`; the set-parent cycle walk uses the same `winners` resolution truth and guards against pre-existing upstream cycles. Pure, dependency-free, exports grow by exactly the two functions.
- [x] **d5 (read surface):** projection is additive, byte-listed in the handler, no Cypher change; the new names are destructured from the **existing** two requires (S1 re-pins the same five specs; story-1 S2 / story-2 S3 both still green); `sortGoals` still the one ordering truth over resolved records.
- [x] **d6 (create-child-goal):** gate first (`isOwner || localTrusted` → 403), 400s for missing parent/name/statement, all checks before all writes, and the mint is line-for-line `create-element`'s: same lookup Cypher (ListHeader OR ClassThreadHeader — the `REL.*` aliases resolve to the same literal relationships as the brain read's Cypher, verified via `src/firmware/install.js:36-38`), `childDTag`, tags `d`/`name`/`z`/`json`, `signAndFinalize` → `publishToStrfry` → `importEventDirect` → `:ListItem`+slug → `MERGE` membership. Absent optionals omitted. Discriminated `{success:true, result:'created', element:{…}}`.
- [x] **d7 (update-goal-intent + serialization):** at-least-one-field 400; `empty-value` named refusal for any provided-but-blank value (the clearing backdoor closed); locate by slug (`goal-not-found`/`ambiguous-slug`); adoption via `validateDecompositionOp`; **capturedOn backfill before the re-sign** (H4 live proof); shallow-merge preserving all other sections verbatim; `regenerateJson`. **The promise-queue mutex is correct** (audited beyond S5's lexical pin): `serializeGoalWrite` chains each task after the previous settles either way, swallows rejections into the chain but returns the caller the task's own promise, and **both full read-validate-write bodies** run inside it — since `regenerateJson`/`importEventDirect` are awaited before a task resolves, the second of two racing adoptions validates against post-write state, so the A∥B mutual-adoption 2-cycle is closed in-contract.
- [x] **d8 (the fold):** `reconcilePrimaryPropertyForConcept` extracted; the standalone endpoint's behavior byte-equivalent apart from the additive `code` key (logged); story-2 S4 re-aimed exactly as planned (mechanism tokens in handler-or-extraction; gate still pinned in the handler); `handleSaveSchema` calls it after the schema `regenerateJson` and reports `primaryProperty`; the three structural non-fit codes (`no-primary-property`/`schema-shape`/`pp-unreadable`) map to `not-applicable` so firmware installs don't turn into noise; other failures surface loudly with the hygiene check standing guard (comment discloses the no-rollback posture). **Live d13 journal verified against current state:** schema 8 keys, pp record 8 stripped keys, required mirrored, hygiene green — the journaled `reconciled before:[5]→after:[8]` outcome is what the instance actually shows. Save-schema's gating untouched. The row-85b race is erased for this path; the standalone endpoint's residual is honestly carried in Consequences (c).
- [x] **d9 (taxonomy):** doc block grows five → **nine** kinds (counted); `classifyDecomposition` emits all three new kinds with the specified subjects and details, one problem per cycle keyed by the smallest member uuid (the additive `cycleOf` annotation feeds it — logged), deterministically sorted; wired **goal-concept-only** via `parseRecord: null` on the project entry (`src/api/brain/index.js:34-36`); row-85c split implemented (`schema-unreadable`; the property-section path stays `property-record-drift` — story-2 U7(e) unamended, U9 pins both sides); row-85d deliberate-ignore comment present at the filter site.
- [x] **d10 (tree UI):** grouping by server `parentUuid` with root fallback (never hides a goal), depth-first walk, default expanded, `▸` rotating 90° open, leaf `·`, 20px/12px depth indent via the `--brain-depth` row property (no new `:root` tokens — existing `--accent`/`--text-muted` only), hint verbatim on `captured` leaves, disclosure `align-self: stretch` on a min-height-44px row = full-row-height hit area (the guide's line-110 rule), `stopPropagation` on the disclosure, row click + Enter navigate, slug-less rows unlinked. Identifiers are `disclose`/`closedIds`/`expand`-family — S5b satisfied without amendment.
- [x] **d11 (minimal detail):** flat leaf route `goals/:slug` (`crumb: 'Detail'`, App.jsx verified), `useBrainGoals` + a winner-select that replicates the resolver's oldest-createdAt/uuid-tie-break rule (same truth as the tree), back link "← Goals" (also on error/not-found branches), metadata line in wireframe order (standing · *part of "{parent}"* linked · captured {date} · origin), both labels verbatim, hint on captured leaves only, register-conformant not-found line, no edit affordances, same gate branches as Goals.jsx.
- [x] **d12 (contract addendum):** the two endpoints exist with exactly the d6/d7 bodies; refusals additionally carry the machine-readable `refusal` key (logged, additive — strengthens the discriminated contract the agent needs; story 5 makes it discoverable).
- [x] **d13 (bootstrap, journaled):** executed as a live operational step, journaled in the story Deviations with the complete field list, and the suite never calls `save-schema` (H1 asserts the live extension instead — confirmed by reading the suite).
- [x] Layering: writes in normalize beside the machinery they ride; brain module untouched in import surface and mutation-freedom (S1/S2 + story-2 S6). **No new dependencies** (zero `package.json` changes).
- [x] **Untouchables byte-unchanged over the full cycle (0-line diff, verified):** `src/api/normalize/relationships.js`, `src/api/normalize/probe.js`, `src/middleware/auth.js` (incl. `PUBLIC_MUTATIONS`), `firmware/`, the four ADR-0015 `LEGACY_*` files, `ui/src/utils/nostrPublish.js` — and no brain UI file imports `nostrPublish`. Whitelist never extended; `HAS_SUBGOAL` nowhere in the diff (PRD §7.9 honored).

## Concept-graph integrity

- [x] Handles in `kind:pubkey:slug` form, constructed at runtime: `39999:${evt.pubkey}:${dTag}`, `39998:${taPubkey}:tapestry-owner-goal`; relationship names resolved through `firmware.relAlias` rather than re-hardcoded.
- [x] Firmware reinstall: **not required** (ADR Consequences — runtime-created concepts; the schema change is a live `save-schema`; installer untouched per the operator decision). The fold's install-time reach is disclosed and convergent-by-construction (d8); install reads only `success`, unchanged.
- [x] New code orients via runtime lookups (concept-graph API, `/api/assistant/pubkey`), never by re-deriving from BIBLE.md.

## Things tests can't catch

- [x] No secrets in committed files.
- [x] No debug logging — the only console use in the impl diff is the three `console.error` catch-idiom lines matching the normalize module's established pattern; no `console.log`, no TODO/FIXME.
- [x] No commented-out code.
- [x] Error paths: gates evaluated before any work in both handlers; every refusal loud, named, and pre-write; the update path's unreadable-record branch refuses rather than rewriting blind (defensive — records that reached it already parsed); handler catches return structured 500s to the already-gated caller class.
- [x] Concurrency: the d7 mutex audited and correct (above); a create-child racing a story-1 `create-element` capture remains possible but is the recorded Consequences (e) hazard, out of this story's scope with `duplicate-slug` detection compensating; the standalone reconcile's unlocked read-compare-write is the acknowledged row-85b residual (operator-traffic, idempotent, checked).
- [x] Security: all Cypher parameterized; both new mutations default-denied for sessionless remotes (H7 + my own 401 checks) and in-handler-gated for non-owner sessions (403); input validated at the boundary (types, trims, 400s); no injection vector via json (parse-and-refuse).
- [x] **Test-diff discipline:** `git diff 7622d77c..6747b16c -- test/` is **empty** — the impl commit touched no test file. The tests commit contains exactly the three planned sibling amendments, audited hunk-by-hunk: story-1 S7 (bans only `achieved`/`abandoned` now; still requires `captured`), story-1 H1 (re-aimed from "every goal captured" to d3 derivation-consistency — green on both sides by construction), story-2 S4 (mechanism tokens accepted in handler **or** extraction; gate still handler-pinned). Nothing anywhere in `test/` was removed or weakened beyond these ADR-prescribed re-aims. Failing-first stands from the plan's recorded run at `d7bd8b53` (9 pass = exactly the documented sentinels, 21 fail each for the right reason).
- [x] **Runner registration:** the new suite gates in the **live** `overallOk` chain — the severed terminator (OPEN.md #43) moved from the story-2 term onto the new term; the dead block below is an untouched context hunk in the diff; `totalSkipped` entry present; the summary line uses the row-58-correct skip-aware form.
- [x] **POV/architecture invariants:** standing and tree attachment re-derived on every read, stored nowhere; the write validation gates only *who may invoke this instance's own maintenance writes* (owner/admin/loopback) — no third-party published event is gated or rejected anywhere; no global-truth claim (the tree is this owner's own instance-local record, a structural property).
- [x] Story ACs and gate rulings were **never edited post-approval** — per-phase story diffs contain only the two linked-artifact fills and the appended Deviations log (verified).

## House rules check

- [x] Concept Graph API authority respected.
- [x] No new lint/typecheck/build tooling.
- [x] No hardcoded 64-hex pubkey anywhere in the full-cycle diff (S12 + my independent grep over every added line); TA runtime-resolved at every use; the `GOAL_CONCEPT_NAME`/slug constants are names, not identity (the legitimate pattern).

## Product-guide adherence (story traces to PRD §5.2/§5.8/§6/§7)

- [x] **Canonical strings verbatim:** cold start, privacy line (unchanged from story 1); the hint line byte-exact in both UI files (`needs a deliverable and boundary before it can be proposed` — the AC 2 ratified form); **"Done means"** / **"Stays inside"** exactly as wireframe §2.
- [x] **Standing words:** DOM text is canonical lowercase (`captured`, `viable`); the small-caps presentation is the design guide's own goal-row spec (story-1 adjudication carried).
- [x] **Full-list jargon audit (Reviewer-owned per the plan — `GoalDetail.jsx` is not covered by story-1's S8):** every owner-facing string in `GoalDetail.jsx` ("Goal", "Checking who you are…", "🔒 Goals are only available to the owner.", "Please sign in to continue.", "← Goals", "Couldn't load this goal —", "Retry", "Couldn't find this goal — it may have been renamed or removed.", "part of", "captured", "Done means:", "Stays inside:", the hint) and every new string in `Goals.jsx` (the hint, aria-labels "open goal detail" / "hide|show the pieces of {name}"): **none** of *element, kind, schema, event, pubkey, superset, concept header, persona, acceptance criteria, lease, payload, endpoint* appears; no exclamation marks; no celebration/urgency/machine-narration phrases; the not-found sentence follows the style guide's error pattern (what went wrong, in the owner's terms, with the way back).
- [x] **Design-guide patterns:** tree per the goal-row spec (glyph, rotation, 20px/12px indent, hint, row-click navigation, disclosure-without-navigation, no status dropdown, read-only); responsive back-link rule honored; touch targets (44px row height, full-height disclosure); do-not-design list clean (no toggle, no gauges, no chat, nothing visitor-facing); category chip and pointer count correctly absent (no categories exist; pointers are story 4).
- [x] PRD §6 (one optional parent; deliverable+boundary for viable leaves; standing derived, never stored), §7.4 (privacy: both writes ride `publishToStrfry` local + `importEventDirect`; **zero** `publishEverywhere`/`nostrPublish` matches in the full-cycle diff outside prose/test-prohibitions; no router config change), §7.7 (above), §7.8 (adopts create-element machinery and runtime identity), §7.9 (referenced, never re-specified — untouchables clean) — all honored.

## Deviations audit (story `## Deviations`, all eight)

1. **Journaled d13 schema extension** — verified live against the running instance: schema and pp node both at 8 keys, required/unique unchanged, hygiene green, and the fold (not a paired reconcile) did the work exactly as journaled. ✓
2. **Machine-readable `refusal`/`code` keys** — verified in the diff; additive on otherwise-specified responses; strengthens the d12 agent contract. Honestly logged. ✓
3. **Refusal-kind provenance (core vs endpoint)** — verified: the five relational kinds originate in `validateDecompositionOp`; `name-collides`/`empty-value`/`goal-not-found` are endpoint-built (d-tag and body knowledge); the route comments document the full set. ✓
4. **`cycleOf` annotation** — verified; additive; it is what makes U8's one-problem-per-cycle determinism work. ✓
5. **`resolveGoalConcept` = create-element's lookup** — verified line-for-line against `handleCreateElement` (no ConceptHeader — matching the write path the child rides, unlike save-schema's wider match). Accurate and the right call. ✓
6. **Hint condition `!hasChildren && standing !== 'viable'`** — verified in both files; equivalent to `=== 'captured'` while two standings exist; correctly flagged for story 6's revisit. ✓
7. **Detail branch titles + not-found line** — the not-found sentence verified verbatim and register-conformant. Minor imprecision in the log: only the auth-loading and gate branches actually title "Goal"; the loading and error branches carry no `<h1>` at all. Behavior is fine (d11 required the branches, not titles); noted as non-blocking #4. ✓ (with that footnote)
8. **Visual verification stopped at the gate branch** — consistent with the plan's declared Limits (no jsdom/Playwright; vite build as the compile gate). I re-ran the build myself (clean) and verified tree behavior through the same endpoint the view consumes, as the plan prescribes. ✓

## Findings

### Blocking

None.

### Non-blocking

1. **src/api/normalize/index.js (`createChildGoal` collision check)** — the `name-collides` dedup compares against *parsed* records only; a goal-concept element whose json is unreadable (reachable only via out-of-contract raw writes, and already flagged `unreadable-record` by hygiene) would not be in `records`, so a colliding name/d-tag against it would slip past the refusal and MERGE over it. Bounded: the state is itself a hygiene defect, and create-element's own subtree name-dedup has the same raw-write blind spots. Optional hardening: also compare against the raw `rowByUuid` names/d-tag bases.
2. **ui/src/pages/brain/GoalDetail.jsx:83** — the Retry affordance repeats story-1's non-blocking finding #1: an href-less `<a onClick>`, not keyboard-focusable. Same mitigation (focus/visibility refetch in the hook); same optional improvement (`<button className="brain-retry">`). Consistent with shipped precedent, so not escalated.
3. **ui/src/pages/brain/Goals.jsx:117** — `role="link"` is set on every row including slug-less (unlinked) ones; `tabIndex` is correctly omitted so they are not tab-reachable, but a screen reader touching one would announce a link with no action. Cosmetic; slug-less rows exist only for out-of-contract records.
4. **Design-guide line 69 vs shipped detail error copy** — the guide's goal-row error string is "Couldn't load this goal's details — retry"; the detail page ships "Couldn't load this goal — Retry". Story 1 established (review, AC-6 audit) that this guide line is a register pattern rather than byte-canonical (the tree shipped "Couldn't load your goals — Retry" and passed), and ADR d11 delegated the detail-page strings to the Implementer register-enforced — so this is consistent, not drift. Flag for book close: reconcile the guide's sentence or ratify the pattern reading in `prd-addendum.md`.
5. **src/api/normalize/index.js (`updateGoalIntent` unreadable-record refusal)** — the one refusal without a `refusal` key (plain `{success:false, error}`). Effectively unreachable (the record already parsed to be found); cosmetic contract asymmetry if deviation #2's discriminated pattern is ever formalized.

### Harness friction

1. **The row-83 background-gate protocol conflicts with the harness's foreground-sleep block.** The documented pattern for the ~24-min gate ("nohup + poll the log with repeated `sleep 60 && tail` in your own turn") is exactly what the Bash tool now refuses (`Blocked: sleep 60 followed by…`); the working pattern is a bounded `until grep -q "^Overall:" …; do sleep …; done` loop, which the harness moves to a *tracked* background task on timeout and re-invokes on completion — which also moots the row-74 nohup-orphan hazard for this phase. Row 83's proposed workflow note should name the until-loop/tracked-task pattern, not sleep-chaining. (Meta note for the main session's ledger sweep; rows 74/83 already exist — append, don't duplicate.)
2. Row 75 for the record: **no recurrence this run** — the full gate passed first try with the router active. Occurrence count stays at four.

## Verdict

**PASS**

The diff is mergeable as-is. All six ACs are covered by tests I ran myself (30/30 standalone; both amended siblings green; full `npm test` **Overall: PASS on the first run**, router active, no drift rerun needed); live behavior matches the spec exactly (schema and primary property extended and in agreement, hygiene green, the three legacy goals untouched at the root, both caller-class gates answering 403/401 first-hand, zero fixture residue after the runs); all thirteen ADR sub-decisions are implemented as ratified — including the d7 mutex whose semantics I audited beyond the lexical pin, the d8 fold whose live outcome matches the journal, and the d2 collision refusal proven live against the exact case-variant d-tag hazard it was designed to close; the impl commit's `test/` diff is empty and the Tester's three sibling re-aims are precisely the planned widen-only set; the untouchables are byte-unchanged over the full cycle; and every owner-facing string passes the full banned-word audit with the canonical strings byte-exact.

## On PASS (same commit)

- [ ] Story `**Status:**` flip to `Done` — **deferred to the main session** (per this review's assignment, the main session owns the status flip and the commit; story-2 precedent).
- [x] Completion detection run: the book (`engineering-team/audits/second-brain/book.md`, PRD-backed, 8 stories) is **not** complete — this is story 3 of 8; pointers + goal detail (4), session read loop (5), proposal loop (6), priority signals (7), and export/restore (8) are all outstanding. **No `/close-book` offer.** Carried reminders for the eventual close: the story-2 PRD §5.7 divergence, and finding #4 above (design-guide line 69's error-string status) for `prd-addendum.md`.
