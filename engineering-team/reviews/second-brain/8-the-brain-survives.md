# Review: Story 8 — The brain survives — export and restore

**Reviewer:** Claude (acting as Reviewer — independent subagent; the main session ran PO/Architect/Tester/Implementer, so this is the OPEN.md row-80(b) independent audit — nothing the Implementer reported was trusted; every gate re-run and every claim re-verified live here)
**Date:** 2026-07-25
**Diff:** `git diff 4b287c5a..35506766` on `feat/second-brain` — story `b32e7c55` → adr `03a68913` → tests `e15988f7` → impl `35506766`; base `4b287c5a` = staging tip at branch creation; tree clean, HEAD `35506766`
**Story:** `engineering-team/stories/second-brain/8-the-brain-survives.md`
**ADR:** `engineering-team/decisions/second-brain/0008-the-brain-survives.md` (Accepted 2026-07-24; d1–d15; the full Q1–Q5 open-question bundle operator-ratified at the gate; builds on second-brain 0001–0007)
**Test plan:** `engineering-team/stories/second-brain/8-the-brain-survives.test-plan.md`
**Binding guides:** `product-team/guides/second-brain-style-guide.md` (pinned: "Export brain." line 24; "Restore drill complete — your brain matches the export." line 27), `second-brain-design-guide.md`, PRD `product-team/prd/second-brain.md` §5.7/§7.4/§7.2/§5.9/§10

## Quality gates (run by reviewer, not trusted)

- [x] **Full `npm test` (authoritative gate) — RE-RUN IN FULL by me — `Overall: PASS`, exit 0.** strfry-router quiesced first (`supervisorctl stop strfry-router`, OPEN.md row 75) and restarted after (verified RUNNING). **0 FAIL lines anywhere.** `Total skipped: 41` — entirely the pre-existing environmental family (suite-level: `profile-tags-publish` 1 · `tag-detail-publish` 12 · `authored-tagging-publish` 16 · `profile-tag-polish-publish` 8; remainder in `event-tagging-firmware-seed`) — zero skips in any brain suite; not a story-8 artifact. No row-75 scan-count drift and no `tl-publication-from-pins-publish` flake occurred on this run (first-try pass).
  - `the-brain-survives suite: PASS (31 passed, 0 failed)` — in-gate.
  - Seven re-pinned siblings in-gate: `capture-a-goal-and-see-it 27/0` · `structures-the-brain-can-trust 24/0` · `break-a-goal-into-pieces 30/0` · `attach-the-world 29/0` · `sessions-read-the-brain 30/0` · `the-proposal-loop 33/0` · `teach-it-what-matters 27/0` — all green under the widen-only TEN re-pin.
- [x] **Standalone story-8 suite, run twice by me:** `node test/the-brain-survives.test.js` → **31 passed, 0 failed, 0 skipped** both times — **all 8 H rows ran LIVE** (stack present). Pre-impl baseline was 6/25/0 per the test plan; 6 + 25 = 31, so every previously-failing test now passes and no documented pass-by-design sentinel (S9/S11/S12/H8/R1/R2) regressed. Suite teardown left zero `harness-survive` residue (my own later discovery scans found none).
- [x] **Seven siblings standalone, run by me:** 27/0/0 · 24/0/0 · 30/0/0 · 29/0/0 · 30/0/0 · 33/0/0 · 27/0/0 — identical to the test plan's re-pin verification. The `+ /lib\/brain\/export$/` widen is present in all seven allowlists (grep-verified, one occurrence each at the documented lines).
- [x] **Impl-commit-touched-no-test check:** `git diff e15988f7..35506766 --stat` lists **only the 7 source files** (`src/lib/brain/export.js` NEW, `src/lib/brain/restore.js` NEW, `src/api/brain/index.js`, `src/api/normalize/index.js`, `ui/src/pages/brain/Goals.jsx`, `ui/src/styles.css`, `scripts/brain-drill.sh` NEW) — 933 insertions, 0 deletions, **no test file touched** after the tests were written. Tests were not weakened.
- [x] **Independent live round-trip** (my own `rev8-audit` sentinel fixtures — never the suites' `harness-survive`; loopback via `docker exec tapestry curl 127.0.0.1:7778` = the `localTrusted` class; TA resolved per-run via `/api/assistant/pubkey` → `11f23fe4…93767`, never hand-transcribed) — **45/45 checks OK**, fully torn down after:
  - **Export envelope + identity freedom:** `{format:'tapestry-brain-export', version:1, takenOn:<ISO date>}`, all five families, every entry exactly `{name, section}`; the serialized artifact contains **no 64-hex anywhere and not the TA pubkey**; `Content-Disposition: attachment; filename="brain-export-<takenOn>.json"`.
  - **AC4 live:** export twice → `contentEquivalent` **true** (compared via the shipped `src/lib/brain/export.js` required host-side — the ONE definition, not a reimplementation).
  - **Restore round-trip:** my cross-linked artifact (parent + viable child goal with back-dated `capturedOn` 2026-06-15 and an out-of-contract `rev8Extra` field, resource, work record, proposal + approving decision via `proposalId`, signal) → `{success:true, result:'restored', restored:{goals:2, resources:1, workRecords:1, proposals:2, signals:1}}` — the exact d6 shape. Read-back: child `parentSlug` = parent (decomposition survived); **`captureDate` NOT restamped** (2026-06-15 intact); standing re-derived `viable`; pointer present with its locator; child spine carries `worked`/`proposed`/`approved`/`preferred`; parent spine carries `passed over` (one fact, two spines); **the restored decision CLOSED its restored proposal** in `GET /api/brain/proposals` (proposalId → slug linkage survived byte-for-byte). Re-export → the rev8 subset **`contentEquivalent` to my artifact** (key-order-insensitive; out-of-contract field intact — raw fidelity proven).
  - **Mint discipline observed live (d7):** the restored goals took **name-derived** d-tags (`rev8-audit-parent-goal-1903378a` — no nonce), the restored records took **nonce** d-tags (`…~a5d9d607` etc.) — the discipline is visible in the store, not just the source.
  - **Collision refusals, each writing nothing:** re-restore → `goal-collides` listing BOTH rev8 goals; a record-slug collision → `record-collides`; **the live brain's own export restored against the live brain → `goal-collides` listing every live goal** (the AC2 protection, proven structurally). After all three refusals the full export was `contentEquivalent` to its before — **nothing written**.
  - **Drill producer refusal:** outcome `'sideways'` → HTTP **400** `{success:false}`; sentinel probe query confirms **nothing written**.
  - **Teardown:** strfry delete per d-tag with count-0 verify (all 7 fixtures), Neo4j DETACH DELETE + rev8-scoped orphan-tag sweep, discovery count 0, no rev8 residue in the final export, **hygiene `sound:true, problems:[]`** after.
- [x] **THE DRILL (AC2's "actually performed once") — journal verified LIVE by me, not from the implementer's logs:** the element exists at `39999:<TA>:restore-drill-2026-07-25-6691948b~5154b7b2` (TA resolved at runtime); wrapper key **`restoreDrill`**; all d9 required fields present (`name`/`slug`/`description`/`exportTakenOn`/`drilledOn`/`outcome`/`target`/`performedBy`); **`outcome:'matched'`**, **`performedBy:'owner'`**, **`drilledOn:'2026-07-25'`**, slug `restore-drill-2026-07-25-5154b7b2`; stored description exactly the d12 baseline: *"restore drill: the export taken 2026-07-25 was restored to a fresh scratch instance (ephemeral container, own assistant identity) and matched the export."* The `tapestry-restore-drill` concept resolves at the runtime handle `39998:<TA>:tapestry-restore-drill` (the FIFTH runtime-created concept). The §10 metric row — "1 journaled export/restore drill" — is now countable by inspection. (Re-running the script was at my discretion; the live journal element is the binding evidence and it checks out in full.)
- [x] **Host-side caller-class gates** (remote class, plain `/usr/bin/curl http://localhost:7778`): `GET /api/brain/export` → **403**; `POST /api/normalize/restore-brain` → **401**; `POST /api/normalize/record-restore-drill` → **401** (default-deny middleware; `PUBLIC_MUTATIONS` untouched).
- [x] **Vite build freshness:** the served dist bundle inside the container (`/usr/local/lib/node_modules/brainstorm/dist/assets/`, built after the impl commit) contains all three d12 strings — the affordance is live, not just in source.
- [x] _Lint / typecheck not configured — skipped. Playwright: none, per the test plan's documented Tester's-discretion call (fetch-download + one confirmation sentence; S10 pins the strings in source, H1/H7 pin the endpoint contract and gates; epic precedent for thin UI surfaces)._

## Spec adherence (all five ACs — live-proven)

- [x] **AC1 — dated, local artifact; all five families; owner-authored scope.** `GET /api/brain/export` (`src/api/brain/index.js:610`, registered `:647`) assembles `{format, version, takenOn, content:{goals, resources, workRecords, proposals, signals}}` from the same EXPLICIT∪IMPLICIT union every brain read uses (`readFamilyRows:176`), each entry `{name, section}` with the RAW stored section verbatim; membership is the family parser's call, so the scope is exactly the owner-authored brain content — not a whole-database backup (no trust scores, no other authors, no config anywhere in the artifact — verified over the live artifact). Decomposition positions ride inside goal sections (`parent` slug — proven by my round-trip read-back); proposals carry their decisions (same family, `proposalId` linkage — proven). Dated (`takenOn` ISO), lands locally (attachment download / blob save). U2/U3/S1/H1/H3 + my independent checks.
- [x] **AC2 — restore drill reproduces; live brain untouched; journaled.** (a) Reproduction: my round-trip proved restore→re-read→re-export equivalence over every family including cross-links; the drill itself proved it against a genuinely fresh scratch (own TA — the journal's target line). (b) The live brain untouched: the drill's only live-brain trace is the journal element (verified); the protection is structural — the live export REFUSES against the live brain on every goal (H5c + my check), and every refusal writes nothing (proven three ways). (c) Journaled: dated, append-only (nonce d-tag mint, no regenerateJson), discoverable from the brain (search + concept page; the fifth runtime concept), verified live above. The pinned sentence is printed by the drill script on match (`scripts/brain-drill.sh:169`, S13). **The drill has actually run once** — the journal element is the §10 evidence. U6–U8/S4/S5/S7/S13/H4/H5/H6.
- [x] **AC3 — no egress.** Both pure cores contain zero require/fetch/http tokens (grep-verified, S8); the export handler and the restore/drill blocks ride local reads + `publishToStrfry`+`importEventDirect` only; `scripts/brain-drill.sh` reaches exactly `http://127.0.0.1:7778` (no https anywhere; both `docker run` lines publish NO ports — scratch reachable only via `docker exec` loopback); Goals.jsx fetches only its own same-origin `/api/brain/export`. The privacy line stays true throughout. S8/S12/H3 + my greps.
- [x] **AC4 — export twice → equivalent content.** One `contentEquivalent` definition (`src/lib/brain/export.js:101` — canonically-sorted content, deep-equal, key-order-insensitive, `takenOn` outside the comparison) serves AC2 and AC4 (d2). Canonical sort makes serialization deterministic (U3); proven live twice (H2 + my round-trip, both via the shipped core).
- [x] **AC5 — copy discipline, portability seed, no regression.** The three owner-facing strings this story adds (`ui/src/pages/brain/Goals.jsx:24-26`) are the pinned "Export brain." + the gate-ratified d12 confirmation/failure sentences, **verbatim** (checked by strict equality in S10 and by my own read); each passes the FULL 12-word banned-jargon list by my own check, carries no exclamation mark and no numerals; no scores or progress theater (the button just disables in flight — d11). Portability seed: no 64-hex anywhere in the diff (grep over all added lines: 0); TA runtime-resolved server-side (`getOwnerAssistantPubkey`) and never present in the artifact (live-verified); restore-on-empty self-provisions all five concepts + the drill concept (`ensureGoalConcept:4856` closes ADR 0001's bootstrap gap; the drill's fresh-TA scratch proved the whole path on a truly empty instance). No regression: stories 1–7 suites green standalone AND in-gate (above).

## ADR adherence (Option A; d1–d15)

- [x] **d1** — artifact format exact (`src/lib/brain/export.js:27-29, 87-94`): format/version constants, five family keys, `{name, section}` entries, parser membership, canonical sort (slug then stringified tie-break, `:61-69`), dated envelope, `brain-export-<takenOn>.json` filename. Identity-free proven live.
- [x] **d2** — ONE `contentEquivalent` (`:101-105`), serving AC2 (my re-export subset compare; the drill's step 6) and AC4 (H2 + my check). `uuid`/`createdAt` never enter the artifact (U2's exact-keys assertion + H1 + my shape check).
- [x] **d3** — `GET /api/brain/export` is the brain module's sixth route; in-handler gate (isOwner ∥ localTrusted → 403, `:611-613`); `readFamilyRows` reuses the module's own EXPLICIT/IMPLICIT cypher constants (the same union); the five existing read functions **byte-unchanged** (diff shows no edits to them); empty family exports `[]`; reads provision nothing (H3: goals/proposals/hygiene byte-identical across double export).
- [x] **d4** — `export.js` pure zero-require CJS (U1 asserts; my read confirms), the artifact format's single owner: `familyEntries`/`canonicalContent`/`assembleExport`/`contentEquivalent`/`validateArtifact` with plain-words shape errors (U5). Tenth require in the brain module (`src/api/brain/index.js:32`); **allowlist re-pinned to TEN in all seven sibling suites in the same diff** (d13 — verified per-file).
- [x] **d5** — `restore.js` pure zero-require CJS (U6 asserts): `planRestore` computes the goal-collision triple (name / slug / caller-supplied `deriveGoalDTag` — the `createChildGoal` guard generalized), family-scoped record-slug collisions, ALL collisions listed in one refusal (U6), ordered verbatim mint plan (U8). All checks precede all writes; refusals write nothing (proven live ×3).
- [x] **d6** — `POST /api/normalize/restore-brain` (`:5207`): gate-first (`:4951-4953`), shape 400s via `validateArtifact` (`:4959-4963`), domain refusals HTTP-200 `{success:false, refusal, collisions, error}` (plain-words error naming the count), the ENTIRE restore inside one `serializeGoalWrite` task (`:4966`), result shape exact (my round-trip asserted it field-for-field). Non-transactional per the documented consequence.
- [x] **d7** — `mintRestoredGoal:4905` name-derived `dtag.childDTag(name, headerUuid)` + verbatim section under `tapestryOwnerGoal`, node slug = section.slug, **capturedOn not restamped** (live-proven); `mintRestoredElement:4928` nonce `randomDTag()` + verbatim section under the family key. Both ride the established local spine (`signAndFinalize` → `publishToStrfry` → `importEventDirect` → `SET :ListItem` → `MERGE superset→elem`); **no `regenerateJson` on any new path** (S5; the diff's only regenerateJson mentions are ADR prose and test assertions — grep-verified).
- [x] **d8** — `ensureGoalConcept:4856` structural copy of `ensureSignalConcept` (resolve → no-op if present → create-concept + save-schema + re-resolve, loud throw), `GOAL_SCHEMA:4828` mirrors the live schema (required name/slug/description; the eight properties; `x-tapestry.unique:['name','slug']`). Restore ensures lazily per family with entries (`FAMILY_ENSURE` map, first-mint-of-family trigger). Proven end-to-end by the drill's truly fresh scratch. Never firmware-seeded.
- [x] **d9** — fifth runtime concept `tapestry restore drill` (`DRILL_SCHEMA:5056`, resolve `:5081`, ensure `:5099`, mint `:5116` nonce d-tag, born final); producer `record-restore-drill:5139` gate-first, 400 on missing/malformed fields and on any outcome outside `matched`/`did-not-match` (live-proven: 'sideways' → 400, nothing written), `target` defaults, one mutex task, `drilledOn` server-stamped (S6's body-read scan + live evidence), `performedBy:'owner'`. Both outcomes journaled (script sets `did-not-match` and still journals — S13 + script read). Drill records NOT exported (they're not one of AC1's five families — the export never reads the drill concept; confirmed in `handleGetExport`'s five `readFamilyRows` calls). No new brain read route (six routes exact — S3 + my grep).
- [x] **d10** — `scripts/brain-drill.sh` implements the full sequence: preflight, live export, ephemeral scratch from the same image (fresh anonymous data volumes, `--rm`, **no published ports** — verified per run line), health waits as bounded until-loops, firmware install + router quiesce in scratch, restore via scratch loopback, re-export, `contentEquivalent` compare via the shipped core, journal to the LIVE brain (both outcomes), pinned sentence on match / plain error + non-zero exit on mismatch, teardown. Two documented environmental accommodations beyond the ADR's sketch — a read-only bind-mount of the working tree's `src/` (code, not data; on a deployed instance a no-op) and a scratch-Neo4j memory shrink (this machine sizes heap from the whole host) — both in-script commented; the §5.9 proof (own TA, empty data volumes) is unaffected. See non-blocking #3.
- [x] **d11 (operator-ratified placement)** — the affordance is a quiet outline button in the Goals-view footer beside the privacy line (`Goals.jsx:222-231`), present in the empty state too (the export row renders outside the empty-state conditional), disabled while in flight, no spinner/progress theater; blob download named per d1. Existing Goals behavior untouched (additive diff only).
- [x] **d12 (operator-ratified strings)** — all verbatim (S10 strict-equality + my read): button label, `Exported — saved to this machine.`, `Couldn't export — nothing was saved. Try again.`, the drill sentence in the script, and the journal description baseline (verified in the STORED live journal element). Jargon-free, numeral-free, no exclamation marks.
- [x] **d13** — the seven sibling re-pins are widen-only (each diff is exactly the one allowlist line); `registerBrainRoutes` gains exactly `/api/brain/export`; `restore.js` is required by normalize only (no brain-pin impact — S2 passes).
- [x] **d14 — untouchables byte-unchanged** (git-verified over the full list in one diff — output EMPTY): `GoalDetail.jsx`, `Proposals.jsx`, `io/ExportPage.jsx`, `relationships.js`, `probe.js`, `middleware/auth.js` (incl. `PUBLIC_MUTATIONS` — R2), `firmware/`, the four ADR-0015 `LEGACY_*` files, `nostrPublish.js`. `serializeGoalWrite` not renamed (defined once, used by both new handlers); the five existing reads and all existing producers untouched; `HYGIENE_CONCEPTS` unchanged (the diff's single mention is ADR prose); `Goals.jsx` additive; no new `:root` tokens (the hover rgba literal has 3 pre-existing precedents in styles.css).
- [x] **d15** — firmware reinstall correctly NOT required: all concepts runtime-created; `firmware/` and installer byte-unchanged. (The drill script *runs* the standard firmware install inside the throwaway scratch — a deployment step, not a firmware change — exactly as the ADR states.)

## Concept-graph integrity
- [x] Handles are `kind:pubkey:slug` throughout: `39998:<TA>:tapestry-restore-drill` (verified resolvable live), the five family headers constructed from the runtime TA. **No hardcoded 64-hex anywhere in the diff** (0 added lines match; S9 + my grep). TA resolved server-side via `getOwnerAssistantPubkey()` and client-side implicitly (the UI calls its own API); the artifact itself carries no identity at all — stronger than the house rule requires.
- [x] Firmware reinstall correctly NOT required (d15 — runtime-created/self-bootstrapped concepts; verified live: the drill concept exists with `firmware/` byte-unchanged).
- [x] New code orients via the runtime graph (concept resolution by name/handle at read/write; the ADR's recon went through the concept-graph API), not by re-deriving from BIBLE.md.

## Things tests can't catch
- [x] **No secrets / no hardcoded pubkey** — clean (the drill script's `OWNER_PUBKEY`/`ADMIN_PUBKEYS` are env passthroughs, no values).
- [x] **No stray debug** — the only console calls added in server code are the two sibling-pattern catch-path `console.error`s; the drill script's `node -e "console.log(…)"` lines are output plumbing; the suite's console.log is its reporter. No commented-out code.
- [x] **Error paths gate-and-validate before any write** — gate → shape 400s → mutex → fetch existing → plan (ALL collisions) → only then ensure + mint. A refusal writes nothing (proven live three ways + H5/H6).
- [x] **Concurrency** — both producers run inside the process-singleton `serializeGoalWrite`, so a restore cannot interleave with any goal write, and first-drill concept provisioning cannot double-run. The export read is unmutexed (five families via `Promise.all`) — same semantics as every sibling brain read; see non-blocking #4 for the (acceptable) torn-read window.
- [x] **Security** — the export is the whole brain, gated in-handler (403) AND effectively remote-denied at middleware for mutations (401); artifact input is shape-validated before any consumption; raw-fidelity restore is bounded by owner-gating + shape validation + parser whitelisting at every read (the ADR's documented compensation). Verified 403/401/401 from the host myself.
- [x] **Read-tolerance** — malformed rows drop at parse on export (U2); a hand-edited artifact either fails shape validation, collides, or restores fields that read paths whitelist-tolerate.
- [x] **Residue** — the suite (twice standalone + in-gate) and my own round-trip left zero fixture residue; hygiene `sound:true` at every checkpoint including after my teardown.

## House rules check
- [x] Concept Graph API authority respected; runtime TA discipline held everywhere (and the one place the epic's rules demand a literal — the ADR-0015 `LEGACY_*` files — is untouched).
- [x] No new lint/typecheck/build tooling; no new dependencies (`package.json` untouched); JS-without-build preserved (the vite build is the existing UI pipeline, not new tooling).
- [x] test.js registration is the standard five-touch: require, run call, summary line, `overallOk` term added to the LIVE chain with the severed terminator flipped exactly as the plan states (`teachItWhatMattersResult.fail === 0;` → `&& … theBrainSurvivesResult.fail === 0;`, OPEN.md #43 dead block left intact), `totalSkipped` array extended. `node --check test/test.js` clean.

## Product-guide adherence
- [x] **Copy:** the style guide's two pinned strings appear verbatim (button label line 24; drill sentence line 27); the two NEW sentences are the operator-ratified d12 set, verbatim in code and in the served bundle; all owner-facing strings jargon-free (full 12-word list), numeral-free, exclamation-free; dates render as the record spine already renders dates; no scores or progress theater anywhere.
- [x] **Design guide:** the do-not-design list is untouched (no chat UI, no gauges, no privacy *toggle* — the export affordance is a one-way save beside the quiet privacy line, the same posture); the screen inventory names no export surface, so placement was gate-ratified (Q1 — the Goals-view footer) per the story's documented deferral; token-driven styles, no new tokens; the empty state keeps the affordance (an empty brain exports a valid artifact). The d12 strings + placement join the book-close guide back-fill bundle (ADR 0006 d16 + 0007 d5 + 0008 d12).

## Findings

### Blocking
_None._

### Non-blocking
1. **`engineering-team/epics/second-brain.md:27`** — story 8's epic marker still reads *(Queued)*; stale after this PASS (the same pattern story-7's review noted; story-7's marker was reconciled in `edc1a230`). My launch instruction pins this commit to exactly the review + story files — left for the main session to reconcile at the next epic-file touch (naturally at close-out).
2. **`test/the-brain-survives.test.js:726`** — S10's banned-jargon scan checks 9 of the story's 12 words (drops `element`, `kind`, `event`; `teach-it-what-matters.test.js:606` scanned all 12). No consequence today: no Goals.jsx string literal contains any of the three (I checked — the full list would pass), and the three new strings pass the full list by my own eye. Optional: widen the list at the suite's next touch.
3. **`scripts/brain-drill.sh:66-72, 88-93`** — two environmental accommodations beyond the ADR d10 sketch: the scratch bind-mounts the working tree's `src/` read-only (so a locally-stale image still drills the code under review; data volumes stay fresh + anonymous — the §5.9 proof is unaffected; on a deployed instance the mount is a no-op) and shrinks the scratch's Neo4j memory (this host sizes heap from the whole machine — fatal for a second instance). Both are in-script documented and sensible; recorded here so the ADR-notes/script gap is on the record, not silent.
4. **`src/api/brain/index.js:618-625`** — the export read assembles the five families outside the write mutex, so a write landing mid-`Promise.all` could yield a torn artifact (e.g., a goal present, its resource absent). Consistent with every sibling read's unmutexed semantics and AC4's "no changes in between" framing; the failure mode is benign (a torn artifact restore-refuses or under-restores loudly — it cannot clobber). Not a defect; noted as considered.

### Harness friction
1. **The live instance's Neo4j is memory-fragile on this machine** (24 GB Docker VM, multiple stacks): it can crash with `ECONNREFUSED 127.0.0.1:7687` under side-by-side pressure — the drill script had to grow explicit wait-for-DB loops and a shrink-scratch-heap step, and the DB had freshly auto-restarted (~1–2 min supervisor recovery) shortly before my audit began. It did **not** bite any of my runs (standalone ×2, siblings ×7, full gate, 45-check round-trip all first-try), but no OPEN.md row records the lesson (row 27/75/94 are adjacent, none cover it). **Recommend the main session add an `ops` row**: "live Neo4j on the dev host can OOM-crash under multi-container pressure; supervisor recovers in ~1–2 min; wait for `POST /api/neo4j/query RETURN 1` and re-run the affected check rather than failing a story; drills/scratch containers must cap their own Neo4j memory." I can't add it — my commit is pinned to two files.
2. Otherwise none: story, ADR, and test plan were internally consistent throughout; the pre-impl 6/25/0 baseline, the seven sibling counts, the d6 result shape, and the drill-journal claims all landed exactly as documented.

## Verdict
**PASS**

## On PASS (same commit)
- [x] Story `**Status:**` flipped to `Done` in place (`engineering-team/stories/second-brain/8-the-brain-survives.md`), Review line filled — this commit.
- [x] **Completion detection run: the book now looks complete.** The anchor (`engineering-team/audits/second-brain/book.md`) is PRD-backed — §8.1 decomposed as the 8-story block — and with this PASS **all 8 stories are Done** (capture/view · hygiene · decomposition · pointers + detail · session read loop · proposal loop · priority signals · export/restore drill); the §10 MVP-countable drill row is journaled and live-verified. Per the workflow the close is **offered, never auto-run**: the operator's "yes" in the main session triggers `/close-book` (build audit + `prd-addendum.md`, including the accumulated guide back-fill bundle — ADR 0006 d16, 0007 d5, 0008 d12 strings + the export affordance placement — and the epic folder moves at close-out). **Not run here.**
