# Review: Story 7 — Teach it what matters (priority signals)

**Reviewer:** Claude (acting as Reviewer — independent subagent; the main session ran PO/Architect/Tester/Implementer, so this is the OPEN.md row-80(b) independent audit — nothing the Implementer reported was trusted; every gate re-run and every claim re-verified here)
**Date:** 2026-07-24
**Diff:** `git diff b0314669..HEAD` on `feat/second-brain` — epic-marker docs `a79b3bdd` → story `042bdc09` → adr `37c950c0` → tests `72b50aa9` → impl `03a8af9c` → OPEN.md row 94 `61d96dfc`; base `b0314669` = staging tip at branch creation; tree clean, HEAD `61d96dfc`
**Story:** `engineering-team/stories/second-brain/7-teach-it-what-matters.md`
**ADR:** `engineering-team/decisions/second-brain/0007-teach-it-what-matters.md` (d1–d15 + Test-class guidance; d4/d5/d7/d8 operator-ratified at the Architecture gate; builds on second-brain 0001–0006)
**Test plan:** `engineering-team/stories/second-brain/7-teach-it-what-matters.test-plan.md`
**Binding guides:** `product-team/guides/second-brain-style-guide.md`, `second-brain-design-guide.md` (both with the documented signal-vocabulary gap — the d5 ratified strings stand in; book-close back-fill planned), PRD `product-team/prd/second-brain.md` §5.6/§6/§7.1–7.8

## Quality gates (run by reviewer, not trusted)

- [x] **Full `npm test` (authoritative gate) — RE-RUN IN FULL by me — `Overall: PASS`, exit 0.** strfry-router quiesced first (`supervisorctl stop strfry-router`, OPEN.md row 75) and restarted after; run took ~25 min (the ADR's ≈24-min budget, not the shorter estimate). **0 FAIL lines anywhere.** `Total skipped: 41` — all in the pre-existing environmental family (pin/TL publish-flow suites + `event-tagging-firmware-seed`: Meili-not-indexed / settings-not-writable), zero skips in any brain suite. Not a story-7 artifact.
  - `teach-it-what-matters suite: PASS (27 passed, 0 failed)` — in-gate.
  - Six re-pinned siblings in-gate: `capture-a-goal-and-see-it 27/0` · `structures-the-brain-can-trust 24/0` · `break-a-goal-into-pieces 30/0` · `attach-the-world 29/0` · `sessions-read-the-brain 30/0` · `the-proposal-loop 33/0` — all green under the widen-only ninth-require re-pin.
  - No OPEN.md row-94 teardown cascade occurred on my runs (the hazard logged this story; recovery procedure documented in the row).
- [x] **Standalone story-7 suite, run twice by me:** `node test/teach-it-what-matters.test.js` → **27 passed, 0 failed, 0 skipped** both times — **all 7 H rows ran LIVE** (stack present). Pre-impl baseline was 10/17 per the test plan; 10 + 17 = 27, so every previously-failing test now passes and no documented pass-by-design sentinel (S3/S9/S10/S12/S13/S14/H6/H7/R1/R2) regressed.
- [x] **Six siblings standalone, run by me:** 27/0/0 · 24/0/0 · 30/0/0 · 29/0/0 · 30/0/0 · 33/0/0 — identical to the test plan's re-pin verification.
- [x] **Impl-commit-touched-no-test check:** `git diff 72b50aa9..03a8af9c --stat` lists **only the 3 source files** (`src/lib/brain/signals.js` NEW, `src/api/brain/index.js`, `src/api/normalize/index.js`) — the impl commit touched **no test file**. Tests were not weakened after they were written.
- [x] **Independent live round-trip** (my own `rev7`-sentinel fixtures — never colliding with the suites' `harness-signal-` names; loopback via `docker exec tapestry curl 127.0.0.1:7778` = the `localTrusted` class; TA resolved per-run via `/api/assistant/pubkey` → `11f23fe4…93767`, never hand-transcribed) — **24/24 checks OK**, fully torn down after:
  - Two **captured/non-viable** sentinel goals created (no deliverable/boundary; `standing === 'captured'` verified) — the ADR d4 no-standing ruling proven live: `record-priority-signal` **succeeded** between them → `{success:true, result:'recorded', signal:{uuid, slug, prefers, over}}`.
  - **Both spines, d5 verbatim:** alpha's entry `type:'preferred'`, summary exactly `chose this over "rev7 beta goal" — rev7 reason: closer to the demo`; beta's entry `type:'passed over'`, summary exactly `"rev7 alpha goal" chosen over this — rev7 reason: closer to the demo`; same dated judged-on both sides (one fact, two views).
  - **Stored element** (read raw via `/api/neo4j/query`): `judgedBy:'owner'`, dated `judgedOn` (ISO day), `framing:'solve-one-today'` — AC2's all-three-present confirmed in the store, not just the projection.
  - **Reversal without a reason** → both new entries with **no em-dash fold** (`chose this over "rev7 alpha goal"` / `"rev7 beta goal" chosen over this`); the **first element's stored json byte-identical** after (append-only confirmed live; nothing re-signed); alpha's original `preferred` entry still present.
  - **Refusals:** self-pair → `{success:false, refusal:'same-goal'}`; unknown slug → `refusal:'goal-not-found'` — each with a spine snapshot identical before/after (nothing written).
  - **AC4:** `GET /api/brain/proposals` **byte-identical** before/after all signal writes — nothing proposed, decided, or launched.
  - **Hygiene** `sound:true, problems:[]` throughout and after teardown.
- [x] **Host-side caller-class gates** (remote class, plain `/usr/bin/curl http://localhost:7778`): `POST /api/normalize/record-priority-signal` → **401** (default-deny middleware); `GET /api/brain/signals` → **404** (no route exists — ADR d10).
- [x] **Post-run residue** — brain clean: **3 legacy goals only** (revolutionize-physics / nosfabrica-success / tapestry-agentic-second-brain), **hygiene `sound:true, problems:[]`**, **strfry count 0** for every rev7 d-tag, **0 `rev7` json residue** (Neo4j by-uuid delete + json-CONTAINS orphan sweep, verified). The **`tapestry-priority-signal` concept persists** (runtime-created; graph now 54 concepts vs 53 at recon) — expected and correct: only fixture *elements* are torn down; the concept is idempotent.
- [x] _Lint / typecheck not configured — skipped. Playwright not applicable — **zero UI diff** (ADR d14): `git diff b0314669..HEAD -- ui/` is **EMPTY** (verified with `git diff --quiet`), so there is no new interactive surface and no vite build needed; the spine rendering is the story-5/6-covered `RecordEntry`._

## Spec adherence (all six ACs — live-proven)

- [x] **AC1 — Pairwise choice, optional reason; unknown/ambiguous refused; self-choice refused; nothing written on refusal.** `recordPrioritySignal` (`src/api/normalize/index.js:4762`) refuses `same-goal` before any read, resolves both slots via `resolveGoalConcept` + `fetchGoalRecords` + `resolveDecomposition` (the `makeProposal` idiom) with per-slot `goal-not-found`/`ambiguous-slug` refusals naming the offending slug, then mints. Reason optional: handler treats a blank reason as absent (`:4750`), 400s a non-string one (`:4746`). Proven live (my round-trip: recorded with reason, recorded without, both refusals wrote nothing) + H1/H3/S1/U2.
- [x] **AC2 — Every signal dated, attributed, framing-tagged.** Schema-level: `required: ['name','slug','description','prefers','over','judgedBy','judgedOn','framing']` (`:4650`) — reason the one optional field. Core stamps `judgedBy:'owner'`, `judgedOn` = today, `framing: SIGNAL_FRAMING_V1` (`:4786-4791`). Proven in the **store** (my raw Neo4j read + H1), not just the projection. S5/S6/U2 pin the source.
- [x] **AC3 — Append-only; visible on BOTH spines, side-worded; no edit/delete affordance.** One stored fact fans out at READ: `groupSignalsByGoal` returns the **same record object** under both its `prefers` and `over` keys (`src/lib/brain/signals.js:70-81`; U3 asserts identity), and `handleGetGoalDetail` words each side from that goal's perspective (`src/api/brain/index.js:296-308`). The sides cannot diverge by construction. Append-only proven live: the reversal coexists, every prior spine line survives, the first element's stored json byte-unchanged (my round-trip + H2); the mint path is nonce/random-d-tag only, **no `regenerateJson`** (S4). No edit/delete affordance can exist — zero UI diff; `RecordEntry`/`GoalDetail.jsx` byte-unchanged.
- [x] **AC4 — Signals never launch or decide anything; proposer unchanged.** `makeProposal`/`decideProposal` contain no signal token (S10 + my read of the diff — the only change near them is one blank line, `:3296`); the signal core contains no `fetch`/`spawn`/`enqueue`/`scheduler`/`launch`/`child_process` (S10 + grep). Proven live: the proposals queue byte-identical before/after signal writes, including a repeat-pair (H4 + my round-trip). The "may cite in plain words" affordance is the ADR d11 conversational-contract addendum — doc-only, correctly absent from code.
- [x] **AC5 — Replaced framing leaves history interpretable.** The framing is carried **by** each element (stored, required) and read back **from** it: `parseSignalRow` extracts the per-row `framing` (`signals.js:57`); no read path consults a global (the brain module has no framing constant at all — only the write path stamps `SIGNAL_FRAMING_V1`). U2 proves distinct tags (`solve-one-today` / `ship-by-friday`) coexist per-row; my round-trip read the stored tag back.
- [x] **AC6 — Copy discipline, no numerics, runtime concept, TA runtime-resolved, no regression.** S11's banned-jargon + no-exclamation + no-score scan passes over the templates' home (`signals.js` string literals — templates at `:96-98,101-102` carry no jargon, no numerals, no `!`; the em-dash fold is a true U+2014, byte-verified). No signal counts surface anywhere (no UI diff; the projection carries no tallies). Concept new/append-only/runtime-created (`ensureSignalConcept`, `:4691`)/never-firmware-seeded (`firmware/` byte-unchanged)/TA-runtime-resolved (no 64-hex anywhere in the diff — S14 + my grep). Stories 1–6 suites green standalone AND in-gate (above); the only legitimate extension is the spine now carrying signal entries (H5 merge proof: `worked` + `preferred` + `passed over` newest-first).

## ADR adherence (Option A; d1–d15)

- [x] **d1** — `tapestry priority signal` → slug `tapestry-priority-signal` → handle `39998:<TA>:tapestry-priority-signal` (TA runtime-resolved); single `prioritySignal` wrapper; the exact `required` list; `x-tapestry.unique:['slug']`; **no `type` enum** (the spine type words are projection output, not stored). `SIGNAL_SCHEMA` (`normalize/index.js:4644-4668`) matches field-for-field; plain-language descriptions. Element `name` = `preferred: <prefers> over <over>` (`:4714`, via `mintSignalElement`).
- [x] **d2** — born final: no lifecycle, no dedup, no natural key. Repeat pairs and reversals each mint fresh (proven live); `mintSignalElement` (`:4711-4732`) is a structural copy of `mintProposalElement` (`randomDTag()` nonce + `dtag.childDTag` + derived unique slug + `publishToStrfry`+`importEventDirect`+`SET :ListItem`+`MERGE superset→elem`); **no `regenerateJson`/re-sign anywhere on the signal path** (S4 + grep).
- [x] **d3** — record-based linkage: `prefers`/`over` are goal slugs in the record; **no relationship-whitelist edge** (`relationships.js` byte-unchanged, R2). The two-goal fan-out happens at READ (`groupSignalsByGoal` two-key; U3 identity assertion) — one stored fact, two derived views.
- [x] **d4 (operator-ratified)** — no standing requirement: resolve + distinct only. No `deriveStanding`/viability token in the signal core (S2; the core imports only `resolveDecomposition`, `:4767` — contrast `makeProposal`'s `:3156` which also takes `deriveStanding`). Proven live between two captured goals (H1 + my round-trip).
- [x] **d5 (operator-ratified wording — verbatim-or-block)** — checked by strict equality, source and live: type words **`preferred`** / **`passed over`** (`signals.js:101`); summaries `chose this over "{other}"` / `"{other}" chosen over this` with the ` — {reason}` em-dash fold only when a reason exists (`:96-98,102`); `{other}` is the other goal's current display name, straight-quoted, slug fallback when gone (`:95`, U4). U4 + H1/H2 + my round-trip all assert the strings by equality — **no deviation found**. The capture confirmation `Noted — "{prefers}" over "{over}".` and the elicitation prompt are d11 conversational-contract items (doc-only this story) — correctly absent from code.
- [x] **d6** — `POST /api/normalize/record-priority-signal` registered beside the story-5/6 producers (`:4828`); gate-first (`isOwner(req) || req.localTrusted → 403`, `:4736-4739` — the sibling handlers' exact pattern); 400s for missing/non-string/empty `prefers`/`over` and non-string `reason`; blank reason = absent; one `serializeGoalWrite` task; refusals loud, named, HTTP-200 `{success:false, refusal, error}`; result `{success:true, result:'recorded', signal:{uuid, slug, prefers, over}}` (uuid present — the test-plan result-shape note; asserted by H1 and my round-trip, and teardown depended on it).
- [x] **d7** — `judgedBy: 'owner'` v1 constant, instance-relative (`:4789`). Proven in the store.
- [x] **d8 (operator-ratified)** — `SIGNAL_FRAMING_V1 = 'solve-one-today'` single named constant (`:4637`), server-stamped (`:4791`); **no caller-supplied framing** — the handler never reads a `framing` from the body (S5's three-pattern scan + my read). The §7.6 swap hatch is deliberately not built.
- [x] **d9** — `ensureSignalConcept()` (`:4691-4706`) byte-parallel to `ensureProposalConcept`: resolve → return if present (no per-write churn); else `create-concept` (the ADR's exact description sentence) + `save-schema` via `invokeNormalizeHandler` → re-resolve, loud throw on failure. Runs inside the producer's mutex + gate. Never firmware-seeded; confirmed live (concept present on the graph, provisioned at the impl H run; 53→54 concepts).
- [x] **d10** — new pure `signals.js` (**zero requires** — U1 asserts it; parser whitelists and never throws); `src/api/brain/index.js` gains **exactly one require — the ninth** (`:31`), `SIGNAL_CONCEPT_SLUG` (`:57`), `readSignals` (`:158-170` — byte-mirrors `readProposals`' explicit∪implicit union, absence-tolerant), and the third entry array in `handleGetGoalDetail` (`:296-308` — side computed from `r.prefers === winner.slug`, other-name from the already-loaded `resolved` set, spread into the **unchanged** merge-and-sort at `:309-316`). Brain stays read-only (S13 — no mutation/strfry tokens). **No new route:** `registerBrainRoutes` untouched in the diff; `/api/brain/signals` 404s live (S9/H6 + my check).
- [x] **d11** — conversational contract addendum is doc-only; no code obligation — satisfied.
- [x] **d12** — copy discipline (AC6 above); the d5 set is the complete new-string surface; UI untouched.
- [x] **d13** — constants where specified: `SIGNAL_CONCEPT_NAME`/`SIGNAL_FRAMING_V1` in normalize (`:4636-4637`), `SIGNAL_CONCEPT_SLUG` in brain (`:57`). No arity/length bounds (per ADR).
- [x] **d14 — untouchables byte-unchanged** (git-verified, each individually): `ui/**` diff **EMPTY** (including the newly byte-pinned `GoalDetail.jsx`, plus `App.jsx`/`Layout.jsx`/`styles.css`), `relationships.js`, `probe.js`, `middleware/auth.js` (incl. `PUBLIC_MUTATIONS` — R2), `firmware/`, the four ADR-0015 `LEGACY_*` files (`src/api/profile-tags/index.js`, `publishTagPin.js`, `useProfileTags.js`, `publishProfileTag.js`), `nostrPublish.js`. `serializeGoalWrite` not renamed (`:2129`); `makeProposal`/`decideProposal` unchanged; no whitelist edge; `HYGIENE_CONCEPTS` unchanged (0 hits in the diff); no new `:root` tokens (no styles touched).
- [x] **d15** — firmware reinstall correctly **NOT** required: runtime-created concept, installer untouched. Confirmed live (concept present with `firmware/` byte-unchanged).
- [x] **Adjudicated deviation — `fetchSignals(headerUuid)` NOT implemented** (the ADR's *Implementation notes* list it as a sibling mirror of `fetchProposals`; `grep -rn fetchSignals src/ test/` → absent). **Ruled defensible, non-blocking:** the helper appears only in the notes, not in any binding decision d1–d15; its sibling exists because `makeProposal` must read existing proposals for the `already-open` refusal, and d2 explicitly rules out any lifecycle/dedup for signals — so the write path never reads existing signals, and the read path is `readSignals` in `brain/index.js` (d10, implemented). A normalize-side `fetchSignals` would have **zero call sites** — dead code this review would itself have to flag. The minimal-diff call honors every binding decision; recorded here so the ADR-notes/code gap is documented, not silent.

## Concept-graph integrity
- [x] Handle is `39998:<TA>:tapestry-priority-signal` (kind:pubkey:slug), TA runtime-resolved: `getOwnerAssistantPubkey()` feeds `readSignals(taPubkey)` server-side; the concept is addressed by **name** in normalize (`resolveSignalConcept`, `:4671`) — **no hardcoded 64-hex anywhere in the diff** (grep over all added lines: none; S14).
- [x] **Firmware reinstall correctly NOT required** — runtime-created / self-bootstrapped; `firmware/` and the installer byte-unchanged. ADR d15 holds; verified live (node resolvable at the runtime handle, name `tapestry priority signal`, graph 53→54).
- [x] New code orients via the runtime graph (`/api/concept-graph/node`, `/summaries` at recon; runtime resolution at read/write), not by re-deriving from BIBLE.md.

## Things tests can't catch
- [x] **No secrets / no hardcoded pubkey** — clean.
- [x] **No stray debug** — the only console call added is the catch-path `console.error('normalize/record-priority-signal error:', ...)` (`:4756`), matching the sibling handlers. No `console.log`, no commented-out code. (One cosmetic stray blank line — non-blocking #1.)
- [x] **Error paths gate-and-validate before any write** — gate → shape 400s → mutex → `same-goal` → resolve/refuse → only then `ensureSignalConcept` + mint. A refusal writes nothing (proven live twice — suite H3 + my round-trip).
- [x] **Concurrency** — the write runs inside the process-singleton `serializeGoalWrite`; with no dedup/lifecycle there is no read-validate-write race to guard; concurrent first-signals serialize through the mutex so `ensureSignalConcept` cannot double-provision (and `create-concept`'s "already exists" tolerance + re-resolve make even that path convergent).
- [x] **Privacy §7.4** — no `publishEverywhere`/`nostrPublish`/outbound `fetch` anywhere in the diff (grep: none); writes ride `publishToStrfry` (local) + `importEventDirect` only. No egress.
- [x] **Read-tolerance** — dangling `prefers`/`over` slugs render with slug fallback (U4), malformed rows drop at parse (U2/U3) — no crash path from bad data.
- [x] **Residue** — both my round-trip and the suites left zero fixture residue; brain at 3 legacy goals; hygiene sound (above).

## House rules check
- [x] Concept Graph API authority respected (recon three-call orientation per the ADR; runtime graph reads).
- [x] No new lint/typecheck/build tooling; no new deps (`package.json` untouched); JS-without-build preserved.
- [x] test.js registration is the standard five-touch (require `test/test.js:188` region, run call, summary line, `overallOk` term added **before** the severed terminator with the OPEN.md #43 dead block left intact — verified in the diff, and the terminator flip `createTapestryResult.fail === 0;` → ` &&` matches the ADR's exact instruction); `totalSkipped` array extended.
- [x] Six sibling re-pins are widen-only (each adds exactly `/lib\/brain\/signals$/` to the identical allowlist) — all six green standalone and in-gate.

## Product-guide adherence
- [x] **Copy is the d5 operator-ratified set, verbatim** (the guides' documented signal gap makes d5 the canonical source until book-close back-fill): type words `preferred` / `passed over`; summaries + em-dash reason fold exactly as ratified; no banned jargon, no exclamation marks, no numerals in any owner-facing string this story adds; relative value rendered in words only — no score/count/tally anywhere (design principle 2, "comparisons, not decimals").
- [x] **Design-guide patterns:** nothing to add — zero UI diff is itself the designed outcome (the three-view inventory is binding; capture rides the conversational lane; the spine rendering is the existing `RecordEntry`, which renders the two-word type as text content unchanged).

## Findings

### Blocking
_None._

### Non-blocking
1. **`src/api/normalize/index.js:3296`** — the impl commit adds one stray blank line after `decideProposal`'s closing brace (whitespace-only; no behavior). Optional: drop it in a future touch of the file.
2. **`engineering-team/epics/second-brain.md:25`** — story 7's epic marker still reads *(Queued)*; after this PASS it is stale (the `a79b3bdd` reconcile in this very diff fixed the same staleness for stories 3–5). My launch instruction pins this review commit to exactly the review + story files, so I leave it for the main session to reconcile (naturally at the next epic-file touch or close-out).
3. **ADR 0007 Implementation notes** list `fetchSignals(headerUuid)`, which was deliberately not built (adjudicated defensible above — no consumer exists in the decided design). Noted so the notes/code gap is on the record; no ADR amendment required since d1–d15 are untouched.
4. **OPEN.md row 94** (filed this story) correctly names `test/teach-it-what-matters.test.js` as sharing the state-bound pre-clean pattern whose stranded-node cascade bit the-proposal-loop during this story's gates — the proposed state-free pre-clean (deterministic goal d-tags + name-based Neo4j sweep) applies to this suite too when triaged. Not a story defect; the hazard did not recur on any of my runs.

### Harness friction
_None blocking._ The story/ADR/test-plan were internally consistent; the pre-impl 10/17 baseline, the six sibling re-pin counts, and the result-shape `uuid` note all landed exactly as documented. (The full-gate duration matched the ADR's ≈24-min budget; the shorter ~10-min estimate circulating in session notes is optimistic when the Meili-dependent suites run their timeouts — not worth a ledger row since the ADR itself was accurate.)

## Verdict
**PASS**

## On PASS (same commit)
- [x] Story `**Status:**` flipped to `Done` in place (`engineering-team/stories/second-brain/7-teach-it-what-matters.md`), Review line filled — this commit.
- [x] Completion detection run: the second-brain book anchor lists 8 stories; stories 1–7 are now Done but **story 8 (`the-brain-survives` — export/restore) is still Queued**, and the story itself names story 8 as the book's close pair (export must carry signals). **The book is not complete — no `/close-book` offer.** The d5 strings (type words, summaries, confirmation) join the ADR 0006 d16 approve string in the style/design-guide back-fill bundle at book-close (return edge).
