# Review: Story 3 — Author the Stamping NIP

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-07-12
**Mode:** docs-mode (protocol-spec workflow §3 — accuracy/consistency audit; Test Design skipped by design)
**Diff:** `git diff b7de6867..d6df79d0` (commit `d6df79d0`, branch `docs/nip-reorg-s3-stamping`, base `origin/staging` @ `f6f78eca`)
**Files touched:** `protocols/drafts/stamping.md` (new, 55 lines), `protocols/drafts/tapestry-concepts.md` (1 hunk, −13/+1), `protocols/drafts/shared-concepts.md` (1 line), `protocols/worksheet.md` (pure append, +8), `protocols/README.md` (1 row), `engineering-team/epics/nip-reorg.md` (1 line — see Findings: ADR-unlisted, accepted). No non-`.md` file in the diff.

## Quality gates (run by reviewer, not trusted)

- [x] `bash scripts/harness-lint.sh` at `d6df79d0` — **clean (0 violations)**; only pre-existing waivers/INFO rows.
- [x] `npm test` at `d6df79d0` — fails overall with **exactly the caveated environmental set**: the same 11 stack-dependent suites recorded in the S1 review (`profile-tags` 10p/3f, `profile-tags-publish` 6p/1f — the known 5/2↔6/1 wobble at 6/1, `tag-detail-publish` 7p/2f, `tag-index-publish` 8p/1f, `profile-tag-polish` 7p/4f, `pin-a-tag-publish` 1p/6f, `tl-publication-from-pins` 9p/1f, `tl-publication-from-pins-publish` 2p/5f, `customize-pin-curation-publish` 0p/3f, `most-pinned-tag-index-publish` 0p/7f, `tag-detail-curated-view-and-pin-polish-publish` 0p/1f), 25 skips. **No new failing suite**; every failure is Meili/Neo4j/stack-bound and unreachable by a `.md`-only diff (no source or test file in the diff). Binding gate remains CI's stack-free job.
- [x] _Playwright — not applicable (docs only)._
- [x] _Lint/typecheck/build not configured — skipped._ No tooling added (no `package.json` change).

## Spec adherence (AC-by-AC, docs-mode)

- [x] **AC1 — spec exists and is indexed.** `protocols/drafts/stamping.md:1-5`: Status 📝 pre-NIP (`:2`); design-only implementation note (`:4` — dual-`z` partially implemented for tag events per `tag-federation` ADR 0003, pins still single-`z`, cloud stamping unimplemented and gated on the Shared Concepts resolver); Sources (`:5`) name all four required items: `community-reference` ADR 0033, tapestry-concepts § "Multi-`z` stamping" as extraction origin, `docs/B_TAG_AFFILIATION_DESIGN_HANDOFF.md` D1 rev 2, `docs/NIP_REORG_DESIGN_HANDOFF.md`. Index row: `protocols/README.md:59`, format matching ADR note 5 and neighboring rows (S1 gate precedent).
- [x] **AC2 — ratified write rule, D2 vocabulary.** Personal `z` required ≥1, may target a private header (`stamping.md:20`); local-first premise (`:14`); up to a cap of shared/cloud handles, affiliation-anchored — declared community via the author's own pointer-`b`, never a concept-global top-k (`:21`); `z` order not load-bearing + consumers MUST NOT depend (`:23`); lazy author re-emit at the same `d`-address, kind 39999 (`:29`); the three named lossiness cases — foreign-authored, inactive-author, kind-9999 with the stated 39999 preference (`:31`); containment-only boundary + membership-assertion single-handle rule (`:33-35`). Framing consumes `b` via [Shared Concepts] (`:12,21,43` — Inherit-From is one hop away by deliberate layering: Shared Concepts owns the aggregate reading Stamping consumes) and `s`/`n` via Class Thread Relationships **by its new name** (`:12,49`).
- [x] **AC3 — read contract, same page.** `stamping.md:37-43`: MAY assume ≥1 `z` + self-containment (`:41`); MUST NOT rely on `z` order, cloud-stamp currency, ancestor stamps pending the Open section (`:42`); query strategy stated (`:43` — observer-resolved cloud, `#z` union, discovery-walk correspondents), matching ADR note 1.
- [x] **AC4 — O1 opened properly.** `stamping.md:45-55`: non-normative disclaimer (`:47`); Widgets / Widgets-for-Carpenters / Widgets-for-Electricians example (`:49`); the three shapes with tradeoffs — (a) read-time expansion pro/con, (b) write-time ancestor stamping pro/cons incl. lazy-heal-only and ≈2-slots-per-chain-level cap pressure, (c) hybrids (`:51-53`) — **neutral, no leaning** (handoff O1's "(a) = ADR 0033 status quo" label deliberately not carried as a lean); co-stated-read-contract rule (`:55`). Worksheet **W14** exists (`protocols/worksheet.md:134-141`), Status Open · raised 2026-07-12, question + three shapes + binding constraint, refs to stamping § Open / class-thread-relationships / handoff O1 / this ADR, named W11's successor. Cross-linked both ways; the anchor `#w14--subsetancestor-stamping-z-expansion-across-class-thread-structure` (`stamping.md:47`) matches GitHub's derivation of the heading at `worksheet.md:134` (em-dash → double hyphen, `/` and parens dropped — same derivation as the verified W11 precedent `#w11--cloud-formation--multi-z-stamping-rules`, heading at `worksheet.md:97`). **W11's existing text untouched** — the worksheet hunk is a pure append (`@@ -130,3 +130,11 @@`, 0 deletions).
- [ ] **AC5 — extraction seam complete: one residual leak.** The pointer side is done: tapestry-concepts § keeps its bold lead-in and is a two-way pointer (`tapestry-concepts.md:53` — write rule/re-stamping/read contract/open question → Stamping; cloud model → Shared Concepts § Clouds; provenance line per ADR note 2), with no stamp mechanics or cloud prose surviving there; `shared-concepts.md:29` now points at the landed spec (parenthetical dropped, per ADR note 3). Duplication greps per ADR §6: "derived top-k" only in `shared-concepts.md:60`; "lazy author re-emit" only in `stamping.md:29`; the containment-only sentence only in `stamping.md:35` (all three also appear in `worksheet.md:103` — W11's graduated historical record, untouched by this diff; re-aim is S4). **But** the reverse direction fails at one clause: `stamping.md:29` restates the rotation cloud property whose sole normative home is `shared-concepts.md:64` — see Blocking finding 1. Until that clause is fixed, "cloud properties only in shared-concepts.md" is not fully true.
- [x] **AC6 — gates and scope guard.** Vocabulary grep of `stamping.md` for `canonical|consensus`: exactly one hit — the `**Canonical:**` metadata field name (`:3`, inside the block marked not-spec-text); zero in normative text. Observer-relative framing kept for all aggregate references (`:21` "the cloud of the community the author declared affiliation with"; `:42` "clouds are an observer's view"; no "the community's cloud" phrasing — grep clean). All relative link targets exist on disk (`nips/decentralized-lists.md`, `drafts/tapestry-concepts.md`, `drafts/shared-concepts.md`, `drafts/class-thread-relationships.md`, `drafts/communities.md`, `drafts/tags.md`, `worksheet.md`); prose §-cites name real headings (Decentralized Lists § Item declaration = `decentralized-lists.md:39`; Shared Concepts § Terminology/Declared affiliation/Aggregated deference/Clouds = `shared-concepts.md:14,31,49,58`; `IS_A_SUPERSET_OF` is the real derived-edge name, `class-thread-relationships.md:11,24,50`). harness-lint clean; `npm test` regression-free (above). `protocols/` changes are exactly the five sanctioned files; the sixth diff file is outside `protocols/` (epic tracker — Findings). No historical records touched (nothing under `done/`, no prior stories/ADRs/reviews edited).

## ADR adherence

- [x] **Section outline (ADR note 1) followed:** header `:1-5`; intro with the no-new-wire-format statement and the three consumed layers `:12-14`; The write rule `:16-25` incl. the design-only italic callout `:25` (cap ~5 / formula / cold-start deferred; gated on resolver + on-wire inherit-`b` — exactly the ADR's specified gating); Re-stamping `:27-31`; Boundary `:33-35`; The read contract `:37-43`; Open section `:45-55`.
- [x] **Sanctioned deviation (i) verified:** the BIBLE §10 citation is dropped from the extracted personal-`z` sentence; `stamping.md:20` cites Decentralized Lists § Item declaration instead (heading exists), with the a-tag constraint attributed to Tapestry Concepts in the intro (`:12`). No BIBLE cite anywhere in the spec body — the spec/implementation-doc boundary is now the right way round.
- [x] **Sanctioned deviation (ii) verified:** the containment-only boundary lives in Stamping solely (`:33-35`), links Communities + Tags for the membership-assertion side; absent from shared-concepts (never carried it — confirmed by grep) and removed from tapestry-concepts.
- [x] tapestry-concepts edit matches ADR note 2; shared-concepts edit matches note 3; W14 entry matches note 4 (placement after W13, status line, refs, successor note); README row matches note 5.
- [x] No unauthorized dependencies/tooling (docs only).
- **Deviations flagged (assessed in Findings):** the epic-file edit (6th file, ADR §6 said "exactly 5") — accepted; the `stamping.md:29` recompute clause and `:42` fourth MUST-NOT bullet — one blocking, one accepted; see below.

## Extraction fidelity (vs `git show f6f78eca:protocols/drafts/tapestry-concepts.md` lines 53–64)

- [x] **Intro paragraph (orig :53):** multi-`z` permission + base-NIP one-`z` recommendation + community-visibility motivation + local-first premise all survive, split correctly between the tapestry-concepts lead-in (`:53`, permission + adoption only) and stamping's intro (`:14`, the full premise). Meaning intact.
- [x] **Design callout (orig :55):** cap/formula/cold-start deferral survives verbatim in substance (`stamping.md:25`, "(~5)" added per ADR and ADR 0033/D1-rev-2's "≤5"). D2 transformation "W1 grapevine-resolved consensus signal" → "observer-resolved deference signal" is meaning-preserving (shared-concepts § Aggregated deference is the named home; its selector bullet keeps "grapevine-resolved" as the reference-deployment gloss). The original's "and the three-branch reconciliation" gate does not survive — sanctioned: ADR note 1 specifies the gating as "resolver + on-wire inherit-`b`", matching S1's already-merged precedent (`shared-concepts.md:4,68`); the full original gating remains on record in W11 (`worksheet.md:103`).
- [x] **Cloud bullets (orig :57-58):** correctly *deleted*, not moved — their normative home has been `shared-concepts.md:58-68` since S1; this diff resolves ADR 0001's transient duplication #1 on the tapestry-concepts side.
- [x] **Affiliation-anchored (orig :59):** fully preserved at `stamping.md:21` — declared community, own pointer-`b`, never global top-k, "not silently routed" rationale intact; "cluster" → "cloud" aligns with the D2 term.
- [x] **The stamp (orig :60):** preserved at `:20-23` — required ≥1, may-be-private, cap of cloud handles, order-not-load-bearing + MUST NOT depend; "highest-consensus-first" → "highest-deference-rank-first" (D2, matches shared-concepts' "membership is deference rank"). BIBLE §10 cite dropped = sanctioned deviation (i).
- [x] **Re-stamping (orig :61):** trigger, same-`d` re-emit, kind 39999, and all three lossiness cases preserved at `:29-31`. One unsanctioned addition in the same paragraph — Blocking finding 1.
- [x] **Containment only (orig :62):** preserved at `:35` with a meaning-preserving clarifier ("never cloud-expanded" — an elaboration of "the two do not overlap" + the single-handle rule, not new policy).
- [x] **Provenance (orig :64):** carried into the tapestry-concepts pointer (`:53`) and the spec header (`:5`).
- [x] Nothing ratified was dropped or weakened beyond the two sanctioned deviations and the ADR-specified gating rewording; additions are itemized in Findings.

## Concept-graph integrity / house rules

- [x] Docs-mode: no concept definitions changed; **no firmware reinstall required** (ADR Consequences, confirmed — no `firmware/` or `src/` file in diff).
- [x] No hardcoded deployment pubkeys; handle shapes in prose are placeholder forms only.
- [x] No new lint/typecheck/build tooling; Concept Graph API authority untouched.
- [x] No secrets, no debug text.

## Findings

### Blocking

1. **`protocols/drafts/stamping.md:29`** — the second sentence, *"Nobody detects rotation on the author's behalf; the author (at write time) and any consumer (at read time) each simply recompute."*, restates — near-verbatim in its tail clause — the **rotation cloud property** whose sole normative home is `shared-concepts.md:64` ("…there is nothing to 'detect' — an author (at write time) and a consumer (at read time) each simply recompute"). In the original block this sentence belonged to the *Rotation is emergent* cloud bullet (orig :58), which S1 moved to Shared Concepts; re-importing it here re-creates precisely the two-homes condition AC5 says this story eliminates ("after this story … cloud properties only in shared-concepts.md"), is unsanctioned by ADR note 1's Re-stamping spec, and contradicts the spec's own discipline one section up (`:21` "…specified in Shared Concepts § Clouds and not restated here"). The first clause is legitimate stamping policy (re-stamping has no trigger service — that is what "lazy" means); only the restated mechanics must go. **Asked change (one line):** keep the stamping-side fact and link out instead of restating, e.g. *"Nobody detects rotation on the author's behalf — rotation is emergent and observer-recomputed ([Shared Concepts](./shared-concepts.md) § Clouds)."*, or end the sentence after "on the author's behalf" with the § Clouds cross-reference.

### Non-blocking

1. **`engineering-team/epics/nip-reorg.md:16`** — sixth diff file; ADR §6 said "diff = exactly 5 files". **Assessed: ACCEPTED.** The edit is the epic tracker's S3 row flipping from `_(planned)_` to the real story link — pure bookkeeping, accurate, outside `protocols/` (so AC6's letter holds), and it mirrors S2's precedent where the same class of edit *was* ADR-listed. Recorded as an ADR-unlisted deviation; S4's ADR should list epic-file bookkeeping explicitly so this stops being a per-review judgment call.
2. **`protocols/drafts/stamping.md:42`** — the fourth MUST-NOT bullet ("any single shared handle's `#z` index being complete…") exceeds the ADR's enumerated read-contract list. Accepted: it is accurate, strictly entailed by affiliation-anchoring plus the observer-relative rule it cites, and it is the premise the ADR's own query-strategy bullet stands on. Authoring latitude within a section the ADR commissioned fresh.
3. **`protocols/drafts/stamping.md:20`** — added rationale clause "the stamp is still required, because the item — not the header — is what must be self-contained": a meaning-preserving restatement of the local-first premise already in the intro (`:14`); introduces no new constraint. Accepted.
4. **`protocols/drafts/stamping.md:25`** — the original callout's "three-branch reconciliation" gate mention does not survive anywhere in the new layering (only in W11's historical record, `worksheet.md:103`). ADR-sanctioned here and consistent with S1's shared-concepts precedent; noted for the record so the drop is traceable to ADR 0003 note 1 rather than silent.
5. **`protocols/worksheet.md:103`** — the ADR §6 duplication greps also hit W11's graduated resolution block ("derived top-k", "lazy author re-emit", the containment sentence). Pre-existing, historical, untouched by this diff; W11's semantic re-aim is S4 per the story's out-of-scope. No action this story.

### Harness friction

1. None new. (The 11-suite environmental failure of the stale local bind-mounted stack is known state; the binding gate is CI's stack-free job. The ADR §6 "diff = exactly 5 files" vs. the established epic-tracker bookkeeping edit is the one contract gap — routed to S4's ADR per Non-blocking 1 rather than OPEN.md, since S4 is already the epic's sweep story.)

## Verdict

**CHANGES_REQUESTED**

One blocking issue, one line: `stamping.md:29`'s restated rotation-recompute clause leaves a cloud property normative in two places, which is the exact condition AC5 obligates this story to end. Everything else verifies: all six ACs otherwise hold with evidence; both sanctioned deviations are in place and correctly bounded; extraction fidelity is meaning-intact under the D2 vocabulary; the O1 section is neutral and W14 is properly opened and anchored; the vocabulary gate is clean; harness-lint is clean; and the test gate shows the known 11-suite environmental set with no new failures. Fix the one clause and this is a fast re-review.

Story status remains `Approved` (not flipped — flip to `Done` happens only on a passing re-review, in that review's commit). Note for the committing agent: harness-lint L1 couples story status only to PASS-final reviews, so committing this report requires no story edit.
