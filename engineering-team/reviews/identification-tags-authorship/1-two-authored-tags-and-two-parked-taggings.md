# Review: Story 1 — Two authored tags and two parked taggings

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-09-22
**Diff:** `git diff 47fc2cd0..7d8581da` on `feat/identification-tags-authorship` — `767981fd` (the Tester's failing tests: the fixtures, three suites, two specs, the plan), `c61c3794` (a Tester-lane correction: S5 reads the BIBLE body), `1ea0e9cc` (the implementation: the library, the check, the route, the copy module, the page, the styles, BIBLE §11/§14, OpenAPI), `7d8581da` (the plan's after-implementation record). 16 files, 780 insertions, 374 deletions. The shared line was re-checked before this review (`git fetch`; HEAD 0 behind `origin/staging` at `7381c459`; `git merge-tree --write-tree HEAD origin/staging` clean).
**Story:** `engineering-team/stories/identification-tags-authorship/1-two-authored-tags-and-two-parked-taggings.md` (Approved 2026-09-22; the owner's four gate decisions in § Open questions)
**ADR:** `engineering-team/decisions/identification-tags-authorship/0001-each-definition-has-its-author-and-two-taggings-are-parked.md` (Accepted 2026-09-22)
**Test plan:** `engineering-team/stories/identification-tags-authorship/1-two-authored-tags-and-two-parked-taggings.test-plan.md` (its "After implementation" section was re-run here in full, not trusted)
**Book:** `engineering-team/audits/identification-tags-authorship/book.md` (acceptance frame, no PRD); epic `engineering-team/epics/identification-tags-authorship.md`.

## Quality gates (run by reviewer, not trusted)

Node 22.23.2 first on the PATH for every run (`node --version` printed `v22.23.2` at the head of each log); the local stack up (`docker exec tapestry supervisorctl status`: `brainstorm RUNNING`, `strfry RUNNING`); the working tree clean at `7d8581da` throughout.

- [x] **The book's gate** (the plan § How to run's recipe verbatim, `GATE_LABEL=identification-tags-authorship-1-review`; 53 suites matched the pattern after the registry's `excluded` were dropped), read with `npm run gate:status -- --label identification-tags-authorship-1-review` (its exit status 0):

  > `20260922T221011Z-96836-9de7 [identification-tags-authorship-1-review] started 2026-09-22T22:10:11.007Z on 7d8581da — PASS, exit 0, 794 passed, 0 failed, 114 skipped, 53/53 suites`

  The record's own fields: `state: finished`, `node: v22.23.2`, `git: { commit: 7d8581da…, branch: feat/identification-tags-authorship, dirty: false }`, `totals: { passed: 794, failed: 0, skipped: 114, suitesPassed: 40, suitesFailed: 0, suitesSkipped: 13 }`.
  **Against the baseline** (the previous book's close gate `20260922T130828Z-49331-2d67` on `a2923f9b+dirty`, 226 suites), compared suite by suite on the record fields `file, verdict, pass, fail, skipped` over my 53: **50 unchanged; the only three that moved are this story's, each by its new cases** — `assistant-attention` 37/0/0 → 39/0/0, `assistant-identification-tags-page` 15/0/0 → 16/0/0, `assistant-taggings-publish` 19/0/0 → 20/0/0; none of my 53 is missing from the baseline; none of this host's known red live suites is in the pattern set. **Against the Tester's after-run** `20260922T220123Z-58022-a4f8` (on `1ea0e9cc`): 53/53 identical.
- [x] **The eight suites alone**, each through its `run()` export (`node -e "require('./test/<suite>.test.js').run().then((r) => process.exit(r.fail ? 1 : 0))"`), every exit 0: `assistant-attention` 39 passed / 0 failed / 0 skipped (H1 executed live), `assistant-identification-tags-page` 16/0/0, `assistant-taggings-publish` 20/0/0 (H1 executed live), `dual-z-writer` 14/0, `assistant-management-page` 24/0/0, `assistant-alert` 15/0/0, `default-deny-mutations` 14/0/0, `assistant-publish-relays` 39/0/0. Same numbers as the plan's record.
- [x] **`npm run test:playwright` equivalent** (a UI change): `BRAINSTORM_BASE_URL=http://localhost:7778 BRAINSTORM_SERVER_ACCESSIBLE=true npx playwright test <the six spec files> --project=chromium --reporter=line`, exit 0: **64 passed, 1 skipped (3.1 min)** — `assistant-identification-tags-page` 14/14 (B0–B13), `assistant-taggings-publish` 7/7 (B0–B6), `assistant-attention` 7/7, `assistant-alert` 10/10, `assistant-management-page` 22/22 + 1 skipped (the hub's identification-tags placeholder case, skipped by design since the previous book), `assistant-publish-result` 4/4. No failed, flaky or retried case in the ANSI-stripped log. The three hub-side specs take their canned answers from the rewritten fixtures (`T.DONE`, `T.PENDING`; `assistant-attention.spec.js:57,154,161,187,204`, `assistant-alert.spec.js:3`, `assistant-management-page.spec.js:3`), so their unchanged sources ran against the two-row answers.
- [x] **The build and the backend under test are this diff's.** The served bundle is `/assets/index-r3Y8NShy.js` (1,982,654 bytes); it contains `Not offered yet` (1), `15f7dafc…7270` (1), `a73a2980…9528` (1), `82b75e47` (4 — the ADR-0015 legacy literals) and `e5272de9…102f` (2 — `ui/src/config/pubkeys.js:6` `DAVE_PUBKEY` and `ui/src/pages/users/Search.jsx:472`'s placeholder, both pre-existing and unrelated; no identification-tags module names it, see § Universal claims 1). The backend process (`bin/control-panel.js`, pid 285792) started 22:00:36 UTC; every changed source file's mtime is earlier (`stat -f '%Sm'`: 17:57:57–18:00:17 EDT = 21:57:57–22:00:17 UTC) and the tree is clean at `7d8581da`, whose source equals `1ea0e9cc`'s (`7d8581da` touched only the plan) — so the running route module (whose handler is built at load, `identificationTaggings.js:238`) is this one. Anonymous probes: `POST /api/assistant/identification-tags/publish {"keys":["my-human"]}` → 401 `Authentication required for this action` (the middleware, before the body check); `GET /api/assistant/attention` → `{"success":true,"signedIn":false}`. No signed-in probe was made and nothing was published (the Tester's signed-in probe is recorded in the plan; the unit E-class and the browser B-class cover the 400 and the one-key POST).
- [x] `bash scripts/harness-lint.sh` before writing this file: exit 0, clean (0 violations, 5 known waivers); re-run after (see § On PASS).
- [x] `src/api/openapi.yaml` parses (`js-yaml`): the publish path has `post` only with responses 200/400/401/403/500; the attention path `get`; the `keys` description reads as the diff says.
- [ ] _Lint not configured — skipped._
- [ ] _Typecheck not configured — skipped._
- [ ] _Build not configured — skipped._

## Spec adherence

- [x] **Every acceptance criterion has a passing test**; the plan's coverage map holds against the suites and specs as they are:
  - **AC-1** → L1 (`test/assistant-attention.test.js:266-275`: the exports; `CANONICAL_TAG_AUTHOR` and `canonicalTagAddress` undefined), L2 (`:277-296`: each offered entry's `author` equals the fixture's hex *and* the fixture npub decoded with `nip19` — Nous `npub1zhma4…` → `15f7dafc…`, Nous' Tapestry Assistant `npub15uazn…` → `a73a2980…`; `address` = `39999:<author>:<slug>`; parked entries `null`/`null`), L3 (`:298-306`: the four in order with `offered/author/address`, `OFFERED_TAGGINGS` the offered two, both frozen). The library: `src/lib/identification-tags/index.js:38-46`.
  - **AC-2** → U3 (two rows; the `(author, d)` pairs scanned are the two signers' taggings and the two definition authors' slugs, `≤ 4` local scans), U9 (two unfinished rows), U12 (found at its own author's address; a same-named definition by another author is `found: false`), U13 (`finished/done/pending` over two entries, never a parked row), U14 (local scans by the owner, the TA and the two definition authors only — `authors.size === 2 + new Set(definitionAuthors).size`), U18 (no scan or read names a parked slug or d; no parked row; a parked tagging present anyway neither holds back nor pushes forward). The hub side: C3 and the attention spec's B1–B4 with the two-row `DONE`/`PENDING` answers (nine / ten counts). The code: `src/api/assistant/attention.js:181-205`.
  - **AC-3** → C1 (`states.parked === 'Not offered yet'`), C4 (`cardState` with `['present','parked'] → done`, `['unknown','parked'] → unknown`, `['parked','parked'] → unknown`, `['missing','parked'] → marked`), C8 (`rowState` is `parked` in `idle/checking/answered/failed`, for no row, a present row and a missing row; an offered entry never is), S5 (`.bs-idtags-row.is-parked`); the page spec's `expectParked` helper (`:176-186`: class `is-parked`, the words, no other state word, computed opacity `< 1`, the box disabled and unchecked) in B1–B6 and B13 (a forced click changes nothing; a press signs only the offered tagging). The code: `ui/src/pages/assistant/identificationTagsCopy.js:52,74`, `IdentificationTags.jsx:100-118,166,188`.
  - **AC-4** → S2 (`authorPubkey: entry.author` in the page, `IdentificationTags.jsx:196`), page-spec B7 (the signed event's `a` is `T.definitionAddress(My Tapestry Assistant)` and its `e` is the answer row's `definition.eventId`; `d`, `p`, `polarity` as before); E4 (`a` = `definitionAddress(My Tapestry Owner)`, `e` the definition's id, `d` by the library rule, both z stamps, polarity `1`, `verifyEvent` true), E11 (the definition scan is under `a73a2980…` and `e5272de9…` is never asked), B1 of the publish suite (the builder with the entry's author). The route: `identificationTaggings.js:143-151,174`.
  - **AC-5** → E2 (`['my-human']` alone and `['my-tapestry-owner','my-human']`: 400 `not-an-assistant-tagging` with the approved words, `getAssistantKeys` never called, nothing imported), E12 (the same with a same-named `my-human` definition present, and `['my-agent']`; nothing signed, published or read), publish-spec B6 (the parked box cannot be checked; the POST names `my-tapestry-owner` only), page-spec B13. The order in the handler: session (`:211-215`, 401) → body (`:217-219`, 400, `ASSISTANT_KEYS` from `OFFERED_TAGGINGS` at `:56-57`) → only then `publishAssistantTaggingsFor` (`:222`), whose first dependency call is `getAssistantKeys` (`:138`).
  - **AC-6** → S5 of the attention suite (`:679-691`; see § Tester-lane correction for its scope); my own reading of the four passages: BIBLE `:496-497` (both §11 rows), `:1076` (§14), `:8` (Last updated, a new leading entry); OpenAPI `:295-305`, `:359-371`, `:382`.
  - **AC-7** → R1/R2 of the three suites; S2/S3 of the publish suite; the five neighbour suites unchanged and green; the `/tags` surfaces, the wire format (`buildAssistantTagging` `:106-126` unchanged), the reading of "present" (`lookupByAddresses` by signer and d, `attention.js:97-137,187-191,198`; U7's other-author tagging still present) untouched.
- [x] **No criterion silently dropped.** The story carries no § Deviations, and none was needed: every file in `1ea0e9cc` is one the ADR's sub-decisions 1–5 name, and I found no behaviour in it that the ADR does not describe (the one extra line is a CSS cursor rule, § Non-blocking 3).
- [x] **No behaviour added that isn't in the story.** The parked rows read "Not offered yet" signed out too (`rowState` answers `parked` before the phase, `identificationTagsCopy.js:52`) — that is the story's own "Given the page in any state" (AC-3) and the plan's B6.

## ADR adherence

- [x] **Sub-decision 1 (the list):** `REQUIRED_TAGGINGS` keeps its name and order; each entry gains `offered` and `author`, `address` becomes `definitionAddress(entry)` (`index.js:29-31,38-43`); `OFFERED_TAGGINGS` frozen (`:46`); `CANONICAL_TAG_AUTHOR` and `canonicalTagAddress` removed from the module and its exports (`:92-102`). The two authors are literals of the list, not read from config, env or the graph (the library has no `require`, L1).
- [x] **Sub-decision 2 (the check):** the plan runs over `OFFERED_TAGGINGS` (`attention.js:183`); the offered entries are grouped by `author` (`:192-196`) and looked up with one `lookupByAddresses({ kind: 39999, author, ds, relays })` per author inside the same `Promise.all` as the signer lookups (`:197-200`); `entries` are the offered ones only (`:203`), so `taggings` has one row per offered tagging and `finished/done/pending` (`:170-172`) range over them. `evaluateIdentificationTags` reads the row's address from the entry (`:162`).
- [x] **Sub-decision 3 (the page):** `rowState` answers `parked` first (`identificationTagsCopy.js:52`); `rowText` gives `COPY.states.parked` (`IdentificationTags.jsx:48`); the row renders under `is-parked` (`:104`, from `is-${rowStateName}`) with a checkbox that is `checked={checked.has(entry.key)}` — never true, since `checked` is rebuilt from `state === 'missing' && definitionKnown` (`:166`) — and `disabled={!boxEnabled}` with `boxEnabled` only for `missing` (`:101,110`); `cardState` drops parked states before deciding (`identificationTagsCopy.js:74-77`); the first card's publish passes `authorPubkey: entry.author` (`:196`) and its set is `state === 'missing'` rows only (`:188`); `.bs-idtags-row.is-parked { opacity: 0.55 }` greys the whole row, name included (`styles.css:8995`).
- [x] **Sub-decision 4 (the route):** `ASSISTANT_ENTRIES = OFFERED_TAGGINGS.filter(signer === 'assistant')` (`:56`), so a parked key fails the existing `ASSISTANT_KEYS.has(k)` check (`:218`) → 400 before any key is read; definitions per author at `entry.author` (`:144-151`); `buildAssistantTagging` gets `authorPubkey: entry.author` (`:174`).
- [x] **Sub-decision 5 (the documents):** BIBLE §11's two rows and §14's bullet name the offered tagging(s), say the parked pair is listed and not published/refused, and drop "two" and "the canonical definitions"; OpenAPI's two descriptions and the `keys` description likewise; "Last updated" moved.
- [x] **Sub-decision 6 (nothing else):** the diff touches no other source; `ui/src/utils/publishProfileTag.js`, `src/api/profile-tags/index.js`, `ui/src/utils/publishTagPin.js`, `ui/src/hooks/useProfileTags.js` are not in it (`git diff 47fc2cd0..7d8581da -- <those four> | wc -l` → 0).
- [x] **Layering:** the route still imports `lookupByAddresses`/`tagRelays` from `./attention` rather than copying them; the copy module stays pure ESM (C-class loads it in Node); the library stays dependency-free.
- [x] **No new dependencies:** `package.json`, `package-lock.json`, `ui/package.json`, `test/registry.js` are not in the diff.
- [x] **Phase discipline:** `767981fd` (test) touches tests, fixtures, specs, the plan and the story's Linked-artifacts line only; `1ea0e9cc` (impl) touches the eight ADR files only, no test; `c61c3794` is one test file; `7d8581da` the plan only (`git show --stat` of each).

## Concept-graph integrity

- [x] **Handles in `kind:pubkey:slug` form:** the definition addresses `39999:15f7dafc…:my-tapestry-assistant` and `39999:a73a2980…:my-tapestry-owner` (`definitionAddress`, `index.js:30`; the `a` tag, `identificationTaggings.js:107`); the local z `39998:<runtime TA>:nostr-user-tag` from `getOwnerAssistantPubkey()` (`:119,156`) and `useConfig().taPubkey` (`IdentificationTags.jsx:146,199`) — on this stack `/api/concept-graph/summaries` names it `39998:8387ec0e…:nostr-user-tag` and `/api/assistant/pubkey` answers `8387ec0e…`, neither of the two list literals; the canonical z is the ADR-0015 literal imported from `../profile-tags` (`:71-73`), unchanged.
- [x] **No concept definition changed; no firmware reinstall needed** (story § Concepts touched; ADR § Consequences "Firmware reinstall required? No").
- [x] **Orientation:** the code reads no graph; the ADR's reflex checks were done at Architecture. I read the `nostr-user-tag` handle from `/summaries` rather than the BIBLE.

## Things tests can't catch

### Universal claims in the diff's comments and documents — each re-run

1. **"The single canonical-author constant is gone" (story AC-1), "no single canonical author remains" (`test/assistant-taggings-publish.test.js:14`).** `grep -rn "CANONICAL_TAG_AUTHOR\|canonicalTagAddress" src ui/src` → exit 1, prints nothing. The ADR's § Implementation notes command runs the same grep over `src ui/src test tests` and says it "must print nothing" — it prints two lines, both the negative guards that assert the absence (`test/assistant-attention.test.js:274` L1, `test/assistant-identification-tags-page.test.js:236` S1). The claim holds; the ADR's command is one scope too wide (§ Non-blocking 1).
2. **"Listed but parked and never looked up / never read" (`attention.js:11-12`, BIBLE `:496`, OpenAPI `:299-300`).** The plan and the author grouping iterate `OFFERED_TAGGINGS` only (`attention.js:183,193`); nothing else in the module names an entry; U18 pins that no scan or read carries a parked slug or its d and that no parked row is answered. The route's entries are `OFFERED_TAGGINGS.filter(...)` (`:56`); E12 pins no read for a parked key.
3. **"Refused before any key is read" (`identificationTaggings.js:7-8,55`, BIBLE `:497`, OpenAPI `:365-367`).** Read in the handler: 401 at `:215`, 400 at `:219`, and the first call that can touch a key, `getAssistantKeys`, at `:138` inside `publishAssistantTaggingsFor`, entered only at `:222`. E1/E2/E12 assert `calls.getAssistantKeys.length === 0` for every refusal.
4. **"Constants of the list, the same on every instance; never a runtime TA pubkey" (`index.js:14-15`, epic guardrail, OpenAPI `:382` "never per instance").** The two literals occur in `src`/`ui/src` only at `index.js:39,41` (`grep -rn "15f7dafc…\|a73a2980…" src ui/src test tests` → those two lines plus the fixtures' `:18-19`); the library has no `require` (L1), so nothing per-deployment feeds them; the runtime TA is read only for the local z (item above).
5. **"Never an `authors:` filter on a read of taggings, never a signer" (`index.js:15`).** Taggings are scanned by *signer* (`bySigner`, `attention.js:187-191,198`); the two authors are `authors:` filters only on the *definition* reads (`:199`; `identificationTaggings.js:150`) — which is what the sentence now says. (The previous header's unqualified "never used as an `authors:` filter" was already untrue of the definitions lookup; the new wording is the accurate one.) Signing uses the Assistant's stored key (`:140,179`) or the viewer's extension (`publishProfileTagAssertionWithReport`); neither literal reaches a signer (grep in item 4).
6. **"Both found on the relays 2026-09-22", "Nous", "Nous' Tapestry Assistant" (`index.js:12-14`, story § Background, BIBLE `:8,1076`, OpenAPI `:303-304`).** `nip19.decode` of the story's two npubs gives exactly the two list literals. Live reads today (a scratchpad WebSocket script, `REQ {kinds:[39999], authors:[<author>], "#d":[<slug>]}`): **nos.lol and relay.primal.net** both answer `024abcc2…` by `15f7dafc…` at `d=my-tapestry-assistant` (content `tag.name` "My Tapestry Assistant", created 1790105874) and `ffdb2d51…` by `a73a2980…` at `d=my-tapestry-owner` ("My Tapestry Owner", 1790111298). Their kind-0 names: `15f7dafc…` "Nous" (display "Nous 🧠"), `a73a2980…` "Nous 🧠's Tapestry Assistant". Both definitions carry `z 39998:82b75e47…:tag` (the legacy literal) and `z 39998:919ba08a…:tag`, and `https://tapestry.brainstorm.world/api/assistant/pubkey` answers `919ba08a…` — so "created through tapestry.brainstorm.world" holds too.
7. **"At most four local scans — one per signer, one per definition author" (`attention.js:177-178`).** `bySigner` has at most two keys (person, assistant) and `byAuthor` at most two (the two authors); U3 and U11 bound the scans and the outside reads at 4.
8. **AC-6's negatives over the documents.** `sed '8d' BIBLE.md | grep -n -i "two identification taggings\|the canonical definitions\|canonical tag definitions\|\"My Tapestry Owner\", \"My Human\"\|My Tapestry Owner and My Human\|my-tapestry-owner and my-human"` → exit 1 (nothing); the same over `src/api/openapi.yaml` → nothing (S5 covers it); `grep -c -i parked` → BIBLE 6, OpenAPI 3. Every BIBLE mention of the four taggings is on lines 8, 496, 497 and 1076, all rewritten in this diff.

### The Tester-lane correction `c61c3794`

S5 originally read the whole of `BIBLE.md`; against the implementation it failed on `/two identification taggings/i`. `grep -n -o -i ".\{70\}two identification taggings.\{60\}" BIBLE.md` → **line 8 only**, inside the "prior:" clause "`… — an assistant's key signs its two identification taggings of its person through one narrow route — assistant-identification-tags #3 / ADR 0003`", i.e. the changelog entry describing what that earlier update said, behind the new leading entry for this story. The body (everything but line 8) carries none of the six phrases (item 8 above). The narrowing is justified and hides nothing: the changelog keeps its history in its own words, and the present-tense claims S5 forbids are absent from the body.

### The four CLAUDE.md invariants

- **POV-first:** "present" is still per session (the viewer and the assistant this instance maps them to, `attention.js:216-219`); the list is instance-wide by design (a shipped convention, Discovery decision 7), and the ADR's reflex checks say so.
- **Decentralized-first:** no one's publication is gated; the 400 refuses only what *this* instance's own signer is asked to issue, and a same-named tagging by anyone still counts as present (U7). The definition authors are never trust filters.
- **Filter at view time:** every answer is re-derived from raw scans on each read; nothing is stored.
- **Local-first:** read-only scans; the route still writes this instance's relay first (`:181-192`); neo4j and the LMDB are untouched.

### The rest

- [x] **No secrets.** The diff carries only public keys; `grep -n -i "nsec1\|privkey *[:=] *['\"][0-9a-f]\{64\}"` over it prints nothing. The Tester's Keychain-reading probe script is not in the repo and I did not run it.
- [x] **No debug code.** `git diff … -- src ui/src BIBLE.md | grep "^+.*\(console\.log\|debugger\|TODO\|FIXME\|XXX\)"` → nothing added. The route's per-row `console.log`/`console.warn` (`:225-227`) are the previous book's documented logging, unchanged.
- [x] **No commented-out code.**
- [x] **Error paths:** a definition lookup that throws stays inside `lookupByAddresses` (`local-unreadable` → `tag-not-found` rows, E6); `definitionsByAuthor.get(entry.author) || {}` (`:166`) guards a missing author map; in `attention.js:203` `definitions.get(p.required.author)` cannot miss because `byAuthor` was built from the same entries.
- [x] **Concurrency:** the two definition lookups and the two signer lookups run in one `Promise.all` and each is bounded by `RELAY_BUDGET_MS` (U10); the page's checkbox set is rebuilt from each answer and a parked key can never enter it.
- [x] **Security:** the body check is unchanged in shape (a non-empty array of strings, each in `ASSISTANT_KEYS`); the author literal that reaches the `a` tag and `content.tagAddress` is a constant, never user input; the target is the session's pubkey.
- [x] **The ADR-0015 legacy z literal is untouched:** `LEGACY_Z_TAG_PUBKEY` at `src/api/profile-tags/index.js:49`, `LEGACY_TA_PUBKEY` at `ui/src/utils/publishTagPin.js:47`, the literals at `ui/src/hooks/useProfileTags.js:10` and `ui/src/utils/publishProfileTag.js:23` — none in the diff.
- [x] **Latency of the per-author lookups (the task's question).** On a local miss the check now opens up to four reads per tag-federation relay (two signers, two definition authors) where it opened three. Acceptable: the reads happen only on a local miss, only against the opt-in `aTagFederationRelays` list (empty by default, `TAG_RELAY_CATEGORIES`), all in parallel and each within the 8 s budget, once per page load through the one shared answer; the ADR § Consequences accepts exactly this and the gate approved it. Ledger `2026-09-22-strict-lookup-third-copy` now understates it — "three sockets per relay … (the viewer's, the assistant's, the canonical author's)" — and should gain one sentence saying four, with the two definition authors in place of the retired one (§ Harness friction 2).

## House rules check

- [x] Concept Graph API authority respected.
- [x] No new lint/typecheck/build tooling.
- [x] The per-deployment TA pubkey is never hardcoded: the two new literals are the definition authors (the epic's guardrail), resolved from nothing per-deployment; the local z still comes from the runtime helpers on both sides.

## Product-guide adherence

No PRD (acceptance-frame book). The one new string, "Not offered yet", is the story § Copy's, approved at the gate, and is verbatim at `identificationTagsCopy.js:19` and in the fixtures (`PAGE_COPY.states.parked`).

## Findings

### Blocking

None.

### Non-blocking

1. **ADR § Implementation notes, first bullet** — the proving command `grep -rn "CANONICAL_TAG_AUTHOR\|canonicalTagAddress" src ui/src test tests` does not print nothing: it prints the two test lines that assert the names are gone. The narrowed form over `src ui/src` is the one that proves the claim. Worth a one-word correction in the ADR when it is next touched; the claim itself is true.
2. **`src/lib/identification-tags/index.js:29-31`** — `definitionAddress` (and the route's per-author grouping) assume an offered entry has an author. A half-unpark (`offered: true, author: null`) would not crash but would scan `authors: [null]` and surface as "Tag not found" on the page. L2/L3 would fail first on such a list, so this is a note for whoever unparks a tagging: fill in both fields together, as the header says.
3. **`ui/src/styles.css:8996`** — `.bs-idtags-row.is-parked .bs-idtags-row-label { cursor: not-allowed; }` is not spelled out in ADR sub-decision 3 (which names the opacity only); it serves the story's "uneditable" and changes nothing else. Fine as is.
4. **Tester-lane tweaks without a plan line** — page-spec B12 now strips query strings from the logged paths (`:410`), publish-spec B4 dropped a redundant `page.route` (`:225`), and U8 gives its second other-author tagging `createdAt: 6000` so two events on one d stay deterministic. Each is right on its merits and test-only.

### Harness friction

1. **The ADR's proving command was written but not run** (§ Non-blocking 1). Ledger `2026-09-22-universal-claim-without-proving-command` already asks that a universal claim come with its proving command; this story shows the second half — the command must be *run after the change*, and its scope must exclude the tests that assert the absence, or it proves nothing. One sentence on that row, not a new row.
2. **Ledger `2026-09-22-strict-lookup-third-copy`** says three sockets per relay per page load and names the canonical author; after this story it is four, by the two definition authors (§ Things tests can't catch, latency). One sentence on that row.
3. **The story's Linked-artifacts "Review:" line** stays "(filled in after Review phase)": this review's sanctioned writes were the review file and the Status line, so the main session fills it at commit. Already covered by `2026-09-22-linked-artifacts-not-written-at-gates`; nothing new to file.
4. **CLAUDE.md's "local-dev value (`82b75e47…` on this machine)"** is not this machine's TA (`/api/assistant/pubkey` → `8387ec0e…`); it did not mislead this review because the code resolves the TA at runtime, and OPEN.md rows 44, 127, 168 and 223 already record it. Nothing new to file.

## Verdict

The diff does what the story, the ADR and the plan say, and nothing else: one list with each definition's author, two parked entries the page greys and nothing reads or issues, the two publishers pointing at the definitions that exist on the relays today, the documents corrected. The book's gate is green on this host with the only movement in the three re-aimed suites, the browser class is 64/64 + 1 by-design skip against the rebuilt UI, and every universal claim in the diff re-ran under its own command.

## On PASS (same commit)

- [x] Story `**Status:**` flipped to `Done` in place (`engineering-team/stories/identification-tags-authorship/1-two-authored-tags-and-two-parked-taggings.md:3`).
- [x] Completion detection performed; the result is reported in the chat to the main session, not here.

**PASS**
