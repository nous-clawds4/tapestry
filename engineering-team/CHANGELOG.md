# Harness Changelog

The ratified-change record for the **harness definition** — the files listed in
[`scripts/harness-def-paths.txt`](../scripts/harness-def-paths.txt) (roles, workflows,
templates, wiring, orientation docs, and the harness scripts). One row per **logical
change** (a migration, a rule, a mode, a sweep — not one per commit), appended at the
bottom, chronological. The **Origin** column is the point: it records which incident,
ledger row, journal entry, review finding, or ask prompted the change — so the
self-improvement loop is auditable (which feedback channels actually produce changes).

**The touch-rule:** a diff touching harness-definition paths also touches this file.
Enforced at session start by `scripts/harness-lint.sh` (check **L10**) and surfaced by
`/whats-open` ("harness definition changed since your branch diverged"). See
`engineering-team/README.md` § "Tuning the team".

> Rows dated before 2026-07-02 were **reconstructed after the fact** during the
> harness-self-improvement book (story #2), from git history, MIGRATION-epic-folders.md,
> the Direction journals, and the 2026-07-02 harness review.

| Date | Change | Why | Origin |
|---|---|---|---|
| 2026-04-30 | Engineering Team harness created — roles, workflows, templates, README (`4acbe321`, landed 2026-05-05; README's "Generated 2026-04-30" is the authoring date) | run every change through PO → Architect → Tester → Implementer → Reviewer with human gates | adoption of Rob Conery's *Eliminate Crappy Slop Code* pattern; adapted from the earlier local pi-harness experiment |
| 2026-05-04 | `/cycle-local\|staging\|prod\|full` skills + canonical `docs/SMOKE_TEST.md` (`48de7b57`) | one honest, repeatable deploy chain with a single smoke definition every cycle inherits | deploy/smoke steps were being re-derived (and re-mistaken) per session |
| 2026-05-11 | CLAUDE.md **architecture invariants** — POV-first, decentralized-first, filter-at-view-time + reflex checks (`7b9659a0`) | stop centralized-SaaS instincts from silently violating the product's core principles | recurring design mistakes in sessions (pre-computed "trusted set" class) |
| 2026-05-13 | Phase slash commands wired in `.claude/commands/` (`40a402d9`); first port-drift fix in CLAUDE.md (`7d91caac`) | make the phases invocable; correct a stale Concept Graph port | harness usable only by reading role files until then; the port-drift class's first documented instance |
| 2026-05-22 | **Docker container-access house rule** in CLAUDE.md (`68841602`) | logs/CLIs live in containers; host paths like `/etc/brainstorm.conf` don't exist | sessions repeatedly probing non-existent host paths |
| 2026-06-04 | **Epic-scoped folders migration** — stories/decisions/reviews per epic, per-epic numbering, whole-folder `done/` retirement, the **Done-flip rule** (Reviewer sets story `Done` on PASS), + `MIGRATION-epic-folders.md` (`dacbcf03`) | kill the flat-namespace merge-collision class; make retirement an epic-level move | three real numbering collisions (ADR 0010→0012, 0015→0021, the 0041 mis-numbering); shipped **without a status backfill** — the gap the 2026-07-02 sweep closed |
| 2026-06-04 | **Product Team flow** — upstream discovery/design harness with natural-language entry (`41dc6d98`, `f8b5aa3e`) | let a non-technical user drive Discovery → PRD → story decomposition | need to design products before engineering them; slash commands as shortcuts only |
| 2026-06-05 | **Book-close return edge** — books, eager anchors, `/close-book`, audit + prd-addendum/seed (`24ed9513`) | close the product↔engineering loop; make "done" durable across sessions | shipped work wasn't feeding back into product scoping |
| 2026-06-06 | **Protocol-Spec docs-mode** workflow (`74fa29ff`) | run BIBLE/ADR spec work through the cycle without fabricating test plans | protocol work didn't fit the code-shaped phases |
| 2026-06-09 | CLAUDE.md **lane-picking at session start** — Product vs Engineering routing (`055ec168`) | concrete-*sounding* requests were defaulting into build mode, skipping Discovery | operator observation of skipped product phases |
| 2026-06-10 | **Direction mode** — Director role, blinded gate-judge, `/direct-feature`, task-timeline pre-registration (`3a2657b2`) | test whether the harness can carry a feature end-to-end with Claude at the gates | autonomy experiment; pre-registered like a human study |
| 2026-06-11 | Commands + BIBLE links **repointed to epic-scoped paths** (`6b3c0bff`) | the 06-04 migration updated only "5 mechanistic files"; wiring lagged | restatement drift — the class the 2026-07-02 review later catalogued in full |
| 2026-06-14 | **OPEN.md ledger + `/whats-open` roll-up** (`0143835a`) | one derived view of open work across all surfaces, from any session | cross-session tracking pain: loose ends lived only in session memory |
| 2026-06-14 | **Gate-5 clarification** — the Reviewer (not the Director) authors the status flip (`f314bbba`) | resolve a role-boundary ambiguity that caused a real kick-back | reputation-info-popup Direction journal — the loop's first end-to-end worked example (journaled defect → operator-ratified harness commit) |
| 2026-07-02 | **Harness review + Appendix A sweep** (PR #337: review `50e94889`; sweep `fce3e7ee`…`f5ecee80`) — port sweep, status backfill, epic retirement, verdict enum, docs-mode notes, cycle-skill portability, Playwright fix, whats-open upgrade, strictness/intake normalization, ledger truth restoration | 61 adversarially-verified findings; one meta-problem: everything stated twice, nothing enforced, lessons had nowhere to land | operator-commissioned audit (`docs/HARNESS_REVIEW_HANDOFF_2026-07-02.md`) |
| 2026-07-02 | **harness-lint** — L1–L9 invariant guard, waiver file, `/whats-open` section (story #1: `91389c71`, `285bace5`; disposition `c5690a66`) | drift surfaces at session start instead of at the next audit | review §5.3 (enforce stage); ADR 0001 |
| 2026-07-02 | **harness-changelog** — this file, `scripts/harness-def-paths.txt`, lint check **L10**, `/whats-open` divergence notice, README touch-rule (story #2) | every harness change becomes a recorded, announced, origin-attributed event | review §5.4 (ratify stage); ADR 0002; story #2 gate decisions (row-per-logical-change, staging merge-base, latest-only L10) |
| 2026-07-02 | **close-book-retro** — post-mortem/harness retro as `workflows/6-book-close.md` step 7 (no-fourth-state rule), build-audit §7 table, close-book command + director.md pointers, origin-drift preflight as step 0 of workflows 1–2, product Phase-7 retro mirror (story #3) | every lesson channel terminated in a step that didn't exist; lessons died silently (live-feed journal note lost; 2026-05-24 Meta intake item open 5+ weeks) | review §5.2 (route stage); ADR 0003; story #3 gate decisions |
| 2026-07-02 | **meta-escalation** — collect_meta pre-pass + top-of-report banner + "Meta items (harness lessons)" section in `whats-open.sh` (thresholds: ≥3 open or >30d, defined in the script only); OPEN.md escalation rule; whats-open command triage step; CLAUDE.md capture sentence (budget-neutral, file still 191 lines) (story #4) | a capture inbox without an escalation rule silts up — the meta type sat unused while its one predecessor aged 5 weeks | review §5.4 (escalation half); ADR 0004; story #4 gate decisions |
| 2026-07-02 | **harness-stats** — `scripts/harness-stats.sh` (phase-commit counts, verdict tallies + kick-back rate + churn, book throughput, slug-matched cycle times with an honest coverage line, summary block, always-exit-0); verdict parser extracted to shared `scripts/lib/review-verdict.awk` (consumed by lint + stats, BASH_SOURCE-resolved); workflow-6 step 7 citation now direct (story #5) | the retro argues from numbers, not impressions; one parser, two consumers | review §5.5 (measure stage); ADR 0005; story #5 gate decisions |
| 2026-07-04 | **enforcement** — `.claude/settings.json` SessionStart hook → `scripts/session-start.sh` (lint + meta state + ≤2s stack probe + open books, always exit 0); `collect_meta` + thresholds + banner extracted to shared `scripts/lib/collect-meta.sh` (consumed by whats-open + the digest); six writing product agents get allow-list-only Write/Edit scoping (`product-team/**` + `OPEN.md`, no ask/deny rules); Bash removed from product-advisor + product-expert; role-isolation claims reworded to exactly what is enforced (eng README, product README ×2, CLAUDE.md ×2 — still 191 lines); whats-open def_paths empty-array guard (story-2 carry-over) (story #6) | prose guarantees became platform guarantees where possible, and honest prose where not; sessions now open with the harness's own health check | review §4.1 (R-E1 + R-E2); ADR 0006; story #6 gate decisions |
| 2026-07-04 | **session-start-restructure** — CLAUDE.md "read all four" + "also check" blocks → one per-task pointer table (file 191→190 lines); AGENTS.md §2 → probe-and-fallback ladder with the stack-free orientation card, §4 don't-load-BIBLE rule scoped to graph-reachable (file 98→102); `scripts/harness-budgets.txt` (caps: CLAUDE.md 190, AGENTS.md 102; R-S4 rule prose lives in its header) enforced by lint **L11** (over-cap violation, missing-file INFO); README contributor-orientation block; `docs/QUICKSTART.md` dead `concept-graph` checkout removed (story #7) | session-start reading becomes task-proportional; the stack-absent path becomes normative text; the always-loaded pair is frozen — un-freezing is an L10-visible CHANGELOG event | review §4.4 (R-S1–S4); ADR 0007; story #7 gate decisions |
| 2026-07-04 | **book-close retro bundle (harness-self-improvement)** — phase-commit slug convention `(<epic> #<n>)` written into workflows 1–5 per-phase-commit sections (was practiced, now normative); `design-architecture.md` stack-absent house rule repointed to the AGENTS.md §2 ladder (was: "ask the user to bring the stack up"); `whats-open.md` step 2 quotes-and-points thresholds instead of restating ≥3/>30d; `harness-stats.sh` dead verdict-case removed + median line gains "(0d = same-day)" | first live retro's ratified fixes: coverage-gap convention (76/95 matched argues the case), one ladder contradiction, one threshold restatement, two instrument polish items | book-close retro, audit §7 (`audits/harness-self-improvement/audit.md`); OPEN.md row 18 |
| 2026-07-06 | **harness-suites-portable** — `.claude/settings.json` finally ships (`.gitignore` un-ignore + tracked hook file, SessionStart → session-start.sh only); shared `scripts/lib/date-epoch.sh` GNU/BSD fallback consumed by collect-meta ages, lint L9, stats durations; new lint **L12** (every def-path row must exist on disk); lint header list gains the omitted L11 line | the SessionStart hook existed only in the cloud session that wrote it — gitignored, zero git history, invisible to the lint (OPEN.md row 20); the >30d meta age trigger and two harness fixture tests were dead on macOS (row 19); enforcement that only works on one platform is prose | OPEN.md rows 19+20; story test-hermeticity-ci #3; book recon 2026-07-05 |
| 2026-07-11 | **brainstorm.world → tapestry.brainstorm.world sweep** — CLAUDE.md prod-deployment name + the `/cycle-*` and `/direct-feature` skills repointed (prod smoke targets, prod URL, `deploy-brainstorm.yml`→`deploy-tapestry.yml`); non-def docs (BIBLE/OPERATIONS/ROADMAP/SMOKE_TEST/NEO4J_OPTIMIZATION) swept in the same PR. Subdomains (`staging.`/`tags.`/relays), the `brainstorm@brainstorm.world` NIP-05 identity, and all dated records left intact | the production/reference deployment moved off the apex `brainstorm.world` (handed to the NosFabrica codebase, different host) onto `tapestry.brainstorm.world` on the same droplet — harness deploy/smoke guidance must point at the live target or `/cycle-prod` smoke-tests NosFabrica | 2026-07-11 cutover (OPEN.md #24; PRs #340/#341/#342) |
| 2026-07-16 | Waivers added for the feat/tags↔staging merge: L1 × `stories/event-tagging/*` + `stories/tag-applicability/4-…` (feat/tags-era Status phrasing predates the lint), L3 × `pov-selectable-tag-surfaces/` + `profile-tag-hardening/` (epic files never created on feat/tags) | staging's repo-wide harness-lint met feat/tags-era artifacts written before it existed; waiver-with-citation is the sanctioned bridge until statuses are normalized and the epic files written | OPEN.md row 38 (feat/tags↔staging integration, CI run 29464735775) |
| 2026-07-16 | Waiver added: **L2 × `epics/tag-event-inspector.md`** — the epic legitimately spans two books (`tag-event-inspector` Closed with its frame met; `profile-tagging-inspector` Open), and L2 reads "Closed book" as "its epics are finished" | an epic retired at a book close was reopened hours later by a new bounded ask, which correctly took a **new** book while the satisfied frame stayed closed — a shape the harness's own docs allow but L2 cannot model. Waiver is the bridge; the rule needs a fix (skip epics listed under any non-Closed book), and the waiver is blunt by construction (no way to scope it to one book) so it must retire with that fix | OPEN.md #47 (tag-event-inspector #2, Planning) |
| 2026-07-18 | **adr-template: test-file changes land in Phase 3** — one line added to `templates/adr.md` → `## Implementation notes`: test-file changes an ADR requires (suite re-aims, runner registration) belong to Phase 3, never to implementation | ADR 0003 D5 scheduled a test edit into Phase 4, colliding with Direction-mode Gate 4's frozen empty-`test/`-diff pin — caught before the Gate-3 boundary and resolved by moving the re-aim into Phase 3; the template line prevents recurrence in both flows | note-tagging-inspector retro (a), operator-ratified at the 2026-07-18 close gate; audit §7; commit `a982d6fb` |
| 2026-07-18 | **L2 waiver retired: `epics/tag-event-inspector.md`** — the epic is retired (operator-ratified at the note-tagging-inspector close: three surfaces across three books, goal met at pattern level), so the blunt spans-books waiver is removed from `scripts/harness-lint-waivers.txt` | the waiver was pre-registered to retire with either the L2 fix or the epic; the epic went first — the general "L2 can't model an epic spanning books" gap re-opens as a fresh row if a future epic spans books (OPEN.md #47 flipped DONE with that note) | note-tagging-inspector book close; OPEN.md #47 |
| 2026-07-18 | **Safe-to-merge gate wired into the cycle skills** — cycle-staging and cycle-prod gain a "Safe-to-merge check" step as the deploy-triggering merge's immediate precursor (post-approval in cycle-prod, approval-stands rule stated), referencing the new canonical `docs/SAFE_TO_MERGE.md` + `scripts/check-safe-to-merge.sh`; cycle-full names the check by delegation in its Stage 2/Stage 4 lists and Halt-on-failure list, carrying no recipe content of its own | no promotion procedure consulted the story-#1 deploy-safety answer before merging — the gate existed but guarded nothing; one canonical recipe (the SMOKE_TEST.md pattern) covers staging, prod, and manual `feat/tags` promotions, and the wait-and-recheck mechanism lives in tested code, not per-run transcription | story deploy-safety-gate #2; ADR deploy-safety-gate/0002 |
| 2026-07-25 | **self-ontology ratified — CLAUDE.md gains a 4th architecture invariant** — `### 4. Local-first: neo4j is the definitive "me"` added to the always-loaded invariants (neo4j = definitive self; tapestry LMDB = subordinate cache; signed events = "letters"; locally-authored graph state must never be discarded as "rebuildable from strfry"), pointing at the new **BIBLE §30 "The Self and Its Stores"**; the preamble becomes "four principles" and names the opposite drift direction, and the reflex-check closing line routes storage/rebuild/backup designs to principle 4. Paid for by deleting the purely-illustrative "The non-technical journey, end to end" block (every normative claim in it is already stated elsewhere in the same section) — **net 0 lines, the 190-line cap holds, no cap change and no `harness-budgets.txt` edit** | the codebase held two contradictory premises: the always-loaded invariants presented events as the substrate and neo4j as a freely re-derivable view, while `relationship-primitives` and the second-brain work write neo4j state with **no event behind it** — so under the old framing a rebuild (import / normalize / firmware reinstall / reconcile) could legitimately destroy authoritative, locally-authored knowledge. A session reading only CLAUDE.md would have "corrected" brain-first work back toward protocol-first. Principles 1–3 are explicitly **not repealed** — they still govern the event/social axis unchanged | story `self-ontology` #1; ADR `self-ontology/0001`; `docs/SELF_ONTOLOGY_DESIGN_HANDOFF.md` (owner decision 2026-07-24, doc stays 🔴 OPEN); review finding (harness-lint L10) |
| 2026-07-25 | **harness-lint gains L13 (adr-consequences) + a bash-3.2 empty-array guard on L8** — new fail-tier `check_L13` requires every **active** ADR (`decisions/*/*.md`, skipping the retired `done/` tree exactly as check_reviews/check_L2 do) to carry the template-required `## Consequences` heading; the one active offender (`task-queue-scheduler/0023`, a fast-tracked stub) is backfilled with a truthful stub Consequences. Separately, `check_L8`'s file-list loop is length-guarded (`[ "${#files[@]}" -eq 0 ] && return 0`, the violation()/whats-open precedent) so it no longer aborts under `set -u` on bash <4.4 (macOS 3.2) when a tree has zero wiring/link-doc files | `## Consequences` is the harvest source for the close-book §5 debt roll-up (`templates/build-audit.md:42`) yet nothing enforced it — `tag-event-inspector/0001` shipped without it, unseen; and `check_L8` would crash on an empty wiring set. Both latent | story `harness-gate-integrity` #1; ADR `harness-gate-integrity/0001`; OPEN.md #46 + #21 |
| 2026-07-26 | **Operational direction ratified — Direction mode gains a second on-ramp** — `roles/director.md` opens with a "Two modes — pick the right one" table (pre-registered/armed vs operational) and a new `## Operational direction` section; the `direct-feature` skill's Stage 0 forks by mode; `templates/book.md` gains a distinctly-headed `## Direction mode (operational) — goal-derived` sibling section; CLAUDE.md's doctrine line is **rewritten in place, net 0 lines (190-cap holds)** to name both modes and forbid ad-hoc per-session gate pre-authorization. An operational run's terms are DERIVED from an owner-ratified goal (deliverable → success criteria, boundary → ceiling, statement → the ask) instead of hand-authored, gated by a new read-only `GET /api/brain/direction/:slug` over the pure core `src/lib/brain/direction.js`: owner-ratified **anchor** (nearest ancestor named by an approved proposal fact; distance is an owner **policy parameter**, v1 = 0, never special-cased), **staleness** detection (goals re-sign, proposal facts never do — so no schema change was needed), and the **boundary-narrowing** invariant judged blind. Baseline commit + pinned governing versions are **knowingly surrendered** and stated in the artifacts. Armed mode is unchanged | arming a book was a ~2,000-word hand-written pre-registration that is mostly *experiment apparatus* (hypothesis, probability, outcome table, takeover-as-failure) — right when the harness itself is under test, pure overhead when the question is "please do this work." The cost of the first was suppressing all use of the second, so Direction mode went unused. The anchor + boundary-inheritance pair ships together because a distant anchor without inheritance is a laundering path | owner goal `hand-work-to-the-engineering-team-without-arming-a-book`; story `operational-direction` #1; ADR `operational-direction/0001`; OPEN.md #41 (resolved — named mode ratified AND the ad-hoc pattern forbidden) |
| 2026-07-26 | **Operational direction — the boundary guard now fails CLOSED (ADR 0002 amends 0001)** — `resolveAnchor` gains a seventh refusal `boundary-unjudged`: a chain with boundary steps that were **not judged** is refused rather than blessed, for every caller. Previously the boundary check ran *only* when a caller injected a verdict function, and the endpoint never injected one — so the same chain was eligible or refused depending on whether the caller remembered. The Director judges each step blind and re-asks via `GET /api/brain/direction/:slug?verdicts=<ordered,list>` (positional, `narrows`/`widens`; a length mismatch or unrecognized token refuses rather than passing silently). Also: staleness fails closed on unknowable currency (a missing `createdAt` ⇒ stale, reported as "could not establish currency", never as "rewritten"), the chain carries `{slug, uuid}` so the endpoint stops re-resolving ancestors by slug, and 0001 d6's response contract is corrected to the shipped shape. `roles/director.md` + the `direct-feature` skill document the two-call flow and require that passed verdicts **match the journaled judge verdicts** | ADR 0001 chose its design over a prose-only one precisely so that "the safety precondition of an autonomous run" would not be "enforced by an agent reading prose about itself" — but the boundary half had regressed to exactly that: code enforced the anchor, prose enforced the boundary (`director.md`'s "an unjudged step is never a pass"). Inert at v1 (distance 0 ⇒ zero steps), and wrong the moment the owner raises `BRAINSTORM_MAX_ANCHOR_DISTANCE` — which 0001 d3 advertises as a safe config change needing "no new machinery." Half of a paired invariant must not be advisory; the owner had already ruled that "shipping the anchor without the inheritance rule is worse than shipping neither" | story `operational-direction` #1; ADR `operational-direction/0002` (amends `0001`); review `operational-direction/1` — CHANGES_REQUESTED, blocking finding 1 |
| 2026-07-28 | **Book-close ordering fixed: epic close-out is a close step, gate runs last** — `workflows/6-book-close.md` step 9 (epic close-out "same breath as the close", L2 pairing named) + step 10 (the gate runs **after** the flip and close-out, certifying the tree the close leaves behind). Shipped in the `add-a-concept-to-a-tapestry` close trail (PR #477); this row discharges the missed touch-rule | as previously written the close gate was **guaranteed red** on every book whose epic close-out hadn't already happened — L2 fires the instant the book flips Closed, and no close step performed the close-out; the add-a-concept close proved the corrected order manually | store-and-show postmortem P14 → OPEN.md #121 (DONE); operator-ratified 2026-07-28 |
| 2026-07-28 | **Judge foreground rule** — `.claude/agents/gate-judge.md` "Environment mechanics": verification commands run foreground, sized to the tool timeout; a judge never ends its turn with its own task running (a backgrounded suite ⇒ the spawn is reaped verdict-less; one-spawn-one-reply means no revival). Shipped in PR #477; this row discharges the missed touch-rule | run 2's first Gate-3 judge backgrounded the ~32-min full suite, ended its turn, and died verdict-less — re-spawned fresh at the cost of a full re-run | add-a-concept audit §7 F1 → OPEN.md #123 (DONE); operator-ratified 2026-07-28 |
| 2026-07-28 | **Operational kickoff must state the two underivable numbers** — `roles/director.md` § Operational direction: an operational book requires an `## Operator instructions at open` section carrying a **deadline** and a **story cap**, refuse-to-run without them (the operational analogue of a missing Armed line); Stopping rules 1/4 reworded to name where each number lives per mode; `direct-feature` Stage 0 operational preflight verifies the section | a goal carries neither number, so an operational run had two of six stopping rules structurally unable to fire — store-and-show ran 3 stories/34h with no brake; add-a-concept's operator-instructions section (cap 2, journaled at kickoff) is the worked precedent | store-and-show postmortem (operator-ratified 2026-07-28); `audits/add-a-concept-to-a-tapestry/book.md` § Operator instructions at open |
| 2026-07-28 | **Completion offer gated on the completion audit** — `workflows/6-book-close.md` Trigger: a Direction-mode book's completion offer is valid only with the final completion judge's APPROVE in hand (skill Stage 3); an unaudited offer is void, same class as approving over a KICK_BACK | run 1's completion report led with a false claim the blinded audit caught — but the audit-before-offer order depended on the Director choosing it; both runs chose correctly, nothing required it | store-and-show postmortem P12 residual (operator-ratified 2026-07-28) |
| 2026-07-28 | **"Journaling is not recording" made a Director rule** — `roles/director.md` § Answering as the user, rule 7: a ratified answer lands in the artifact the consuming role reads (epic decision list / story / ADR) in the same act as journaling it | failed 3× in one run; the third time an operator ratification lived only in the journal — which no role reads — and an Architect nearly reached Gate 2 mandated into collision with a live `Accepted` ADR and no recorded authority | store-and-show postmortem P9 → OPEN.md #120 (DONE); operator-ratified 2026-07-28 |
| 2026-07-30 | **Run-scoped L2 waiver for a reopened epic** — `scripts/harness-lint-waivers.txt`: `epics/tapestries.md` waived against check L2 while the open Direction book `take-a-concept-back-out` runs story #6 under the reactivated epic; the closed `add-a-concept-to-a-tapestry` book also names the epic and L2 has no reopen carve-out (blind spot filed as OPEN.md #129, with the proposed rule fix: L2 passes an epic that any Open book lists); waiver removal owed at the book's close. This row also discharges the missed touch-rule — the waiver landed in `edf08f4d` without it, and L10 caught exactly that at the next full-suite gate | a legitimately reopened epic red-lined every full `npm test` mid-run; the lint's own waiver valve is the designed remedy | take-a-concept-back-out journal 2026-07-30 (Tester surfaced at Test Design; Director dispositioned) → OPEN.md #129 |
| 2026-08-04 | **L2 waiver removed at the take-a-concept-back-out book close** — `scripts/harness-lint-waivers.txt`: the run-scoped `epics/tapestries.md` waiver (added 2026-07-30, row above) came out at close as owed — the epic re-retired Done with story #6, so L2 is green on its own merits; OPEN.md #129 flipped DONE with the rule-fix proposal kept visible. This row discharges the missed touch-rule — the removal landed in `6078d5ca` (book-close commit) without it, and L10 caught exactly that on the branch's CI Test run (30929460589), the F6 retro lesson materializing at commit time as predicted | the close commit edited a harness-definition file (the waivers list) and per L10 every such logical change carries its CHANGELOG row; the pre-commit lint runs were clean because L10 inspects committed state only | take-a-concept-back-out audit §7 F1/F6; OPEN.md #129 (DONE 2026-08-04); close-trail PR #483, this fix PR follows |
